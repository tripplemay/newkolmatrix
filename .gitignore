.DS_Store
.obsidian/
*.swp
*.swo
.agent-id

# Horizon UI Pro 付费模板（设计参考基座，本地保留，不入 git）
# 含 171MB .fig 超 GitHub 限制；付费资产不入库。设计系统批次将 token/样式提取到 design-draft/
db4rDjuaSCqaEFW9XcFo_horizon-tailwind-react-nextjs-pro-3.0.0/

# Next.js / Node（DS-FOUNDATION F001 scaffold）
node_modules/
.next/
/out/
/build
next-env.d.ts
*.tsbuildinfo
.vercel
# Playwright（F006 visual baseline 用；baseline PNG 本身入 git）
/test-results/
/playwright-report/
/playwright/.cache/
# vitest 覆盖率报告（M1-A-BRIEF fixing-1）。
# 与上面三条同一约定：生成产物忽略，刻意基线（tests/screenshots/baseline/*.png）入库。
# 此前漏登记，导致 acceptance 要求接入 CI 的 `npm run test:unit:coverage` 每跑一次
# 就弄脏 9 个 tracked 文件，并在仓内留下一份会过期却宣称具体覆盖率数字的假快照。
/coverage/
# 环境变量（.env.example 是模板，保留入 git；真实 .env / *.local 绝不入 git）
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
npm-debug.log*
# 注：package-lock.json 保留入 git（可复现构建）

# Claude agent worktrees
.claude/worktrees/
