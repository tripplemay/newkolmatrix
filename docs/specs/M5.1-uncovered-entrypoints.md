# M5.1 未覆盖入口清单（M5.2 的工作面）

> **本文件由普查产出，不要手写。** 重新生成：`npm run census:entrypoints -- --write`
> 普查实现：`scripts/test/m51b-entrypoint-census.ts`（readFileSync 递归遍历，**不用 `git grep`**——
> 后者只搜已跟踪文件，新文件未 commit 时恒空绿）。计数与实物的一致性由
> `tests/unit/architecture-doc-freshness.test.ts` 机械钉住。

M5.1b 只做**最小闭环**（spec D-6）：证明机制成立，不做全站收口。下表是「全部入口 − 已覆盖」的差集——
**开关 `DB_APP_ROLE_RUNTIME=1` 一打开，这些入口的数据访问会当场抛 `MissingTenantScopeError`**
（fail-closed，不是静默零行）。M5.2 的任务就是把它们逐条接上租户作用域。

## 底数（本次普查实测）

「扫描数」= 该类文件总数；「入口面」= 其中**确实带租户**的（`不碰租户` 的 redirect 壳 / 纯客户端页 /
打包器脚本没有可接的作用域，不是待办）。差集只在入口面上算。

| 类型 | 扫描数 | 入口面 | 已覆盖 | 未覆盖 |
|---|---:|---:|---:|---:|
| API route | 31 | 29 | 4 | 25 |
| page（RSC） | 18 | 6 | 1 | 5 |
| 例程 | 4 | 4 | 1 | 3 |
| script | 50 | 39 | 1 | 38 |
| **合计** | **103** | **78** | **7** | **71** |

### 与 spec 底数的对账（逐条如实登记，不是照抄）

spec（M5.1b §F007 acceptance ③）写的底数是「31 API route + 13 admin page + 3 例程 + ~20 scripts」。本次实测：

- **API route 31** —— 与 spec 的 31 一致。
- **page 18**，其中 `src/app/admin/` 下 **13** —— spec 的「13 admin page」是**只数 admin/** 的口径，
  与实测 admin 数一致；差出来的 5 个是 login / signup / 根页 / preview 两页（均不碰租户）。
- **例程 4** —— spec 写 3，**实测 4**。差因：`src/lib/jobs/routines/` 下确实只有 3 个文件，
  但 `ROUTINES` 注册表有 4 条（`kol-sync` 的执行体在 `src/lib/kol-sync/sync.ts`，不在 routines/ 目录下）。
  本清单按**注册表**计数——调度真正会跑的是注册表，不是目录。
- **script 扫描 50 / 入口面 39** —— spec 写 ~20，**实测入口面 39**。
  spec 的 ~20 是估数（原文即带 `~`）；实测偏多主要是 `scripts/test/` 下历次批次留下的验收探针也带租户。

## 已覆盖（M5.1b 最小闭环）

| 入口 | 类型 | 租户来源 |
|---|---|---|
| `src/app/api/actions/[id]/reject/route.ts` | API route | 会话面（登录会话） |
| `src/app/api/actions/[id]/route.ts` | API route | 会话面（登录会话） |
| `src/app/api/actions/route.ts` | API route | 会话面（登录会话） |
| `src/app/api/nav-badges/route.ts` | API route | 会话面（登录会话） |
| `src/app/admin/knowledge/page.tsx` | page（RSC） | 会话面（登录会话） |
| `src/lib/jobs/scheduler.ts` · health-scan | 例程 | 无会话面（显式指名） |
| `scripts/test/gate-smoke.ts` | script | 无会话面（显式指名） |

## 未覆盖（M5.2 待接线）

`租户来源` 决定接法：会话面用 `withSessionTenant`（`src/lib/db/tenant-entry.ts`）；
无会话面在解析出 tenantId 后显式 `withTenant`；引导面**本就不该包**——它们发生在租户已知之前，
走 `privilegedDb`（F003 白名单），包了反而会把「登录/注册」重新锁死（审计 B2-1）。

### API route（25）

| 入口 | 租户来源 |
|---|---|
| `src/app/api/actions/[id]/confirm/route.ts` | 会话面（登录会话） |
| `src/app/api/actions/[id]/execute/route.ts` | 会话面（登录会话） |
| `src/app/api/agent/plan-ack/route.ts` | 会话面（登录会话） |
| `src/app/api/auth/[...nextauth]/route.ts` | 引导面（privilegedDb 白名单） |
| `src/app/api/auth/register/route.ts` | 引导面（privilegedDb 白名单） |
| `src/app/api/delivery/deals/[id]/keys/route.ts` | 会话面（登录会话） |
| `src/app/api/delivery/deals/[id]/refs/route.ts` | 会话面（登录会话） |
| `src/app/api/delivery/deliverables/[id]/route.ts` | 会话面（登录会话） |
| `src/app/api/delivery/payout/route.ts` | 会话面（登录会话） |
| `src/app/api/handoffs/route.ts` | 会话面（登录会话） |
| `src/app/api/insight/adopt/route.ts` | 会话面（登录会话） |
| `src/app/api/insight/share/route.ts` | 会话面（登录会话） |
| `src/app/api/kols/[id]/contact/route.ts` | 会话面（登录会话） |
| `src/app/api/match/candidates/[id]/verdict/route.ts` | 会话面（登录会话） |
| `src/app/api/match/plans/[id]/approve/route.ts` | 会话面（登录会话） |
| `src/app/api/match/refresh/route.ts` | 会话面（登录会话） |
| `src/app/api/materials/[id]/parse/route.ts` | 会话面（登录会话） |
| `src/app/api/materials/route.ts` | 会话面（登录会话） |
| `src/app/api/projects/[id]/goal/route.ts` | 会话面（登录会话） |
| `src/app/api/projects/route.ts` | 会话面（登录会话） |
| `src/app/api/reach/override/route.ts` | 会话面（登录会话） |
| `src/app/api/reach/quote/route.ts` | 会话面（登录会话） |
| `src/app/api/reach/refine/route.ts` | 会话面（登录会话） |
| `src/app/api/reach/send/route.ts` | 会话面（登录会话） |
| `src/app/api/signals/inbound/route.ts` | 无会话面（显式指名） |

### page（RSC）（5）

| 入口 | 租户来源 |
|---|---|
| `src/app/admin/campaigns/[id]/page.tsx` | 会话面（登录会话） |
| `src/app/admin/campaigns/page.tsx` | 会话面（登录会话） |
| `src/app/admin/creators/page.tsx` | 会话面（登录会话） |
| `src/app/admin/insight/page.tsx` | 会话面（登录会话） |
| `src/app/admin/today/page.tsx` | 会话面（登录会话） |

### 例程（3）

| 入口 | 租户来源 |
|---|---|
| `src/lib/jobs/scheduler.ts` · nightly-screen | 无会话面（显式指名） |
| `src/lib/jobs/scheduler.ts` · kol-sync | 无会话面（显式指名） |
| `src/lib/jobs/scheduler.ts` · weekly-draft | 无会话面（显式指名） |

### script（38）

| 入口 | 租户来源 |
|---|---|
| `scripts/jobs/run-health-scan.ts` | 无会话面（显式指名） |
| `scripts/jobs/run-kol-sync.ts` | 无会话面（显式指名） |
| `scripts/jobs/run-nightly-screen.ts` | 无会话面（显式指名） |
| `scripts/jobs/run-weekly-draft.ts` | 无会话面（显式指名） |
| `scripts/ops/normalize-datasource.ts` | 无会话面（显式指名） |
| `scripts/seed/canonical-projects.ts` | 无会话面（显式指名） |
| `scripts/seed/demo-handoff.ts` | 无会话面（显式指名） |
| `scripts/seed/dev-user.ts` | 无会话面（显式指名） |
| `scripts/seed/import-kol-csv.ts` | 无会话面（显式指名） |
| `scripts/seed/visual-kols.ts` | 无会话面（显式指名） |
| `scripts/test/agent-tools-smoke.ts` | 无会话面（显式指名） |
| `scripts/test/agentloop-e2e.ts` | 无会话面（显式指名） |
| `scripts/test/db-smoke.ts` | 无会话面（显式指名） |
| `scripts/test/delivery-e2e.ts` | 无会话面（显式指名） |
| `scripts/test/f006-eval-probe.ts` | 无会话面（显式指名） |
| `scripts/test/f006-evaluator-probe.ts` | 无会话面（显式指名） |
| `scripts/test/f006-maxreached-deep-probe.ts` | 无会话面（显式指名） |
| `scripts/test/f009-eval-ui-gate-probe.ts` | 无会话面（显式指名） |
| `scripts/test/f009-injection-probe.ts` | 无会话面（显式指名） |
| `scripts/test/f010-eval-ui-gate-probe.ts` | 无会话面（显式指名） |
| `scripts/test/f011-eval-probe.ts` | 无会话面（显式指名） |
| `scripts/test/frontdesk-e2e.ts` | 无会话面（显式指名） |
| `scripts/test/insight-e2e.ts` | 无会话面（显式指名） |
| `scripts/test/m2a-f007-tool-verify.ts` | 无会话面（显式指名） |
| `scripts/test/m2b-f004-pagedata-verify.ts` | 无会话面（显式指名） |
| `scripts/test/m45-adv-f010-prisma-undefined-probe.ts` | 无会话面（显式指名） |
| `scripts/test/m45-g2-evidence.ts` | 无会话面（显式指名） |
| `scripts/test/m45-g3-tenancy-probe.ts` | 无会话面（显式指名） |
| `scripts/test/m45-g5-cleanup-diff.ts` | 无会话面（显式指名） |
| `scripts/test/m45-g5-cleanup-residue.ts` | 无会话面（显式指名） |
| `scripts/test/m45-g5-db-snapshot.ts` | 无会话面（显式指名） |
| `scripts/test/m45-g5-gate-http-chain.ts` | 无会话面（显式指名） |
| `scripts/test/m47-g4-db-census.ts` | 无会话面（显式指名） |
| `scripts/test/m48-eval-db-census.ts` | 无会话面（显式指名） |
| `scripts/test/m48-eval-orphan-detail.ts` | 无会话面（显式指名） |
| `scripts/test/orchestration-smoke.ts` | 无会话面（显式指名） |
| `scripts/test/reach-e2e.ts` | 无会话面（显式指名） |
| `scripts/test/rv/rv-timeout-wire.ts` | 无会话面（显式指名） |

## 判定口径（读表前先看这个）

- **已覆盖** = 该文件（例程为该注册条目的执行体块）里出现 `withSessionTenant` 或 `withTenant(`。
- **租户来源**按源码标记判定，优先级：`withSessionTenant`/`requireSession*` → 会话面；
  零参 `buildToolContext` 调用（不带租户）→ 会话面；`systemContext`/`systemTenantId` → 无会话面；
  只 import `db/privileged` → 引导面；其余 import `db/prisma` → 无会话面。
- 完全不碰租户的 `scripts/` 文件（纯前端探针等）不计入入口面——它们没有可接的作用域。
- **引导面条目留在「未覆盖」表里是刻意的**：它们不是待办，是**不该做**的登记项，
  免得 M5.2 有人照着「未覆盖」逐条包过去，把登录重新锁死。
