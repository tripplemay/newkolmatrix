# M2-A-MATCH 首轮验收 Signoff（2026-07-22）

> **验收形态：** fan-out（orchestration §4）——9 features + 就绪回归各 1 个上下文隔离 evaluator subagent，
> FAIL/PARTIAL 自动进对抗复核（本轮零触发）。本文件为**机械汇总**：结论逐字取自各 subagent
> 结构化返回与其独立撰写的分报告，主上下文未改写、未筛选、未软化任何判定（铁律 12 / 独立性铁则 3）。
> **署名：** Andy/evaluator-subagent（快车道单实例署名约定）

## 结果总览

| # | Feature | 判定 | 分报告 |
|---|---|---|---|
| F001 | schema：MatchPlan + PlanKol + MatchCandidate 三表 + zod 契约 | **PASS** | [M2-A-MATCH-verify-F001.md](M2-A-MATCH-verify-F001.md) |
| F002 | domain/match-score.ts 可解释评分纯函数 | **PASS** | [M2-A-MATCH-verify-F002.md](M2-A-MATCH-verify-F002.md) |
| F003 | 候选生成 + 组合生成服务（src/lib/match/） | **PASS** | [M2-A-MATCH-verify-F003.md](M2-A-MATCH-verify-F003.md) |
| F004 | 批准动线 + →reach 守卫解锁（S10 消解） | **PASS** | [M2-A-MATCH-verify-F004.md](M2-A-MATCH-verify-F004.md) |
| F005 | match 语法面接真 + mock 退役 | **PASS** | [M2-A-MATCH-verify-F005.md](M2-A-MATCH-verify-F005.md) |
| F006 | nightly-screen 例程 + scheduler 注册表化 | **PASS** | [M2-A-MATCH-verify-F006.md](M2-A-MATCH-verify-F006.md) |
| F007 | match_plan/evaluate_creator 工具 + canvas ADR-28 + uiSyntax 注入 | **PASS** | [M2-A-MATCH-verify-F007.md](M2-A-MATCH-verify-F007.md) |
| F008 | 侧栏徽标服务接真（U4，消解 D-B 两批悬置） | **PASS** | [M2-A-MATCH-verify-F008.md](M2-A-MATCH-verify-F008.md) |
| F009 | 收尾：SW-R1 探针退役 + OBS-1 image part 迁移 + 文档翻牌 | **PASS** | [M2-A-MATCH-verify-F009.md](M2-A-MATCH-verify-F009.md) |
| READINESS | 批次级就绪回归（spec §5 末段口径） | **PASS** | [M2-A-MATCH-verify-READINESS.md](M2-A-MATCH-verify-READINESS.md) |

**判定：10/10 PASS · fix_rounds=0 · 对抗复核触发 0 次 · status → done**

## 各项结论（subagent 返回原文）

### F001 — PASS

**结论：** F001（MatchPlan/PlanKol/MatchCandidate 三表 + zod 契约）十项 acceptance 全 PASS。验证实物：commit 36db288 的 schema.prisma :251-343 逐字段核对、migration 20260722161835 通读（纯 expand：2 enum + 3 table + 11 index + 5 FK，零 DROP、零 RLS 语句）、dev DB 实测（information_schema 列清单/pg_enum 值域/pg_indexes 幂等键/pg_class relrowsecurity=false 即 D4 不建 RLS/_prisma_migrations applied=true）、物理约束插入实测（默认值 verdict=pending·doubts=[]·scorePending=false·status=draft·recommended=false 全命中；重复 (projectId,kolId) 触发 P2002；假 projectId 触发 P2003；删 plan 后 PlanKol Cascade orphans=0）、zod 契约读侧宽松降级/写侧非空（14/14 单测绿 + Evaluator 独立 6 项边界探测全符合）、D20 标注在 schema 注释（:253-255/:262-263）、schema+migration+引用代码同 commit、工具链三绿（lint 0 warnings / tsc exit 0 / test:unit 307/307）。D-H 纪律：Match 三表测前测后均零行清态。L2 用量=0（纯 schema 层无网关依赖）。

**复现/验证步骤：** 1. npx prisma generate && npx tsc --noEmit && npm run lint && npm run test:unit（三绿 + 307/307）；2. npx vitest run tests/unit/match-schemas.test.ts（14/14）；3. node --env-file=.env --import tsx scripts/test/m2a-f001-db-verify.ts（三表/枚举/索引/RLS/物理约束/D-H 闭环）；4. node --import tsx scripts/test/m2a-f001-edge-probe.ts（zod 独立边界 6 项）

**证据：** DB 实测：MatchCandidate_projectId_kolId_key [UNIQUE] 在库；MatchPlanStatus=draft/approved/superseded + CandidateVerdict=pending/kept/dropped（pg_enum）；三表 relrowsecurity=false；migration applied=true。物理实测：「6a defaults: verdict=pending doubts=[] scorePending=false matchScore=null」「6b unique(projectId,kolId): P2002 ✓」「6c PlanKol cascade orphans=0 ✓」「6d FK projectId: P2003 ✓」「D-H pre/post counts 均 {MatchPlan:0,PlanKol:0,MatchCandidate:0}」。工具链：lint「✔ No ESLint warnings or errors」/ tsc exit 0 / vitest「Test Files 28 passed, Tests 307 passed」。代码锚点：src/lib/data/schemas/match.ts:40-43,54-57（safeParse→null 不抛 D2）:66-70（reasons min(1) 写侧拒空）；prisma/schema.prisma:253-255,262-263（D20 标注）；git show 36db288 --stat（schema+migration+match.ts+单测同 commit）

### F002 — PASS

**结论：** M2-A-MATCH F002（domain/match-score.ts 可解释评分纯函数）首轮验收，acceptance 8/8 逐条 PASS。依据实物：代码逐行核对（纯函数边界=唯一 type-only import；降级/加权/常量/三处复用注明逐项对上）+ 单测运行（match-score 12/12，零 mock 零 IO）+ 全套 test:unit 307/307（28 files）+ tsc exit 0 + lint 0 errors 0 warnings + 覆盖率 domain 98.21% lines/100% funcs + DB 实测 D-H 清态（Match 三表+PendingAction+OperationLog 全 0 行）。三处复用铁律超出注明本身做了实物交叉：全仓 computeMatchScore 消费点仅 F003 服务（generate-candidates.ts:167）与 F007 工具（evaluate-creator.ts:120），domain 外零权重内联重算。命名注记：acceptance 概念名 matchScore.compute 实物为 computeMatchScore，与 health.ts 先例（computeHealth）D7 约定一致，功能等价非缺陷（role-context §13 条款）。F002 无 L2 项（纯函数无 IO），零网关用量。未修改任何产品代码，未起 dev server。

**复现/验证步骤：** cd /Users/yixingzhou/project/newkolmatrix && npx prisma generate && npx tsc --noEmit && npm run lint && npx vitest run tests/unit/match-score.test.ts && npm run test:unit && npx vitest run --coverage；D-H 核查：node --env-file=.env -e 用 PrismaClient 对 matchPlan/planKol/matchCandidate/pendingAction/operationLog 逐表 count（预期全 0）；复用点核查：grep -rn computeMatchScore src/ scripts/（预期仅 generate-candidates.ts 与 evaluate-creator.ts 两消费点）

**证据：** 1) 纯函数：match-score.ts:13 唯一 import 为 `import type { AudienceSlice }`（type-only），全文件无 DB/网关/fetch；2) 降级：:119-128 audienceDemo null→score=similarity 直通+pending=true+REASON_AUDIENCE_PENDING='受众数据待接入'(:28)，test :30-39 断言 score=0.83/pending/reason，脏形状×6 不抛错(test :54-64)；3) 来源注明：:138 '受众契合 X%（来源：游戏知识库受众画像）'，test :106-113；4) 权重：MATCH_WEIGHTS{similarity:0.7,audience:0.3} 导出(:20-25)+和为1断言(test :24-26)；5) 三处复用：文件头 :9-11 明记铁律，grep 全仓消费点=generate-candidates.ts:167+evaluate-creator.ts:120，MATCH_WEIGHTS domain 外零命中；6) L1：`npx tsc --noEmit`→exit 0；`npm run lint`→'✔ No ESLint warnings or errors'；`npm run test:unit`→'Test Files 28 passed (28) / Tests 307 passed (307)'；`vitest run --coverage`→domain 97.84% Stmts/97.27% Branch/100% Funcs/98.21% Lines；7) D-H：测毕 Prisma count 实测 {"matchPlan":0,"planKol":0,"matchCandidate":0,"pendingAction":0,"operationLog":0}

### F003 — PASS

**结论：** F003（候选生成 + 组合生成服务 src/lib/match/）acceptance 12/12 项逐条 PASS。验证实物：generate-candidates.ts / build-plans.ts 代码逐行核对（受众画像+查询构造、P7 embed 注入、pgvector topN=20 SQL 范式、matchScore.compute 单一真相源、P4 upsert update 分支不含 verdict、doubts/preJudge 规则、P1 无 LLM 三组规则函数、mock 命名语义经 git show 693c215 逐字核对且 B recommended=mock best、≤10 人 + assertPlanKolReasons 写侧非空、metrics 四字段规则、$transaction 内 supersede 只圈 draft）；集成测试真库实跑 8/8 PASS（夹具独立租户，afterAll Cascade 清理）；D20 变异断言活性在隔离 worktree 实证 3/3 精准击杀（verdict 回退 / supersede 漏 status 过滤 / dropped 过滤丢失，主工作树产品代码零触碰）；Evaluator 对抗性补测真实规模池（dev tenant 2524 带 embedding KOL）验证 topN=20 截断、二跑幂等、三组各 10 人上限、metrics 实值（budgetUsd 恒 null、reachTotal=Σfollowers、risk 分档）；[L2] 按授权最小用量 1 次真网关 embedText 验证默认绑定生产路径（真 cosine ~0.45、分档一致）；lint 0 警告 / tsc 0 错误 / test:unit 28 文件 307 用例全绿。D-H 终态：Match 三表 + PendingAction + OperationLog 全零、仅 dev tenant 无夹具残留。

**复现/验证步骤：** 1. npx prisma generate && npx tsc --noEmit && npm run lint（全绿）；2. npm run test:unit（307/307，含 tests/integration/match-services.test.ts 8/8 真库 mock 向量）；3. npx vitest run tests/integration/match-services.test.ts --reporter=verbose（六条变异断言逐 case 过）；4. 变异活性：git worktree add <tmp> HEAD 后分别植入 ①upsert update 加 verdict:'pending' ②updateMany where 去 status:'draft' ③findMany where 去 verdict:{not:'dropped'}，各跑集成测确认对应断言 FAIL 后复原并移除 worktree；5. 真实规模/L2：scratchpad 脚本以 dev tenant 临时项目跑 generateCandidates（mock 向量二跑 + 1 次真网关默认绑定）+ buildMatchPlans，核对 total=20/幂等/3 组 10 人/metrics，测毕删除临时数据并复核三表计数为 0。

**证据：** tsc exit 0；next lint「No ESLint warnings or errors」；vitest「Test Files 28 passed / Tests 307 passed」；match-services.test.ts 8/8 ✓（幂等断言 1 / P4 verdict 断言 2 / dropped 断言 3 / supersede 断言 4 / approved 断言 5 / 无 embedding 不入池断言 6）；变异击杀：①「× P4 verdict 保留（变异断言 2）…2 failed」②「× approved 永不动（变异断言 5）…1 failed」③「× dropped 出局（变异断言 3）…1 failed」；真实规模实测 run1 {total:20,created:20} → run2 {created:0,updated:20}，3 plans 各 members:10、budgetUsd:null、reachTotal Σfollowers 真值（28810/28810/54630）；[L2] 真网关 top3 matchScore 0.4508/0.4500/0.4480 preJudge '?'；mock 命名 git show 693c215:src/lib/data/mock/env-match.ts L46/57/68 与 build-plans.ts:196-215 逐字一致；schema.prisma:340 @@unique([projectId,kolId])；generate-candidates.ts:187-197 update 分支不含 verdict；build-plans.ts:220-254 $transaction + updateMany status:'draft'；终态 {"matchPlan":0,"planKol":0,"matchCandidate":0,"pendingAction":0,"operationLog":0,"tenants":["dev"]}

### F004 — PASS

**结论：** F004（批准动线 + →reach 守卫解锁/S10 消解）首轮验收，acceptance 7 项逐条判定全 PASS。依据实物：① 代码层——approve-plan.ts:72-85 单选事务原子（approved+approvedBy/At + 其余 draft→superseded 同 $transaction）、:90-94 advanceStage 衔接且失败不回滚（route 响应 advance:{ok,reason,cur} 注明）；env-guards.ts:122-127 →reach D9 占位替换为 MATCH_PLAN_NOT_APPROVED 真判定，hasApprovedMatchPlan 必填非可选、守卫保持纯函数，两调用点（env-advance.ts:74-84 + campaigns/[id]/page.tsx:43-72 RSC→ProjectDetail canEnter）同步组装；grep 确认 →reach 分支字面量零残留（现存命中仅类型定义 + delivery/insight M3 spec 明文保留 + 展示文案）；approve 路径 grep 零 PendingAction 写入点。② 测试实跑——tests/integration/match-approve.test.ts 7/7 过（打真库，D20 六条变异断言逐条在案：半事务/守卫恒放行/重放重写/多选/advance 连带回滚等）+ env-guards.test.ts →reach 真判定单测 4 条；全量 L1：vitest 307/307（28 files）、tsc exit 0、lint 0 errors 0 warnings。③ 路由层直调实测（不起 server，遵守 :3000 端口纪律）——19/19 断言：approve 200（publicId 双口径）/重放 200 already=true advance=null/superseded 409/不存在 404；全程 PendingAction 恒 0（internal 铁律）；批准前 canEnter(reach) 拒→批准后放行；refresh 缺参 400/假项目 404/真跑 200（候选 topN=20 + 3 组，approved 不动 P4）。[L2] 真网关用量：embedText ×1（bge-m3 短文本单往返，spec §4 授权口径内）。D-H 复原实测：临时项目级联删除后终态计数 == 基线（Match 三表/PendingAction/OperationLog 全零）。commit 693c215 归属 features.json F004（铁律 10 核对通过）。

**复现/验证步骤：** npx prisma generate && npx vitest run tests/integration/match-approve.test.ts tests/unit/env-guards.test.ts（39/39）&& npm run test:unit && npx tsc --noEmit && npm run lint；grep -rn "DEPENDENCY_NOT_IMPLEMENTED" src/（仅类型定义+delivery/insight M3+文案映射）；grep -rn "hasApprovedMatchPlan" src/（两调用点组装）；路由层直调：node --env-file=.env --import tsx scratchpad/f004-route-probe.ts（19 断言，含 L2 embedText ×1 与 D-H 终态复原断言）

**证据：** match-approve.test.ts 7/7 PASS（「批准 B：事务原子（B approved + A/C superseded）+ S10 真推进 match → reach + 留痕 ✓」等）；npm run test:unit → "Test Files 28 passed (28) / Tests 307 passed (307)"；tsc exit=0；lint "✔ No ESLint warnings or errors"；直调探针 19 断言含 "internal 动作零 PendingAction（0 == 基线 0）"、"refresh 真跑 → 200（candidates.total=20, plans:3）"、"D-H：终态计数 == 基线（清态复原）"；关键文件行：src/lib/match/approve-plan.ts:72-94、src/app/api/match/plans/[id]/approve/route.ts:41-43、src/lib/domain/env-guards.ts:58-62,122-127、src/lib/domain/env-advance.ts:74-84、src/app/admin/campaigns/[id]/page.tsx:43-72、tests/integration/match-approve.test.ts:1-13（D20 变异断言清单）

### F005 — PASS

**结论：** F005（match 语法面接真 + mock 退役）首轮验收 PASS。依据实物：0b81a18 commit diff、match-format.ts/surface-data.ts/match-index.tsx/page.tsx 代码核验、本机 L1（lint 0/0 + tsc 0 + vitest 307/307）、CI run 明细（F005 首推仅 Visual 预期红=旧 linux 基线；HEAD 3d93f72 五 job 全绿含 Visual）、DB 层行为实测（lazy 生成/无凭据静默降级/cur<match 边界/rationale sentinel 改→验→复原，L2 真 embedText ×1 最小用量）、mock 退役 needle grep 零代码残留、V5 22 元素布局 diff 零变更。页面运行时（两视口/点击动线）按批次口径委托 READINESS 视觉/探针套件，本判定以代码/测试/DB 层为准。测毕 D-H 清态复原（Match 三表 0 + PendingAction 0 + OperationLog 0，终态复查确认）。观察项（非缺陷）：验收窗口内并行 gate:smoke 的 PA/OL 中间态造成脚本 2 项全表计数断言瞬时误红，该进程自清后归零，与 F005 无关。

**复现/验证步骤：** 关键验证命令：1) npx prisma generate && npx next lint && npx tsc --noEmit && npm run test:unit（307/307）；2) grep -rn "env-match\|mockMatchPlans\|matchPlanListSchema" src/ tests/ scripts/（零代码引用）；3) node --env-file=.env --import tsx <f005-verify.ts>（scratchpad 脚本：无凭据降级→cur 边界→L2 真 lazy 生成→视图契约/P5/P6/裁决#2 断言→sentinel 改验复原→D-H 清态）；4) gh run view 29943149976 / 29951617484 --json jobs（F005 首推仅 Visual 预期红；HEAD 五 job 全绿）。

**证据：** ① mock 退役：src/lib/data/mock/env-match.ts 已删；grep -rn "env-match|mockMatchPlans|matchPlanListSchema" src/ tests/ scripts/ → 仅 5 处注释退役注记零 import；mock/index.ts:13 翻牌。② P2 lazy：surface-data.ts:47-59；DB 实测「A1-A4 无凭据不抛错+空态+warn+零写库 / B1 cur=brief 不触发 / C1 plans=3 C2 {mp:3,pk:30,mc:20}」。③ P5/P6/裁决#2：实测样例 plan {cost:"待核",bars:[5,4,4,4,4,4],reach:"3万",risk:"高",people:"10 人"}；C9 20/20 候选 scorePending→match:null 与 DB 严格对账。④ 改→验→复原：D1 sentinel 经 loadMatchSurfaceData 实时可见 basis===SENTINEL，D3 复原、E 终态全表 0。⑤ 布局：git diff 693c215..0b81a18 match/index.tsx 仅供给侧变更，V5 🔒 元素 JSX 零改动。⑥ L1：next lint 0w0e / tsc exit 0 / vitest 307/307（28 文件含集成测）。⑦ visual：workbench.spec.ts:42-50 waitFor+空态硬断言×2；CI run 29943149976（F005）仅 Visual 红其余四 job 绿；HEAD run 29951617484 五 job 全绿。

### F006 — PASS

**结论：** F006（nightly-screen 例程 + scheduler 注册表化）acceptance 11 个可判点逐条 PASS。依据实物：commit a3caabe 落盘代码（scheduler.ts / routines/nightly-screen.ts / scripts/jobs/run-nightly-screen.ts）、与 M1-C 版 948d327 的逐行 diff 对比（runExclusive/ROUTINES_DISABLED/失败不炸进程/health-scan cron 与执行体逐字保持）、本机测试运行输出（tsc 0 err、lint 0 err 0 warn、vitest 307/307、nightly-screen 集成测 5/5、health-scan 回归 3/3）、dev DB L2 真机实测（authorized embedText×1 单次运行：料理次元 20 候选/3 组 draft/PlanKol 30 行可解释 reasons/actor=match 留痕；PendingAction=0 证 internal only）。D-H 已复原：测毕 Match 三表 0/0/0 + PendingAction 0 + OperationLog 0，与验收前基线一致。P4 语义核到服务层实物（generate-candidates.ts:187-197 upsert update 分支无 verdict；build-plans.ts:220-224 updateMany 只圈 draft 同事务）并经集成测 approved-不动例活证。

**复现/验证步骤：** 1. npx prisma generate && npx tsc --noEmit（exit 0）2. npm run lint（0 warn 0 err）3. npm run test:unit（307/307）4. npx vitest run tests/integration/nightly-screen.test.ts --reporter=verbose（5/5：注册表两例程 cron 逐字 / 闭环+cur≠match 圈定 / 幂等 / approved 不动 / 失败逐项目消化）5. git show 948d327:src/lib/jobs/scheduler.ts 与现版 diff 对照语义保持 6. [L2] npm run routine:nightly-screen 单次真机（embedText×1）后查 DB 产物再按 D-H 清理复原

**证据：** scheduler.ts:39-64 ROUTINES 两条目 + :102-109 循环注册；:25 NIGHTLY_SCREEN_CRON='30 2 * * *' 错峰；:73-87 runExclusive 与旧版逐字一致。nightly-screen.ts:34-37 where{cur:'match'}；:42-71 逐项目 try/catch；:46-61 OperationLog(kind=auto,actor='match',payloadJson.routine='nightly-screen') 照 health-scan.ts:47-56 先例。package.json:34 routine:nightly-screen。测试输出：「Test Files 28 passed / Tests 307 passed」；nightly-screen 集成测「5 passed (5)」含「approved 不动（P4 例程面）✓」「失败逐项目消化 ✓」。L2 真机：「✅ 1 项目（成功 1 / 失败 0）」→ DB 实测 candidates=20（scorePending 20，FR-11.6 降级预期）/plans 3 组（B recommended=true，budgetUsd null P6，reachTotal 真值）/PlanKol 30 行三段 reasons/log payloadJson={routine:'nightly-screen',candidates:20,plans:3}；PendingAction=0。清理后终态 {matchPlan:0,planKol:0,matchCandidate:0,pendingAction:0,operationLog:0}。

### F007 — PASS

**结论：** F007（match_plan/evaluate_creator 工具 + canvas ADR-28 + uiSyntax 注入）首轮验收 PASS。依据实物：代码静读（两工具 class:internal 注册、registry.ts:100 match 人格四工具、canvas-registry type 优先/工具名回退 + registerCanvasRenderer 重名拒、buildSystemPrompt 第五段 uiSyntax 注入、CopilotPanel 判定带 output）+ L1 全绿（lint 0 警告、tsc exit 0、test:unit 307/307，其中 match-tools.test.ts 13/13）+ 真机 executeTool 直调 17 项断言全过（独立夹具租户隔离并行 subagent；候选/组合生成走 P7 mock 向量零网关；evaluate_creator 走真网关 embedText，L2 最小用量共 4 次短文本，score=0.18 与 MATCH_WEIGHTS 公式可复算一致，pending 降级/EMBEDDING_MISSING/KOL_NOT_FOUND 全路径实测）+ agent:smoke/orch:smoke 回归无损。search_kols 卡零变更（输出无 type 字段走回退键，KolResultCards 本批未改动）。[L2] 可选真对话未执行（授权口径内可选项，执行链已由直调覆盖）。D-H 清理：夹具租户级联删除 + smoke 产生的 PendingAction/OperationLog 定向清除，终态 Match 三表/PendingAction/OperationLog 全 0。观察项（不影响判定）：commit message 称 18 用例、实物 13 用例，叙述性偏差已记录。

**复现/验证步骤：** 1) npx prisma generate；2) npx vitest run tests/unit/match-tools.test.ts（13/13）；3) npm run test:unit（307/307）+ npx tsc --noEmit + npx next lint；4) node --env-file=.env --import tsx scripts/test/m2a-f007-tool-verify.ts（真机直调 17 断言，自带夹具租户+自清理；含 3 次真网关 embedText）；5) npm run agent:smoke && npm run orch:smoke（回归；smoke 会留 1 条 __smoke_outbound__ PendingAction+gate OperationLog，需手动清）

**证据：** scripts/test/m2a-f007-tool-verify.ts 直调 17/17 断言过（match_plan 三口径解析/现行轮 3 组 A|B|C/成员 matchScore 降序+非空 reasons/metrics budgetUsd 恒 null；evaluate_creator reasons=["向量相似度 0%（项目画像 × 创作者画像)","受众契合 60%（来源：游戏知识库受众画像)"] pending=false score=0.18=0.7×0+0.3×0.6）；npx vitest run tests/unit/match-tools.test.ts → 13 passed；npm run test:unit → 307 passed (28 files)；tsc exit=0；next lint ✔ 0 warnings；src/lib/agent/registry.ts:100 tools 四件、:63 「你的产出形态：${uiSyntax}」；src/components/copilot/canvas/canvas-registry.tsx:50-53 type 优先路由、:24-32 受控 register 重名抛错；src/components/copilot/CopilotPanel.tsx:100 hasCanvasRenderer(toolName, part.output)；search-kols.ts SearchKolsOutput 无 type 字段；清理终态 {matchPlan:0,planKol:0,matchCandidate:0,pendingAction:0,operationLog:0}

### F008 — PASS

**结论：** F008 侧栏徽标服务接真（U4）首轮验收：基于实物（代码 diff、route handler 直调、集成测/全量 vitest 实跑、CI run 状态、重生前后基线图逐像素对账、psql 库态查询）逐条判定 9 项 acceptance 全 PASS。API /api/nav-badges 计数正确且联动（0→+1 pending→复原三态实证）；NAV_BADGE_MOCK 退役零代码残留、sidebar 布局零变更（diff 仅换数据源+fetch 逻辑）；计数 0 隐藏/洞察退役由代码与基线实拍双重确认；13 页基线全量对账成立（12 页 darwin+linux 重生，agent-canvas 位于 /preview 无侧栏无漂移字节不变，1500px 借绿已按 §4.2 强制重生处置）；lint/tsc/test:unit(307) 本机绿 + CI@HEAD 五 job 全 success，test:visual 由 READINESS 实测 13/13（端口纪律）。1 条非阻断观察 OBS-F008-1：revalidate 失败沿用旧真值与 acceptance 字面「全隐藏」有偏差，但 D2 实质（不显假数不抛错）成立且首载失败路径严格符合，代码/commit 均明示该选择——留 Planner 知悉，不阻断。

**复现/验证步骤：** 1) npx prisma generate && npx tsc --noEmit && npm run lint && npm run test:unit（28 files/307 全绿）2) npx vitest run tests/integration/nav-badges.test.ts（2/2）3) node --env-file=.env --import tsx 直调 route GET：dev tenant 返回 {today:0,projects:4}，+1 pending→{today:1,…}，删后复原 4) grep -rn NAV_BADGE_MOCK 全仓代码零残留（仅 features.json acceptance 文本）5) git show 46c0359 -- src/components/sidebar/index.tsx 核布局零变更 6) 对比 46c0359~1 与 46c0359 的 campaigns-darwin.png（3/4/2→隐藏/4/隐藏）7) gh run view 29951617484 核 CI 五 job success 8) psql 核 D-H 清态（Match 三表/PendingAction/OperationLog 全 0）

**证据：** route 直调：status 200 {"today":0,"projects":4} → +1 pending {"today":1,"projects":4} → cleanup 复原；badge-counts.ts:19-22 两条 tenantId count；sidebar/index.tsx:27-33 badgeFor（>0 才显、/insight 不映射=退役）+ :48-66 useEffect([pathname]) mount+路由 revalidate + catch 吞错；tests/integration/nav-badges.test.ts 2/2 passed（pending 计/executed 不计/租户隔离/空租户 0）；tsc exit 0、lint 0/0、vitest 307/307；CI@3d93f72 run 29951617484：Lint/Typecheck/Unit+integration/Visual regression/Build 全 success；5ed8dfc 重生 12 张 linux 基线；基线实拍：46c0359~1 今天=3/项目=4/洞察=2 → 46c0359 今天隐藏/项目=4/洞察隐藏；psql：MatchPlan|PlanKol|MatchCandidate|PendingAction|OperationLog=0，Tenant=1(dev)，Project=4；READINESS 报告 §5 test:visual 13/13 passed（darwin 23.9s）

### F009 — PASS

**结论：** M2-A-MATCH F009 首轮验收（隔离 evaluator subagent，fan-out 分片）：SW-R1 探针退役 + OBS-1 image part 迁移 + 文档翻牌，acceptance 8 项 L1 全 PASS。依据实物：磁盘文件存在性（f007-browser-check.mjs 已删、替代探针+f010 保持）、全仓 needle grep（零代码残留，余留为翻牌注记与历史审计报告）、parse.ts:88-106 FilePart 迁移实物 + src 零 type:'image' 运行代码残留、独立复跑 lint/tsc/test:unit（307/307）与 knowledge-parse 集成测试（mock LLM+真库 9/9）、architecture.md 与 agent-architecture.md 翻牌逐条 grep 代码实物反证（MATCH_WEIGHTS 0.7/0.3、registerCanvasRenderer type 路由、uiSyntax 注入、ROUTINES 双例程 cron、MATCH_PLAN_NOT_APPROVED 真判定、三表 schema）。[L2] 真 vision 未执行——spec §4 授权范围仅 embedText/真对话，acceptance 自身写明留授权，合规非缺陷。D-H 退出态实测 Match 三表/PendingAction/OperationLog 全零行。

**复现/验证步骤：** 关键验证命令：ls scripts/test/ | grep f007；grep -n "f007" package.json（应零命中）；grep -rn "f007-browser-check\|f007:browser" . --exclude-dir={node_modules,.next,.git}（应仅余注记/历史报告/spec 文本）；grep -rn "type: 'image'\|type:'image'" src/（应仅 parse.ts:85 注释）；npx prisma generate && npx tsc --noEmit && npm run lint && npm run test:unit（307/307）；node --env-file=.env node_modules/.bin/vitest run tests/integration/knowledge-parse.test.ts（9/9）；git show 3d93f72 -- docs/dev/architecture.md docs/dev/agent-architecture.md 对照 src 实物 grep（MATCH_WEIGHTS/registerCanvasRenderer/你的产出形态/ROUTINES/MATCH_PLAN_NOT_APPROVED）。

**证据：** 1) ls scripts/test/ 无 f007-browser-check.mjs + git show 3d93f72 --stat（该文件 -85 行）；grep "f007" package.json → exit 1 零命中。2) 全仓 grep f007-browser-check|f007:browser：代码命中仅 m1c-readiness-f007-l1-substitute.mjs:3-7 翻牌注记；package.json:28 f010:e2e 保持。3) src/lib/knowledge/parse.ts:88-106 buildImageUserContent 返回 {type:'file',data,mediaType(兜底'image')}；src grep type:'image' 唯一命中 parse.ts:85 注释。4) tests/unit/knowledge-parse-output.test.ts:87-91 断言无 type:'image' part 且旧 image 字段零残留；vitest 9/9 passed。5) npm run lint →「No ESLint warnings or errors」；prisma generate + tsc --noEmit exit 0；npm run test:unit → 28 files 307/307 passed；tests/integration/knowledge-parse.test.ts 9/9 passed。6) 文档新鲜度实物核对：match-score.ts:20-25 MATCH_WEIGHTS{0.7,0.3}；canvas-registry.tsx:24/:49 register API+type 优先；registry.ts:63「你的产出形态」/:100 match 四件；scheduler.ts:22 '0 2 * * *'/:25 '30 2 * * *'/:39-54 ROUTINES；env-guards.ts:62/:125-127 hasApprovedMatchPlan→MATCH_PLAN_NOT_APPROVED；schema.prisma:273/:299/:321 三表。7) D-H：prisma count 实测 {"MatchPlan":0,"PlanKol":0,"MatchCandidate":0,"PendingAction":0,"OperationLog":0}。

### READINESS — PASS

**结论：** M2-A-MATCH 批次级就绪回归（spec §5 末段口径）@ HEAD 3d93f72，九道门禁全部本次自跑全绿：lint 0/0 · tsc exit 0 · test:unit 28 文件/307 tests 全过（受控重跑实证自身零残留）· build exit 0（三新路由在清单）· test:visual 13/13（伪凭据=CI 基线态，F005 lazy 静默降级当场活证）· p2:f001 12/12 · p2:f002 14/14 · p2:f004 15/15 · m1c-readiness-f007-l1-substitute 10/10 · f008:browser 严态（跑前清 Match 三表零行）12/12 · f010:e2e 真对话 6/6（canvas 实渲 20 张 KOL 卡）。f007 原样探针确认已按 U3 退役（脚本不存在 + package.json 零引用）。接手时发现前次中断运行遗留的报告草稿与 f008/f010 两处探针脚本未提交修缮：草稿叙述不采信、全部门禁从零自跑；两处修缮逐行对照产品代码独立审计（match/index.tsx:330 环节页 meta 歧义锚 / CopilotPanel.tsx:321 aside 全仓唯一 / KolResultCards.tsx:100 canvas 头独有格式），结论=校准/加严非弱化，属 Evaluator 测试域职权内产物。产物纪律：零产品代码改动（git status 仅 scripts/test/ + docs/test-reports/）；:3000 测毕已杀；DB 终态与起始清态逐项一致（Match 三表 0 / PendingAction=0 / OperationLog=0 / Handoff=1 同一批前既有行 id=cmrsw008l00009y0984ad4qbn，seed 幂等跳过故按「终态=起始态」准则保留）。L2 最小用量：真对话 ×1 + lazy embedText 事件 ×2，报告注明。观察项 3 条非 blocker（OBS-R1 首访同步生成秒级阻塞 by-design 有 F006 例程兜底 / OBS-R2 旧 id 深链空态 by-design D2 / OBS-R3 并行 fan-out 共享 dev DB 瞬时污染——依赖 D-H 清态的门禁应取证当刻复核清态）。

**复现/验证步骤：** 关键验证命令（顺序执行，:3000 需空闲）：1) npx prisma generate && npm run lint && npm run typecheck && npm run test:unit；2) node --env-file=.env --import tsx scripts/test/m2a-readiness-db-state.ts 确认 D-H 清态（Match 三表/PendingAction/OperationLog 全 0）；3) npm run build；4) set -a && source .env && set +a && export AIGCGATEWAY_BASE_URL=http://127.0.0.1:9 AIGCGATEWAY_API_KEY=probe && npx playwright test（13/13）；5) set -a && source .env && set +a && node scripts/serve-standalone.mjs &（真凭据 standalone）；6) npm run p2:f001 && npm run p2:f002 && npm run p2:f004 && node scripts/test/m1c-readiness-f007-l1-substitute.mjs；7) 清 Match 三表复现严态后 npm run f008:browser；8) npm run f010:e2e（真对话 L2）；9) 测毕 kill :3000 + 清 Match 三表/PendingAction/OperationLog，复跑 db-state 脚本核对终态=起始态。

**证据：** lint「✔ No ESLint warnings or errors」exit 0；tsc exit 0；vitest「Test Files 28 passed (28) / Tests 307 passed (307)」exit 0，跑后即测 PendingAction/OperationLog=0；build exit 0 含 /api/match/plans/[id]/approve、/api/match/refresh、/api/nav-badges（ƒ）；playwright「13 passed (23.9s)」exit 0 + webServer stderr「[match] 首访 lazy 生成失败，降级空态占位（CI 无凭据属预期）: fetch failed」；standalone 启动日志「[jobs] 例程调度已启动（health-scan @ 0 2 * * * / nightly-screen @ 30 2 * * *）」；p2:f001「12 passed, 0 failed」/ p2:f002「14 passed, 0 failed」/ p2:f004「15 passed, 0 failed」/ f007-substitute「PASS 10 / FAIL 0」/ f008「PASS 12 / FAIL 0」（严态）/ f010「6 通过 / 0 失败」+「画布渲染 ≥1 张 KOL 候选卡片（实得 20）」+「[demo-handoff] 已存在，跳过（幂等）」，各 exit 0；lazy 真网关活证=项目页访问后 Match 三表 3 plans/30 PlanKol/20 candidates（=3 组+topN20 设计值）；脚本修缮审计锚点=src/components/envs/match/index.tsx:330（「{candidates.length} 位候选」）、src/components/copilot/CopilotPanel.tsx:321（aside 全仓唯一 grep 单命中）、src/components/copilot/canvas/KolResultCards.tsx:100（「{output.count} 位候选 · 「{output.query}」」）；f007 退役=ls scripts/test/f007-browser-check.mjs「No such file」+ grep -c f008 package.json f007:browser=0；DB 终态 JSON=起始态逐项一致（Tenant=1/Project=4/Kol=2524/embedding 2524/Match 三表 0/PendingAction=0/OperationLog=0/Handoff=1 同 id/Material=0/GameKnowledge=0）；git status 终态仅 scripts/test/ 与 docs/test-reports/ 变更。

## 附注（机械记录）

- 首轮 workflow 有 8 个 subagent 因 API 连接故障（ECONNRESET / 证书验证）未产出结论，resume 重跑后全部完成；F004 与 READINESS 结论取自首轮（缓存原样）。
- READINESS 验收中对两个既有探针（f008-browser-check.mjs / f010-e2e-check.mjs）做锚点校准（断言语义零变更，逐字理由见探针内注释与 READINESS 分报告）；产品代码零改动。
- Evaluator 新增测试产物：scripts/test/m2a-f001-db-verify.ts / m2a-f001-edge-probe.ts / m2a-f007-tool-verify.ts / m2a-readiness-db-state.ts / m2a-readiness-residue-probe.ts（测试域，随本报告入库）。
- L2 用量记录（spec §4 口径）：真对话 ×2（f010:e2e）+ embedText 若干次单往返（F003/F004/READINESS 各 1-4 次，均最小用量）。
- D-H 复原：各 subagent 报告终态一致 = Match 三表/PendingAction/OperationLog 全零、Project=4、Kol=2524、Handoff=1（demo 行同 id）。
