# M4.5-AGENT-LOOP Signoff 2026-07-25

| 项 | 值 |
|---|---|
| 批次 | **M4.5-AGENT-LOOP**（agent 循环放开面：步数预算 / 循环内接力 / 准备并行度 + 四特性） |
| 阶段 | `reverifying`（`fix_rounds=1`）→ 建议 `done` |
| 被测 SHA | **`fd751ad`**（main HEAD，CI 5 jobs 全 success）；首轮被测 `dcf525b`（产品代码终态 `f13a326`），修复 commit `fd751ad` |
| 授权层级 | **L1 only**（L2 真网关 / 真实外呼 / 计费 / 生产写入**未授权**，本轮零消耗；缺口逐条登记见 §7） |
| 执行者 | 隔离 evaluator subagent（fresh context，自行从磁盘取证；`generator_handoff` / commit message / session_notes 一律按「待检验的声明」处理，不作为证据） |
| 结论 | **11 / 11 PASS · 批次可签收（can_signoff = true）** |

---

## 1. 逐 Feature 终判（首轮 + 复验机械汇总）

| id | 标题 | 首轮 | 复验 | 终判 | 依据 |
|---|---|---|---|---|---|
| F001 | loop 遥测 → OperationLog | PASS | 回归面核证 | **PASS** | 产品代码自首轮零改动（§4.1）；`agentloop:e2e` 「两次会话各落一行 loop 遥测」「遥测 personaSwitches=1 / finalAgentId=洞察」实跑绿 |
| F002 | 按人格步数预算 + maxDuration 校准 | PASS | 回归面核证 + 加码变异 | **PASS** | 同上；本轮另加**代码侧**变异 `EXTENDED_MAX_STEPS 10→9` → 3 条档位断言同时翻红（§5.2 B5），证明「registry 单一真相源」是双向绑定而非文档自证 |
| F003 | compute_roi_portfolio + 洞察追问指引 | PASS | 回归面核证 | **PASS** | 产品代码零改动；e2e「组合追问：ROI 不可横向排名并如实说明原因」绿 |
| F004 | 行动计划卡（propose_plan + PlanCard + 认可留痕） | PASS | 回归面核证 | **PASS** | 产品代码零改动；e2e「计划留痕落 OperationLog」「计划认可落一行留痕」「🔒 计划已被认可，但 outbound 仍停在 pending」绿 |
| F005 | handoff_to 循环内接力（prepareStep） | PASS | 回归面核证 | **PASS** | 产品代码零改动；e2e「接力落 Handoff 行」「视野收窄到洞察子集」「🔒 接力后调编排独占工具被拒」「越权建项目未发生」绿 |
| F006 | CopilotPanel 多人格会话 UI | PASS | 回归面核证 + 视觉门复跑 | **PASS** | 产品代码零改动；`agent-loop` / `agent-loop-mobile` 两视口本机复跑绿（§6） |
| F007 | 批量备好聚合确认面 | PASS | 回归面核证 | **PASS** | 产品代码零改动；e2e「聚合确认面列出这 2 件」「逐项确认全部成功」「副作用恰好 2 次」绿 |
| F008 | 画布渐进渲染（裁决 C） | PASS | 回归面核证 + 视觉门复跑 | **PASS** | 产品代码零改动；`agent-canvas` / `agent-loop` 基线本机复跑绿 |
| F009 | loop 测试床（mock LanguageModel 注入缝） | PASS | 回归面核证 | **PASS** | 产品代码零改动；本轮全部失败路径复现均由该测试床驱动，反证其可用性 |
| **F010** | **agentloop:e2e + 文档翻牌 + doc-freshness 扩展** | **PARTIAL** | **三条缺陷逐条实测闭环** | **PASS** | 见 §3（缺陷闭环证据）+ §5（断言活性独立验证） |
| F011 | check_compliance（compliance 人格首件） | PASS | 回归面核证 | **PASS** | 产品代码零改动；工具清单机械门覆盖（抹掉 `check_compliance` → 红，§5.2 B4） |

> 首轮六份报告（原样保留，未改写任何判定）：`M4.5-AGENT-LOOP-verify-G{1..5}.md` + `-F010-adversarial.md`。

---

## 2. 复验的取证边界（先说不采信什么）

| 声明来源 | 本轮处理 |
|---|---|
| `progress.json.generator_handoff.fixes` / `mutation_evidence` / `behavior_evidence` / `l1_status` | **全部按「待核对的声明」处理**，逐条独立复现；下文所有数字均为本轮实测，不引用其自述 |
| commit `fd751ad` message | 同上（只用于定位改了哪些文件，不用于证明改对了） |
| 首轮六份报告的结论 | 作为**复验范围的输入**，不作为「已验证」的替代；F010 三条缺陷全部自行重新复现 |
| Generator 自述的 9 条变异清单 | **不采信**；本轮自行设计 15 条变异（含 5 条他未试的绕过路径），见 §5 |

---

## 3. F010 三条缺陷的闭环证据（全部本轮实测）

### 3.1 缺陷 ① —— e2e 清理段失败路径崩溃并污染 dev 库（首轮 UPHELD）→ **已闭环**

**复现装置：** 隔离 worktree `/tmp/m45-rv1`（detached `fd751ad`，用毕 `git worktree remove`），变异**产品代码**闸门：
`src/lib/agent/execute.ts` 的 `if (tool.class === 'outbound' && !ctx.confirmationToken)` → `if (false && …)`——即首轮认定的「这条 e2e 最该抓住的红线回归」。

**同一变异，修前 / 修后对照（同一台机、同一 dev 库、同一小时内）：**

| 对照组 | 脚本版本 | 退出码 | 终端**末条**信息 | dev 租户净增（逐行 id 差集快照，非推断） |
|---|---|---|---|---|
| **修前**（`git checkout dcf525b -- scripts/test/agentloop-e2e.ts`，闸门变异不变） | 首轮版本 | 1 | Prisma `Invalid value for argument \`in[0]\`: Can not use \`undefined\` value within array` @ `agentloop-e2e.ts` 清理段首句 —— **ASSERT FAIL 文本完全消失** | **+6 logs · +2 shares · +1 handoff · +1 project** |
| **修后**（`fd751ad`） | 修复版本 | 1 | **`[agentloop-e2e] ❌ ASSERT FAIL: 2 件都落 pending`（原始首因可见）** | **0 shares · 0 handoffs · 0 projects · 0 pending**；唯一增量 = 按 append-only 口径**显式保留**的 2 行 `SHARE_CREATED` 标记 |

修后运行末尾另打印：`[清理] 按 append-only 口径**保留**（不删）SHARE_CREATED 标记留痕 2 行 …` —— 该行由清理段**最后一步**输出，其出现本身即证明前 7 步全部跑完（首因不被掩盖 + 清理不中断）。

**另加两条首轮与 Generator 都未跑过的失败路径（本轮自行设计）：**

| # | 场景 | 为什么值得测 | 实测 |
|---|---|---|---|
| **T2** | 在 run2 的 mock 脚本里插入一步**非 outbound 工具**（`compute_roi_portfolio`），闸门**不动** | 首轮对抗复核 §2.4 登记的「潜伏面」：无任何产品回归也能让 `pendingActionId` 变 `undefined` | exit 1、`❌ ASSERT FAIL: 2 件都落 pending`、清理段跑完、**dev 净增全 0**（连标记行都为 0，因未发生执行）→ 潜伏面同时被这次修复关掉 |
| **T3** | 让清理段**第一步自身抛错**（注入 `id: { in: [undefined] }` 模拟 DB 故障），叠加闸门变异 | 直接验 `cleanupStep` 的行为契约（Generator 只在源码层断言过，没跑过行为） | 打印 `⚠ 清理步骤失败（不中断后续清理）：shareLink(…)` → **后续 7 步照跑**（handoff / project / 夹具 logs 残留全 0，只有被变异那一步管的 shares 留下）→ 末条仍是 `❌ ASSERT FAIL: 2 件都落 pending` |

**裁定：** 缺陷 ① 的三个可检验要素——(a) 清理是否跑完 (b) 首因是否可见 (c) dev 残留是否归零——**逐条实测成立**，且修前/修后对照锁定自变量就是这次修复本身。**闭环。**

> 附带核实（首轮报告未列、Generator 自述中提到的同源漏点）：`ShareLink` 清理原先只按 `gateLogId ∈ createdPA`；本批备的是 `scope='quarterly'`（`projectId` 恒 null），闸门回归时 `gateLogId` 亦为 null → 两把键同时落空。本轮修前对照组实测残留 **+2 shares** 即为该漏点的实物；修后同场景 shares 归零。属实。

### 3.2 缺陷 ② —— `SHARE_CREATED` 留痕缺明文兜底（首轮 DOWNGRADED 为 soft-watch）→ **已闭环**

**核对对象：** `docs/specs/M4.5-AGENT-LOOP-spec.md` §9「Soft-watch 兜底登记 · S-M45-1」。

逐条对照 `evaluator.md` §14(c)「每条 soft-watch 必须在 progress.json / spec / signoff 中**明文写兜底**」：

| §14(c) 要件 | spec §9 实物 | 判定 |
|---|---|---|
| 写在 progress.json / spec / signoff 之一 | 写在 spec §9（另在本 signoff §8 复述） | ✅ |
| 是**处置定论**而非待决选项（首轮缺口恰在此：G5 §7 写的是「精确清理 **or** 明文登记，二选一」） | 「**处置：保留，不删。**」——单一定论，无二选一 | ✅ |
| 事实描述可核 | 「三把键全不命中，2/2 必现」——本轮实测复现：正常路径跑一次 dev 租户 `OperationLog` 114 → 116，新增 2 行 `kind=auto / ref=null / projectId=null / summary 含 SHARE_CREATED`，其余五张表 ±0 | ✅ |
| 与项目既定口径一致 | 援引 M4 signoff S5 / `project-status.md` O2 / `patterns/testing-env-patterns.md` §9，三处均为「append-only 不建议删」 | ✅ |
| **知晓后果**（patterns §9 要求「删或留必须是显式决定」） | 「会进 dev 租户活动流；本机重生含 feed/雷达的视觉基线前须把它计入」 | ✅ |
| 显式化机制（从静默泄漏 → 有账可查） | 脚本清理段末尾打印保留行数——**本轮三次运行逐次实测**：正常路径打印 `2 行`、T2（未发生执行）打印 `0 行`、闸门变异路径打印 `2 行`，计数与快照差集**逐次相符** | ✅ |

**裁定：闭环。** 该处置**不要求改删除逻辑**（照 G5 原建议去删反而与项目 append-only 既定口径冲突——首轮对抗复核已裁定），要求的是一句处置定论 + 可查账机制，两者均已落地且可机械复核。

### 3.3 缺陷 ③ —— 4 处已作废 as-built 陈述未翻牌 + doc-freshness 覆盖面盲区 → **已闭环**

**(a) 4 处文档实物逐条核（读文件，非读 diff）：**

| # | 位置 | 首轮实物（作废陈述） | 本轮实物（`fd751ad`） | 判定 |
|---|---|---|---|---|
| a | `agent-architecture.md:161` | `streamText agent loop（stepCountIs 5, …）` | `streamText agent loop（stopWhen: stepCountIs(persona.maxSteps)：深链 10 / 其余 5，…）` | ✅ 翻牌 |
| b | `agent-architecture.md:170` | `toUIMessageStreamResponse（流式 UIMessage…）` | `createUIMessageStream 包一层（写 persona_switch data part）→ createUIMessageStreamResponse` + `X-Agent-Id（= **起始**人格，P9）` | ✅ 翻牌 |
| c | `architecture.md:938`（acceptance 枚举的 §8 面内） | `**流式回传**：toUIMessageStreamResponse()…；人格身份经响应头 X-Agent-Id…` | `createUIMessageStream 包一层（写 persona_switch data part，M4.5 F006）→ createUIMessageStreamResponse()`；`X-Agent-Id（= **起始**人格；循环内接力后的当值人格走流内 data-persona_switch 事件，见 §8.4）` | ✅ 翻牌（含 X-Agent-Id 语义收窄） |
| d | `architecture.md:278`（§2 对话通道） | `SSE 流式（toUIMessageStreamResponse）` | `SSE 流式（createUIMessageStream + createUIMessageStreamResponse）` | ✅ 翻牌 |

**全仓复扫（本轮独立执行，判据比首轮更宽）：**
`grep -rn "toUIMessageStreamResponse" docs/ src/ framework/ tests/ scripts/` → 仅剩 `docs/archive/architecture_f5-v1.0-draft.md`（归档草稿，非口径权威文档）、首轮报告引文、新断言自身。
`grep -rn "stepCountIs" --include='*.md' docs/ framework/` → 权威文档中无「后跟数字字面量」的残留（命中项为 archive、spec P3 决策原文 `stepCountIs(budget)`、首轮报告引文）。
**另行加扫首轮未扫的三类同族漂移**（`maxDuration` 值 / `MAX_STEPS` 档位 / 「未实装·演进目标」标记是否已被本批实装）→ **零新发现**：`maxDuration=120` 文档四处与 `route.ts:38` 实物一致；档位表 `EXTENDED_MAX_STEPS 10` / `DEFAULT_MAX_STEPS 5` 与 `registry.ts:61/63` 一致；`architecture.md` 的「未实装」条目（§8.3 五层装配 ④ 环节上下文、§8.9「子 Agent 调用形态」、§8.6.1 承诺-兑现断言、§10.1.2 统一 envelope）经比对实物**均仍未实装**，无「已实装却仍标未实装」的批内反向漂移（`role-context/evaluator.md` 文档新鲜度 clause 判据）。

**(b) acceptance 枚举的其余翻牌面复核（读实物，不采信首轮结论）：** `architecture.md` §8.3.2 步数预算与遥测段（✅ M4.5 F001/F002）+ §8.4:996 接力段（✅ M4.5 F006/P9）+ §9.2 工具表四行新工具（`compute_roi_portfolio` :916 / `check_compliance` :917 / `propose_plan` :918 / `handoff_to` :919）+ §8.6 名册三行（orchestrator :1037 / insight :1042 / compliance :1043）+ §10.1 路由表（`/api/agent` :1364 / `plan-ack` :1365 / `/api/actions` :1366）+ `agent-architecture.md` §柱二扩容 · M4.5 段（:61）—— **逐条在场**。

**(c) 新加的机械门是否真能抓到同类漂移** —— 见 §5.2，**5 条同族变异全部翻红**，其中一条为**代码侧**变异（证明绑的是实物）。

**裁定：闭环。**

---

## 4. 未破坏其余 10 条已 PASS feature（回归面核证）

### 4.1 首要证据：修复 commit 的产品代码 diff 为**空集**

```
$ git diff --stat dcf525b fd751ad -- src/ prisma/ package.json next.config.js tests/screenshots/
(空)
```

`git diff --name-status dcf525b fd751ad` 全清单：`.auto-memory/` ×1 · `docs/dev/*.md` ×2 · `docs/specs/*.md` ×1 · `docs/test-reports/*` ×6（首轮报告落盘）· `features.json` · `progress.json` · **`scripts/test/agentloop-e2e.ts`（F010 自己的交付物）** · `tests/**`（首轮 evaluator 探针 ×6 + 本轮新增回归钉 ×2）。

→ F001–F009 / F011 的**实现代码与视觉基线在字节层与首轮被测物完全一致**。回归风险的来源被结构性排除，无需重跑首轮全套；下列交叉证据用于确认「一致」这件事本身成立。

### 4.2 交叉证据（本轮实跑）

| 门 | 命令 | 结果 |
|---|---|---|
| Typecheck | `npx tsc --noEmit`（`npx prisma generate` 先行，patterns §3） | **exit 0 / 0 error** |
| Lint | `npm run lint` | **✔ No ESLint warnings or errors**（0 error / 0 warning，§15 矩阵无需触发） |
| 单测 + 集成测 | `npm run test:unit`（空网关凭据运行） | **101 files / 1231 tests passed**（首轮基线 100/1223，净增 8 = 本轮回归钉） |
| E2E 闭环 | `npm run agentloop:e2e` | **exit 0，24 断言全绿**（逐条覆盖 F001/F003/F004/F005/F007 的红线断言，见 §1 表） |
| CI（权威） | `gh run view 30181381744`（headSha=`fd751ad`） | **5 jobs 全 success**：Lint / Unit + integration tests / Typecheck / Build / **Visual regression** |
| 视觉（本机 darwin） | `npm run test:visual` | 4 passed / 11 failed —— **与首轮失败集逐条相同**，机械成因同一（§6） |

### 4.3 未重跑的既有批次入口（如实登记，非遗漏）

`gate:smoke` / `reach:e2e` / `delivery:e2e` / `insight:e2e` / `orch:smoke` / `agent:smoke` 本轮**未跑**。理由：(1) 其被测产品代码自首轮零改动（§4.1）；(2) 其中 `gate:smoke` 会向 dev 租户新增 `__smoke_outbound__` pending 行、`insight:e2e` 每跑净增 1 行标记（M4 O2），跑它们会改变 dev 数据态并进一步污染本机视觉基线，与本轮「不改动 dev 数据态」纪律冲突。CI 的 Unit + integration job 已覆盖这些域的单测/集成测面。

---

## 5. 本轮新增 8 条回归断言的**独立**活性验证

> 不采信 Generator 自述的 9 条变异清单。本轮自行设计 **15 条变异**：8 条按「把代码/文档还原成缺陷形态」验断言承重，**5 条按 Generator 未试过的绕过路径**验断言强度，2 条验「正确收敛不该误报」。全部在隔离 worktree `/tmp/m45-rv1` 内，跑完 `git checkout` 还原，主工作树零改动。

基线：`tests/unit/e2e-cleanup-hygiene.test.ts` + `architecture-doc-freshness.test.ts` + `agent-step-budget.test.ts` = **31 tests 全绿**。

### 5.1 承重变异（期望翻红）—— 8 / 8 全红 ✅

| # | 变异 | 命中断言 | 结果 |
|---|---|---|---|
| A5 | `ShareLink` 清理改回只按 `gateLogId` | 「ShareLink 清理不依赖 gateLogId」 | **红** |
| A6 | 恢复 `pendingActionId!` 非空断言 + 去掉过滤 | 「入清理清单的 id 必经过滤」 | **红** |
| A7 | 一步 `cleanupStep` 拆成裸 `deleteMany` | 「每条 deleteMany 都包在 cleanupStep 里」 | **红** |
| A8 | `cleanupStep` 的 catch 内 `throw err` | 「catch 内不得有 throw」 | **红** |
| B1 | `agent-architecture.md:161` 还原成 `stepCountIs 5` | 「stepCountIs 后跟数字字面量」 | **红** |
| B2 | `architecture.md:938` 还原成 `toUIMessageStreamResponse()` | 「不得残留已作废流式 API 名」 | **红** |
| B3 | `agent-architecture.md:170` 还原成旧 API 名 | 同上（证明**新覆盖的那份文档**真的被读） | **红** |
| B4 | `agent-architecture.md` 抹掉 `check_compliance` | 「工具清单覆盖注册表实物」 | **红** |

### 5.2 加码变异（Generator 未试的方向）

| # | 变异 | 我的预期 | 实测 | 结论 |
|---|---|---|---|---|
| **B5** | **改代码侧**：`EXTENDED_MAX_STEPS 10 → 9`，**文档一字不动** | 若断言只是自证，会绿 | **3 条同时红**（含新增的 `agent-architecture.md` 档位断言） | 档位断言**绑的是 registry 实物**，双向成立 ✅ |
| **B7** | 把 `route.ts` 的流式 API 改回旧名（**前提失效**） | 好的断言应炸而非静默放行 | **红**（前提断言先报「route 已不再用 createUIMessageStreamResponse，本断言需重写」） | 前提自检在场 ✅ |
| **B8** | 代码与文档**同向**改（10→9 两边一起） | 正确收敛不该误报 | 红——但红在 `architecture.md` 档位表（我只改了两处文档、漏了第三处） | **不是误报**：三处文档同源钉死，说明门比预期更严 ✅ |
| **B6** | 新增一条同族陈旧句，但**不含关键词**（写「loop 最多跑 5 步（全人格一致）」，不写 `stepCountIs`） | 机械门抓不到 | **绿** | **已知边界**（关键词门抓不了任意散文），登记 soft-watch S-RV1-5，不判缺陷 |
| **A2** | 裸 `deleteMany` **跨行写**（`await prisma.handoff` ⏎ `.deleteMany(`） | 逐行正则看不见 | **绿（绕过）** | 登记 S-RV1-2 |
| **A3** | `cleanupStep` 的 catch 用 `return Promise.reject(err)` 重新抛（**无 `throw` 关键字**） | 只搜 `throw` 会漏 | **绿（绕过）** | 登记 S-RV1-1，**并做行为复证 ↓** |
| **A4** | `.filter(() => true)`（形式有 filter、实际不过滤） | 只查 `.filter(` 字面会漏 | **绿（绕过）** | 登记 S-RV1-3 |

**A3 的行为复证（把「理论绕过」变成「实测事实」）：** 在 worktree 施加 A3 + 强制首步清理失败后实跑 `agentloop:e2e` —— ⚠ 告警照打，但异常经 `Promise.reject` 逃出 `finally`，**清理链在此中断**，dev 租户实测残留 **+12 logs · +2 pending · +2 shares · +1 handoff · +1 project**，即**与原缺陷同款的损害形态**，而那条源码断言**全程绿灯**。

**这三条绕过对判定的影响 = 无。** 理由三条，逐条可核：
1. 这 8 条回归钉**不在 F010 的 acceptance 内**——acceptance 要求的是「doc-freshness 测试扩展（新工具 / 新 maxSteps 字段入断言面）」，清理段卫生钉是 Generator 自愿加码的护栏；
2. 每条钉对**它被写来防的那个缺陷形态**都翻红（§5.1 8/8），符合「活断言」的定义；
3. 绕过路径的后果 100% 限于测试环境的 dev 库卫生，零产品面、零用户可见面。
→ 按 `evaluator.md` §6，不构成 PARTIAL 要件；按 §14(c) 登记为带明文兜底的 soft-watch（§8 S-RV1-1/2/3）。

---

## 6. 视觉门（本轮 Evaluator 独占执行）

| 面 | 结果 |
|---|---|
| **CI（权威，linux）** | `fd751ad` 的 **Visual regression job = success** |
| **本机（darwin）** | `npm run test:visual` → **4 passed / 11 failed**，与首轮 G5 的失败集**逐条相同** |
| 通过的 4 张 | `agent-canvas` · `agent-loop` · `agent-loop-mobile` · `creator-drawer` —— **本批新增视觉面（PlanCard 渐进态/最终态、PendingBatchCard、接手标注、边界卡切换）全部落在这几张里，全绿** |
| 失败的 11 张成因（本轮独立复算，不引用首轮） | ①2 条为 30s 超时——等「今天没有需要你确认的事」空态等不到（`dashboard.spec.ts:15` 自注：基线态 = 零 PendingAction）②9 条像素红经 `m45-g5-diff-bbox.py` 逐像素算包围盒：**全部为同一签名** `bbox=(245,167)-(1495,817)`、`pixels_left_of_panel=387`（左块 = 侧栏待确认徽标，右块 x≥1152 = CopilotPanel 内聚合卡） |
| 自变量 | 本机 dev 租户 `pending=2`（`toolName='__smoke_outbound__'`，**更早批次 smoke 残留，非本批产物**——本轮快照实测确认），CI 库为 0 |
| 判定 | **非产品缺陷、非基线过期**：数据依赖（`project-status.md`「本地长寿命 DB 基线自然翻红属预期」既定口径）。本机 11 红与首轮**同集同因**，反证本次修复未引入任何新的视觉回归 |
| 基线文件 | `git status tests/screenshots/` → **零改动**（本轮未重生、未修改任何基线） |

---

## 7. L2 验证缺口登记（**未授权，本轮零消耗**；既不据此判 FAIL，也不得当作已验证）

| # | 缺口 | 影响面 | 解锁条件 |
|---|---|---|---|
| L2-1 | 真模型是否在**恰当时机**调 `handoff_to`、交接摘要是否天然不夹结论 | F005/F006 产品价值面 | 真网关最小用量（需授权） |
| L2-2 | 10 步深链在真网关下的**墙钟耗时**是否落在 `maxDuration=120s` 内（mock 每步 ≈0ms，离线测不出 P95） | F002 唯一未被离线覆盖的风险面 | 同上 |
| L2-3 | 长链下真模型**说出口的话**是否遵守行动承诺诚实条款（L1 只能证条款在场 + 断言组活着） | F002 诚实红线 | 同上 |
| L2-4 | `/api/agent` **HTTP 全链**（真 POST → 流式响应 → 预算/接力生效）——route 层无模型注入缝 | F001/F002/F005 | 同上 |
| L2-5 | 渐进渲染**真流表现**：aigcgateway(deepseek-v3) 是否真把 `function.arguments` 分片下发（整段下发则渐进态只闪一帧）。建议用例：抓 `/api/agent` SSE 原始流断言 `tool-input-delta` 片数 > 1 | F008 观感面 | 同上 |
| L2-6 | 洞察人格在真模型下是否真守归因追问条款 / 模型是否**选中** `compute_roi_portfolio`·`check_compliance`（工具描述可发现性） | F003/F011 产品价值面 | 同上 |
| L2-7 | 浏览器内真实渲染：`useChat` 消费 `data-persona_switch` 后面板换头/换边界卡；聚合卡「依次确认全部」的真实 fetch 链与分项结果渲染 | F006/F007 UI 动线（S-RV1-6 的同族） | 本地/staging 浏览器实测（须先清 dev 库 pending 以免与视觉门互扰） |
| L2-8 | 生产/staging 部署面（M4 / M4.5 **均未部署**，全部结论基于本机 L1 + CI） | 上线烟测 | 人类闸门触发 deploy 后 |
| L2-9 | `check_compliance` 与**真实素材解析产出**的红线配合、跨项目组合在**真实规模**（数十项目/多币种）下的体量与截断 | F003/F011 | staging 真库 |

---

## 8. Soft-watch 全量清单及逐条兜底处置（`evaluator.md` §14(c) 口径）

> 规则：每条必须有**处置定论**（不是「有问题再说」），且不得与项目既定口径冲突。登记权在 Evaluator（signoff 由 Evaluator 撰写）。

### 8.1 本批产物面（M4.5 新增）

| # | 内容 | 归属 | **兜底处置（定论）** |
|---|---|---|---|
| **S-M45-1** | `agentloop:e2e` 每跑净增 2 行 `SHARE_CREATED` 标记 `OperationLog`（`ref`/`projectId` 皆 null，三把清理键全不命中） | F010 | **已写定于 `docs/specs/M4.5-AGENT-LOOP-spec.md` §9：保留不删**（append-only 语义，同 M4 S5 / O2 / patterns §9）+ 知晓后果（本机重生含 feed/雷达基线前须计入）+ 脚本跑完报账。本轮三次运行复核计数与快照差集逐次相符 ✅ |
| **S-RV1-1** | 「`cleanupStep` catch 内不得再抛」是源码级 `throw` 关键字断言，可被 `return Promise.reject(err)` 绕过；**已行为复证等价于原缺陷**（残留 12 logs+2 pending+2 shares+1 handoff+1 project） | F010 加固面 | **不阻断本批**（非 acceptance 项，零产品面）。**兜底：** 记入 backlog 顺手项 `BL-E2E-CLEANUP-PIN`——把 `cleanupStep` 导出并加一条**行为级**单测（喂一个必抛的 fn，断言 `await cleanupStep(...)` 正常 resolve），行为断言天然免疫写法绕过。**触发时机：** 下一个改动 `scripts/test/agentloop-e2e.ts` 清理段的批次必须一并做（写入 §9 待办） |
| **S-RV1-2** | 「裸 `deleteMany` 必包 cleanupStep」是**逐行**正则，跨行写法（`await prisma.x` ⏎ `.deleteMany(`）绕过 | F010 加固面 | 同 S-RV1-1 归入 `BL-E2E-CLEANUP-PIN`；行为级断言落地后本条自然消解。**在此之前的兜底 = 本条已明文登记**，下一个 evaluator 复核清理段时按此清单逐条比对（不依赖记忆） |
| **S-RV1-3** | 「id 必经 `.filter(`」查的是字面量，`.filter(() => true)` 绕过 | F010 加固面 | 同上归入 `BL-E2E-CLEANUP-PIN` |
| **S-RV1-4** | 修复后 `ShareLink` 清理 = 「跑前 id 基线差集 + `createdAt >= runStartedAt`」，**爆炸半径扩大**：同一时间窗内由**其它进程**在 dev 租户创建的 ShareLink 也会被删（原版本只删 `gateLogId ∈ createdPA`） | F010 | **接受，不改**——(a) 只作用于 dev 租户 + 测试脚本；(b) 项目纪律本就是「验收期同一 dev 库不并行跑 e2e」（首轮编排已按此分两波）；(c) 换成更窄的键会重新踩回「闸门回归时两把键同时落空」的原缺陷，得不偿失。**知晓后果并写在此处 = 显式决定**（patterns §9 口径）。**触发复议条件：** 若将来出现并行验收编排，须改为「记录本次运行创建的 share id」 |
| **S-RV1-5** | 文档机械门只抓关键词（`stepCountIs` / 旧 API 名 / 工具名 / 档位值），**抓不到不含关键词的语义漂移**（实测 B6：新增「loop 最多跑 5 步」句子不翻红） | F010 机械门边界 | **接受为已知边界**——任何关键词门都有此性质，不作为缺陷。**兜底 = 双层**：机械门守可枚举漂移 + 批末人工新鲜度复核守语义漂移（本轮已按更宽判据执行，见 §3.3(a)，零新发现）。**沉淀建议：** `framework/proposed-learnings.md` 记「新鲜度 grep 判据必须先证明能看见目标」（首轮 S-G5-9 同源） |
| **S-RV1-6** | 「持续副作用」结转：dev 租户 `OperationLog` 现 **114 行**，其中 41 行为 `SHARE_CREATED` 标记（M4 `insight:e2e` + M4.5 `agentloop:e2e` 历次运行累积） | 全局 | 与 S-M45-1 同处置（留不删）。**兜底 = 一条硬前置：** 本机重生任何含 feed/雷达的视觉基线前，先清 dev 租户 `pending` 与新增标记行，否则基线把测试残留拍进产品基线 |

### 8.2 首轮登记、本轮结转（原样保留处置，不改写首轮判定）

| # | 内容 | 归属 | 兜底处置 |
|---|---|---|---|
| O-G1-1 / O-G1-2 | 会话**异常终止 / 客户端断开**时不落遥测行（`onEnd` 不触发），遥测分母只含成功会话 | F001 | 不阻断（acceptance 枚举的三分支不含异常终止，且异常本身有 `console.error` 不静默）。**兜底：** 若后续把遥测当**账单/可用性**口径用，必须先补「异常/中断也落行」分支——记入 `BL-COST-CAP` 前置条件（该 backlog 已存在） |
| O-G2-1 | 接力后 `ctx.agentId` 仍为**起始**人格 → insight 当值时创建的 pending 记 `agentId='orchestrator'`，今天页雷达深链落回退分支 | F005 | 不阻断（acceptance 未覆盖；遥测 `finalAgentId` 是对的）。**兜底：** 下批次裁决「接力时是否派生 `ctx.agentId`」，随 `BL-AGENT-ROUTINE` 一并提出 |
| O-G2-2 | `handoff_to`「不能交接给自己」比对起始人格而非当值人格 | F005 | 当前等价成立（仅 orchestrator 持有该工具）。**兜底触发条件写死：** 一旦给第二个人格发接力权或放开链式接力，该校验必须先改——记入 `BL-AGENT-ROUTINE` 验收前置 |
| O-G2-3 | 「切换后 system 含目标人格 prompt」若用 `duty` 断言是弱断言（全人格 prompt 都列全员 duty） | 测试口径 | **已闭环于首轮**：G2 探针已改用 `isolation`/重读条款；沉淀建议入 proposed-learnings |
| O-G2-5 | 接力只在**单次 loop 内**有效，下一轮请求回落 `context.agentId` | F006 语义 | P1「时刻隔离」的自然结果，非缺陷。**兜底：** 若产品要「一句话推进」跨轮连续性，属新需求，走 backlog |
| O-G3-1 | `check_compliance` 不尊重 `ctx.db`（链头读取走全局 prisma，事务内新建红线不可见） | F011 | 非缺陷（工具只读、不参与写事务）。**兜底：** 若后续需要「事务内一致快照读」，须改共享读取器签名（涉 4 个消费方），届时立独立 feature |
| O-G3-2 | `Project.gameId` 若指向别租户 Game，核查单会吐出该租户红线 | F011 | 正常应用路径造不出该数据（`projects/create.ts:48` 带 tenantId 过滤）；同款模式在 M1-D 既有代码中更宽。**兜底：** 随 **M5 真实认证 + RLS** 一并收口（已在 project-status 下批候选中） |
| O-G3-3 | `toAiSdkTools` 对未知工具名静默 `continue`（人格名单写错名字不报错） | 全局 | 已被各工具同源断言覆盖。**兜底：** 建议加一条「全人格名单内工具名必在注册表」的通用断言，记入 `BL-E2E-CLEANUP-PIN` 同批顺手项 |
| G4-O1 | `/api/agent/plan-ack` 兜底 catch 与全站 `gateErrorResponse` 不同款（任何异常回 400 + `error.message` 原文，且不 `console.error`） | F004 | 不在 acceptance 内、无闸门含义。**兜底：** 下批次统一到 `gateErrorResponse` 口径（泛化文案 + 500 + 服务端 log），记入 backlog 顺手项 |
| G4-O2 | plan-ack 幂等是 read-then-write，无 DB 唯一约束（12 路并发实测 1 行，3 次稳定） | F004 | 最坏后果 = 审计留痕多一行，端点零执行权。**兜底：** 若后续要求强序列化，加部分唯一索引或 upsert；触发条件 = 出现重复留痕实证 |
| G4-5.5 | `pickToolRenderMode` 分支互斥依赖**顺序**而非条件 | F008 | 当前实装正确。**兜底：** 下次动该函数时加显式 `state !== 'output-available'` 护栏，把互斥从顺序约定升为条件约定 |
| S-G5-3 | 常驻面板聚合卡使 9 张 admin 视觉基线新增**数据依赖** | F007 | 本轮实测复现（§6）。**兜底：** 与 S-RV1-6 合并为同一条硬前置（重生基线前先清 dev pending）；建议写进 `web-runtime-patterns` §4.5 重生序 |
| S-G5-4 | `BATCH_EMPTY_MSG` 分支在两个生产消费点均不可达（死分支） | F007 | 空态诚实由今天页既有文案承担（判定不受影响）。**兜底：** 下批次二选一（接入或删除），记入 backlog 顺手项 |
| S-G5-5 | 「切换 → 面板重绘」缺穿透式断言（vitest 无 jsdom，三段拼接覆盖） | F006 | **兜底：** 已并入 **L2-7**（浏览器交互探针），授权后执行 |
| S-G5-6 | e2e 越权负向断言写成 `A \|\| toolErrors.length > 0`（任意工具错误都能满足） | F010 | 实际把关的是紧随其后的「项目数未变」断言（本轮实跑两条相邻断言均绿）。**兜底：** 收紧为「错误确实来自 `create_project`」，并入 `BL-E2E-CLEANUP-PIN` |
| S-G5-7 | 仓内无 `.nvmrc`，本机 Node v25.7.0 与 CI Node 20 无版本契约（patterns §4 的前提目前不成立） | 全局 | 本轮复核：本批无 jsdom 类用例（`vitest.config.ts` 固定 `environment:'node'`），风险不适用。**兜底：** 建议补 `.nvmrc`，记入 backlog 顺手项 |
| S-G5-8 | `agent-architecture.md` 零 doc-freshness 覆盖 | F010 | **本轮已闭环**（§3.3(c)，4 条新断言 + 5 条同族变异全红） |
| S-G5-9 / O-ADV-2 | 「0 findings」的 grep 判据本身需先证明能看见目标；`framework/patterns/README.md:16` 把 `testing-env-patterns.md` 的适用角色只标 **Evaluator**，而 §9 正文是写给 Generator 的（本批两条缺陷正是该分发缺口的直接后果） | 框架面 | **不改 `framework/` 其它文件**（铁律）。**兜底：** 作为框架提案交 Planner 裁定（建议角色列改 `Generator / Evaluator`，触发条件补「新增/修改 e2e、smoke、验收脚本的清理段」） |
| O-ADV-1 | 首轮 G5 探针的 tsc error（会红 CI typecheck） | 首轮产物 | **已闭环**：commit `72d7542` 作者自修，本轮 `tsc --noEmit` exit 0 + CI 全绿 |
| M4 结转 | O11（§12.6 所称 OUTBOUND_TOOL_NAMES 断言为前瞻描述债）· 洞察侧栏徽标恢复 · F003-low-1（resend SDK signal） | 跨批 | 照旧不阻断，随各自后续批次处理（`project-status.md` 已登记） |

---

## 9. 副作用核证（本轮复验自身）

| 面 | 核证方式 | 结果 |
|---|---|---|
| **dev 租户** | 复验前后用 `m45-g5-db-snapshot.ts` 逐行拍快照（OperationLog / PendingAction / ShareLink / Handoff / Project / Tenant 全 id 序列），期间产生的全部行按 id 差集精确回删（`m45-g5-cleanup-diff.ts`） | 本轮共产生 26 logs · 8 shares · 2 handoffs · 2 pending · 2 projects（正常路径 1 次 + 失败路径变异 5 次），**已全部回删**；终态 `logs=114 pending=2 share=0 handoff=0 project=4 tenants=1`，与复验前**六张表逐行 id 序列完全一致** ✅ |
| **真实外呼** | e2e 脚本自身 `delete process.env.AIGCGATEWAY_*` + 测试床 fetch 哨兵 `networkCalls===[]`；全部 vitest 以空网关凭据运行；本轮未跑任何真网关用例 | **零外呼 / 零 token / 零计费** ✅ |
| **真实公开暴露** | `create_share_link` 执行面恒 `MockShareLinkService`（`src/lib/ops/share/` 结构上无真实现分支，配非 mock provider 直接抛 `not_implemented`） | **零真实公开链接** ✅ |
| **资金动作** | 未触碰 payout / delivery 面 | 无 ✅ |
| **产品代码 / 交付脚本 / 视觉基线** | `git status --short`（主工作树） | **完全空**——未改任何 `src/` `prisma/` 配置 / `scripts/test/agentloop-e2e.ts` / `tests/screenshots/**` ✅ |
| **状态机文件** | 未改 `progress.json` / `features.json`（由编排方机械汇总）；**未 commit / 未 push** | ✅ |
| **变异 worktree** | `/tmp/m45-rv1`（detached `fd751ad`），全部 15 条变异只在其中发生，用毕 `git worktree remove --force` | `git worktree list` 仅剩主工作树 ✅ |
| **本轮唯一新增仓内文件** | 本 signoff 报告（变异驱动器 `mutate.py` 落在 `/tmp/rv1/`，**不入仓**；`test-results/` 已删） | ✅ |
| **端口 3000** | 复验前 `lsof` 确认无残活 `next-server`（首轮踩过 `reuseExistingServer` 复用旧构建的坑）；Playwright 自起自停 standalone 产物（`npm run build` exit 0 后跑） | 无残活 ✅ |

---

## 10. 环境前置核证（防已知误报，对照 `framework/patterns/testing-env-patterns.md`）

| 项 | 实测 |
|---|---|
| §3 `prisma generate` 先于 `tsc` | ✅ 已执行 |
| §4 Node 版本 vs `.nvmrc` | 本机 `v25.7.0`；**仓内无 `.nvmrc`**（S-G5-7）。本轮无 jsdom / localStorage 类用例（`vitest.config.ts` 固定 `environment:'node'`），该风险不适用 |
| §5 E2E 单例 PASS / 整组 FAIL 的隔离问题 | 不适用（视觉门单 worker 串行；本轮 e2e 单跑） |
| §6 跨 tenant SQL superuser 视角 | 不适用（单租户 dev，D4；无 RLS） |
| §9 清理段键位 | **本轮的验收对象本身**，见 §3.1 / §3.2 |
| 网关凭据 | 本机 `.env` 持真 key；全部 vitest 与探针以空凭据前缀运行（与 CI 无凭据环境同构），e2e 脚本自行 `delete` |
| SHA 对齐（`evaluator.md` §12） | 不适用：M4 / M4.5 **均未部署**，无 staging SHA 可比；上线属人类闸门（见 L2-8） |

---

## 11. 签收结论

**批次判定：PASS —— 11 / 11 feature 全部达标，可切 `done`。**

1. **F010 首轮三条缺陷逐条闭环**，且每条都由本轮**自行复现的实测对照**支撑（修前/修后同变异对照 + 两条 Generator 未跑过的失败路径 + 文档实物逐条比对 + 更宽判据的全仓复扫零新发现）；
2. **8 条新增回归断言全部为活断言**（8/8 承重变异翻红），另经 7 条加码变异（含 1 条代码侧、1 条前提失效、3 条绕过路径）标定其强度边界，3 条绕过路径已行为复证并带明文兜底登记，均不构成 acceptance 未达成；
3. **其余 10 条 PASS feature 未被破坏**：修复 commit 的产品代码 diff = 空集，L1 五门 + CI 5 job + 本机视觉失败集同构，四路交叉一致；
4. **红线零触碰**：闸门（outbound 恒停 pending，计划认可不解锁执行权）、时刻隔离（接力后调旧人格独占工具被拒 + 越权建项目未发生）、诚实条款（模型低报 outbound 被服务端复核纠正）、无批量端点——本轮 e2e 实跑逐条断言在场；
5. **零真实外呼 / 零真实公开暴露 / 零资金动作**，dev 租户逐行还原；
6. 全部 soft-watch（本批 6 条 + 结转 18 条）**逐条有明文处置定论**（§8），满足 `evaluator.md` §14(c)；
7. L2 面 9 条缺口如实登记为**未验证**（§7），不计入 PASS 依据。

> **上线前必读（人类闸门）：** M4 与 M4.5 **均未部署**。本 signoff 的全部结论基于本机 L1 + CI，不含任何 staging / 生产实测（L2-8）。

---

_本报告由隔离 evaluator subagent（fresh context）直接落盘；未修改任何产品代码 / 交付脚本 / 视觉基线 / 状态机文件，未 commit、未 push。全部复现在 `/tmp/m45-rv1` worktree 内完成并已移除，dev 租户逐行核证还原。_
