# M4.5-AGENT-LOOP 首轮验收报告 — G2 组（F002 / F005）

| 项 | 值 |
|---|---|
| 批次 | M4.5-AGENT-LOOP（`progress.json` status=verifying，fix_rounds=0） |
| 本组负责 feature | **F002**（按人格步数预算 + maxDuration 校准 + 长链诚实回归）· **F005**（handoff_to 循环内接力） |
| 被测 SHA | `dcf525b`（main HEAD，`git log --oneline -1` 实核） |
| 验收形态 | 隔离 evaluator subagent（fresh context，自行从磁盘取证；不接受任何实现过程叙述） |
| 环境 | macOS · Node v25.7.0（仓内无 `.nvmrc`）· 本地 Postgres `localhost:5434`（容器 `newkolmatrix-dev-db`）· `npx prisma generate` 已先于 tsc 执行 |
| 授权层级 | **仅 L1**（离线 / mock）。L2（真网关 / 真实外呼 / 计费 / 生产写入）未授权 → 见 §6 缺口登记 |
| 并行纪律 | 夹具租户 `test-tenant-m45-g2-<pid>` / `test-tenant-m45-g2ev-<pid>`；变异 worktree `/tmp/m45-mut-G2`（已 `git worktree remove`）；**未运行** `test:visual` / playwright / 任何浏览器任务；**零产品代码修改**、零 commit、未触碰 `progress.json` / `features.json` |

**结论：F002 = PASS · F005 = PASS**（判定依据见 §3 / §4 逐条表；观察项见 §7，均不构成 acceptance 未达成）。

---

## 1. 取证与方法

不复用「Generator 自带测试全绿」作为结论，方法是三层：

1. **读实物**：`src/lib/agent/registry.ts` · `loop.ts` · `to-ai-sdk-tools.ts` · `tools/handoff-to.ts` · `handoff.ts` · `execute.ts` · `context.ts` · `app/api/agent/route.ts` · `docs/dev/architecture.md §8.3.2`
2. **自写探针**（不依赖 Generator 断言的口径）：
   - `tests/integration/m45-g2-evaluator-probe.test.ts`（18 用例）
   - `scripts/test/m45-g2-evidence.ts`（观测值打印器——「断言绿」与「观测值是我以为的那个」是两件事）
3. **真源码变异**（检测器活性证明，D20 口径）：在 read-only worktree `/tmp/m45-mut-G2`（pin 到 `dcf525b`）上改产品代码，验断言必翻红 → §5

L1 环境前置（`framework/patterns/testing-env-patterns.md`）：§3 `prisma generate` 已先跑；§4 Node 版本项目未锁 `.nvmrc`，本组被测面为 node 环境集成测试（非 jsdom / localStorage），不适用该误报模式；§9 清态按夹具租户 + 业务标记双键清，见 §6。

### 本组跑过的命令

```
npx prisma generate                                   # OK
npx tsc --noEmit                                      # exit 0，零错误
npm run lint                                          # ✔ No ESLint warnings or errors
npx vitest run tests/unit/agent-step-budget.test.ts \
  tests/unit/agent-honesty.test.ts \
  tests/integration/long-chain-honesty.test.ts \
  tests/integration/handoff-loop.test.ts \
  tests/integration/agent-loop-testbed.test.ts \
  tests/integration/m45-g2-evaluator-probe.test.ts     # Test Files 6 passed / Tests 57 passed
npx vitest run tests/unit/architecture-doc-freshness.test.ts \
  tests/integration/loop-telemetry.test.ts             # 29 passed（支撑证据）
node --env-file=.env --import tsx scripts/test/m45-g2-evidence.ts
```

---

## 2. 关键观测值（`scripts/test/m45-g2-evidence.ts` 原始输出摘录）

```
=== [1] 逐人格步数预算实测（打不住的模型） ===
persona        registry.maxSteps  loop.maxSteps  实际步数  finishReason  出网
orchestrator   10                 10             10        tool-calls    0
strategy       5                  5              5         tool-calls    0
match          5                  5              5         tool-calls    0
reach          5                  5              5         tool-calls    0
delivery       5                  5              5         tool-calls    0
insight        10                 10             10        tool-calls    0
compliance     5                  5              5         tool-calls    0

=== [2] 9 步长链（第 1 步接力 orchestrator→insight） ===
步数=9 finishReason=stop
工具序列=["handoff_to","create_share_link",×7]
人格切换事件=[{"from":"orchestrator","to":"insight","atStep":1}]（遥测 personaSwitches=1, finalAgentId=insight）
  step1 system: 诚实锚点缺失=无 重读条款=无 长度=1192      ← 接力前 = 编排 prompt
  step2..step9 system: 诚实锚点缺失=无 重读条款=有 长度=1740 ← 接力后 = 洞察 prompt + 重读条款
pending 信封数=7 / ShareLink 落库数=0 / 出网=0

=== [3] 接力负面（隔离绕过尝试，第 2 步当值 = insight） ===
  - handoff_to:     AI_NoSuchToolError: Model tried to call unavailable tool 'handoff_to'.
  - payout:         AI_NoSuchToolError: Model tried to call unavailable tool 'payout'.
  - create_project: AI_NoSuchToolError: Model tried to call unavailable tool 'create_project'.
  payout PendingAction=0 / Project 数=1（= 夹具项目本身，越权建项目未发生）
  第 2 步模型可见工具=["propose_plan","compute_roi","compute_roi_portfolio","draft_report","create_share_link"]

=== [5] 副作用核证（夹具租户内） ===
  ShareLink 0 · PendingAction 7（全 pending 态）· Handoff 2 · OperationLog 16 · Payout 0
[清态] 夹具租户已删除
```

---

## 3. F002 逐条 acceptance 判定

| # | acceptance 条目 | 判定 | 实测证据 |
|---|---|---|---|
| 1 | registry AgentPersona 携带 maxSteps（insight/orchestrator=10 其余=5） | ✅ | `registry.ts:43` 字段 + `:61/:63` 档位常量（5/10）+ 7 人格逐个声明；实测截停点表（§2[1]）**行为层**逐人格复核，非只读常量 |
| 2 | stopWhen=stepCountIs(persona.maxSteps) 消费单一真相源（无第二处硬编码，grep 证） | ✅ | 全工作树 `rg -n "stepCountIs"`（**含未跟踪文件**）：`src/` 下唯一消费点 `loop.ts:231 stopWhen: stepCountIs(maxSteps)`，`maxSteps = loopBudget(persona) = persona.maxSteps`（`loop.ts:82-84,200`）；`src`/`scripts` 全域无 `stepCountIs(<数字>)`。行为侧交叉证：改 registry 档位 → 实际截停点同步改（§5 MUT-A） |
| 3 | maxDuration=120 | ✅ | `src/app/api/agent/route.ts:38 export const maxDuration = 120;`；全仓另两处 `maxDuration=60` 属 match/materials 路由（非本批面）。另核：`chatModel()` 无自带 fetch 超时（`AIGC_TIMEOUT_MS=15s` 仅作用于 `embedText` 直连），无隐藏的分步墙钟上限抵消该预算 |
| 4 | ≥8 步 mock 链上「工具返回 pending 而模型宣称已发出/已完成」类断言在场 | ✅ | `tests/integration/long-chain-honesty.test.ts`（8 步 outbound 链，`COMPLETION_CLAIMS` / `AWAIT_CONFIRM_HINTS` 断言组在场）；本组另加 **9 步含接力长链** 探针（Generator 未覆盖的路径）：接力后每一步 system 仍带 4 个诚实锚点，7 次 outbound 全 pending、ShareLink 落库 0 |
| 5 | 变异（去掉诚实条款 / 伪造成功文案）翻红 | ✅ | Generator 自带证据面变异 A/B/C 在场；本组另做**真源码变异**：剥离 `registry.ts` BASE_SYSTEM 诚实条款 → 5 条断言翻红（含 `agent-honesty` 单测与本组探针），见 §5 MUT-B |
| 6 | 既有 5 步人格行为零变化（回归） | ✅ | strategy/match/reach/delivery/compliance 五人格实测均恰在第 5 步截停、`finishReason=tool-calls`（§2[1]）；非接力人格 ToolSet 仍为自身子集（`loop.ts:183-186` canHandoff 分支），无接力会话 `prepareStep` 返回 undefined（零配置变更） |
| 7 | doc：architecture §8 步数预算段登记 | ✅ | `docs/dev/architecture.md §8.3.2`「步数预算与 loop 遥测（✅ M4.5 F001/F002）」含档位表（深链 10 / 常规 5）+ 单一真相源纪律 + maxDuration 120；`tests/unit/architecture-doc-freshness.test.ts` 以 `getPersona().maxSteps` 实物钉死该表（本组复跑绿） |

**口径说明（不构成扣分）：** acceptance 字面写「route.ts stopWhen=…」，实装在 `lib/agent/loop.ts`——F009 把 loop 装配从 route 抽出（带 model/ctx 注入缝），route 只留 HTTP 边界并调 `runAgentLoop`（`route.ts:97`）。单一真相源 → 请求路径的消费链完整成立，且 `architecture.md §8.3`、`agent-architecture.md` 均已按 as-built 登记。判 PASS。

---

## 4. F005 逐条 acceptance 判定

| # | acceptance 条目 | 判定 | 实测证据 |
|---|---|---|---|
| 1 | handoff_to 注册且仅 orchestrator 持有（同源断言） | ✅ | 注册于 native 表、`class:'internal'`、无 `buildHarm`（`tools/handoff-to.ts:114-124`）；`registry.ts` 7 人格中仅 orchestrator `tools` 含 `handoff_to`；行为侧复核：insight 会话内该工具**既不可见也不可执行**（可见工具 = insight 子集，调用被 `NoSuchTool` 拒，Handoff 行零增长） |
| 2 | 调用落 Handoff 行（fromAgent/toAgent/summary/artifactRef?，projectId 随 ctx） | ✅ | 本组直查 **DB 行本体**（非工具产物）：`tenantId`=夹具租户 · `projectId`=ctx 项目 · `fromAgent=orchestrator` · `toAgent=delivery` · `artifactType=deal` · `artifactRef=g2-deal-1` · `summary` 原文一致 |
| 3 | prepareStep 切换真实生效：**负向断言——切换后调旧人格独占工具被拒** | ✅ | mock-model 测试床实测：接力后 `create_project` / `handoff_to` / `payout` 三个非当值工具全部被拒（`AI_NoSuchToolError`），Project 计数不变、PendingAction(payout)=0；两道防线均经变异证伪（§5 MUT-C / MUT-D） |
| 4 | 切换后 system 含目标人格 prompt + 重读条款 | ✅ | 切换后 system 含 insight 的 **isolation**「只读结果数据，不改动执行动作」且**不含** orchestrator 的 isolation（用 isolation 而非 duty 断言：名册段含全员 duty，duty 断言是弱断言）；含「重新读取」「不要采信交接摘要里的任何金额、状态或判断结论」；长度 1192 → 1740 的跃迁与切换点一致 |
| 5 | 交接信封只传摘要 + 引用不传结论性数据（zod 限制字段） | ✅ | 投喂 `amount:987654 / status:'approved' / verdict:'我已核过…'` → **落库行序列化后三者均不出现**；`inputSchema` 仅 toAgent/artifactType/artifactRef/summary 四字段；且切换后的 system 段**不含**交接摘要正文（转述不进上下文当事实） |
| 6 | outbound 人格绑定不变（delivery 独占 payout 等，负向断言） | ✅ | ① 接力到 insight 后调 payout → 拒 + PendingAction 零增长；② 同一步内「handoff_to(delivery) + payout」→ payout 当步即拒（切换在下一步才生效，不存在「换身份 + 立刻用新工具」的同步窗口）；③ **红线复核**：接力后 insight 调 outbound `create_share_link` → 仅得 `status:'pending'` + `harm.irreversible:true`，`ShareLink` 落库 0、PendingAction 停在 `pending`——接力**不构成闸门绕过**（另核 `buildToolContext` 从不签发 `confirmationToken`，模型侧无自放行路径） |
| 7 | 遥测 personaSwitches 计数联动（F001） | ✅ | 接力会话遥测载荷实测：`agentId=orchestrator`（起始）· `finalAgentId=insight` · `personaSwitches=1`；无接力会话 `personaSwitches=0`、`finalAgentId=orchestrator` |
| 8 | 入参契约（目标人格必须合法 AgentId 且 ≠ 当前） | ✅ | `toAgent:'no_such_agent'` → zod「入参校验失败」；`toAgent` = 当前人格 → `不能把工作交接给你自己`；**失败的接力不触发切换**（本组新增负例）：自交接被拒后 `personaSwitches=[]`、后续步 system 仍是编排人格且无「【交接说明】」 |

**额外的越权面复核（acceptance 未列，本组主动加）：** 接力**不可链式**——切到 insight 后再调 `handoff_to(delivery)` 被拒，Handoff 只落 1 行、`personaSwitches` 仍为 1。即「一次会话最多换一次身份，且换出去之后收不回来」。

---

## 5. 检测器活性证明（真源码变异，worktree `/tmp/m45-mut-G2` @ `dcf525b`）

「断言全绿」在未证明断言会红之前不构成证据。四个变异体（改的是**产品代码**，不是测试证据对象）：

| 变异体 | 改动 | 期望 | 实测 |
|---|---|---|---|
| **MUT-A**（F002） | `registry.ts`：insight/orchestrator `EXTENDED_MAX_STEPS` → `DEFAULT_MAX_STEPS` | 深链档断言 + 长链断言翻红 | **7 条翻红**：`expected 5 to be greater than or equal to 8` · `expected 5 to be 10` · `expected 5 to be 9` · pending 数 8→5 等 |
| **MUT-B**（F002） | `registry.ts`：BASE_SYSTEM 行动承诺诚实条款整段删除 | 逐步 system 锚点断言翻红 | **5 条翻红**：`第 1 步 system 缺诚实条款锚点：工具真实返回成功`（Generator 长链测试 + `agent-honesty` 单测 + 本组 9 步接力探针同时红） |
| **MUT-C**（F005） | `loop.ts`：`prepareStep` 返回值去掉 `activeTools`（只切 system） | 视野收窄断言翻红 | **3 条翻红**：切换后可见工具仍是编排子集 → `expected ['confirm_brief_goal',…] to deeply equal ['compute_roi',…]`；接力后 outbound 备好数 7→0 |
| **MUT-D**（F005） | `loop.ts` `prepareStep` 恒返回 undefined **且** `to-ai-sdk-tools.ts` 执行侧硬挡移除（两道防线同时拆） | 负向断言翻红且**真副作用发生** | **8 条翻红**：`🔒 负向断言…` → `expected 0 to be greater than 0`（越权 create_project 无报错 = 真的建成了）；`子集外工具即便被直接调用也拒绝执行` → `promise resolved "{ created: true, …}"`；链式接力变为可行（Handoff 落 2 行） |

四个变异体全部按预期翻红 → F002/F005 的核心断言**不是死断言**。worktree 已 `git worktree remove --force`，`git worktree list` 无残留（`/tmp/m45-mut-G4` 属另一组）。

---

## 6. 副作用核证与清态

| 项 | 结果 |
|---|---|
| 真实外呼 | **0**。测试床全程装 fetch 哨兵（任何出网请求即抛错并记录），所有场景 `networkCalls=[]`；模型为 `ai/test` 的 `MockLanguageModelV4` |
| 真实公开暴露 / 资金动作 | **0**。`ShareLink` 落库 0（7 次调用全停 pending）· `Payout` 0 · `PendingAction` 全部 `status=pending`，无一进入 confirm/execute |
| 越权副作用 | **0**。越权 `create_project` 未建行；`payout` 连 PendingAction 都未产生（拦在执行入口之前） |
| 夹具清态 | `test-tenant-m45-g2-*` / `test-tenant-m45-g2ev-*` **零残留**（按 tenantId 级联删 ShareLink/Handoff/OperationLog/PendingAction/Project/Tenant，收尾复查 0 行） |
| dev 租户污染 | 近 3h 内 dev 租户 Handoff/PendingAction/ShareLink/Payout 新增均为 **0**（本组探针一律注入夹具 ctx，不走 `buildToolContext` 的 dev 租户） |
| 产品代码 | 未改。新增文件仅 `tests/integration/m45-g2-evaluator-probe.test.ts` + `scripts/test/m45-g2-evidence.ts` + 本报告 |

---

## 7. L2 验证缺口登记（未授权，既不判 FAIL 也不算已验证）

| # | 缺口 | 为什么 L1 到不了 |
|---|---|---|
| **L2-G2-1** | **10 步深链在真网关下的墙钟耗时是否落在 `maxDuration=120s` 内** | mock 模型每步耗时 ≈0，离线跑不出真实 P95。若真链 10 步超 120s，请求会被截断——这是 F002 唯一未被离线覆盖的风险面 |
| **L2-G2-2** | 真模型是否**在恰当时机**调 `handoff_to`、摘要是否天然不夹结论 | 脚本化 mock 只验机械面（切换/隔离/落行），不验模型判断质量 |
| **L2-G2-3** | 长链下真模型**说出口的话**是否遵守诚实条款 | 现有断言中「pending 却宣称已发出」作用于**脚本文本**：L1 能证的是「条款逐步在 system 中在场 + 断言组是活的」，模型是否遵守属 L2 |
| **L2-G2-4** | `/api/agent` HTTP 全链（真 POST → 流式响应 → 步数预算/接力生效） | route 层无模型注入缝，HTTP 驱动必须打真网关；L1 只能证 `route.ts → runAgentLoop` 的静态接线 + tsc |

---

## 8. 观察项（soft-watch，均**不**构成 F002/F005 acceptance 未达成，转 Planner 裁决）

| # | 观察 | 实测依据 | 归属建议 |
|---|---|---|---|
| **O-G2-1** | **接力后「备好」的动作归属人格错标**：`ToolContext.agentId` 恒为**起始人格**，接力不更新它 → insight 当值时创建的 pending，`PendingAction.agentId='orchestrator'`（`gate.ts:128`）；今天页雷达深链靠 `agentId→STAGE_AGENT` 反查环节，而 `orchestrator` 不在 `STAGE_AGENT` 值域（`stage-routing.ts:12`）→ 落回退分支 `project?.cur \|\| 'brief'`。同族影响：`OperationLog.actor` 与各 mock 通道 `actor` 同为起始人格 | 实测输出：`PendingAction create_share_link status=pending agentId=orchestrator`（当值为 insight）×3 | acceptance 未覆盖（F005 只要求 Handoff 行 + 切换生效 + 遥测计数，遥测的 `finalAgentId` 是对的）。建议下批次裁决：接力时是否派生 `ctx.agentId` |
| **O-G2-2** | `handoff_to` 的「不能交接给自己」比对的是 `ctx.agentId`（会话**起始**人格）而非**当值**人格。当前等价成立（仅 orchestrator 持有该工具 + 链式接力被拒）；若后续批次给第二个人格发接力权或放开链式，该校验会失准 | `tools/handoff-to.ts:87` | 潜在债，记 soft-watch |
| **O-G2-3** | 「切换后 system 含目标人格 prompt」若用 `duty` 断言是**弱断言**——`ROSTER_SECTION` 在每个人格的 prompt 里都列了全员 duty，故未切换时也成立。硬证据应取 `isolation` / 重读条款 / `visibleToolsPerStep` | Generator 的 `handoff-loop.test.ts:208` 用 duty（但同用例另有强断言，故结论不受影响）；本组探针已改用 isolation | 测试口径沉淀建议 |
| **O-G2-4** | `docs/dev/agent-architecture.md:161` 的 hello-agent 数据流图仍写 `streamText agent loop（stepCountIs 5, …）`，M4.5 后实际为按人格 5/10 | `rg -n stepCountIs` | **不属 F002**（F002 要求的 `architecture §8` 段已登记且被 doc-freshness 钉死）；属 F010「文档翻牌 + 批末新鲜度复核」面，转该组/Planner |
| **O-G2-5** | 接力只在**单次 loop 内**有效：本轮结束后，下一轮请求回落 `context.agentId`（CopilotPanel 仅在展示层从消息流解析当值人格）。这是 P1「时刻隔离」的自然结果，但产品语义上「一句话推进」的连续性依赖前端是否续传 agentId | `CopilotPanel.tsx:65-69,233-240` | 属 F006 产品语义面，本组不判定 |

---

## 9. 判定

| feature | 判定 | 一句话依据 |
|---|---|---|
| **F002** | **PASS** | 7 条 acceptance 全部有实测证据：7 人格实际截停点逐个等于 registry 声明（10/10/5×5）、全工作树仅 `loop.ts` 一处消费且无数字字面量、`maxDuration=120`、9 步含接力长链的诚实条款逐步在场、既有 5 步人格零变化、architecture §8.3.2 已登记；档位与条款各被一个真源码变异体证伪（MUT-A/MUT-B 共 12 条断言翻红） |
| **F005** | **PASS** | 8 条 acceptance 全部有实测证据：仅 orchestrator 持有、Handoff 行按 DB 实查字段齐全且结论性字段被 zod 剥离、切换后视野/system 双切且旧人格独占工具三种越权尝试全被拒（零副作用）、outbound 人格绑定不变且**接力后 outbound 仍只得 pending 信封**（红线未被绕开）、遥测 personaSwitches 联动、入参契约含自交接与非法人格两负例；两道隔离防线经 MUT-C/MUT-D 证伪（拆掉即出现真实越权副作用） |

---

*报告由隔离 evaluator subagent（G2 组）在 fresh context 下产出，结论不接受协商改写。产出物：本报告 + `tests/integration/m45-g2-evaluator-probe.test.ts` + `scripts/test/m45-g2-evidence.ts`。*
