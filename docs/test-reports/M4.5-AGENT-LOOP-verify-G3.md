# M4.5-AGENT-LOOP 验收报告 — G3（F003 / F011）

| 项 | 值 |
|---|---|
| 批次 | M4.5-AGENT-LOOP（首轮 verifying，fix_rounds=0） |
| 验收范围 | **F003** compute_roi_portfolio + 洞察追问指引 · **F011** check_compliance（compliance 人格首件） |
| 被测 SHA | `dcf525b`（main HEAD，验收期间产品代码零改动，见 §6 副作用核证） |
| 验收人 | Andy/evaluator-subagent（G3，隔离上下文） |
| 日期 | 2026-07-25 |
| 授权层级 | **仅 L1**（离线 / 本地库 / mock）。L2（真网关、真实外呼、计费、生产写入）未授权 → §5 登记为验证缺口 |
| 环境 | macOS · 本地 Postgres `localhost:5434`（容器 `newkolmatrix-dev-db`）· `npx prisma generate` 先行 · vitest 4.1.10 |
| 结论 | **F003 = PASS · F011 = PASS** |

---

## 0. 取证方式（不接受任何转述）

1. 自行从磁盘读 `progress.json` / `features.json`（acceptance 全文）/ `docs/specs/M4.5-AGENT-LOOP-spec.md` / `.auto-memory/` T0+T1；
2. 通读被测实物源码：`src/lib/agent/tools/compute-roi-portfolio.ts`、`src/lib/agent/tools/check-compliance.ts`、`src/lib/agent/registry.ts`、`src/lib/agent/tools/index.ts`、`src/lib/agent/to-ai-sdk-tools.ts`、`src/lib/agent/persona-router.ts`、`src/lib/insight/metric-snapshot.ts`、`src/lib/knowledge/query.ts`；
3. **不复用** Generator 的夹具与断言，另写独立探针 `tests/integration/m45-f003-f011-evaluator-probe.test.ts`（27 断言，夹具带 `g3-<pid>` 前缀，用完清理并核证零残留）；
4. **变异测试证明测试有牙齿**：隔离 worktree `/tmp/m45-mut-G3`（`dcf525b` detached）内注入 12 个缺陷（含 1 个等价变异对照组），逐个跑测试看是否翻红，结束后 `git worktree remove`；
5. 一次性取证脚本 `scripts/test/m45-g3-tenancy-probe.ts`（租户/事务边界，非 CI 资产）。

---

## 1. F003 — compute_roi_portfolio 工具 + 洞察追问指引

### 1.1 逐条 acceptance 判定

| # | acceptance 条款 | 判定 | 实测证据 |
|---|---|---|---|
| 1 | `compute_roi_portfolio` 注册且挂 insight 人格（同源断言） | ✅ PASS | `getNativeToolNames()` 含该名（`tools/index.ts:48` 装配）；`listPersonas().filter(p=>p.tools.includes(...)).map(p=>p.id)` **恰为 `['insight']`**（registry.ts:224-230）。**另加可达性验证**：按 `loop.ts` 真实路径 `personaToolSubset(insight) → toAiSdkTools()` 构出 ToolSet，key 中确有 `compute_roi_portfolio` 且经其 `execute` 真跑通（桥接对未知名是静默 `continue`，只断言名单不足以证明模型调得到） |
| 2 | class=internal 无 buildHarm | ✅ PASS | `getTool('compute_roi_portfolio')` → `class='internal'`、`source='native'`、`buildHarm===undefined`；直调后本租户 `PendingAction` 计数 **0**、`OperationLog` **0**（不过闸门、无留痕副作用） |
| 3 | 输出 = 既有装配/纯函数产物组合（不内联重算，grep 证 + 与直算逐字相等） | ✅ PASS | 独立探针对每个项目断言 `row.facts` ≡ `loadTenantProjectSpends({tenantId})` 对应项、`row.roi` ≡ `computeRoi(...)`、`row.gaps` ≡ `attributionGaps(...)` **逐字 toEqual 相等**（夹具含 USD 100.55 + EUR 50 双币种，覆盖非 USD 排除与分位精度：`spend=100.55`、`nonUsdExcluded=[{EUR,1,50}]`）。源码 grep：含 `loadTenantProjectSpends`/`computeRoi(`/`attributionGaps(`，无 `prisma.payout`/`.aggregate(`/`Decimal` |
| 4 | 每项目含 roi（分子缺→insufficient_evidence 诚实透传）+ gaps + spend 口径 | ✅ PASS | 全部项目 `roi.roi===null` 且 `roi.basis==='insufficient_evidence'`、`gaps.gaps.length>0`；`summary.roiComputable=0`、`rankable=false`、`notRankableReason` 如实说明「不能按花费假装排效果」。无金额项目 `spend=null`（**不填 0**）、`currency=null`、`spendSource='none'` |
| 5 | 输出 JSON 往返无损 | ✅ PASS | `JSON.parse(JSON.stringify(out))` ≡ `out`；`spend` 为 `number`（无 Decimal/Date 泄漏） |
| 6 | insight systemPrompt 含归因追问条款且单测钉文案锚点 | ✅ PASS | `INSIGHT_ATTRIBUTION_CLAUSE` 导出常量（registry.ts:81-86）整段出现在 insight systemPrompt；三锚点齐备「证据不足就说证据不足」「不强行归因」「主动追问」；**人格专属**：7 人格中含该条款者 `['insight']`（不污染其余 6 位） |
| 7 | 输入契约单测（可选 projectIds 过滤，空=全项目） | ✅ PASS | `{}` 与 `{projectIds:[]}` 均得 `scope='all'` 且项目集合一致；`{projectIds:[a,b]}` → `scope='filtered'` 按请求序；非法入参 `['']` / `'x'` 均抛 `入参校验失败`；**取不到的 id 落 `missingProjectIds` 不静默丢** |

### 1.2 越权 / 边界（acceptance 之外的对抗面，Evaluator 自加）

| 面 | 结果 |
|---|---|
| 跨租户项目 id 混入 `projectIds` | ✅ 不泄露：邻居租户项目落 `missingProjectIds`，不进 `projects`，输出全文不含其项目名 |
| 本租户「全项目」视图 | ✅ 不含邻居租户项目 |
| `ctx.db`（事务客户端）是否被尊重 | ✅ 是：事务内新建项目对本工具可见（工具 `db = ctx.db ?? prisma` 且把 `ctx.db` 透传给装配层） |
| 全样本无 spend 真源时的 `summary.totalSpend` | ✅ `null`（缺失不伪装成零）——**此断言为本次变异测试 M3 暴露的覆盖缺口，由 Evaluator 补齐**（产品实现本身正确） |

---

## 2. F011 — check_compliance（compliance 人格首件）

### 2.1 逐条 acceptance 判定

| # | acceptance 条款 | 判定 | 实测证据 |
|---|---|---|---|
| 1 | 注册且挂 compliance 人格（tools 空数组起填充，同源断言） | ✅ PASS | `getNativeToolNames()` 含 `check_compliance`（index.ts:49）；持有者恰为 `['compliance']`，且 `compliance.tools === ['check_compliance']`（空数组首次填充）；ToolSet 可达性同 F003 方式验证通过，并确认 compliance ToolSet **不含** `compute_roi_portfolio`（人格隔离双向成立） |
| 2 | class=internal 无 buildHarm | ✅ PASS | `class='internal'`、`source='native'`、`buildHarm===undefined`；直调后 `PendingAction`=0、`OperationLog`=0 |
| 3 | 输入 projectId（+可选待查文本）→ 经既有链头读取器取 `compliance_redline`（不内联重查，grep 证） | ✅ PASS | 源码代码行含 `getKnowledgeHeads(`，**不含** `gameKnowledge.findMany` / `supersededById`；实测：被 `supersededById` 取代的 v1 旧红线不出现、`audience` 类知识不混入，输出仅 `[v2 现行红线]`。入参支持 id/publicId/**slug** 三口径（slug 实测命中） |
| 4 | 输出结构化核查单（红线条目 + 逐条 Material 溯源引用） | ✅ PASS | 每条含 `knowledgeId/content/confidence/sources[]/unsourced`；本租户素材 `fileName` 正确回填，**跨租户不可见素材 `fileName=null` 而非编造文件名**，且输出全文不含邻居租户素材名 |
| 5 | 无红线知识 → 空态诚实（「暂无红线知识」明示，不编造） | ✅ PASS | 未关联游戏 → `items=[]`、`gameId=null`、note 含「暂无红线知识…**不得据此判定**「合规通过」」；有游戏无知识 → 同款诚实空态；`verdict` 恒 `'not_judged'`（传了待查文案也不给判定），note 说明为何不判定 |
| 6 | 输出 JSON 往返无损 | ✅ PASS | 往返 `toEqual` 通过（无 Date 泄漏）；**待查文案正文不回传**（只记 `textLength`，含 CJK/emoji 长度口径一致），输出全文不含文案原文 |
| 7 | 输入契约单测 | ✅ PASS | `{}` / `{projectId:''}` 抛 `入参校验失败`；**跨租户项目 id 抛「项目不存在」**（不返回空清单冒充「无红线」——这点很关键，空清单冒充等于变相说「没红线」） |
| 8 | 范围注明：不强制嵌入 create_share_link / draft 流程 | ✅ PASS | 代码 grep：`create-share-link.ts` / `draft-report.ts` / `email-drafting.ts` / `propose-plan.ts` 均无 `check_compliance` / `checkCompliance` 引用；文档同源：`docs/dev/agent-architecture.md:219` 明写「已立工具但**未强制嵌入** draft/share 流程——自动接线留后续裁决」 |

---

## 3. 变异测试（证明上述「全绿」不是测试没牙齿）

隔离 worktree `/tmp/m45-mut-G3`（`dcf525b`），每次只注入一个缺陷 → 跑 `compute-roi-portfolio.test.ts` + `check-compliance.test.ts` + 本探针 → 还原。

| # | 注入的缺陷 | 结果 |
|---|---|---|
| M1 | 静默抹掉 `notRankableReason`（诚实说明消失） | **KILLED**（2 failed / 37 passed） |
| M2 | 用花费凑 ROI 分子（`reach/conversions/actualExposure = spend`，强行归因） | **KILLED**（5 failed / 34 passed） |
| M3 | 无真源时 `totalSpend` 填 0（缺失伪装成零） | 初测 **SURVIVED** → Evaluator 补断言后 **KILLED**（见 §1.2；产品实现正确，属测试覆盖缺口，已由本探针闭合） |
| M4 | 取不到的项目 id 静默丢弃 | **KILLED** |
| M5 | 请求 id 不再按本租户事实过滤（跨租户越权） | **KILLED** |
| M6 | 绕过链头读取器直查 `gameKnowledge.findMany`（旧红线混入） | **KILLED**（3 failed） |
| M7 | 空态谎报「未发现红线问题，合规通过」 | **KILLED** |
| M8 | 溯源素材查询丢掉租户过滤（跨租户文件名泄露） | **KILLED** |
| M9 | 取不到素材名时编造 `合规素材.pdf` | **KILLED** |
| M11 | 人格名单里工具名拼错（桥接静默跳过，模型实际调不到） | **KILLED**（3 failed） |
| M12 | compliance 人格 tools 回退为空数组 | **KILLED**（3 failed） |
| M10 | **对照组**：等价变异（`void text;`，不改行为） | SURVIVED（预期——证明不是任何改动都翻红，测试无假阳性噪声） |

结论：F003/F011 的诚实语义（不凑数 / 不编造 / 缺失不填零 / 空态不谎报）、口径复用（链头、装配层、纯函数）、人格绑定与租户边界**均有活着的断言守护**。

---

## 4. 命令输出摘录

```
$ npx prisma generate                       # 先于 tsc（环境前置，见 testing-env-patterns）
✔ Generated Prisma Client

$ npx vitest run tests/integration/compute-roi-portfolio.test.ts \
                 tests/integration/check-compliance.test.ts
 Test Files  2 passed (2)
      Tests  28 passed (28)

$ npx vitest run tests/integration/m45-f003-f011-evaluator-probe.test.ts   # Evaluator 独立探针
 Test Files  1 passed (1)
      Tests  27 passed (27)

$ npx vitest run <上述三个文件>
 Test Files  3 passed (3)
      Tests  55 passed (55)

$ npx tsc --noEmit ; echo $?
0

$ npx next lint --file tests/integration/m45-f003-f011-evaluator-probe.test.ts
✔ No ESLint warnings or errors

$ npx vitest run tests/unit/architecture-doc-freshness.test.ts      # 文档新鲜度（工具表对实物）
 Test Files  1 passed (1)   Tests  17 passed (17)

$ npx vitest run tests/unit/agent-step-budget.test.ts tests/unit/agent-honesty.test.ts \
                 tests/unit/tool-module-cycles.test.ts tests/integration/loop-telemetry.test.ts
 Test Files  4 passed (4)   Tests  39 passed (39)      # 邻接面（registry/人格/模块图）无回归
```

> 说明：按并行纪律，本组**未运行** `npm run test:visual` / Playwright / 任何浏览器任务（浏览器门本轮由另一组独占）。F003/F011 本批无 UI 面（两者均为 internal 工具，画布渲染归 F008）。

---

## 5. L2 验证缺口登记（未授权，既不判 FAIL 也不视为已验证）

| # | 缺口 | 为什么 L1 到不了 | 触发方式（待授权） |
|---|---|---|---|
| G3-L2-1 | 洞察人格在**真实模型**下是否真的遵守归因追问条款（证据不足时不凑数、主动指出补哪项证据） | L1 只能钉 systemPrompt 文案锚点在场，钉不了模型行为 | 真网关跑 insight 会话，人工判读是否出现「用花费替代分子」的表述 |
| G3-L2-2 | 真实对话中模型是否会**选中** `compute_roi_portfolio` / `check_compliance`（工具描述的可发现性） | L1 只验注册/可达/直调正确性，不验模型选择 | 真网关多轮会话观察 tool-call 序列 |
| G3-L2-3 | `check_compliance` 与**真实素材解析产出**的 `compliance_redline` 配合（真实红线文本质量、溯源完整度） | 本地夹具红线为人造文本 | 真素材上传 → 策略 Agent 解析 → 再跑核查单 |
| G3-L2-4 | 跨项目组合在**真实规模数据**（数十项目 / 多币种）下的输出体量与截断表现 | 本地夹具仅 2-4 项目 | staging 真库 |

---

## 6. 副作用核证（并行纪律）

| 项 | 核证结果 |
|---|---|
| 产品代码改动 | **零**。`git status` 仅显示未跟踪的测试产物；`src/` `prisma/` 配置 文档基线 视觉基线均未修改 |
| 本组新增文件 | `tests/integration/m45-f003-f011-evaluator-probe.test.ts`（回归资产）· `scripts/test/m45-g3-tenancy-probe.ts`（一次性取证脚本，非 CI）· 本报告 |
| git 操作 | **未 commit / 未 push**；未改 `progress.json` / `features.json` |
| DB 夹具隔离 | 全部带 `g3-<pid>` / `evaltenant-g3-<pid>` 唯一前缀；`afterAll` 清理 + 探针内自校验「残留=0」 |
| DB 零残留复核 | 查询含 `g3` / `evaltenant` / `test-tenant-m45-*` / `g3scratch` 的租户 → `[]`；库内租户总数 = **1**（仅 dev 租户） |
| worktree | `/tmp/m45-mut-G3`（唯一路径）已 `git worktree remove --force`，目录不存在；未触碰其他组的 G2/G4 worktree |
| 浏览器 / 端口 3000 | 未触碰 |

---

## 7. 观察项（不影响判定，建议入 soft-watch）

| # | 观察 | 事实依据 | 定性 |
|---|---|---|---|
| O-G3-1 | `check_compliance` **不尊重 `ctx.db`**：链头读取走全局 prisma（`getKnowledgeHeads(gameId, kinds)` 签名无 db 参数，M1-D 设计），事务内新建的红线对本工具不可见 | 实测 `scripts/test/m45-g3-tenancy-probe.ts` ①：事务内建红线 → 工具读到 `items=[]` | 非缺陷：工具只读、不参与写事务；acceptance 只要求「经既有链头读取器取」，实物照做。若后续有「事务内一致快照读」需求，需改共享读取器签名（涉及 4 个消费方） |
| O-G3-2 | 若 `Project.gameId` 指向**别租户**的 Game，核查单会吐出该租户红线（`getKnowledgeHeads` 只按 gameId 过滤） | 实测同脚本 ②：A 租户视角读到 B 租户红线 = true | 非本批缺陷：① 正常应用路径造不出这种数据（`lib/projects/create.ts:48` 解析 gameId 带 `tenantId` 过滤）；② 同款模式在 M1-D 既有 `knowledge-context.ts`（prompt 知识注入）中更宽（连 project 查询都不带 tenantId），F011 反而更严；③ 当前单租户 dev（D4）。建议随 M5 真实认证 + RLS 一并收口 |
| O-G3-3 | `toAiSdkTools` 对未知工具名是静默 `continue`——人格名单写错名字不会报错，只会让模型「看不见」该工具 | to-ai-sdk-tools.ts:41；变异 M11 靠同源断言才抓到 | 已被 F003/F011 的同源断言 + 本探针的 ToolSet 可达性断言覆盖；建议后续给全人格加一条「名单内工具名必在注册表」的通用断言，避免每件新工具各写一遍 |

---

## 8. 结论

| feature | 结论 | 一句话依据 |
|---|---|---|
| **F003** | **PASS** | 7 条 acceptance 全部有独立实测证据：工具注册/insight 人格同源且模型链路真可达、internal 无 buildHarm 零 PendingAction、facts/roi/gaps 与装配层及两纯函数逐字相等（含双币种精度）、分子缺一律 `insufficient_evidence` 且 `rankable=false` 附诚实原因、JSON 往返无损、归因追问条款为 insight 专属且钉死锚点、输入契约含空=全项目与 missing 如实回报；11 个非等价变异体全部被测试杀死。 |
| **F011** | **PASS** | 8 条 acceptance 全部有独立实测证据：compliance 空数组首次填充且工具真可达、internal 无 buildHarm、经链头读取器取数（旧版红线与他类知识均不混入，grep 证无内联重查）、逐条 Material 溯源且不可见素材不编名字、无红线/无游戏均明示「暂无红线知识·不得据此判定合规通过」、verdict 恒 not_judged 且不回传文案正文、输入契约含 slug 口径与跨租户拒绝、范围声明与代码/文档实物一致；相关变异体（绕链头、谎报合规通过、编造文件名、租户过滤丢失、人格空数组）全部被杀死。 |
