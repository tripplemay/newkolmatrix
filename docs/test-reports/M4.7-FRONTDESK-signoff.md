# M4.7-FRONTDESK 复验汇总（reverifying · round 1）

> # ⛔ 不予签收
>
> **11 条终判：7 PASS · 4 PARTIAL（F006 / F007 / F009 / F010）。**
> 签收条件（11/11 PASS）未达成 —— `progress.json.docs.signoff` **必须保持 null**，
> 四条 PARTIAL 的 feature 在 `features.json` 中改回 `pending`，批次回 `fixing`。
> 本文件是「首轮 + 两轮修复」的机械汇总，**不是** signoff。

| 项 | 值 |
|---|---|
| 批次 | M4.7-FRONTDESK（`progress.json` status=`reverifying`，fix_rounds=1） |
| 被测 SHA | **`ba7feed`**（main HEAD，工作树对 `src/` `prisma/` `docs/dev/` 零改动）；首轮被测 `958c5e4` |
| 执行者 | Andy/evaluator-subagent（隔离上下文 · 复验波次，独占本机） |
| 授权层级 | **L1 only**（L2 未授权、未执行 —— 见 §6） |
| 日期 | 2026-07-26 |
| 结论 | **7 PASS · 4 PARTIAL**；产品红线未被击穿；两条产品机制经我独立实测**真实闭合** |

**取证纪律：** 全部结论出自磁盘实物、本轮实跑输出、真浏览器实测与我自行设计的 15 条变异。
`progress.json.generator_handoff`、commit message、`session_notes`、代码注释一律**只作被检验的声明**——
本轮就有三处声明被实测证伪（§3 RV-1 / §3 RV-2 / §5 R-8）。
未修改任何产品代码、未改视觉基线、未 commit / push、未改状态机文件。

---

## 0. 一句话结论

**首轮 11 条 PARTIAL 的缺陷绝大多数真闭环，且经我独立设计的变异逐条证活；两条产品机制（真超时闸 / 撞顶告知）我用真挂死上游与真浏览器复证，是真闭合不是纸面闭合。** 但复验抓到 **6 条新缺陷**，其中三条属编排者点名的两类高发形态：

1. **RV-1（F009，高）——「修了但断言守不住」的教科书样本**：acceptance 要的「逐表断言残留」写进去了，却被包在 `cleanupStep` 里，而 `cleanupStep` 的契约就是**吞掉异常绝不外抛**。实测把 handoff 清理步骤删掉（= 首轮 D-G4-6 的原变异）→ 脚本照样 **exit 0**，dev 库留下 2 行孤儿。只多了一行没人消费的 stderr 警告。
2. **RV-2（F007，中—高）——生产默认闸零机械覆盖**：本批头号产品机制的**生产路径**没有任何钉子。摘掉 `specialist-loop` 与 `loop` 的两个默认 `AbortSignal.timeout(...)`（保留测试注入缝）→ **1414 条全绿**。仓内每一条超时用例都注入短闸，没有一条走生产默认。
3. **RV-3 / RV-4（F010，中）—— as-built 反向漂移与「咨询抬预算」的不实陈述**：首轮 D-G4-9 点名的 4 处已翻牌，但**同一类漂移在姊妹文档里原样留着两处**（`agent-architecture.md:41` / `:178` 仍写主 loop `stopWhen: stepCountIs(persona.maxSteps)`，与同文档 `:69` 自相矛盾）；且「接力/**咨询**到深链专家时本轮预算抬升」这句在 registry 注释与 F010 亲手写的 as-built 段里都在，**实物只在接力分支抬**。

另外，编排者点名核对的 **R-8「未修，登记 soft-watch」不属实**——它在仓里**任何登记处都不存在**（§5）。

---

## 1. 方法与环境

| 手段 | 内容 |
|---|---|
| 环境前置 | `npx prisma generate` 先于 tsc/测试 ✅ · 本地 Postgres `localhost:5434` 可达 ✅ · Node v25.7.0（仓内无 `.nvmrc`，既有 soft-watch） |
| L1 全量 | `tsc --noEmit` **0 error** · `next lint` **✔ 无告警** · `vitest run` **118 files / 1414 tests 全绿**（含我方探针后 119/1416） · `next build` **EXIT=0** · `frontdesk:e2e` / `agentloop:e2e` 双绿 · `playwright test` **15 passed / 2 failed**（见 §4 RV-5） |
| 独立变异 | 隔离 worktree `/tmp/m47-rv` @ `ba7feed`，**15 条**逐条注入→跑全量→还原（`git worktree remove --force` 已执行，`git worktree list` 仅剩主树）。**不复用** G1–G4 / 对抗复核任何驱动器 |
| 真挂死实测 | 本地 HTTP 服务「收下请求永不响应」+ **真 `chatModel()` + 真 provider 链路 + 不注入任何 signal**，走生产默认闸（`scripts/test/rv/rv-timeout-wire.ts`） |
| 真浏览器实测 | 真 route 产出的 SSE **字节**（`tests/integration/m47-rv-probe.test.ts` 落盘）→ Playwright 回放给真 `CopilotPanel` → 断言 `[data-testid=budget-notice]` 可见（`tests/visual/rv-budget-notice.spec.ts`），并配**活性对照**（自然收敛字节 → 必须找不到） |
| 夹具隔离 | 租户 slug `rv-<pid>-*`；逐表清 + 逐表断言残留；收尾全库普查 |

---

## 2. 11 条终判

| feature | 首轮 | 终判 | 一句话依据 |
|---|---|---|---|
| **F001** ToolContext model 注入缝 + 受限子 loop | PARTIAL | **PASS** | D1 真闭环：越权样本换 `create_project` + 行级判据，**RV-M10（双防线同时放宽，首轮 MUT-5 原变异）→ RED（5 文件 12 条）**；首轮当时既有用例 10/10 全绿 |
| **F002** consult_specialist + 前台人格改造 | PARTIAL | **PASS** | D2 闭环：`architecture.md` §8.7 旧 isolation 串已删，全仓 `grep 只分派与汇总` 在 `docs/dev/` + `src/` **0 命中**，并改为「以 registry 为单一真相源，本节不再逐字复述」 |
| **F003** 路由降级为线索 | PARTIAL | **PASS** | D1 三处注释全部翻牌（含 `:392` 与实物相反的 remount 描述）；D2 语义注释已补；OBS-3 已按实物更正；**OBS-4 闭环：RV-M11（追加相反指令，首轮 MUT-C′ 原路径）→ RED**（整段 `toBe` 字面量，非常量拼接）。附低危卫生缺陷 RV-7 |
| **F004** ctx.agentId 派生 + 留痕归属 | PARTIAL | **PASS** | F004-D1 闭环：**RV-M9（`gate.ts` actor 写死前台，首轮 MUT-G 原变异）→ RED（2 条）**；首轮同变异 1341 条全绿。F004-D2 闭环：acceptance 原文场景（reach + `send_outreach` 经真前台全链）已入库并随 RV-M10 翻红 |
| **F005** 诚实透传 | PARTIAL | **PASS** | S-M47-G3-2 闭环：**RV-M7（工具出口 `insufficientEvidence` 恒 false，首轮 A2）→ RED(3)**、**RV-M8（清空 `insufficientReasons`，首轮 A6）→ RED**；两条首轮均为 GREEN。acceptance #5 字面项见 §4 说明（L1 不可实现，替代方案已就位 + L2 缺口登记） |
| **F006** 成本硬上限 + 撞顶诚实告知 | PARTIAL | **PARTIAL** | G3-3/4/6 与 R-1/R-6 全部闭环（撞顶告知我在**真浏览器**实测抵达用户眼前）；但 **S-M47-G3-7 未闭环**（`registry.ts:200` 仍称「咨询→抬预算」，实物只在接力分支 `budgetChain.add`）；S-M47-G3-5 落库口径仍未决 |
| **F007** 咨询失败降级 | PARTIAL | **PARTIAL** | 超时支**真闭环**（我用真挂死上游 + 生产默认路径实测 45,036ms 降级、结构化失败、留痕 1 行）；但 **RV-2：生产默认闸零机械覆盖**——摘掉两个默认 `AbortSignal.timeout` 后 **1414/1414 全绿** |
| **F008** 前台 UI 一个声音 + 协作痕迹 | PARTIAL | **PASS** | D-G4-2/3/4/5 闭环（人格名改 registry 同源、痕迹展示「问了什么」、零变化回归断言入库、darwin 基线已重生）；D-G4-1 按实物正确性判 PASS 并登记 acceptance 措辞待收口（§4） |
| **F009** 测试床嵌套 + frontdesk:e2e 全链 | PARTIAL | **PARTIAL** | D-G4-7、D-G4-8/R-5 闭环；但 **RV-1：acceptance 明列的「逐表断言残留」被 `cleanupStep` 吞掉，首轮 D-G4-6 的原变异仍 exit 0 且留 2 行孤儿** |
| **F010** 文档翻牌 + doc-freshness 扩断言 | PARTIAL | **PARTIAL** | D-G4-9 的 4 处已翻牌；但 **RV-3**（同类漂移在 `agent-architecture.md:41/:178` 原样留着，与同文档 `:69` 自相矛盾）+ **RV-4**（F010 亲手写的 as-built 段 `:82` 与 `:69` 声称「咨询抬预算」，与实物相反） |
| **F011** backlog 顺手项收口 | PARTIAL | **PASS** | **当初记录的绕过路径全部翻红**：RV-M5（`.filter(() => true)`）→ RED；D-G4-7 原变异（裸 `deleteMany`）→ RED；S-M46-6 / O-G3-3 / S-G5-6 首轮已 RED 且实物未动。新同类绕过 RV-M4/RV-M6 登记 soft-watch（源码级正则固有上限，acceptance 未要求） |

---

## 3. 新缺陷（本轮发现，附复现步骤）

### RV-1 · [F009] 「逐表断言残留」被 `cleanupStep` 吞掉 —— 断言存在但**永远不会让脚本失败**（高）

**事实：** `scripts/test/frontdesk-e2e.ts:234-251` 新增了逐表清态断言，残留非 0 时 `throw`。但它被包在 `cleanupStep(...)` 里，而 `scripts/test/cleanup-step.ts:12` 的契约明写「**吞掉自身异常并喊出来，绝不向外抛**」——这正是 F011/S-RV1-1 要求并用行为级单测钉死的语义。两条正确的纪律叠在一起，结果是**断言无法影响退出码**。

**实测（隔离 worktree `/tmp/m47-rv` @ `ba7feed`，施加首轮 D-G4-6 的原变异 M-F009-1）：**

```
# 删掉 cleanupStep('handoff(projectId = 夹具项目)', …) 这一步后：
  ✓ …13 条断言全绿…
[frontdesk-e2e] ✅ 全部断言通过（零外呼 · 零真实对外副作用）
  ⚠ 清理步骤失败（不中断后续清理）：清态断言（逐表，残留非 0 即失败）
      — 清态断言失败，仍有残留：{"logs":0,"handoffs":2,"pas":0,"projects":0}
EXIT=0                                    ← 退出码没变
```

跑后 dev 库普查 `handoff: 0 → 2`，两行 `projectId` 指向已被删除的夹具项目（软引用无 FK 的孤儿行形态，M4.6 D3 原样）。**与首轮 D-G4-6 记录的结果逐字相同**，只多了一行没有任何消费者的 stderr 警告。

acceptance 原文点名的理由是「只断言 tenant 残留 = 假信心」。当前形态是**另一种假信心**：断言在场、看得见、但守不住。

- 复现：① `node --env-file=.env scripts/test/rv/rv-census.mjs` ② 删除 `frontdesk-e2e.ts` 中 handoff 那一步 `cleanupStep` ③ `npm run frontdesk:e2e` → exit 0 ④ 再普查 → `handoff +2`
- 残留已由我手工清回 0 并核证（§7）。
- 修复方向（不代做）：清态断言不能走 `cleanupStep`——它不是「清理步骤」，是**验收步骤**。放到 `finally` 末尾用独立机制记录失败并在 `main()` 返回前决定退出码（例如置 `process.exitCode = 1`），既不破坏「清理段绝不再抛」的纪律，又能让残留真的红。

---

### RV-2 · [F007] 生产默认超时闸零机械覆盖 —— 摘掉后 1414 条全绿（中—高）

**事实：** 仓内**每一条**超时用例都注入短闸：
`timeout-and-budget-notice.test.ts:130` 传 `abortSignal: AbortSignal.timeout(300)`、`:151` 传 `consultTimeoutMs: 300`；对抗复核 `m47-adv-probe.test.ts` 的 P9 传 `consultTimeoutMs: 400`，P9c 直接调 `generateText({ abortSignal: AbortSignal.timeout(800) })`（**不经 `runSpecialistLoop`**，测的是 signal 能否穿过 `resilientFetch`，不是产品的默认接线）。
生产路径两处默认——`specialist-loop.ts:177-179` 的 `?? AbortSignal.timeout(ctx.consultTimeoutMs ?? SPECIALIST_TIMEOUT_MS)` 与 `loop.ts:287` 的 `?? AbortSignal.timeout(LOOP_TIMEOUT_MS)`——**没有任何用例走到**。

**实测：**

| 变异 | 内容（保留全部测试注入缝，只摘生产默认） | 实测 |
|---|---|---|
| **RV-M1b** | `specialist-loop`：`params.abortSignal ?? (ctx.consultTimeoutMs ? AbortSignal.timeout(ctx.consultTimeoutMs) : undefined)` | **118 files / 1414 tests 全绿（EXIT 0）** |
| **RV-M2** | `loop.ts`：`abortSignal: params.abortSignal`（测试从不给主 loop 传，故等价于只摘默认） | **118 files / 1414 tests 全绿（EXIT 0）** |
| 对照 RV-M1 | 连注入缝一起摘（不隔离） | RED（2 条）—— 证明翻红来自注入路径，不是默认路径 |

即：任何人把这两行的 `??` 默认收掉，仓内**没有一条断言会红**，产品立刻回到首轮实测的「等 undici ~301s」状态。这与本批已三次登记的「机械钉覆盖 N-1 段 = 留静默门」（S-M46-7 / D-G4-7 / F004-D1）同族，且**由本轮修复自身引入**。

**产品行为本身正确（我另行实测确认，不因缺钉子而怀疑实物）** —— `scripts/test/rv/rv-timeout-wire.ts`，真挂死上游 + 真 `chatModel()` + **不注入任何 signal / consultTimeoutMs**：

```json
{ "SPECIALIST_TIMEOUT_MS": 45000, "LOOP_TIMEOUT_MS": 110000,
  "A.elapsedMs": 45036, "A.ok": false,
  "A.failureReason": "咨询超时（专家未在时限内返回）：The operation was aborted due to timeout",
  "A.answer": "", "A.loggedRows": 1, "A.notUndiciFallback": true,
  "B.elapsedMs": 110021, "B.err": "", "B.text": "" }
```

- 复现：`node --env-file=.env --import tsx scripts/test/rv/rv-timeout-wire.ts`（约 155 秒）
- 修复方向：把默认闸做成**常量→行为双向绑定**（把常量调小到 ~1.5s 的隔离用例，或收编本文件的线级探针）。这是 F006 acceptance 对三个成本常量已经要求过的形态（「改常量值 → 实际行为同步改，双向绑定实证，非文档自证」），两个新常量没跟上。
- 附带（见 §8 soft-watch）：`B.err=""` / `B.text=""` —— **主 loop 撞墙钟时用户拿到的是完全空白的回复且无任何说明**，与 F006 修好的「步数撞顶」互为镜像。

---

### RV-3 · [F010] 同类 as-built 反向漂移在姊妹文档里原样留着两处（中）

首轮 D-G4-9 ② 指出 `architecture.md:938` 仍写「`stopWhen: stepCountIs(persona.maxSteps)` 限制 loop 步数」，而 M4.7 F006 起主 loop 的 `stopWhen` 是谓词。该处已翻牌。但**同一句话在 `agent-architecture.md` 里有两处，均未翻**：

| 位置 | 陈旧陈述 | 实物 |
|---|---|---|
| `docs/dev/agent-architecture.md:41` | `runAgentLoop（… streamText({ …, stopWhen: stepCountIs(persona.maxSteps), prepareStep, onEnd })）` | `src/lib/agent/loop.ts:286` = `stopWhen: ({ steps }) => steps.length >= currentBudget()` |
| `docs/dev/agent-architecture.md:178`（架构图） | `streamText agent loop（stopWhen: stepCountIs(persona.maxSteps)：深链 10 / 其余 5…）` | 同上 |

且**同一文档 `:69` 明写**「故主 loop 的 `stopWhen` 是谓词而非静态 `stepCountIs`」→ 文档内自相矛盾，与 D-G4-9 ② 对 `architecture.md` 的定性逐字同族。

F010 acceptance #8 规定的复核判据是「**不带左括号且含 `docs/` 范围**」——该判据若真的跑过整个 `docs/` 范围，这两处必然命中（我用 `grep -rn "stepCountIs" docs/dev/` 一次就抓到）。机械钉抓不到它们：`architecture-doc-freshness.test.ts:187` 只匹配 `stepCountIs[(\s]\s*[0-9]`，后面跟标识符时不命中（首轮已指出）。

- 复现：`grep -rn "stepCountIs" docs/dev/` 对照 `grep -n "stopWhen" src/lib/agent/loop.ts`

### RV-4 · [F010 / F006] 「咨询也抬本轮预算」是与实物相反的陈述，且写进了 F010 亲手补的 as-built 段（中）

**实物：** `loop.ts:300` `budgetChain.add(target)` **只在 `prepareStep` 的接力（handoff）分支**。咨询不入链、不抬预算（首轮 G3 P7 实测：前台咨询 insight 后仍 5 步截停，与 spec D-3 表一致）。

**四处相反陈述：**

| 位置 | 陈述 |
|---|---|
| `src/lib/agent/registry.ts:200` | 「**咨询**/接力到深链专家（insight）时，本轮预算自动抬到链上最大值」 |
| `src/lib/agent/loop.ts:93` | 「前台是常规档（5）；它接力/**咨询**到深链专家（insight，10）…」 |
| `docs/dev/agent-architecture.md:69` | 「接力/**咨询**到深链专家时本轮预算按**链上最大档位**抬升（`chainBudget`）」 |
| `docs/dev/agent-architecture.md:82` | 同上 —— **这一行正是 F010 acceptance #2 要求补的「三条 as-built 事实」中的成本上限那条** |

这是首轮 **S-M47-G3-7**（当时评为「低」，仅点了 `registry.ts` 与 `loop.ts`）**未闭环**，且它现在同时坐在权威文档的 as-built 段里 —— 与 D-G4-9 ③ 定性完全同族：**以「as-built / 代码保证」之名描述一个不成立的事实**。`fix_round1` 的处置清单里完全没有这一条。

- 复现：`grep -n "budgetChain" src/lib/agent/loop.ts`（唯一 `add` 在 :300 接力分支）对照上表四处

### RV-5 · [F008 / 基线稳定性] `en-today` darwin 基线与 append-only 测试标记行耦合，本机视觉门再次红（中—低）

`npx playwright test` 实测 **15 passed / 2 failed**：

1. `project env=match` —— **已登记**的本机限制（`workbench.spec.ts:38-42`）。我独立核实属实：dev 库项目 `xg` 有 **3 条 MatchPlan**，用例断言的是 CI 空库下的空态文案。**接受**。
2. `today dashboard`（`en-today`）—— **未登记**。逐像素比对：差异 bbox `(396,183,745,982)`、7350px，放大后是 KPI 卡「Agent 今日完成」**14 → 22**。

根因链已闭合取证：`src/app/admin/today/page.tsx:295` 的 KPI = `operationLog.count({ tenantId: dev, kind:'auto', createdAt >= startOfToday })`；实测 `{"autoToday":22,"markerToday":22,"nonMarkerToday":0}` —— **今天这 22 行全部是 `create_share_link:SHARE_CREATED` 标记行**，即 M4.5 spec §9 / S-M45-1 显式决定「append-only、留不删」的 e2e 产物。

后果：这张基线**每跑一次 e2e 或全量套件就漂一次**，重生后当天即失效。D-G4-5 的实质危害（「开发者本机跑视觉门恒红，最终会停止运行它」）对 `en-today` 依然成立。同时暴露一条**规则冲突**：S-M45-1 说标记行「留不删」，而 §4.5 重生序（S-RV1-6）说「本机重生前先清 dev 租户 pending 与 **e2e 标记行**」——两条纪律互斥，需 Planner 收口。

CI 不受影响（visual job 在空库上 `seed:projects` + `seed:visual-kols`，不跑任何 `*:e2e`，计数确定）。故**不构成 F008 的 PARTIAL 驱动**，登记为 soft-watch。

- 复现：`node --env-file=.env scripts/test/rv/rv-kpi.mjs` → `autoToday` 即 KPI 值；对照 `tests/screenshots/baseline/en-today-darwin.png` 上的数字

### RV-6 · [交付卫生] 对抗复核 R-8 声称「已登记 soft-watch」—— 仓内任何登记处都不存在（中—低）

编排者点名核对此项。逐处查证结果：

| 应登记处 | 实查 |
|---|---|
| `src/lib/agent/registry.ts` `SPECIALIST_TIMEOUT_MS` 的 docblock（:79-90） | 只讲「为什么必须有闸」，**没有一个字**说 45s 是无实测数据支撑的猜测。对照：三个成本常量在 spec D-3 与 `agent-architecture.md:82` 都明写「**数字是猜的**，无真实延迟数据」 |
| `backlog.json` | 5 条，无新增（`BL-COST-CAP` / `BL-AGENT-ROUTINE` / `BL-TOOL-STREAM-OUTPUT` / `BL-E2E-CLEANUP-PIN` / `BL-TENANT-SCOPE-PROJECTREF`） |
| `progress.json`（`generator_handoff` / `session_notes`） | 无 R-8 字样 |
| `.auto-memory/project-status.md` | 仍是 M4.6 快照，无本批 soft-watch 续记 |
| `docs/`（除对抗复核报告自身） | 0 命中 |

R-8 的实质是：R-3 的不等式（`2 × SPECIALIST_TIMEOUT_MS < LOOP_TIMEOUT_MS`）是**靠把子闸从 60s 收紧到 45s** 满足的，把「挂死不降级」的风险换成了「正常请求被误判超时」的风险，而 45s 同样无数据支撑。这个取舍**现在直接决定用户会不会在健康请求上看到「专家未在时限内返回」**，比三个步数常量更需要标注。作为对照，`gateway.ts:29` 的 `AIGC_TIMEOUT_MS` 是 env 可覆盖的（默认 15s），两个新闸是硬编码。

### RV-7 · [F003 / 交付卫生] `route.ts` 修复引入重复注释块与悬空 JSDoc（低）

```ts
// src/app/api/agent/route.ts:143-146
        // M4.7 F003 起**恒为前台**：受理人格不再由页面/客户端决定，
        // 故该头不再承载「这轮是谁在答」——专家参与经流内 consultation 痕迹呈现。
        // M4.7 F003 起**恒为前台**：受理人格不再由页面/客户端决定，该头因此不再
        // 承载「这轮是谁在答」——专家的参与经流内 consultation 痕迹呈现。
```

同一句话写了两遍（措辞略异）——F003-D2 的修复留下的复制粘贴残留。另 `:53-61`：原属 `resolveContext` 的单行 JSDoc 现在悬在新增的 `resolveContextForTest` 的 JSDoc 之上，`resolveContext` 自身失去文档注释。零功能影响，属交付卫生。

---

## 4. 首轮缺陷逐条闭环核证（含我方独立变异）

### 4.1 变异矩阵（15 条，隔离 worktree `/tmp/m47-rv` @ `ba7feed`，逐条注入→跑全量 1414→还原）

| # | 变异 | 目标 | 首轮结果 | 本轮实测 |
|---|---|---|---|---|
| RV-M1 | 摘 `specialist-loop` 全部 abortSignal（含注入缝） | F007 对照组 | — | **RED**（2） |
| **RV-M1b** | 只摘**生产默认**闸（保留注入缝） | F007 | — | **GREEN ⚠ → RV-2** |
| **RV-M2** | 只摘主 loop 生产默认闸 | F007 | — | **GREEN ⚠ → RV-2** |
| RV-M3 | 伪造完成文案（`已为您整理完毕`，锚点常量同步改，绕开 `/已(完成\|为你\|全部)/` 的「您≠你」） | F006 acceptance「伪造完成文案 → 红」 | — | **RED**（`m47-adv-probe` P7 的字面量断言接住） |
| **RV-M4** | `typeof id === 'string' \|\| true`（形式满足正则、语义恒真） | F011 S-RV1-3 同类 | — | **GREEN ⚠ → soft-watch** |
| RV-M5 | `.filter(() => true)`（**S-RV1-3 当初记录的原路径**） | F011 | GREEN | **RED** ✅ |
| **RV-M6** | 别名变量裸 `await db.project.deleteMany(...)` | F011 S-RV1-2 同类 | — | **GREEN ⚠ → soft-watch** |
| RV-M7 | 工具出口 `insufficientEvidence` 恒 false（**首轮 A2**） | F005 S-M47-G3-2 | GREEN | **RED**（3） ✅ |
| RV-M8 | 工具出口清空 `insufficientReasons`（**首轮 A6**） | F005 S-M47-G3-2 | GREEN | **RED** ✅ |
| RV-M9 | `gate.ts` `actor` 写死 `'orchestrator'`（**首轮 MUT-G**） | F004-D1 | GREEN（1341 全绿） | **RED**（2） ✅ |
| RV-M10 | 时刻隔离**双防线同时放宽**（**首轮 MUT-5**） | F001-D1 | GREEN（10/10） | **RED**（5 文件 12 条） ✅ |
| RV-M11 | 线索段追加相反指令（**首轮 MUT-C′**） | F003-OBS-4 | GREEN（15/15） | **RED** ✅ |
| **RV-M12** | 组件内重新硬编码人格名映射，只换变量名 `AGENT_LABEL → LABEL_MAP` | F008 D-G4-2 | — | **GREEN ⚠ → soft-watch** |
| RV-M13 | 撞顶判据退回宽形态（只看步数） | R-1 / R-6 | — | **RED**（4） ✅ |
| RV-M14 | frontdesk-e2e 清理段裸 `deleteMany`（**首轮 D-G4-7 原变异**） | F009 D-G4-7 | GREEN（9 passed） | **RED** ✅ |

> 变异基线：worktree 未变异时 118 files / 1414 tests 全绿，确保「红」来自变异而非环境。

### 4.2 两条产品机制的独立实测（不采信任何声明）

**① 真超时闸 —— 用真挂死的模型/上游验，不采信「已加 abortSignal」**

不是 mock、不是注入短闸：本地起「收下请求永不响应」的 HTTP 服务 → `AIGCGATEWAY_BASE_URL` 指向它 → 真 `chatModel()` → 走 `executeTool('consult_specialist')` 与 `runAgentLoop`，**全程不注入任何 signal 或 `consultTimeoutMs`**。实测 45,036ms / 110,021ms 精确落在两个 registry 常量上，降级为 `ok:false` + 可辨认的超时前缀 + `answer:''` + 留痕 1 行。**这是真闭合。** 首轮「D-4 承诺在挂死场景不可达（实为 ~301s 后才可达）」已被消除。

**② 撞顶告知 —— 验它是否真的抵达渲染层**

三段独立取证，每段都不复用上一段的结论：

| 段 | 取证 | 结果 |
|---|---|---|
| 传输层 | 真 route POST（我自建 `m47-rv-probe.test.ts`，**字面量**匹配，刻意不 import `BUDGET_NOTICE_PART` / `BUDGET_NOTICE_ANCHOR`——仓内既有断言两侧同源，改常量即两边一起改） | 响应体 4337 字节内含 `data-budget_notice` + `{"steps":5,"consultCount":0,"notice":"我没答完就到步数上限了（用满 5 步）。…"}` |
| 渲染层 | 把**上一段真 route 产出的字节**经 `page.route` 回放给真浏览器里的真 `CopilotPanel`（`tests/visual/rv-budget-notice.spec.ts`） | `[data-testid=budget-notice]` **可见**，含「我没答完就到步数上限了」+「把问题拆小一点再问我一次」 |
| 活性对照 | 换成自然收敛（末步出文本）的真字节回放 | 同一选择器 `toHaveCount(0)`，且答案正文照常渲染 → 上一条不是「页面上恒有一个 notice 元素」 |

**R-2 的原话（用户端零告知）至此真正闭合。** 这一层此前只有两条源码级 grep + 响应体字节，从未在浏览器里验过。

### 4.3 两处「acceptance 字面项与实物冲突」的处置（如实登记，不软化）

| 项 | 事实 | 我的处置 |
|---|---|---|
| **F005 #5**「e2e 链上实证：**前台输出**不含数值结论（机械断言）」 | 在 mock-model 测试床里，前台最终文本**由测试脚本作者写死**——对它做断言定义上就是同义反复，零鉴别力（首轮 G3 已如实预告）。交付的替代方案是在**工具出口层**断言（专家 answer + `insufficientEvidence`），经 RV-M7/M8 证明**有鉴别力**。`scripts/test/frontdesk-e2e.ts` 全文 `insufficientEvidence` 仍 **0 命中** | 判 **PASS**：这是 L1 能给的最强形态。字面项转 **L2 缺口**（真模型是否圆掉）。建议 Planner 在下批把该 acceptance 改写为「工具出口层机械断言 + L2 人工复测」 |
| **F008 #7**「`PersonaSwitchNote` 语义从『换人』改为『咨询』」 | 实物未改文案，改的是注释：说清该组件只服务**仍然存在的 `handoff_to` 路径**（`agentloop:e2e` 实跑 `personaSwitches = 1`，路径真实可达），咨询由 `ConsultationNote` 承担。把它改成「咨询」反而会让它对 handoff 路径**说错话** | 判 **PASS**（按实物正确性），acceptance 措辞与实物冲突**如实登记**待 Planner 收口 —— 沿用本批首轮 G2 对 F003 #6「工作区层零变化」的同款处置口径 |

---

## 5. R-1 … R-9 逐条处置核证（对抗复核残留缺口）

> 编排者的委派写作 R-1..R-8；实物里 **R-9 也存在**（对抗复核附录 B.3），一并核证。

| id | 关联 | 声称处置 | 我的独立核证 | 判定 |
|---|---|---|---|---|
| **R-1** | F006 撞顶判据过宽 | 已收口 | `loop.ts:329-333` = `steps.length >= currentBudget() && (lastStep?.toolCalls.length ?? 0) > 0`，沿用 `specialist-loop` 正确形态。**RV-M13 退回宽判据 → RED（4 条，覆盖用户面 + route 响应体 + 遥测）** | ✅ **真闭合** |
| **R-2** | F006 告知到不了用户眼前 | 已收口 | 我在**真浏览器**实测抵达渲染层（§4.2），并配活性对照 | ✅ **真闭合（本轮首次在渲染层验证）** |
| **R-3** | F007 超时预算算不拢 | 已收口（60s→45s） | `2 × 45_000 = 90_000 < 110_000` ✓，不等式由 `timeout-and-budget-notice.test.ts:107-110` 钉死 | ✅ 闭合（取值问题见 R-8） |
| **R-4** | `abortSignal` 依赖 provider 兑现 | 由探针覆盖，无需产品改动 | 前提成立：`m47-adv-probe.test.ts` / `m47-adv-route-probe.test.ts` 均已入库（`git ls-files` 命中），CI 跑 `test:unit:coverage`，`vitest.config.ts:42` include 含 `tests/integration/**`。**但 P9c 走的是 `generateText` 直调，不经 `runSpecialistLoop`** —— 它证明 signal 能穿过 `resilientFetch`，不证明产品默认接线在场（→ RV-2） | ⚠️ **部分**：线级覆盖在，产品默认接线覆盖缺 |
| **R-5** | e2e 解析未回喂 loop | 已收口 | `frontdesk-e2e.ts:71-91`：`resolveContextForTest({… agentId:'match' …})` → 断言 `=== FRONT_DESK_AGENT_ID && !== 'match'`（**输入≠期望**）→ 产物直接 `copilot: resolved` 驱动同一轮 loop。实跑绿 | ✅ 闭合 |
| **R-6** | 遥测与用户面口径分歧 | 已收口 | `loop.ts:356-359` 把同一个 `trulyTruncated` 传给遥测；`loop-telemetry.ts:112` `budgetHit = input.truncated ?? …`。**RV-M13 → 含 R-6 用例翻红** | ✅ 闭合 |
| **R-7** | route-probe 未入库 | 已收口 | `tests/integration/m47-adv-route-probe.test.ts` 已在 `ba7feed`，随 CI 跑（本轮全量 118 files 含它） | ✅ 闭合 |
| **R-8** | 45s 无真实数据支撑 | 「**未修，登记 soft-watch**」 | **登记不属实** —— 代码注释 / `backlog.json` / `progress.json` / `.auto-memory/` / `docs/` 逐处查证均无（§3 RV-6）。且 `SPECIALIST_TIMEOUT_MS` 的 docblock 恰恰**没有**沿用三个成本常量「数字是猜的」的既定写法 | ❌ **未修 + 未登记**（声称与实物不符） |
| **R-9** | `frontdesk-honesty` / `consult-failure` 漏清 Handoff | 未在 commit message 中提及 | 实物**已修**：两文件 `afterAll` 均补 `handoff.deleteMany` + `handoffs: 0` 逐表断言。连同 `consult-budget.test.ts`，S-M47-G3-6 同族三个文件全部收口。本轮跑全量套件后 dev 库孤儿 `Handoff = 0` | ✅ **闭合**（做了但没说） |

---

## 6. L2 验证缺口（未授权，未执行 —— 既不判 FAIL 也不当作已验证）

| # | 缺口 | 为什么重要 |
|---|---|---|
| L2-1 | **真模型是否照条款办**：`insufficientEvidence=true` 时前台是否真不给数值结论；撞顶时是否真说「未答完」；咨询失败时是否真说「问了没拿到」而不是自己圆一个 | F005 acceptance #5 的字面项**只能在这一层验**（§4.3）。L1 全部是 mock-model 驱动的机械面 |
| L2-2 | **真网关延迟下三个成本常量是否合理**（`MAX_CONSULTS_PER_TURN=2` / `SPECIALIST_MAX_STEPS=3` / 前台 `maxSteps=5`） | spec D-3 自陈「数字是猜的，无真实延迟数据」。本轮零延迟数据产出 |
| L2-3 | **两个超时闸取值是否合理**（`SPECIALIST_TIMEOUT_MS=45s` / `LOOP_TIMEOUT_MS=110s`） | R-8 的实质。45s 若低于 3 步子 loop 的网关 P95，健康请求会被误判超时并对用户说「专家未在时限内返回」——把一种不诚实换成另一种。**必须在真网关上取一次分布** |
| L2-4 | 真模型自主选专家的质量（会不会问错人、会不会该咨询时不咨询） | 本批产品价值的核心，L1 完全够不到 |
| L2-5 | 生产环境「单一前台」的真实体验（本批触发源就是生产手测） | 上线前应人工复测原始故障场景：项目环节页问跨环节问题，看前台是否还会把用户推走 |

---

## 7. 副作用核证（清态 / 孤儿行 / 产物）

**dev 租户逐表账目**

| 表 | 会话前 | 会话后 | 说明 |
|---|---|---|---|
| project | 4 | **4** | 归零 |
| handoff | 0 | **0** | RV-1 变异跑产生的 2 行孤儿已由我手工清回 0 并二次核证（内容：`orchestrator→match / 咨询match：现在的组合方案如何？` 与 `orchestrator→insight / 咨询insight：这个项目 ROI 如何？`，`projectId` 均指向已删夹具项目） |
| shareLink | 0 | **0** | 归零 |
| pendingAction(pending) | 0 | **0** | 归零（首轮遗留的 2 条 `__smoke_outbound__` 已在 fix 轮清除，本轮核实为 0） |
| operationLog | 138 | **142** | +4 **全部**为 `create_share_link:SHARE_CREATED` 标记（frontdesk:e2e ×2 含变异跑、agentloop:e2e ×2）。M4.5 spec §9 / S-M45-1 显式决定 append-only、留不删，**非泄漏**，但见 RV-5 |

**全库孤儿普查（tenant 已不存在的软引用行）**

`handoff 0 · operationLog 0 · pendingAction 0 · project 0`；`shareLink **2**`（时间戳 `08:50:47` / `16:42:45`，**早于本会话全部动作**，即首轮 S-M47-G3-10 登记的既有残留，未处置以免销毁他人证据）。

**夹具与工具**

- 夹具租户 `rv-<pid>-*`：探针 `afterAll` / 脚本收尾逐表清 + 逐表断言；收尾普查 `rv-* tenants = 0`、`m47* tenants = 0`
- 变异 worktree `/tmp/m47-rv` 已 `git worktree remove --force`；`git worktree list` 仅剩主树
- **产品代码 / 视觉基线 / 状态机文件零改动**：`git status --short` 仅 3 项未跟踪（全为我方测试产物），`src/` `prisma/` `docs/dev/` `tests/screenshots/` 无任何 diff
- 未 commit、未 push

**本轮新增测试产物（未 commit，交编排方处置）**

| 文件 | 用途 | 入库建议 |
|---|---|---|
| `tests/integration/m47-rv-probe.test.ts` | 2 条：真 route 字节 + **字面量**锚点（不引用被测常量）/ 自然收敛对照；顺带把字节落盘给浏览器实测 | **建议收编** —— 它是仓内唯一不与 registry 同源的告知文案钉子 |
| `tests/visual/rv-budget-notice.spec.ts` | 2 条：渲染层实测 + 活性对照。**字节缺席时自动 skip，不会打红 CI**（已实测 skip 行为）。若收编，需把「先跑上面那个 vitest 落盘」串进 `test:visual` 前置，否则 skip ≠ 保护 | 建议收编（配前置） |
| `scripts/test/rv/rv-timeout-wire.ts` | 生产**默认**闸的线级实测（真挂死上游、零注入）。约 155 秒，不适合进 CI 主线 | 建议保留为按需脚本；RV-2 的常规收口应另写常量调小版 |
| `scripts/test/rv/rv-census.mjs` · `rv-kpi.mjs` | 只读普查（dev 逐表 / 孤儿行 / today KPI 归因） | 可选 |

---

## 8. Soft-watch 全量清单及兜底（不阻断，须有人接）

### 8.1 本轮新增

| id | 内容 | 兜底 |
|---|---|---|
| **S-RV2-1** | RV-2 的产品面：主 loop 撞 `LOOP_TIMEOUT_MS` 时用户拿到**完全空白的回复且无任何说明**（实测 `B.text=""` / 无 error 浮出）——与 F006 已修好的「步数撞顶」互为镜像。面板对 `error` 类 part 亦无渲染分支 | 无。建议下批比照 `onBudgetExhausted` 补一条超时告知 data part |
| **S-RV2-2** | RV-M4：`typeof id === 'string' \|\| true` 形式满足 S-RV1-3 的正则、语义恒真 —— 与当初记录的 `.filter(() => true)` 同类。且 `e2e-cleanup-hygiene.test.ts:227` 的「行为级」`filterIds` 是**测试文件内自己定义的副本**，对脚本零覆盖 | 源码级正则仍是脚本侧唯一防线（acceptance 未要求更强）。归 `BL-E2E-CLEANUP-PIN` |
| **S-RV2-3** | RV-M6：别名变量 `const db = prisma; await db.x.deleteMany(` 绕过 S-RV1-2 的裸 `deleteMany` 判据 | 同上 |
| **S-RV2-4** | RV-M12：`ConsultationNote` 的「不硬编码人格名」只靠源码级 `not.toMatch(/AGENT_LABEL\s*[:=]/)`，换个变量名即可复现原缺陷（全量 1414 绿） | 产品在 HEAD 正确；建议改为行为级（渲染出的名字 === `getPersona(id).name`） |
| **S-RV2-5** | RV-5：`en-today` darwin 基线与 append-only 标记行耦合，每跑一次 e2e/全量套件即漂；且 S-M45-1「留不删」与 §4.5 重生序「先清 e2e 标记行」两条纪律互斥 | CI 不受影响。需 Planner 收口规则冲突（或让 today KPI 排除标记行 / 让基线用固定时间窗） |
| **S-RV2-6** | RV-7：`route.ts` 重复注释块 + 悬空 JSDoc | 纯卫生，下批顺手清 |
| **S-RV2-7** | 两个超时闸是**硬编码**，不像 `AIGC_TIMEOUT_MS` 那样可 env 覆盖 —— 上线拿到 L2-3 数据后调值需改代码 + 重新部署 | 与 R-8 一并处理 |

### 8.2 首轮登记且仍然成立

| id | 内容 | 状态 |
|---|---|---|
| S-G1-1 / S-G1-2 | 时刻隔离**防线②在子 loop 内不可达**（ToolSet 只装目标子集）；`TOOL_NOT_IN_SUBSET_MSG` 在该路径同样不可达 | 未变。实现处已如实标注、未虚报；RV-M10 证明放宽 ToolSet 时既有断言会红 |
| S-G1-3 | `docs/product/KOLMatrix-PRD.md:497` 与 `interaction-prototype-v2.html:1022` 仍是旧 duty/isolation（`只分派与汇总`），而 `registry.ts:131` 注释自称「与原型一致」 | 未变。产品文档归 Planner |
| S-M47-G3-5 | 子 loop 撞顶不落 `OperationLog`；`budgetHitScope` 只有 `'front' \| 'none'`，无 `'specialist'` | 未变。fix 轮新增了 scope 字段并把设计理由写进注释（子 loop 的 budgetHit 记在 consultation 产物上），属**有据的设计决定**而非疏漏，但 acceptance「区分前台/子 loop」在**落库层**仍不成立 → 口径待 Planner 裁决 |
| O-G4-1 | 面板副标题仍写「Match · 本环节专家」等（`mock.ts:315/331/352/384/405`），紧贴「编排 Agent」下方 —— 界面同时说两种身份 | 未变，且已烙进新基线 |
| O-G4-2 | `ConsultationNote` 的「未答完」（budgetHit）态无确定性样本，视觉零覆盖 | 未变 |
| O-G4-3 | `Handoff.summary` 写作 `咨询insight：…`（原始 agent id 而非人格名），而该表是 D-5 指定的协作痕迹统一数据源，会原样呈现给用户 | 未变 |
| O-G4-4 | `creator-drawer` 基线含相对时间文本（`Apify 采集 · N 天前`），每天漂 ~585px 被 1500px 阈值吸收 | 未变。与 S-RV2-5 同族 |
| O-G4-5 | `/preview/agent-loop` 页头说明未提本批新增的咨询痕迹三态 | 未变 |
| S-M45-1 | e2e 每跑净增 `SHARE_CREATED` 标记行，append-only 留不删 | 显式决定；但见 S-RV2-5 的规则冲突 |
| 无 `.nvmrc` | 本机 Node v25.7.0 下 L1 全绿 | 未变 |
| **本轮消解** | S-M47-G3-6（`consult-budget.test.ts` 漏清 Handoff）· S-M47-G3-4（`consultCount` 只在不进 CI 的 e2e）· S-M47-G3-10 的**新增**部分 | 三条已闭环；`shareLink` 2 行历史孤儿仍在（未处置，见 §7） |

---

## 9. 回 fixing 的最小修复清单（按优先级，不代做）

1. **RV-1（F009，必修）**：清态断言移出 `cleanupStep`，改为能影响退出码的形态。修完须用 D-G4-6 原变异（删 handoff 清理步）复证 **exit ≠ 0**。
2. **RV-2（F007，必修）**：给两个默认闸补常量→行为双向绑定用例（常量调小版），修完须用 RV-M1b / RV-M2 复证翻红。
3. **RV-4（F006 + F010，必修）**：`registry.ts:200` / `loop.ts:93` / `agent-architecture.md:69` / `:82` 四处「咨询抬预算」改为与实物一致（只有接力抬）。
4. **RV-3（F010，必修）**：`agent-architecture.md:41` / `:178` 翻牌为谓词形态；批末复核判据须真的跑遍 `docs/` 全范围。
5. **RV-6（R-8，必修·登记类）**：把「45s / 110s 无实测数据支撑、上线后按 L2-3 校准」写进 `SPECIALIST_TIMEOUT_MS` docblock **并**进 `backlog.json`（沿用三个成本常量「数字是猜的」的既定写法）。
6. **RV-7（低）**：清 `route.ts` 重复注释块与悬空 JSDoc。
7. **S-RV2-5 / S-RV2-1（建议）**：交 Planner 裁决规则冲突与主 loop 超时的用户面告知。

---

## 10. 红线复核（本轮实测，未被击穿）

| 红线（spec §2） | 本轮证据 |
|---|---|
| outbound 两步票据闸门 | `frontdesk:e2e` 实跑：专家备的 outbound 停 pending、副作用零发生、人确认后恰好发生 1 次；RV-M10 变异下 `PendingAction.agentId='reach'` 类断言全线翻红 ✅ |
| 时刻隔离（双防线） | RV-M10 摘掉双防线 → **12 条断言翻红**（首轮同变异 10/10 全绿）✅ |
| 行动承诺诚实 | RV-M3（伪造完成文案）→ RED；RV-M13（撞顶判据退回宽形态，会对答完的会话说「我没答完」）→ RED ✅ |
| 例程零直通 | 本批改动未触碰例程；`agentloop:e2e` 全绿 ✅ |
| 零外呼 / 零真实副作用 | 全部探针 fetch 哨兵 `[]`；`ShareLink` 恒走 mock，dev 库 `shareLink` 会话前后均为 0 ✅ |
