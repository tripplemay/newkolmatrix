# M4.7-FRONTDESK 验收报告 — G2 组（F003 路由降级为线索 / F004 留痕归属）

- **验收角色：** Evaluator（隔离 subagent，署名 `Andy/evaluator-subagent · G2`）
- **验收时间：** 2026-07-26
- **被验收 SHA：** `958c5e4`（main HEAD；F003 实装 `b0ac691`，F004 实装 `a8d9a39`）
- **授权层级：** **L1 only**（L2 未授权 —— 真网关 / 真投递 / 生产写入一律未执行，见 §6 验证缺口）
- **本组范围：** F003、F004。其余 feature 由其他 evaluator 并行验收，本报告不作评估
- **结论：** **F003 = PARTIAL** · **F004 = PARTIAL**
  - 两条 feature 的**产品行为面全部实测通过**（含我独立设计的真链探针 + 变异测试）
  - PARTIAL 的成因**全部在交付面**：F003 = 三处与实物不符的陈旧注释 + acceptance 明写的「语义注释明示」只兑现一半；F004 = acceptance 与 `PendingAction.agentId` 并列写着的 `OperationLog.actor` **零机械覆盖**（变异实证：改坏它，交付的 1341 条测试全绿）

---

## 0. 取证方式与独立性声明

- 全部结论基于**磁盘实物**：`features.json` acceptance 原文、`docs/specs/M4.7-FRONTDESK-spec.md`（D-1..D-7）、`git show` 逐 commit diff、产品源码、以及本人执行的测试输出
- `progress.json.session_notes`、commit message、代码注释中的自述**一律按「被检验的声明」处理**，不作为证据
- 不复用实现方交付的用例作为唯一证据：另起 `tests/integration/m47-g2-evaluator-probe.test.ts`（7 条）按 acceptance **原文场景**重新取证
- **零产品代码改动**（变异全部在一次性 worktree `/tmp/m47-g2` 内进行，已 `git worktree remove`）
- 未运行本波禁跑项：`test:visual` / playwright / `agentloop:e2e` / `frontdesk:e2e`

### 环境前置（对照 `framework/patterns/testing-env-patterns.md`，排除已知误报）

| 项 | 实测 |
|---|---|
| `npx prisma generate` 先于 tsc/测试 | 已执行 ✅ |
| 本地 Postgres `localhost:5434` | `select 1` 通 ✅ |
| `tsc --noEmit`（含本人新增探针文件） | exit 0 ✅ |
| 全量 vitest @ `958c5e4`（worktree + 本人探针） | **112 files / 1341 tests 全绿** ✅ |

---

## 1. F003「路由降级为线索」逐条 acceptance 对照

| # | acceptance 条款 | 判定 | 实测依据 |
|---|---|---|---|
| 1 | 对话人格恒为前台——负向断言：项目环节页（如 `?env=match`）发起的会话，受理人格仍是前台（**真 route** 路径实证，非服务层自证） | **PASS** | `tests/integration/project-context-route.test.ts:249` 直接 `import { POST } from 'src/app/api/agent/route'` 并投递真 `Request`（与 `framework/patterns/testing-env-patterns.md §8`「服务层测试 ≠ HTTP 链测试」判据一致，同族先例 m45/m46 探针）。客户端刻意传 `agentId:'match'` + `stage:'match'`，实测 `X-Agent-Id = orchestrator`，且第一步 system 含前台 isolation 与 `consult_specialist`。变异 MUT-A / MUT-F 均翻红（§3） |
| 2 | `STAGE_AGENT` / `defaultAgentForRoute` **保留**（grep 证未删、消费点仍在） | **PASS（附 OBS-3）** | 二者均未删除。`STAGE_AGENT` 有**产品消费点**：`src/app/admin/today/page.tsx:71-73`（AGENT_STAGE 反查表）+ `:200`（雷达图标）+ `stage-routing.ts:68`。`defaultAgentForRoute` **已无产品运行消费点**——仅经 `orchestrator.ts:79 agentForRoute` 导出，实际调用者只有 `scripts/test/orchestration-smoke.ts`。字面「消费点仍在」成立，但代码注释给出的理由与实物不符（见 OBS-3） |
| 3 | 当前环节作为**线索**注入 system（同 M4.6 `project-context.ts` 空值纪律：无环节不注水） | **PASS** | `src/lib/agent/stage-hint.ts:29` 非法/空 stage 一律返回空串。真 route 三条用例实测：合法 stage → 注入 `【当前位置】`；不带 stage / 非法 stage → 不注入且人格段本体仍在（防「整体为空的假通过」）。变异 MUT-E（去掉空值纪律）翻红 2 条 |
| 4 | 且明写「这是用户当前所在位置，不限制你能做什么」 | **PASS（附 OBS-4）** | `STAGE_HINT_NOT_A_LIMIT` 常量含「**不限制**你能做什么——需要别的专业判断时，照常咨询对应专家」，真 route 实测出现在模型实收 system 中。字面语义锚点（非引用常量的同义反复）存在并生效：变异 MUT-C（把该句改成「只处理该环节范围内的问题…」）翻红 ✅。但存在一条作者未设防的绕过路径（OBS-4） |
| 5 | `X-Agent-Id` 恒为前台（**语义注释明示**） | **PARTIAL** | 行为面 PASS：三个文件（route 探针 / m45 探针 / m46 探针）均断言 `X-Agent-Id === FRONT_DESK_AGENT_ID`，变异 MUT-A 翻红。**注释面未兑现**：响应头处注释（`route.ts:120-122`）仍写「【P9 语义】值 = **起始人格**：一轮会话内可能经 handoff_to 换人」，**未明示恒为前台**；「恒为前台」这句只出现在 12 行之外的 `resolveContext` 注释里 |
| 6 | 无回归：工作区层会话行为零变化 | **PASS（口径说明见 §5）** | 全量 1341 条绿；本人独立探针实测工作区层路由 `/admin/insight` 会话装配正常（前台人格 + 可见 `consult_specialist` + system 含前台 isolation）。需如实说明：`/admin/creators`、`/admin/knowledge`、`/admin/insight` 等工作区层页面的**受理人格**由 match/strategy/insight 变为前台——这是本批设计意图本身（spec §1），与该条字面表述冲突，不作缺陷计（§5） |
| 7 | 集成测经**真 route** 核实（M4.6 D1 教训） | **PASS** | 同 #1。另经 m45 / m46 两个既有真 route 探针交叉复核，两者对 `X-Agent-Id` 的断言由「等于客户端指定人格」改为「恒为前台」——经变异实证**是收紧而非放宽**（MUT-A 下两处均翻红） |

### F003 findings

| 编号 | 级别 | 内容 |
|---|---|---|
| **F003-D1** | 缺陷（交付面） | **三处与实物相反/陈旧的注释，全部在 F003 亲手改的 `CopilotPanel.tsx` 里**：① 文件头 `:6`「多人格切换：进不同 route 自动切人格；context key 变化 → 对话清空 + 新专家开场白」—— F003 之后 route 不再切人格；② `:384`「用 useSearchParams 读 ?env=（项目详情切环节**专家**）…兜底避免重写瞬态**落默认人格**」—— 环节不再选专家；③ `:392`「key=contextKey（**含 agentId，随 route/env/stage-专家 变化**）→ 整个 chat remount（**对话清空** + 新专家开场白）」—— **与实物相反**：`buildContextKey` = `route:projectId:env:agentId`，不含 stage，而 agentId 现已恒定，故在同一项目内切 `?env=` **不再** remount、对话**不再**清空。误信该注释的后续维护者会对 UI 状态生命周期做出错误假设 |
| **F003-D2** | 缺陷（交付面） | acceptance #5 的「（语义注释明示）」只兑现一半：`X-Agent-Id` 输出点的注释未明示恒为前台（详见上表 #5） |
| **F003-OBS-3** | 观察 | `stage-hint.ts:10-11` 注释称「STAGE_AGENT / defaultAgentForRoute **保留**：雷达深链等非对话用途仍在用它们（**见 stage-routing.ts 的消费点**）」——与实物不符两处：(a) 雷达深链用的是 `STAGE_AGENT`，`defaultAgentForRoute` 与之无关；(b) `stage-routing.ts` 根本不消费 `defaultAgentForRoute`。F003 之后 `defaultAgentForRoute` 的唯一调用者是 smoke 脚本 |
| **F003-OBS-4** | soft-watch | 线索段的「不限制」语义只被**正向存在性**钉住，段内**不存在相反指令**这件事没有钉子。实测绕过：在常量末尾追加「不过本环节以外的事情，请告诉用户切换到相应的助手那里再提问，你这边不作答。」——本批要根治的缺陷原样复发，而 `project-context-route.test.ts` **全绿**（15/15）。既有否定式正则 `/只(处理\|负责\|做).{0,10}环节\|去找对应专家(?!。?$)\|自行前往/` 未覆盖该措辞。这正是仓内已登记的「黑名单式否定断言不可穷尽」同一坑的又一实例 |

---

## 2. F004「ctx.agentId 派生 + 留痕归属」逐条 acceptance 对照

| # | acceptance 条款 | 判定 | 实测依据 |
|---|---|---|---|
| 1 | 子 loop 内 `ctx.agentId` = 目标专家（非前台） | **PASS** | `specialist-loop.ts:132-137` 派生 `subCtx.agentId = targetAgent`（且 `confirmationToken` 不继承）。行为实证见 #2 |
| 2 | `PendingAction.agentId` 记**实际干活的专家**（acceptance 原文场景：前台咨询触达专家备一封邮件 → 该 pending 的 agentId = **reach** 而非 orchestrator） | **PASS** | 本人探针按 acceptance **原文场景**重跑并走**真前台全链**（前台 loop → `consult_specialist` → 子 loop → `send_outreach`）：`PendingAction.agentId === 'reach'`、`status='pending'`、`OutreachMessage/Thread` 计数为 0（闸门红线未破、零外呼）。**注**：交付用例 `provenance-attribution.test.ts:74` 用的是 insight + `create_share_link` 且**服务层直调** `runSpecialistLoop`，与 acceptance 字面场景不同——该缺口已由本探针补足 |
| 3 | `OperationLog.actor` 记**实际干活的专家** | **FAIL（覆盖面）／行为 PASS** | 行为正确：本人探针查 `OperationLog(kind='gate', ref=pa.id).actor === 'reach'` ✅。但**交付的测试与脚本对这一半零覆盖**——变异 MUT-G（把 `gate.ts:136` 的 `actor: ctx.agentId` 写死成 `'orchestrator'`）后，**全量 112 files / 1341 tests 全绿**，只有本人探针翻红。全仓 `grep '\.actor'` 亦确认：无任何用例断言闸门行的 actor 归属专家；`scripts/test/frontdesk-e2e.ts` 断言了 `pending.agentId==='insight'`，同样未断言 actor |
| 4 | **M4.5 接力路径同样派生**（O-G2-1 闭环，回归实证） | **PASS** | `loop.ts:199` `currentAgentId: () => track.current` + `to-ai-sdk-tools.ts:59-61` 调用前派生 `callCtx`。交付用例 `provenance-attribution.test.ts:122` 实证接力后 pending 记 insight；变异 MUT-D（去派生）/ MUT-F（捕获快照）均翻红 ✅ |
| 5 | 雷达深链 `agentId → STAGE_AGENT` 反查**不再落回退分支** | **PASS（附 OBS-2）** | 本人探针：reach 场景下 pending.agentId 反查命中 `'reach'`，并附活性证明（前台 id 确不在 `STAGE_AGENT` 值域，否则断言无分辨力）。产品侧回退分支在 `today/page.tsx:177` `(item.agentId && AGENT_STAGE[item.agentId]) \|\| project?.cur \|\| 'brief'`。OBS-2：交付用例与本探针**都是复刻**该查表逻辑（`toRadarItem` 未导出），非调用产品函数 |
| 6 | `handoff_to` 的「不能交接给自己」改比对**当值人格**（O-G2-2 闭环） | **PASS（附 OBS-3）** | 交付用例**零覆盖**此条；本人探针补证：经产品 `toAiSdkTools(..., { currentAgentId: () => 'insight' })` 调 `handoff_to({toAgent:'insight'})` → 抛 `不能把工作交接给你自己` 且不落 Handoff 行；**活性对照**：不传 `currentAgentId`（M4.5 原状）同一调用成功落行 → 证明该断言有分辨力。变异 MUT-D 下本条翻红 ✅ |
| 7 | 变异证断言活着（派生改回起始人格 → 红） | **PASS** | 本人独立复现三条变异（MUT-D/E2/F），全部翻红，见 §3 |

### F004 findings

| 编号 | 级别 | 内容 |
|---|---|---|
| **F004-D1** | 缺陷（交付面，PARTIAL 主因） | acceptance 把 `PendingAction.agentId` **与** `OperationLog.actor` 并列写为「记实际干活的专家」，但交付物只给前者上了钉子。变异实证：`gate.ts` 的 actor 写死为前台后，**1341 条测试无一翻红**。这正是本批 spec §6.3 自己立的规矩「机械钉的覆盖面必须与它守护的那句话逐项对齐（N-1 段 = 静默门）」被违反的实例——留痕归属的两个字段，只锁了一个 |
| **F004-D2** | 缺陷（交付面，轻） | acceptance 原文的实证场景是「前台咨询**触达**专家备**一封邮件** → agentId = **reach**」；交付用例换成 insight + `create_share_link`，且是**服务层直调** `runSpecialistLoop`（未经 `consult_specialist` 工具与前台 loop）。行为等价性已由本人真链探针独立证实，但交付证据本身未覆盖 acceptance 指名的路径 |
| **F004-OBS-2** | 观察 | 雷达深链断言是**复刻产品查表逻辑**（`Object.entries(STAGE_AGENT)` 反转）而非调用产品代码——`today/page.tsx` 的 `toRadarItem`/`AGENT_STAGE` 均未导出。若产品侧反转表被改坏（例如漏写某环节），测试仍会绿 |
| **F004-OBS-3** | soft-watch | O-G2-2 的新语义在**产品运行路径上不可达**：`handoff_to` 仅 orchestrator 持有，接力后目标人格的 `isToolActive` 会先以「工具不在子集」拒绝，走不到自交接校验。本条闭环成立于 ctx 语义层，行为层无自然触发点（我的探针是直接经 `toAiSdkTools` 构造当值人格来触发的） |
| **F004-OBS-4** | soft-watch | 人确认执行路径（`/api/actions/[id]/confirm|execute`）用 `buildToolContext()` **无参**构造 ctx → `agentId` 回落 `DEFAULT_AGENT_ID='orchestrator'`，因此 `irrev` / 失败 `auto` 留痕的 actor 恒为前台，与该 pending 上记录的专家归属不一致。不在 F004 acceptance 面内（属人触发动作），如实登记 |

---

## 3. 变异测试全表（断言活性证明）

方法：`git worktree add /tmp/m47-g2 958c5e4 --detach`，逐条改**产品代码**后跑测试，跑完 `git checkout -- src` 复原；结束移除 worktree。产品仓零改动。

| 变异 | 改动 | 交付用例 | 本人探针 | 判定 |
|---|---|---|---|---|
| **MUT-A**（F003） | `route.ts:resolveContext` 恢复「客户端指定 → route 推导」 | **RED**（`project-context-route` 2 条：🔑 环节页恒前台 + M4.6 项目页 header） | 绿（探针不走 HTTP） | 断言活 ✅ |
| **MUT-B**（F003） | `stageHintSection` 去掉 `STAGE_HINT_NOT_A_LIMIT` 那行 | **RED**（1 条） | 绿 | 断言活 ✅ |
| **MUT-C**（F003） | `STAGE_HINT_NOT_A_LIMIT` 改成「请只处理该环节范围内的问题，其他事让用户去找对应专家。」 | **RED**（字面语义锚点用例） | 绿 | 断言活 ✅（commit 自述的同义反复修复属实） |
| **MUT-C′**（F003·绕过） | 保留原句，**追加**「不过本环节以外的事情，请告诉用户切换到相应的助手那里再提问，你这边不作答。」 | **绿 15/15** | 绿 | **未拦住** → OBS-4 |
| **MUT-E**（F003） | `stageHintSection` 去掉空值纪律（无/非法 stage 也注水） | **RED**（2 条：不注水 + 非法值不注水） | 绿 | 断言活 ✅ |
| **MUT-F**（F003·绕过） | 响应头仍前台，但 `loop.ts` 按 stage 回落装配专家人格（**头说谎**） | **RED**（🔑 用例：system 不含前台 isolation） | RED（3 条） | 断言活 ✅（🔑 不是只看头） |
| **MUT-D**（F004） | `to-ai-sdk-tools` 去掉 `callCtx` 派生 | **RED**（接力 pending 记 insight） | RED（O-G2-2 当值自交接） | 断言活 ✅ |
| **MUT-E2**（F004） | `specialist-loop` 子 ctx 不派生 agentId | **RED**（2 条） | RED（3 条） | 断言活 ✅ |
| **MUT-F2**（F004） | `loop.ts` `currentAgentId` 改为捕获快照（O-G2-1 病因原样） | **RED**（接力用例） | 绿 | 断言活 ✅ |
| **MUT-G**（F004·绕过） | `gate.ts:136` `actor: ctx.agentId` → 写死 `'orchestrator'` | **全量 1341 全绿** | RED（1 条） | **未拦住** → F004-D1 |

---

## 4. 红线复核（本组视角，非本组 feature 的结论不作判定）

| 红线（spec §2） | 本组实测 |
|---|---|
| outbound 两步票据闸门 | 真链探针中专家备的 `send_outreach` **停在 pending**，`OutreachThread`/`OutreachMessage` 计数 0，零外呼（fetch 哨兵 `[]`）✅ |
| 时刻隔离 | 变异 MUT-D 下 `handoff_to` 越权路径仍被 `isToolActive` 挡（执行侧防线独立于视野收窄）✅（完整验收属 F001/F002 组） |
| 行动承诺诚实 | 不在本组 acceptance 面内（F005/F007） |
| 例程零直通 | 本组改动未触碰例程 ✅ |

---

## 5. acceptance #6「工作区层会话行为零变化」的口径说明（不作缺陷计）

字面读，该条不成立：`/admin/creators`、`/admin/knowledge`、`/admin/insight` 等工作区层页面此前经
`defaultAgentForRoute` 分别落 match / strategy / insight 人格，F003 后一律落前台。但这正是本批
spec §1 目标态表格「用户面对 = **始终同一个助手**」的直接后果——在本批设计下不存在「保住路由派生人格」
的实现路径。故按**实质口径**判定：工作区层会话的机械面无回归（全量 1341 绿 + 独立探针实证装配正常），
判 PASS，并把字面冲突如实登记于此，供 Planner 在下批 acceptance 措辞时收口。

---

## 6. 验证缺口（如实登记，不得当作已验证）

| 缺口 | 说明 |
|---|---|
| **[L2] 未授权，未执行** | 真网关下「模型是否真的把线索读成线索而非权限」「前台是否真的不再把用户推走」——本组全部证据是 mock-model 驱动的机械面。生产手测是本批触发源，L2 面必须在上线前人工复测 |
| **客户端 `?env=` → `stage` 链路** | acceptance #1 的字面入口是 URL `?env=match`；服务端链已真 route 实证，`CopilotPanel.deriveContext` 的 URL→stage 映射**无任何自动化断言**（vitest 配置排除 jsdom 组件测；Playwright 本波禁跑）。风险有限：服务端已一律不采信客户端 `agentId`，客户端即使回退也无法重现根因 |
| **F003-D1 ③ 的实物判定基于源码判读** | 「切 `?env=` 不再 remount / 对话不再清空」由 `buildContextKey` 组成（不含 stage）+ agentId 恒定推得，未在浏览器运行时实证（Playwright 禁跑） |
| **`frontdesk:e2e` 未运行** | 本波禁跑。F003 把 m45 探针的「outbound 在真 route 上停 pending」覆盖**迁移**到该脚本——源码核实迁移目标确实存在（`scripts/test/frontdesk-e2e.ts:123-133` 断言 pending 落一条且 `agentId==='insight'`），但其**执行结果**属 F009 组，本组不作判定 |

---

## 7. 跨组发现（非本组 feature，仅登记转交）

**`tests/integration/consult-budget.test.ts`（F006 交付物）漏清软引用表 `Handoff`，每跑泄 3 行孤儿。**

- 实测：跑该文件前后，全库「无对应 Tenant 的 Handoff」计数 `85 → 88`；对照组（`provenance-attribution` / `project-context-route` / 本人探针 / `consult-failure` / `frontdesk-honesty` / `specialist-loop` / `handoff-loop`）**均 0 增量**
- 成因：该文件 `afterAll`（`:41-53`）只清 `operationLog / pendingAction / project / tenant`，残留断言同样漏掉 `Handoff`；而 `consult_specialist` 每次咨询按 D-5 裁决**会落一行 Handoff**
- 这正是本批 spec §6.5 自己立的硬要求「软引用表（`OperationLog` / `Handoff`）不级联，必须逐表清 + 逐表断言残留」被违反的实例
- 当前 dev 库孤儿 `Handoff` 88 行（全部产生于今日 15:00–17:00 UTC 的本波验收跑动，无历史存量）；孤儿行不进 `/api/handoffs`（按 dev 租户过滤），对视觉基线无影响。**未清理**：其中部分由并行 evaluator 的在跑进程产生，删除他人在跑证据风险更高，交由 fixing 轮在修复该文件时一并清

---

## 8. 本次落盘的验收产物

| 文件 | 说明 |
|---|---|
| `tests/integration/m47-g2-evaluator-probe.test.ts` | 本组独立探针 7 条（未 commit，按编排纪律留在工作区）。其中 `OperationLog.actor 同样记 reach` 一条即 **F004-D1 的现成补钉**，可直接采用 |
| `docs/test-reports/M4.7-FRONTDESK-verify-G2.md` | 本报告 |

## 9. 修复建议（供 fixing 轮，均为交付面小改）

1. **F004-D1（必修）**：给 `OperationLog.actor` 上钉子——直接采用本报告 §8 的探针文件，或在 `provenance-attribution.test.ts` 加一条 `expect(gateLog.actor).toBe('insight')`
2. **F004-D2（建议）**：把 acceptance 原文场景（reach + `send_outreach` + 经 `consult_specialist` 真链）纳入回归，而非只留服务层直调
3. **F003-D1（必修）**：`CopilotPanel.tsx` 三处注释翻牌（`:6` / `:384` / `:392`），尤其 `:392` 与实物相反的 remount 描述
4. **F003-D2（必修）**：`route.ts` 响应头处补一句「F003 起 = 恒为前台」的语义注释
5. **F003-OBS-3（建议）**：修正 `stage-hint.ts:10-11` 的保留理由（雷达深链用的是 `STAGE_AGENT`；`defaultAgentForRoute` 现仅 smoke 脚本消费）
6. **F003-OBS-4（建议，或转 soft-watch）**：把线索段断言从「含某句」升级为「整段正向精确匹配」（全段 `toBe`），杜绝追加相反指令
