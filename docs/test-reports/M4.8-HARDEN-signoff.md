# M4.8-HARDEN Signoff — 首轮验收（verifying）

> **被测 SHA：** `c9236af1cc65cc64a0e7ecd50f15c86def0428d9`（验收期间工作树对 `src/` `prisma/` `docs/` `.github/` `package.json` **零改动**，逐条变异后均已还原并以 `git diff --quiet` 核证）
> **验收者：** Evaluator（隔离 subagent / fresh context，署名 `Andy/evaluator-subagent`）
> **日期：** 2026-08-04
> **结论：** **7/7 PASS**（无阻断项；6 条非阻断观察 + 5 条 soft-watch 登记）

---

## 0. 独立性与取证纪律声明

- 本报告的每一条判定都基于**我自己跑出来的命令输出、我自己读的代码与 diff、我自己设计并实跑的变异探针**。Generator 的 commit 正文与编排者的任何描述只当线索，**逐句按铁律 13 核证**（核证结果见 §8 I-2）。
- 我**未修改任何产品代码**。变异探针一律「改 → 跑 → 还原 → `git diff --quiet` 核证」；新增产物只有 4 个测试/取证文件（§9）。
- **L2（真网关 / 生产 / 计费）未授权，本轮零执行、零外呼**；所有 loop 用例全程挂 `installNoNetworkSentinel`，实测 `networkCalls === []`。

---

## 1. L1 门（我自己跑的）

| 门 | 命令 | 实测结果 |
|---|---|---|
| 类型 | `npx tsc --noEmit` | **0 error**（exit 0） |
| Lint | `npx next lint` | **✔ No ESLint warnings or errors** |
| 单测全量（HEAD 净树） | `npx vitest run` | **Test Files 123 passed (123) / Tests 1469 passed (1469)** |
| 单测全量（加我的探针后） | `npx vitest run` | **124 passed (124) / 1470 passed (1470)**（+1 文件 +1 用例 = 我新增的 F004 负向④ 钉） |
| e2e 循环面 | `npm run agentloop:e2e` | ✅ 全部断言通过（零外呼 · 零真实对外副作用） |
| e2e 前台面 | `npm run frontdesk:e2e` | ✅ 全部断言通过；两层清态断言均绿（第二层整表普查净增 = 申报值） |
| 视觉门（全量） | `npx playwright test` | **19 passed / 2 failed** —— 两条 failed 与 M4.7 签收记载的**同两条本机已登记项**完全一致（`today dashboard` 基线 ↔ dev 数据态 / append-only 标记行；`workbench env=match` ↔ 本机库有 3 条 MatchPlan 故空态文案不出现，M4.7 signoff §RV-5 与 backlog `BL-VISUAL-DATA-ISOLATION` 在案）。M4.7 当时是 15 passed / 2 failed，本批 +4 passed（= 新增 rv-timeout-notice 四条），**failed 条数与身份均未变化 → 无新增视觉回归** |
| 视觉前置 | `npm run test:visual:fixtures` | 2 files / 4 tests passed，两份字节样本落盘（1286 / 1114 bytes） |
| 构建 | `npm run build` | 成功（视觉门前重新构建 + playwright 自起 standalone，遵 `web-runtime-patterns.md` §4.5） |

---

## 2. diff 复核范围与 feature 归属（铁律 10）

`git diff --stat 51599ff..c9236af` = 26 文件 / +2521 −74。逐 commit 核对 tag ↔ `features.json` 条目：

| commit | tag | 触及文件面 | 归属核证 |
|---|---|---|---|
| `505119a` | F001 | project-context / loop / specialist-loop + 3 测试 | ✅ 与 F001 描述一致 |
| `c644d88` | F002 | knowledge-context / loop / specialist-loop + 1 测试 | ✅ |
| `b6e91a6` | F003 | 仅 `tests/unit/project-scope-census.test.ts` | ✅ 纯钉子，符合「与修复点解耦」 |
| `945d0b9` | chore | 仅 spec D-3 表（8→9 处更正） | ✅ 无产品面改动；更正内容我独立复核属实（§3 F003） |
| `d4b16e4` / `667e5c1` / `c9236af` | chore | 仅 `features.json` / `progress.json` | ✅ |
| `0b7ad6a` | F004 | route / registry / loop / loop-timeout-log（新）+ 测试 + **CopilotPanel(+22)** | ⚠️→✅ CopilotPanel 属 F005 面，但 commit 正文已说明理由（仓内不变式钉 `m47-adv-probe` P7b 要求「route 写的每种 data part 面板必须有渲染分支」，写入端单独落地即翻红）。我实读该不变式测试确认此约束存在 → **归属合理，非越界** |
| `89597a7` | F005 | CopilotPanel / package.json / 新 visual spec / fixture / log-settle + **loop-timeout-notice.test.ts(+5)** | ✅ 后者是给 F004 测试文件接 settle 等待，commit 正文如实登记 |
| `dba4d33` | F006 | loop-telemetry / loop + 测试 | ✅ |
| `7877b85` | F007 | 两份 doc + doc-freshness 测试 | ✅ |

**无归属外的产品代码改动**；`backlog.json` 本批未被任何 commit 触及（见 §8 I-6）。

---

## 3. 逐 feature 判定

### F001 — findProjectByRef / projectContextSection 收口 tenantId（含 S-RV3-3 携带） → **PASS**

| acceptance 分项 | 我的证据 |
|---|---|
| `findProjectByRef(ref, tenantId)` tenantId 必选（无默认值） | 实读 `project-context.ts:42-53`：位置参数无 `?`、无默认值；`tsc --noEmit` 0 error ⇒ 全调用点显式传 |
| where 含 tenantId 且与 OR **同层** | 实读 `:46-50`：`where: { tenantId, OR: [...] }` ✅ |
| `projectContextSection(projectId, tenantId)` 同步 | 实读 `:67-70`；调用点 `loop.ts:287`（`ctx.tenantId`）、`specialist-loop.ts:149`（`subCtx.tenantId`） |
| 行为级负向（三口径逐一 → null；段落 id-only；不含别家项目名；整段正向精确匹配） | 实跑 `project-context-injection.test.ts` 16/16 绿；用例含「自证夹具：三个 ref 在自己租户下都解析得到」（排除假通过）+ 「真 loop 装配路径断言 `systemPerStep[0]`」（断的是模型真正收到的正文） |
| 正向回归 + DB 故障降级不回退 | 同文件「同租户三口径照常返回名」「取名失败（真 DB 故障）降级」两组用例绿（后者用 `vi.spyOn(prisma.project,'findFirst').mockRejectedValue` 驱动真 catch 分支） |
| 变异证活：摘 where 的 tenantId → 负向红 | **我自己的两条更强变异**（不复用作者变异）：<br>**E1** 把 tenantId 挪进 OR 第一分支（表面「有作用域」的第二条绕过）→ 行为级 2 条红（`三口径 → 均 null`、`段落降级 id-only`）+ census 2 条红；<br>**E2** 给 `projectContextSection` 加一条「查不到就用无租户条件再查一次」的善意回落 → 行为级 2 条红（含**真 loop 装配路径**那条）+ census 2 条红。两条均还原后复跑绿 |
| S-RV3-3 携带（真 route POST 遥测等待同步点） | 实读 `project-context-route.test.ts:60-80,143-148,204-230`：包一层 `logLoopTelemetry` 只做观测、`postAgent` 返回前 `await Promise.all`，且**同步点自身有钉**（断 `pendingTelemetry === 0`）。**变异 E16**：摘掉 `await Promise.all(seam.telemetryWrites)` → 该钉红（1 failed / 9 passed），还原绿 ⇒ 钉子活的 |
| 第二租户入清理登记表、逐表断言残留、净增 0 | 实读 `afterAll`：`handoff/operationLog/pendingAction/project/tenant` 逐表清（`cleanupStep` 包，清理段自身不再抛）+ 逐表 count 断言 + **按 projectId 的第二口径孤儿普查**。我的独立 DB 普查（§9）：**我全程多轮跑测后，dev 库无任何本轮新增孤儿行**（最新孤儿行时间戳 `2026-08-04T04:04`，早于我第一条命令 `04:37`） |
| tsc / lint / agentloop:e2e / frontdesk:e2e 无回归 | §1 全绿 |

### F002 — gameKnowledgeSection 收口 tenantId → **PASS**

| acceptance 分项 | 我的证据 |
|---|---|
| 签名 `(projectId, tenantId, kinds)` 必选、where 含 tenantId | 实读 `knowledge-context.ts:61-74`：`where: { tenantId, OR: [...] }` ✅ |
| 跨租户 → `''`（不注水、不抛错） | 实跑 `knowledge-injection.test.ts` 绿；用例含「自证夹具：别家项目在**它自己租户**下确实渲染得出知识段」（夹具带 gameId + 独有内容链头，排除「本来就没知识」的假通过）+ 「跨租户三口径逐一 → `''`」+ 「跨租户不抛错（三种 kinds 组合）」 |
| 正向回归 | 同文件既有取数/过滤/降级用例全绿 |
| 调用点 3 处同步传参 | 实读 `loop.ts:274`（主）、`loop.ts:312`（接力后 `systemForAgent`）、`specialist-loop.ts:152` ✅ |
| 变异证活 | **E3**（我的变异，同样取「第二条绕过」形态）：把 tenantId 挪进 OR 第一分支 → 行为级「跨租户三口径逐一 → 空串」红 + census 2 条红；还原后绿 |
| 夹具租户复用清理登记表 | 实读 `afterAll`：两租户 × `gameKnowledge/project/game/tenant/handoff/operationLog/pendingAction` 逐表清 + 逐表断言 + 按 projectId 孤儿普查 ✅ |

### F003 — 全仓三口径解析普查钉（census） → **PASS**（附 4 条绕过面 soft-watch）

| acceptance 分项 | 我的证据 |
|---|---|
| 扫描 `src/` 全部三口径解析点、断言 where 含 tenantId | 实读 `project-scope-census.test.ts`：`readFileSync` 递归遍历（非 `git grep`，未 commit 文件也扫得到）+ 注释/字符串遮蔽 + 花括号配平取 where/OR，判据是「与 OR **同层**有 tenantId」（把 OR 整段挖掉再找 ⇒ 「tenantId 藏进 OR 分支」判违规） |
| 覆盖多行 OR 格式（set-goal 形态），先验活 | 该断言实测绿；且 `multilineOr` 字段被单独钉（`set-goal.ts` 必须恰 1 处且识别为多行）✅ |
| **不从修复点清单派生**——新增未收口点无需登记即红 | **我自己的活性探针**：往 `src/lib/agent/__m48_probe.ts` 塞一处**全新的**三口径无 tenantId 解析（prettier 常规格式、别名接收者 `client.project.findFirst`）→ census 当场红并点名 `src/lib/agent/__m48_probe.ts:4`；删文件后复绿。**检测器活着，且不认识修复点清单** |
| 当前实物全过 + 豁免清单为空且写死 | `EXEMPTIONS = []` 硬编码在断言文件内 ✅。**终态判据我独立复核**（不经 census 脚本）：`grep -rn -A8 "project.findFirst" src` 逐点人工核 —— 18 处 findFirst 中三口径 OR 共 **9 处**，逐处 where 均与 OR 同层带 tenantId；其余为单 id + tenantId。与 spec D-3 更正后的 9 行表**逐行一致**（`945d0b9` 那次 8→9 更正属实） |
| 变异证活：临时加未收口点（单行/多行各一）→ 均红；摘 F001 修复 → census 也红 | 我的 E1/E2/E3 三条产品面变异**每条都让 census 与行为级双双翻红** ⇒ 两层同时守、失效模式不重叠（M4.7 规律 2 成立） |

**绕过面（我的探针实测，登记为 soft-watch，不改变判定）：** acceptance 的字面范围是 `prisma.project.findFirst`，以下四种写法**当前扫不到**（各写一处未收口解析点，census 仍 12 passed）：① `project.findMany` 三口径（src/ 现有 8 处 findMany，均带 tenantId，故非现存缺口）；② `findFirstOrThrow`；③ `where` 提取为局部常量再传入；④ 点号后带空格的 `client . project . findFirst`（不合仓内 prettier 风格）。①②可用一行正则拓宽消除，③属源码级扫描的固有边界。

### F004 — 主 loop 超时：onLoopTimeout + 流内告知 + OperationLog 留痕 → **PASS**（补钉 1 条）

| acceptance 分项 | 我的证据 |
|---|---|
| loop 增 `onLoopTimeout`，按 `signal.reason` 判超时；注入与默认两路都挂监听 | 实读 `loop.ts:340-373`：监听挂在 `params.abortSignal ?? AbortSignal.timeout(LOOP_TIMEOUT_MS)` 这一个 signal 上（两路同一条），判据 `isTimeoutAbort`（`name === 'TimeoutError'`）+ `settled`。测试两路都覆盖：注入 30ms 闸（回调用例）与**生产默认闸**（route 用例把 `LOOP_TIMEOUT_MS` mock 成 2s，走不注入 signal 的真实路径） |
| 负向三条（正常完成 / budget 截停 / 非超时 abort 均不误响） | 实跑 `loop-timeout-notice.test.ts` 8/8 绿，三条负向逐条带**前提活性断言**（「前提：会话确实跑完了」「前提：撞顶回调确实响了」「前提：确实中断了」）⇒ 负向不是空转 |
| 变异（我的，非复用作者的） | **E5** 把判据放宽一格（`TimeoutError` **或** `AbortError` 都算超时）→ 负向③ 红。这正是「用户自己关页面却被告知超时中断」的形态 |
| route 写 `data-timeout_notice`，文案取自 registry 常量、语义如实 | 实读 `route.ts:123-143` + `registry.ts:129-149`；测试对文案**正向精确钉全串** + 锚点 + `not.toMatch(/已(完成\|为你\|为您\|全部\|办好)/)`；route 用例对响应体字节做**字面量**断言（刻意不 import 常量，避免同源自证） |
| OperationLog 一行留痕、只记元数据、隐私哨兵 | 实读 `loop-timeout-log.ts`（marker `agent_timeout` 独立于 `agent_loop`，避免混进既有遥测查询）；测试断言 `kind='auto'`、`actor`、`payload.{agentId,elapsedMs,steps,kind}`，并对整行 JSON 断言不含用户正文与工具入参。**变异 E6**（我的）：让 writer 往 `payloadJson` 夹带 `'m48-item'`（工具入参正文）→ 隐私断言当场红 ⇒ 哨兵有牙 |
| 回调抛错不打死会话 | 用例绿。**观察（非阻断）**：我摘掉回调外层 try/catch（E15）后该用例**仍 8/8 绿**，但 vitest 报 `Errors 1 error` 且 **exit code = 1** ⇒ 拦得住，只是失败信号不落在断言上（登记 S-M48-4） |
| 变异证活（摘监听 / 摘 OperationLog / 改文案） | 作者已实跑记录在 commit；我未复跑同款，改跑上述**更强的三条**（E5/E6 + 下面的 E7），结论一致 |

**我发现并补齐的一格（缺陷未发生，缺的是钉）：** 实物里还有第四条误报路径——**模型抛错的异常收场**（`onEnd` 不触发，闸的定时器还在走），loop 用 `result.finishReason.then(markSettled, markSettled)` 兜住。**变异 E7** 摘掉这条兜底 → `loop-timeout-notice.test.ts` **8/8 照样全绿**（该防线此前零覆盖）。我据此新增 `tests/integration/m48-eval-probe.test.ts`：模型立即抛错收场 → 事后让闸到点 → 断言 `onLoopTimeout` 不响。该钉在 **HEAD 实物上绿、在 E7 变异体上红**（实测两次），零 DB 写入、零外呼。

### F005 — CopilotPanel 流级 error/abort + timeout notice 渲染 + 浏览器级钉 → **PASS**

| acceptance 分项 | 我的证据 |
|---|---|
| `useChat` 解构 `error` + 流级错误分支（`data-testid="stream-error"`，镜像 amber 卡） | 实读 `CopilotPanel.tsx:285`（解构）与 `:395-405`（渲染，原始 message 只挂 `title` 不进正文）✅ |
| `data-timeout_notice` 渲染（`data-testid="timeout-notice"`） | 实读 `:145-157`（`notice ? <div …> : null`） |
| 浏览器级钉（镜像 rv-budget-notice），两视口实测 | **我自己跑**：`npx playwright test rv-timeout-notice.spec.ts` → **4 passed**（默认 1512×982 + 移动 430×932 + 活性对照 + 500 流级错误） |
| fixture 串进 `test:visual:fixtures` | 实读 `package.json:14-15`；**我自己跑** `npm run test:visual:fixtures` → 2 files / 4 tests passed，两份样本落盘 |
| **核到 CI 层**（实读 ci.yml 核证命令链） | **我自己实读**：`.github/workflows/ci.yml` visual job（`:116-154`）= pgvector service + `DATABASE_URL` + `prisma migrate deploy` + 两条 seed + `npm run build` + **`run: npm run test:visual`**；`test:visual = test:visual:fixtures && playwright test`；`playwright.config.ts:6` `testDir: './tests/visual'` ⇒ 新 spec 无需登记即被收，且前置样本在同一命令链内产出（该 job 带库，fixture 打库可行）✅ |
| 样本缺失必须红而非 skip | **我自己实测**：`mv /tmp/m48-timeout-body.txt` 后跑 spec → `1 failed / 3 did not run`，红在 `beforeAll` 抛（**不是 skip**）；还原后复绿 |
| 无超时/无错误会话渲染零变化 | spec 用例③（正常收敛真字节 → `timeout-notice` 与 `stream-error` 皆 count 0）绿 |
| **我自己加的第二条绕过探针** | 既有四条都断言**固定字面量**，挡不住「面板把文案硬编码、不读 data part」。我新增 `tests/visual/m48-eval-probe.spec.ts`：把真字节样本里的 `notice` 值换成独有标记再回放 → 卡片必须显示**样本里的那句**（实测 passed ⇒ 内容确实数据驱动，registry 单一真相源在渲染端成立）；附一条「空 notice 不渲染空壳卡」（passed） |

### F006 — budgetHitScope 扩 'specialist'/'both'（四象限双向钉） → **PASS**

| acceptance 分项 | 我的证据 |
|---|---|
| 类型四值 + 派生逻辑 | 实读 `loop-telemetry.ts:72`（union）、`:157-165`（2×2 派生，两层独立事实）、`loop.ts:472-480`（主 loop 聚合 consultation 产物 `budgetHit`，**结构判据**非工具名） |
| 四象限行为测试各一条 + 双向钉 | 实跑 `telemetry-budget-scope.test.ts` 8/8 绿：none/front/specialist/both 四格 + 两条反方向（专家没撞不得误报 / 咨询产物不得算进前台）+ 纯函数四格穷举。象限③ 还从**落库行**再钉一次（`payloadJson path ['budgetHitScope'] equals 'specialist'` 查得到、`budgetHit=false`、`v>=2`） |
| 变异证活（我的两条） | **E8** 让 `budgetHit = frontHit \|\| specialistHit`（专家撞顶带翻会话级口径）→ 2 条红（象限③ + 落库反向钉）；**E9** 把 `specialistBudgetHit` 改成 `consultBudget.used > 0`（「问过专家」= 「专家没答完」）→ 2 条红（反方向两条）。均还原后绿 |
| docblock 同步改写 | 实读 `loop-telemetry.ts:93-106`：旧句「记在各自 consultation 产物…」只作为**被替换的原文**被引用并说明已改。全仓 `grep 记在各自` 在 `docs/dev/` **零残留**（仅 spec 的「现状」段与该 docblock 引用） |
| `LOOP_TELEMETRY_VERSION` 是否 bump 由实物判并在 commit 正文说明依据 | 实读 `dba4d33` 正文：bump 依据是 **`'none'` 的语义变了**（v1 = 前台没撞；v2 = 两层都没撞），不是「多了两个取值」。我独立复核消费端：全仓无任何 `v === 1` 的查询/断言（唯一相关断言是 `>= 2` 的下界钉，刻意不与常量比）⇒ bump 不破坏既有查询，理由成立 |

### F007 — 文档翻牌 + doc-freshness 扩断言（含 S-RV3-4 携带） → **PASS**（1 条非阻断观察）

| acceptance 分项 | 我的证据 |
|---|---|
| 两份文档就本批 as-built 同步 | 实读 `7877b85` 全 diff 并**逐句对实物核**：遥测行四值 + `v=2` + `consultCount` + 超时独立留痕（`agent_timeout` / `loopTimeoutNotice`）、`§8.3.2` payloadJson 行、`§租户与认证` 的 M4.8 收口句、agent-architecture「M4.8 加固（as-built）」三条 —— **均与实物一致**（逐条对照 §3 各 feature 的实读结论） |
| doc-freshness 扩断言（枚举 = 实物 / 超时文案常量入表） | 实跑 `architecture-doc-freshness.test.ts` 29/29 绿 |
| 变异：源码/文档任一侧漂移必须红 | **我自己的三条**：**E10** 只改源码 union（加 `'unknown'`）、文档不动 → 红（这才是「文档新鲜度」真正要守的方向）；**E13** 只改 `architecture.md` 一份的枚举行（另一份不动）→ 红；**E11** 改坏两形态表**表头单元格**（共享→共用）→ S-RV3-4a 红；**E12** 把「M4.7 新增工具（1 件）」计数 +1 → S-RV3-4b 红。四条逐条还原后 29/29 复绿 |
| 批末新鲜度复核判据不带左括号 + 含 `docs/` 范围，先验活 | **我自己重跑一遍复核**（不采信作者记录）：① `grep -rn budgetHitScope docs/dev/` → 4 命中（判据活）；② 旧两值枚举 `'front' \| 'none'` 在 `docs/dev/` **0 残留**（仅 spec 的「现状」段，属历史陈述，正确）；③ `agent_timeout` / `loopTimeoutNotice` 在两份文档均在场；④ 「零告知 / OperationLog 零行」旧断言在 `docs/dev/` **0 残留** |
| backlog 两子项销账更新 | **观察（非阻断）**：`backlog.json` 的 `BL-E2E-CLEANUP-PIN` 末态**确已**把两子项标注为本批携带（`【S-RV3-4 → 已由 M4.8-HARDEN F007 携带】`、`（project-context-route 部分已由 M4.8-HARDEN F001 携带）`，decisions 亦有对应条目），**账面状态正确**；但 `git log -- backlog.json` 显示该文本写于**立项 commit `51599ff`**，本批 7 个 feature commit **无一触及** `backlog.json` —— 即「已由 F007 携带」这句在落笔当时尚无实物。判为**记账时序问题而非记账缺失**，登记 S-M48-5，建议 done 收尾时改写为完成态并引用实现 commit |

---

## 4. 我自建的变异 / 探针清单（22 条，全部实跑）

| # | 面 | 变异 / 探针 | 预期 | 实测 |
|---|---|---|---|---|
| E1 | F001/F003 | tenantId 挪进 OR 第一分支（伪作用域） | 双层红 | 行为级 2 红 + census 2 红 ✅ |
| E2 | F001/F003 | 查不到时无租户条件再查一次（善意回落） | 双层红 | 行为级 2 红（含真 loop 路径）+ census 2 红 ✅ |
| E3 | F002/F003 | 知识段 tenantId 挪进 OR 分支 | 双层红 | 行为级 1 红 + census 2 红 ✅ |
| E4a | F003 | 新违规点，`client . project . findFirst`（点后空格） | 红 | **未命中**（登记 S-M48-2） |
| E4a2 | F003 | 新违规点，prettier 常规格式 + 别名接收者 | 红 | ✅ 红并点名文件行 |
| E4b | F003 | 新违规点写成 `project.findMany` | — | 未命中（范围外，S-M48-2） |
| E4c | F003 | `where` 提取为局部常量 | — | 未命中（源码级扫描固有边界，S-M48-2） |
| E4d | F003 | `findFirstOrThrow` | — | 未命中（S-M48-2） |
| E5 | F004 | 超时判据放宽到 `AbortError` | 负向③红 | ✅ 1 红 |
| E6 | F004 | 留痕夹带工具入参正文 | 隐私断言红 | ✅ 1 红 |
| E7 | F004 | 摘 `finishReason` 落定兜底 | ? | **8/8 全绿 ⇒ 该防线无钉**（我已补钉，见 §3 F004） |
| E7′ | F004 | 我的新钉 vs E7 变异体 / vs HEAD | 红 / 绿 | ✅ 红（1 failed）/ ✅ 绿 |
| E8 | F006 | `budgetHit = front \|\| specialist` | 红 | ✅ 2 红 |
| E9 | F006 | 咨询发生即判专家撞顶 | 反方向红 | ✅ 2 红 |
| E10 | F007 | 源码枚举加值、文档不动 | 红 | ✅ 1 红 |
| E11 | F007 | 两形态表表头单元格改坏 | 红 | ✅ 1 红（S-RV3-4a） |
| E12 | F007 | 「新增工具（N 件）」计数漂移 | 红 | ✅ 1 红（S-RV3-4b） |
| E13 | F007 | 只改一份文档的枚举行 | 红 | ✅ 1 红 |
| E15 | F004 | 摘 `onLoopTimeout` 的 try/catch | ? | 断言仍全绿，但 `Errors 1` 且 **exit 1**（S-M48-4） |
| E16 | F001 | 摘 route 测试的遥测等待同步点 | 红 | ✅ 1 红（`pendingTelemetry`） |
| E17 | F005 | 抽掉字节样本 | 红非 skip | ✅ `1 failed / 3 did not run` |
| E18/E19 | F005 | 样本 notice 换标记串 / 置空 | 页面跟着变 / 不渲染空壳 | ✅ 2 passed（我的新 spec） |

> 所有产品面变异均以 `cp` 备份 → `python3` 定点改 → 实跑 → `cp` 还原 → `git diff --quiet` 核证；`docs/` 变异同法。终态 `git status --short` 只剩我的 4 个新增文件（§9）。

---

## 5. 逐 feature 结论

| Feature | 判定 | 一句依据 |
|---|---|---|
| F001 | **PASS** | tenantId 必选 + 同层 where；跨租户三口径逐一负向（含自证夹具与真 loop `systemPerStep`）；我的两条「第二条绕过」变异使行为级与 census 双层同时翻红；S-RV3-3 同步点自带活钉（E16 证活） |
| F002 | **PASS** | 知识段签名收口，跨租户返 `''` 且不抛；自证夹具排除「本来就没知识」的假通过；E3 变异双层红 |
| F003 | **PASS** | 独立扫描不从修复点派生（我塞的全新违规点当场被点名）、豁免空且硬编码、多行 OR 有先验活；终态判据我用独立 grep 复核 9 处全带 tenantId（spec 8→9 更正属实）。4 种写法绕过面登记 S-M48-2 |
| F004 | **PASS** | 注入与默认两路同一条监听、三条负向各带前提活性断言；判据放宽（E5）与隐私夹带（E6）均被当场抓住；第四条误报路径原无钉，我已补钉并双向证活 |
| F005 | **PASS** | 我自己跑出 4 passed（两视口）、fixtures 落盘、CI 命令链我实读核证、样本缺席实测为红非 skip；我另加的「文案数据驱动」探针证明面板未硬编码 |
| F006 | **PASS** | 四象限 + 两条反方向 + 落库层再钉一次；E8/E9 两条反向变异各 2 红；v bump 依据（`'none'` 语义变化）经我复核消费端后成立 |
| F007 | **PASS** | 两份文档逐句对实物核证一致；扩断言在源码侧漂移（E10）与文档侧漂移（E11/E12/E13）四条变异全红；批末复核判据我重跑一遍且先验活。backlog 记账时序登记 S-M48-5（非阻断） |

---

## 6. 需要 fixing 的项

**无。** 本轮未发现阻断性缺陷（无产品行为缺陷、无 acceptance 未兑现项）。下述 §8 六条为非阻断观察，建议在 done 收尾或下一批次顺手处理。

---

## 7. L2 未消耗面（未授权，本轮零执行）

| 项 | 状态 |
|---|---|
| `LOOP_TIMEOUT_MS = 110s` 在真网关 / 反代下的可达性（M4.7 L2-6） | [L2] 未执行，待授权 —— 本批只做可观测，不校准取值 |
| 三个成本常量真实校准（BL-AGENT-COST-CALIBRATE / S-RV3-1 硬前置：先补主闸绑定钉再改值） | [L2] 未执行，待授权 |
| 超时告知在**真**网关慢响应下的端到端表现（本轮全部由 mock 模型 + 注入/缩短闸驱动） | [L2] 未执行，待授权 |
| M4.6 / M4.7 / M4.8 生产部署与真网关复测 | [L2] 未执行，待人类闸门 |

---

## 8. 非阻断观察（issues，均不阻断签收）

| # | 级别 | 内容 | 建议 |
|---|---|---|---|
| I-1 | 低 | **dev 库净增 2 行孤儿 `OperationLog`**（`2026-08-04T04:04:15Z` / `04:04:37Z`，payload `v:1` ⇒ 写于 F006 bump 之前，形状 `steps:2 / toolNames:['propose_plan']` = F005 fixture 的「正常收敛」用例）。**HEAD 代码不复现**：我在 `04:37` 之后跑了全量 vitest ×2、fixtures、四个测试文件多轮、两套 e2e、全量 playwright，普查显示**无任何 04:04 之后的新孤儿行** ⇒ 是建造期（`waitForLogSettle` 落地之前）那几轮留下的 | 由人类或下批顺手清理这 2 行；纪律上属 spec §4「净增 0」的执行面残余 |
| I-2 | 低 | **交付叙述与实物差一（铁律 13）**：`7877b85` 正文写「vitest 全量 **124 files** / 1469 passed」，我在净树实测为 **123 files / 1469 passed**（tests 数一致，files 数差 1；`dba4d33` 正文的 123 files / 1465 与我复算一致） | 无需返工，登记备查；提醒「files 数」也属需机械依据的陈述 |
| I-3 | 低 | census 四种绕过写法（§3 F003） | 一行正则拓宽即可覆盖 `findMany` / `findFirstOrThrow`；`where` 提常量属源码级固有边界 |
| I-4 | 低 | F004 第四条误报路径（模型抛错收场）原无钉 —— 摘掉兜底 8/8 全绿 | **我已补钉**（`tests/integration/m48-eval-probe.test.ts`），本条已闭合 |
| I-5 | 信息 | 摘掉 `onLoopTimeout` 的 try/catch 时，断言不红，靠 vitest 的 unhandled error 使 exit=1 拦住 | 若要「钉在断言上」，可在回调抛错用例里加一条「本次运行无 unhandled error」的显式断言 |
| I-6 | 低 | backlog 两子项的销账文本写于立项 commit（落笔早于实物），F007 未再触及 `backlog.json` | done 收尾时改写为完成态并引用实现 commit |

### 8.1 soft-watch 登记（供 Planner 在 done 收尾裁决触发口径）

| 编号 | 内容 | 触发条件（建议） | 兜底归属 |
|---|---|---|---|
| **S-M48-1** | dev 库 2 行建造期孤儿 `OperationLog`（`2026-08-04T04:04`，HEAD 不复现） | 立即（一条 delete）或下一次本机跑视觉基线重生前 | 与 `BL-VISUAL-DATA-ISOLATION` 的「重生前先清」纪律同族 |
| **S-M48-2** | `project-scope-census` 的调用面正则只认 `.project.findFirst`：`findMany` / `findFirstOrThrow` / `where` 提常量 / 点号后空格四种写法可绕过（前两种一行正则可消除；后两种属源码级扫描固有边界） | 下一个动 `tests/unit/project-scope-census.test.ts` 或新增任何 Project 三口径解析点的批次 | M5（真实认证 + RLS）落地后由 RLS 兜底，本钉降为二线 |
| **S-M48-3** | 交付叙述里的**计数类**陈述（test files 数）同样需机械依据 —— 本批实测 123 vs 正文 124 | 下一次 commit 正文写全量测试计数时 | 铁律 13 既有条款，无需新机件 |
| **S-M48-4** | 「回调抛错不打死会话」类用例目前靠 vitest 的 unhandled-error 记账（exit 1）拦，未落在断言上 | 下一个动 `loop-timeout-notice.test.ts` 或新增同族回调（budget / persona_switch / timeout 之外的第四个）的批次 | 现状可拦（exit 1），仅失败可读性差 |
| **S-M48-5** | backlog 子项销账文本写于立项 commit（落笔早于实物），批末未复核 | done 收尾（本批内） | Planner 的 done 收尾流程 |

---

## 9. 副作用自查

| 项 | 结果 |
|---|---|
| 产品代码 | `git diff --stat -- src prisma docs .github package.json` = **空**（所有变异已还原） |
| 工作树新增 | 仅 4 个我的产物：`tests/integration/m48-eval-probe.test.ts`（F004 负向④ 钉，HEAD 上绿）、`tests/visual/m48-eval-probe.spec.ts`（F005 数据驱动探针，2 passed）、`scripts/test/m48-eval-db-census.ts`、`scripts/test/m48-eval-orphan-detail.ts`（两个**只读**取证脚本） |
| 自建测试数据 | 我的 vitest 探针使用**内存 ctx**（`tenantId='m48-eval-probe-tenant'`）+ 注入空 telemetryWriter ⇒ **零 DB 写入**；两个取证脚本只 `findMany/count`，无写操作 |
| DB 普查（口径：残留 `test-tenant-*` 租户 + 各表 tenantId 指向不存在租户的孤儿行） | `testTenantsLeft: []`；`operationLog.orphans: 6`（4 条 `2026-07-27` 既有 + 2 条 `2026-08-04T04:04` 建造期，见 I-1）；`shareLink.orphans: 2`（`2026-07-26` 既有）；`handoff/pendingAction/project/game/gameKnowledge` 孤儿 **0**。**我的验收活动净增 0** |
| dev 租户行数变化 | `operationLog.total` 115 → **118**（+3）：来自我跑的两套 e2e **按 append-only 口径显式保留**的 SHARE_CREATED 标记留痕（`agentloop:e2e` 2 行 + `frontdesk:e2e` 1 行，两脚本各自在收尾如实申报）。挂在**存活的 dev 租户**上，非孤儿；按 S-M45-1「留不删」纪律不清。⚠️ 本机下次重生含 feed/雷达的视觉基线前须先清（`web-runtime-patterns.md` §4.5 重生序） |
| 外呼 | 零。所有 loop 用例 `networkCalls === []`；playwright 全部 `page.route` 拦截；未触碰真网关 / 生产 / 计费 |
| 临时文件 | `/tmp/m48-timeout-body.txt`、`/tmp/m48-notimeout-body.txt`（fixtures 正常产物）、`/tmp/m48-eval/*`（我的备份，仅本机） |

---

## 10. 签收

**7/7 PASS。** 本批两条 backlog（`BL-TENANT-SCOPE-PROJECTREF` / `BL-LOOP-TIMEOUT-VISIBILITY`）在**行为层**已兑现，且钉子结构满足本仓既有纪律：正向精确匹配优先于黑名单、行为级与源码级两层不重叠、双向钉（该响的响 + 不该响的不响）、收编测试钉核到 CI 那一层、清理登记表外再压一层不从登记表派生的普查。

签收前提（均为非阻断）：§8 六条观察建议在 done 收尾一并处理，其中 I-1（2 行孤儿留痕）与 I-6（backlog 记账时序）建议本批内闭合。

— Evaluator（`Andy/evaluator-subagent`，fresh context），2026-08-04
