# M5.1-TENANT-INJECTION · F001 spec-lock scope 稽核

> **性质：** 派活链路的 **scope 稽核**工件，不是验收报告。它只判「有没有越界」，**不判实现对不对**。
> F001 的 acceptance 逐条判定属 Evaluator 在 verifying 阶段的职责，与本文件无关。
> **执行：** 隔离 spec-lock-critic subagent（只读工具集，不改任何代码与状态文件）
> **日期：** 2026-08-06
> **稽核对象：** kimi（`local-cli--kimi--generator`）交回的 F001 沙箱产物 @ `b0b1120201f5`
> **收编 commit：** `e6ecef1`

---

## 总判

**有条件放行**（`violation: false`）。文件集与跨 feature 边界均未越界；两条注释里的事实性断言经 grep 证伪，须更正。

---

## 1. 未越界（逐项有命令依据）

| 检查 | 结果 |
|---|---|
| 触及面 | `src/lib/db/`（1 改 3 新）+ `tests/integration/`（1 新）+ `docs/test-reports/`（派活契约强制产物，路径由 `validate-dispatch.sh:406` 硬校验） |
| 治理/配置文件 | `progress.json` / `features.json` / `docs/specs/` / `prisma/` / `.github/` / `.claude/` / `framework/` / `package.json` **全部零改动** |
| `.env`（spec §5.1 明令不许改） | `.env` 与 `.env.example` **逐字节相同**（6152 字节），且两者 `grep DB_APP_ROLE_RUNTIME` 均零命中 |
| 既有测试面（D-8） | `find tests -name '*.test.ts'` 基线 142 → 沙箱 143；`git status` 中 tests/ 下只有 1 个 `??`、**零个 `M`** |
| 调用点零改写（D-1 核心承诺） | `grep -rl "db/prisma" src` 基线 73 → 沙箱 **73**，导入面逐位不变 |

### `tests/` 新文件不算越界的理由

F001 acceptance ② 的括号字面「diff 不含 tests/」与同条的 ①④⑤ 自相矛盾：①要求三分支各有正向用例、④要求 raw 转发须有正向用例、⑤要求变异后用例必须红 —— 没有用例文件，这三项物理上无法交付。且 `vitest.config.ts:42` 的收集范围只认 `tests/`，无第三条路。故 ② 的可执行读法是「**既有** tests/ 文件零改动」（即 D-8），实测成立。

## 2. 未提前吃掉 F002 / F004

- **F004（15 处 `$transaction` 迁移）：** 基线 15 处所在的 10 个文件**无一出现在 diff 里**。沙箱多出的 5 处全在本 feature 新文件内（4 处注释 + `tenant-scope.ts:71` 是 `withTenant` 自己开事务）。
- **F002（嵌套语义）：** `tenant-scope.ts:67-75` 只有单层路径，无同租户复用判断、无跨租户抛错；`getTenantTxScope` 在该文件内除定义外零引用。
- **F002（两条硬回归）：** 新测试文件 `current_setting` 0 次、`set_config` 0 次、嵌套 `withTenant` 0 处、无 ≥12 并发循环。

### ⚠️ 放行条件（硬性，转 F002）

`tenant-scope.ts:72` 的 `SELECT set_config('app.tenant_id', $1, true)` 是 F002 acceptance ①(a) 的实现物，已提前落地。判为**不越界**（F001 自己的 ④ 要求「事务内 raw 查询看得见同事务写入的行」，没有真开事务的 helper 就交不出；`MissingTenantScopeError` 也必须住在这个文件里），但：

> **F002 不得以「F001 已实现」抵扣其 acceptance ①(a)**，仍须交出 `set_config` 断言 + 「第三参 `true` 改 `false` → B3 回归必须红」的变异实跑。若 F002 的 handoff 出现「已在 F001 落地，此处复用」类叙述而无新增用例，当场判 VIOLATION。

## 3. 规格符合性

D-2 三层分工 · D-2「复用不重写」（`app-role.ts` 零改动）· D-4 三分支顺序 · §4.3 `set_config` 第三参字面量 `true` —— 逐条与 spec 对上，无偏离。

## 4. 注释事实性断言核对（铁律 13）——**2 假 3 真**

稽核方对每条断言实跑 grep，不采信自述。

**✅ 属实：**「73 个 src 导入点」（第 73 个是 `instrumentation.ts:14` 的动态 import）·「206 处模型调用 / `tx.` 前缀 34」（按 spec D-1 口径精确复现）·「15 处 `$transaction`」·「`app-role.ts` 未改」且其行号引用实读核对无误。

**❌ 证伪（须更正）：**

| 位置 | 现文 | 实测 |
|---|---|---|
| `src/lib/db/tenant-scope.ts:13-14` | 「全仓此刻没有任何 withTenant 调用点」 | 同批产物自带 **5 处**：`tests/integration/tenant-injection-proxy.test.ts:101,117,131,223,247`。正确表述应为「**产品代码**零调用点」 |
| `src/lib/db/runtime.ts:8-9` | 「只有 ① tenant-scope.ts ② src/instrumentation.ts 能 import 本模块」 | **两头都错**：真实 importer 是 `src/lib/db/prisma.ts:23` 与 `src/lib/db/tenant-scope.ts:19`；**漏列 prisma.ts**，而 `instrumentation.ts:13-15` 动态 import 的是 `db/prisma` 与 `db/app-role`，**并不 import runtime** |

**为什么第二条要紧：** F003 的普查钉白名单要按「谁能 import 这些模块」建。照 `runtime.ts:8-9` 现文写死白名单，会漏掉真实 importer `prisma.ts`、并写入一个根本不存在的 `instrumentation.ts` —— **钉一建起来就是错的**。

## 5. 转下游的两条登记

- **F002：** `withTenant` 目前无嵌套守卫，嵌套调用会**静默开第二事务**（正是 B1-4 的病灶形态）。当前不可达（产品代码零调用点），但在 F002 落地前是 fail-open 的。
- **F003：** `tests/integration/tenant-injection-proxy.test.ts:24-25` 也 import 了 `db/privileged` 与 `db/runtime`，而 F003 acceptance ③ 的扫描面只写了 `src/` —— 这个测试侧 importer 落在钉的射程外，**F003 开工时须显式决定收不收**，不得默认略过。

---

## 附：稽核方实际跑过的命令（「没发现问题」的举证面）

`git status --short --ignored=matching` · `git diff b0b1120201f5` · `cmp .env .env.example` · `grep -n DB_APP_ROLE_RUNTIME .env{,.example}`（含主仓）· `git status --short -- docs/ progress.json features.json backlog.json .github/ harness.json .claude/ framework/ package.json prisma/` · 基线↔沙箱双跑 `grep -rl db/prisma` / `grep -rn '\$transaction'` / `find tests -name '*.test.ts'` · `grep -rn withTenant`（src、tests、scripts、prisma 四目录）· `grep -rn "db/privileged\|db/runtime"` · spec D-1 口径的 206/34 复现 · `vitest.config.ts:42` 收集范围 · `.github/workflows/ci.yml` + `package.json` scripts

> CI 侧附带核实：`test:unit:coverage` = `vitest run --coverage` 会收集新增的 integration 文件；`ci.yml:88` 已配 `DATABASE_URL_APP`、`ci.yml:106` 跑 `bootstrap-app-role.sh` 建 kol_app，故新测试文件顶层的 env 硬抛不会把 CI 拖红。
