# M5.1b-TENANT-INJECTION — F003 / F004 复验报告（reverify round 1）

| 项 | 值 |
|---|---|
| 批次 | M5.1b-TENANT-INJECTION |
| 阶段 | `reverifying`（`fix_rounds=1`） |
| 被验 HEAD | `41ba4dba6c5ed5afd6e2eb2e48ed29d5ffd60fb0` |
| 验收范围 | **F003**（引导白名单 + 源码级普查钉）· **F004**（`$transaction` 迁移到 `withTenant`） |
| 执行者 | 隔离 evaluator subagent（独立 worktree `.claude/worktrees/agent-a6218f8f5e218e5ae`） |
| 日期 | 2026-08-08 |
| **结论** | **F003 = PASS**（附 3 条盲区登记）· **F004 = PASS**（附 1 条清态登记） |

> 判据纪律：commit 正文 / 代码注释 / `generator_handoff` 一律作**被验对象**处理，不作通过依据（铁律 13）。
> 下面每条判定后面跟的是我自己跑出来的命令输出或 `文件:行`。

---

## 0. 环境、边界与并发

- worktree 内 `node_modules` 符号链接主仓（未 `npm install`），`.env` 复制自主仓。
- 冒烟：`npx vitest run tests/unit/health.test.ts` → 1 file / 32 tests passed。
- Node v25.7.0 · `newkolmatrix-dev-db` 容器 healthy（127.0.0.1:5434）· `.prisma/client` 已生成。
- 另有 2 个 evaluator 并行共用同一本机 dev Postgres。**本轮未观察到任何与 F003/F004 无关的失败**，
  故无「观察到但不可复现」条目。
- **L2 边界申报：** `reach:e2e` 用脚本自带降级开关跑（`SKIP_DRAFT_LLM=true`），脚本自报
  `gateway chat 0 次 · 真实投递 0 封（mock）`。真实资金动作 0 · 生产写入 0 · 未触发部署。
- **本轮 fix 的 `src/` 改动是纯注释**（机械证明，非目测）：

```
git diff 8c5fee1 41ba4db -- src/ | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)' \
  | grep -vE '^[+-]\s*//' | grep -vE '^[+-]\s*\*' | grep -vE '^[+-]\s*/\*\*?$' | grep -vE '^[+-]\s*$'
  → 空输出（零条非注释行）
```

因此 F003/F004 的**产品行为**与首轮判 PASS 的各条相同；本轮所有新增鉴别力都落在测试面。

---

## 1. 基线

```
npx vitest run tests/unit/bootstrap-whitelist-census.test.ts \
               tests/unit/db-layer-importer-census.test.ts \
               tests/integration/f004-transaction-atomicity.test.ts
  → Test Files 3 passed / Tests 21 passed

npx vitest run            → Test Files 148 passed (148) / Tests 1875 passed (1875)
npx tsc --noEmit          → exit 0
npx next lint             → ✔ No ESLint warnings or errors
```

变异用的基线 `shasum -a 256`（还原时逐位对账，见 §5）：

```
03261c72…3174d  tests/unit/bootstrap-whitelist-census.test.ts
55eeb858…2da14  tests/unit/db-layer-importer-census.test.ts
32296179…31089  tests/integration/f004-transaction-atomicity.test.ts
58c8e812…4976d  src/lib/agent/gate/gate.ts
e25977b3…bf228  src/lib/db/tenant-scope.ts
```

---

## 2. F003(a) —— 重写后的「白名单恒不可从别处推导」守卫，实打新判据

首轮实测：旧守卫（`/BOOTSTRAP_WHITELIST[\s\S]{0,200}process\.env/` + `= [` 开头）对派生清单
**完全无鉴别力**。本轮它被重写为「取声明整段 → 抹字符串 → 骨架里只许出现 6 个标识符 + 禁展开 +
禁模板串 + 骨架长度 > 200」（`tests/unit/bootstrap-whitelist-census.test.ts:209-260`）。

### 2.1 先立活性判据：一个只有 F003 钉看得见的真后门

`src/lib/__evalprobe_backdoor.ts` —— 取 `globalThis.privilegedDb` 单例（`privileged.ts:45-47` 挂的），
**不 import `db/privileged`**。基线（未变异）实测：

```
× bootstrap-whitelist-census (F003)  AssertionError: 发现**未登记**的特权连接使用点：src/lib/__evalprobe_backdoor.ts
✓ db-layer-importer-census  (F008)   4 passed        ← F008 恒盲（无 import 语句）
Tests  1 failed | 12 passed (13)
```

→ 这个探针是「只有 F003 白名单机制在守」的真实后门。下面每条变异都带着它跑，
**「钉全绿」= 后门被自动吸收进白名单**，不是「变异空转」。

### 2.2 六条变异的实测结果（全部我自己跑，非只读推断）

| # | 变异形态 | 实测输出 | 抓到？ |
|---|---|---|---|
| **M1** | **上一轮用过的那条**：`const BOOTSTRAP_WHITELIST = [...deriveFromScan()]`（理由做长、含「租户/会话」以避开长度断言的偶然拦截） | `Tests 1 failed \| 4 passed`；`AssertionError: 白名单声明里出现了展开运算符 —— 清单被从别处灌入` | **抓到** ✅（且红的正是 ⑤ 那条，其余 4 条按预期不红） |
| **M2** | **我自造**：声明保持字面量不动，**在声明之后 push 扫描派生条目**<br>`(BOOTSTRAP_WHITELIST as Array<…>).push(...DERIVED_EXTRA)` | `Test Files 2 passed / Tests 13 passed` —— 含后门探针在场，**F003 + F008 双绿** | **漏** ❌ |
| **M3** | **我自造**：把一份「长得像字面量声明」的诱饵藏进模板字符串放在前面，真声明改成 `= deriveFromScan();`（守卫用 `code.indexOf('const BOOTSTRAP_WHITELIST')` 取第一处，只剥注释不剥字符串 → 取到诱饵） | `Tests 5 passed (5)` —— 后门探针在场仍全绿 | **漏** ❌ |
| **M5** | **我自造**：`JSON.stringify(扫描结果)` → `JSON.parse(...)` 赋值 | `Tests 1 failed \| 4 passed`；`AssertionError: 找不到白名单声明的结尾 \n];: expected -1 to be greater than 544` | **抓到** ✅（但机制是「找不到结尾」，不是识别出派生） |
| **M6** | **我自造**：getter（`WHITELIST_HOLDER.list` 每次调用现扫） | `Tests 1 failed \| 4 passed`；同上 `expected -1 to be greater than 627` | **抓到** ✅（同 M5，靠结尾找不到） |
| **M4a** | **我自造**：`SRC_ROOT = 'src/lib'`（扫描面偏移） | `Tests 2 failed \| 3 passed`；`发现未登记…__evalprobe_backdoor.ts` + `白名单里这些文件已不再使用 privilegedDb：src/app/api/auth/[...nextauth]/route.ts, src/app/api/auth/register/route.ts` | **抓到** ✅（靠「陈旧登记」那条，非 ⑤） |
| **M4b** | **我自造**：`SRC_ROOT` 不动，`collectSources` 里加一行 `if (entry === 'jobs') continue;`，后门探针放进 `src/lib/jobs/` | `Test Files 2 passed / Tests 13 passed` —— **双钉全绿** | **漏** ❌ |

### 2.3 判定

**acceptance ⑤ = PASS。** 三条理由，全部来自原文与实物，不是宽容：

1. ⑤ 的主语是**白名单这件实物**（「白名单恒不可从别处推导」），as-built `:70-102` 是纯字面量数组，
   不读 env、不读配置、不从扫描派生 —— 合规。
2. ⑤ 自己指定的范式 `tests/unit/project-scope-census.test.ts` **我独立读过**（`:42-48` 的字面量数组 +
   `:209` 的 `expect(EXEMPTIONS).toEqual([])`）：该范式里**没有任何「检测派生」的守卫**，
   唯一的机械断言是「清单必须为空」，而 F003 的白名单刻意非空、不适用。照 ⑤ 指定的范式做，
   产出就是 as-built 的样子。
3. 那条多加的守卫**不再是恒绿的假保证**：首轮把它打穿的那条形态（M1），本轮实测当场红，
   且只红它一条（D-8.1 要求的变异证活，我自己跑出来了）。

**但登记两件事（见 §6 盲区）：** M2 / M3 / M4b 三条派生形态仍能全身而退，其中 **M2（声明后 push）
是未来维护者「让清单自动同步」时最自然会写的那一种**；而钉文件头对新守卫**未登记任何盲区**
（它只登记了标识符扫描 / import specifier 那两维的旧盲区）。用例名「白名单**恒不可**从别处推导」
在 M2/M3 下仍然绿着 —— 名字比它机械上守得住的东西强。这是交付质量问题（本项目自己的登记纪律），
不是 acceptance ⑤ 的缺项。

---

## 3. F003(b) —— spec §4 硬约束第 2 条（tests/ 侧射程）

**约束原文**（`docs/specs/M5.1b-TENANT-INJECTION-spec.md:120-121`）：

> **F003 须显式决定**要不要把 `tests/` 侧的 `db/privileged` / `db/runtime` importer 纳入普查钉射程
>（其 acceptance ③ 现只写 `src/`）。决定写进代码注释，**不得默认略过**。

首轮实测「全仓找不到任何这样的代码注释」。本轮实物：

- `tests/unit/bootstrap-whitelist-census.test.ts:43-54` —— 标题即
  「【tests/ 侧是否纳入射程 —— 显式决定：**不纳入**（M5.1b spec §4 硬约束第 2 条要求表态）】」，
  其后是**理由**（守的是产品越权面；夹具是脚手架不是越权面；纳入后所有 reason 会退化成同一句，
  清单失去复查价值）+ **代价如实登记**（tests/ 侧若拿 privilegedDb 去「验产品行为」两钉看不见，
  典型误用是用特权连接验 RLS，会恒绿）+ 兜底归属（F006 acceptance ②(f)）。
- `tests/unit/db-layer-importer-census.test.ts:31-43` —— 同一决定写在 `SRC_ROOT` 正上方，
  理由按本钉自身职责（文档漂移面）另写一遍，并登记代价「注入面完整性这一结论只对 src/ 成立」。

**判定：兑现。是「决定 + 理由 + 代价」，不是复述口径。** 与首轮明确区分：首轮两钉只有
`const SRC_ROOT = 'src';` 一行硬编码、零表态。

**对该决定里两条援引性事实的核实（避免又一次「与 X 一致」而未核 X）：**

| 援引 | 核实 | 判定 |
|---|---|---|
| 「F006 acceptance ②(f) 明确要求跨租户零行的证据必须来自 kol_app 连接」 | `features.json` F006 acceptance ②(f) 原文：「该零行有 **DB 层证据**（kol_app 连接直查 raw SQL 得 0 行），不是只有应用层 where 的 404」 | **属实** |
| 「F002 / F005 / F006 的夹具都这么写，理由各自写在文件头」 | F005 `tests/integration/als-context-propagation.test.ts:22`（文件头）· F006 `tests/e2e/tenant-rls-min-loop.e2e.test.ts:19-21`（文件头）· F002 `tests/integration/tenant-scope-semantics.test.ts:55`（在函数旁注，**不在文件头**） | **实质属实，「文件头」这个词对 F002 不准**（一处措辞不精确，不改判定） |

### F003 逐条汇总

| acceptance | 判定 | 依据 |
|---|---|---|
| ① 先普查后改 + 判据先证活 + 差异登记 | **PASS** | 我独立重跑普查（§4.1），as-built 白名单 5 文件与实物逐位相符 |
| ② 白名单每处改 privilegedDb + 旁注理由 + 开关关行为不变 | **PASS** | 5 处 import 旁均有理由；auth 相关 6 文件 **205 tests passed** |
| ③ 普查钉（扫 src/ 全仓、写死白名单、readFileSync、不用 git grep、红且点名） | **PASS**（沿用首轮同一口径登记：钉的判据是「标识符使用」而非字面 import specifier，后者由 F008 钉承担） | `:104-111` readFileSync 递归；`:70-102` 字面量；点名实证见 §2.1 |
| ④ 变异证活三条 + 抓不到的如实登记 | **PASS** | 钉文件头 `:30-39` 已把首轮证伪的「盲区不重叠」改成实测正确的登记；**我独立复现该登记**：`src/lib/__evalprobe_concat.ts`（`'lib/db/'+'privileged'` + `'privileged'+'Db'`）在场时两钉 `13 passed` 全绿 → 登记属实 |
| ⑤ 白名单恒不可从别处推导 | **PASS**（附盲区） | §2.3 |
| ⑥ tsc / lint | **PASS** | exit 0 / `✔ No ESLint warnings or errors` |
| spec §4 硬约束第 2 条 | **兑现** | §3 |

---

## 4. F004 —— acceptance ③ 第 2 点（`gate.ts` `executePendingAction`）

### 4.1 我自己重跑的两份普查（不采信被验方数字）

**判据先证活**（`audit-methodology.md` §8）：`grep -rn 'privilegedDb' src/lib/auth/index.ts` 抓到
`:15 import` / `:30` / `:58` 三处已知点 → 模式看得见目标。

```
grep -rl 'privilegedDb' src --include='*.ts' --include='*.tsx'   → 10 文件
逐文件核 → 仅注释/字符串提及的 4 个：instrumentation.ts · db/runtime.ts · db/tenant-scope.ts · jobs/scheduler.ts
真实使用 = 5 文件（+ 模块自身 db/privileged.ts）= 写死在钉里的白名单，逐位相符
```

```
grep -rn '\$transaction' src --include='*.ts' --include='*.tsx'
真调用点仅 2 处：src/lib/auth/register.ts:110（F003 白名单事务，acceptance ② 明令不迁）
                 src/lib/db/tenant-scope.ts:181（withTenant 自身实现，非迁移对象）
其余 7 处命中全部是注释文本
```
→ 13 处产品调用点确已迁走，与首轮结论一致。

### 4.2 重放对抗复核那条「不新增 import」的变异

`src/lib/agent/gate/gate.ts:373` `db: tx` → 取 `globalThis.runtimeClients.get('__default__')`
（`Prisma` 类型已在 `:18` type-only import，**未新增任何 import 语句**，故 F008 的 importer 钉不会替它翻红）。

```
npx vitest run tests/integration/f004-transaction-atomicity.test.ts \
               tests/integration/create-share-link.test.ts \
               tests/integration/payout-gate.test.ts \
               tests/unit/db-layer-importer-census.test.ts

× f004-transaction-atomicity > gate.ts:365 … > 🔒 工具先真写一行再抛错 → 回滚后零残留（而不是「压根没写过」的零）
  AssertionError: 工具的业务写入没有随调用方事务回滚 —— 它逃出了那个事务: expected 1 to be +0

✓ create-share-link.test.ts   全部 11 tests 绿（含 :218「pending→confirm 窗口内项目被删」）
✓ payout-gate.test.ts         全部 12 tests 绿（含「绕过前端直打 execute」）
✓ db-layer-importer-census    8 tests 绿（未新增 import，钉如预期不动）
Tests  1 failed | 38 passed (39)
```

**红了 1 条，红的正是该红的那条。** 首轮被登记为「已覆盖」的两个文件在同一变异下 23 条全绿 ——
再次实证它们对本病灶零鉴别力，本轮把它们从覆盖身份上摘掉（文件头 `:14-27` 的更正段）属实。

### 4.3 新用例自身的空绿风险 —— 我另打了一条反向变异

新用例配了一条「防空绿」对照（`:378-403`：不经闸门直调 execute → 必须看得见那一行）。
**这条对照本身有没有活性？** 我把探针工具的 `db.operationLog.create(...)` 摘掉（改成 `void db;`）：

```
✓ 🔒 工具先真写一行再抛错 → 回滚后零残留（…）           ← 退化成空绿，如预期不红
× 探针工具确实会写（防止上一条因「工具压根没执行」而空绿）
  AssertionError: 探针工具没有真的写入 —— 上一条用例是空绿: expected 0 to be greater than 0
Tests  1 failed | 7 passed (8)
```

→ 防空绿断言**有真实活性**，且失败信息直接点名「上一条用例是空绿」。

另外三重结构性防空绿（我读实物核过，非推断）：主断言链要求
`rejects.toThrowError(new RegExp(PROBE_BOOM))` —— 工具必须真跑到并抛出**自己那条** boom 才算数；
探针工具名 `f004_rollback_probe_${process.pid}` 与租户 slug 都带 pid 前缀，并行 evaluator 不串味。

### 4.4 acceptance ③ 的 7 个点在 HEAD 上的守卫

| # | 点 | 有鉴别力的用例 | 本轮核实方式 |
|---|---|---|---|
| 1 | `gate.ts` `confirmPendingAction` | `f004-transaction-atomicity.test.ts:152` | 首轮变异证活（摘 withTenant 嵌套复用 → 红） |
| 2 | `gate.ts` `executePendingAction` | **`f004-transaction-atomicity.test.ts:361`（本轮新增）** | **§4.2 变异红 + §4.3 防空绿反向变异红** |
| 3 | `gate.ts` `rejectPendingAction` | `:169` | 同 1 |
| 4-6 | `delivery/register.ts` ×3 | `:190 / :206 / :226` | 同 1 |
| 7 | `auth/register.ts` | `tests/integration/auth-register.test.ts:192` | 本轮 205 tests 全绿 |

**7/7 齐，缺口已补。**

### 4.5 acceptance ④ 三套脚本（开关关，输出原样）

```
[gate-smoke] ✅ 全部断言通过（两步票据 + 7 态 + 并发竞态 + D20 变异）
[gate-smoke] 测试数据已清理（含 F003 夹具）                                    exit=0
  ✓ G7: failed 无 irrev 行（工具未成功 ⇒ 不留 irrev；回滚传播另由 f004 原子性用例守）
    ← 本轮把首轮点名「名不副实」的那句断言描述改对了；改的是描述，不是判据

[delivery-e2e] ✅ 交付闭环全绿
[delivery-e2e] 用量申报：真实资金动作 0 · 真实 key 发放 0（mock 适配器）        exit=0

[reach-e2e] ✅ E2E 闭环全绿                                                     exit=0
[reach-e2e] L2 用量申报：gateway chat 0 次 · 真实投递 0 封（mock）
    ← 用脚本自带 SKIP_DRAFT_LLM=true 降级跑（§0 已申报）
```

### F004 逐条汇总

| acceptance | 判定 | 依据 |
|---|---|---|
| ① 迁移前重跑普查 + 与 spec 对账 | **PASS** | §4.1，我独立重跑 |
| ② 逐处迁移可审 + register 白名单单独断言 | **PASS** | 首轮已变异证活（改 register 的 import → 2 条红）；本轮 `src/` 零非注释改动，结论沿用 |
| ③ 7 个点各一条「回滚后零残留」行为级用例 | **PASS**（首轮 6/7 → 本轮 7/7） | §4.2 / §4.3 / §4.4 |
| ④ gate:smoke / delivery:e2e / reach:e2e 开关关全绿 | **PASS** | §4.5，三个 exit=0 |
| ⑤ 摘掉 withTenant 回滚传播 → ③ 对应用例必须红 | **PASS** | 首轮实跑 5/6 红且唯一保持绿的是 privilegedDb 那条反向断言；本轮 `tenant-scope.ts` 仅注释变更（§0 机械证明），结论沿用 |
| ⑥ tsc / lint / 全量 vitest 绿 | **PASS** | `tsc exit 0` · lint clean · `148 files / 1875 tests passed` |

---

## 5. 变异清单与还原对账（**未用 `git checkout`**）

| # | 变异 / 探针 | 观察 | 还原证明 |
|---|---|---|---|
| M1 | 白名单 → `[...deriveFromScan()]` | 1 条红（⑤） | 基线文件覆盖，`shasum` = `03261c72…3174d` = 基线 ✅ |
| M2 | 声明后 push 派生条目 | **双钉全绿（漏）** | 同上 ✅ |
| M3 | 模板串诱饵声明 + 真声明改派生 | **全绿（漏）** | 同上 ✅ |
| M4a | `SRC_ROOT = 'src/lib'` | 2 条红 | 同上 ✅ |
| M4b | `collectSources` 跳过 `jobs` 目录 | **双钉全绿（漏）** | 同上 ✅ |
| M5 | `JSON.parse(JSON.stringify(扫描结果))` | 1 条红（⑤） | 同上 ✅ |
| M6 | getter 现扫 | 1 条红（⑤） | 同上 ✅ |
| M7 | `gate.ts:373` `db: tx` → `globalThis.runtimeClients`（**不新增 import**） | 1 条红，正是新用例 | **反向编辑**还原，`shasum` = `58c8e812…4976d` = 基线 ✅ |
| M8 | 探针工具去掉写入（验防空绿断言的活性） | 防空绿那条红并点名 | 基线文件覆盖，`shasum` = `32296179…31089` = 基线 ✅ |
| P1 | `src/lib/__evalprobe_backdoor.ts`（globalThis 取特权单例，不 import） | 基线下 F003 红点名 / F008 绿 | 已 `rm` |
| P2 | `src/lib/__evalprobe_concat.ts`（拼接路径 + 拼接键） | 双钉全绿（复现登记的盲区） | 已 `rm` |

**还原后复跑：** `bootstrap-whitelist-census + db-layer-importer-census + f004-transaction-atomicity`
→ `3 files / 21 tests passed`；全量 `148 files / 1875 tests passed`。
`git status --short` 仅 `?? node_modules`（我建的符号链接，非产物）。

**主仓 `/Users/yixingzhou/project/newkolmatrix` 的 `src/` 全程未触碰**（全部变异只发生在本 worktree）。

**dev 库清态：**

```
Tenant slug LIKE 'f004atomic-%'                    → 0
Tenant slug LIKE '%evalprobe%' / '%evalmut%'       → 0
OperationLog summary LIKE '%gate365-probe%'        → 0
PendingAction toolName LIKE 'f004_rollback_probe%' → 14 → 定向删除孤儿行 14 → 0
```

---

## 6. 盲区与如实登记（判不了 / 未覆盖 / 未处置）

1. **【新，本轮实测】F003 ⑤ 的新守卫有三条未登记的绕过形态：**
   (a) **声明后 push 派生条目**（声明本身保持字面量）；(b) **模板串里的诱饵声明**
   （守卫用 `indexOf` 取第一处、且只剥注释不剥字符串）；(c) **扫描面偏移**
   （`collectSources` 里跳一个目录 —— 这条落在 acceptance ③「扫 src/ 全仓」那一面，
   而该面除了 `files.length > 50` 之外没有任何守卫）。
   三条形态下真实后门探针都被静默吸收。**钉文件头对新守卫零盲区登记**，
   用例名「恒不可」强于它机械上守得住的东西。建议（供 Planner 排期，非本轮判定驱动）：
   在 `:209` 那条用例的注释里补登记这三条，或把判据从「取第一处声明的文本切片」
   改成「断言运行期的 `BOOTSTRAP_WHITELIST` 与源码里那段字面量逐条等同」。
2. **【承接首轮，本轮未处置且我已复现】两道普查钉在「拼接路径 + 拼接键」这一维盲区重叠**：
   `src/lib/__evalprobe_concat.ts` 在场时 `13 passed` 双钉全绿。
   钉文件头 `:30-39` 已把首轮证伪的「盲区不重叠」改成正确表述并说明为何不修（要上 AST/插桩），
   **登记内容经我实测属实**。不影响本轮判定。
3. **【承接首轮，本轮未处置】`NestedTransactionOptionsError` 零测试覆盖**：
   `grep -rn NestedTransactionOptionsError src/ tests/ scripts/` → 产品侧 `tenant-scope.ts:149/156/167`
   与三处注释提及，`tests/` 侧唯一命中是 `f004-transaction-atomicity.test.ts:284` 的**注释**。
   它是 F004 新用例换判据的直接原因（gate 的 90s 事务选项），却本身没有任何断言守着。
   不在 F003/F004 任何一条 acceptance 内 → 登记，不判缺项。
4. **【新，清态】`f004-transaction-atomicity.test.ts` 每跑一次泄漏孤儿行**：
   `PendingAction.tenantId` 在 `prisma/schema.prisma:736-748` 是裸 `String`、**无 FK 无级联**，
   而 `afterAll` 只删 `Tenant` + 按 `slug` 前缀普查 `Tenant`。实测 dev 库孤儿行：
   `f004_confirm ×40` · `f004_reject ×40`（首轮之前就有的形态）· `f004_rollback_probe_* ×14`（本轮新探针）。
   `generator_handoff.l1_evidence.residue` 写「dev 库 f004atomic- 前缀 tenants=0 / logs=0」——
   **陈述本身为真，但该判据（Tenant.slug 前缀）在结构上看不见 PendingAction**，
   因为那些行不带任何前缀（`audit-methodology.md` §8：判据要先证明能看见目标）。
   F004 的 acceptance 无清态条款（清态是 F006 ⑤ 的），故**登记不判缺项**；我已定向删除 14 条探针孤儿行。
5. **「先普查后改」的时序不可事后机械核实** —— 我只能核实普查**结论**正确（已独立复现），
   无法证明它发生在改动之前。
6. **开关开（`DB_APP_ROLE_RUNTIME=1`）下的 F003/F004 行为本轮未覆盖** —— 该面归 F006 的 `rls:e2e`。
7. **生产 / staging 实测未做（L2 未授权）** —— 全部结论来自本机 dev Postgres 与进程内测试。

---

## 7. 结论

| feature | 首轮 | 本轮 | 驱动 |
|---|---|---|---|
| **F003** | PARTIAL | **PASS** | (a) 旧恒绿守卫已换成有鉴别力的正向判据（首轮打穿它的那条形态实测当场红），acceptance ⑤ 约束的白名单实物本就合规；(b) spec §4 硬约束第 2 条已用「决定 + 理由 + 代价」兑现，两钉射程一致 |
| **F004** | PARTIAL | **PASS** | acceptance ③ 唯一缺口 `gate.ts` `executePendingAction` 已补，新用例经我重放「不新增 import」等价变异**单独翻红**，且其防空绿对照本身经反向变异证明有活性；④ 三套脚本 exit=0 |

---

*本报告由隔离 evaluator subagent 在独立 worktree 内产出，结论未经任何协商。*
