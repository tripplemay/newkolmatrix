# M5.1b-TENANT-INJECTION 复验（fix-1）— F007 复验 + F002/F005/F006 回归 + 全批 L1

| 项 | 值 |
|---|---|
| 批次 | M5.1b-TENANT-INJECTION |
| 轮次 | reverifying（`fix_rounds=1`） |
| 被验 HEAD | `41ba4dba6c5ed5afd6e2eb2e48ed29d5ffd60fb0` |
| 对比基线 | `8c5fee1`（fix-1 之前） |
| 执行者 | Andy/evaluator-subagent（隔离上下文，独立 git worktree） |
| 工作副本 | `/Users/yixingzhou/project/newkolmatrix/.claude/worktrees/agent-acf8d086054a5d8b3` |
| 日期 | 2026-08-08 |
| L2 授权 | 未授权 —— 未连真实外部服务、未触发部署、零计费、零生产写入。本机 dev Postgres（`localhost:5434`，容器 `newkolmatrix-dev-db`）与进程内测试属允许范围 |

## 结论速览

| 项 | 结论 |
|---|---|
| **F007（复验）** | **PASS** —— 收窄**不**削弱 acceptance ②；必要性经我独立实测证实；但登记的代价陈述有一条**事实错误**，且最小性未达（见 §2.5，登记为 finding 但不改判） |
| F002（回归） | 无回归。src 改动经机械证明只涉注释；证据文件未被触碰；钉在 HEAD 仍有鉴别力（实打变异 2 红） |
| F005（回归） | 无回归。三上下文用例面全绿 |
| F006（回归） | 无回归。`rls:e2e` 15 passed；自有夹具前缀零残留 |
| L1 | tsc exit 0 · lint 无告警 · vitest 148 files / 1875 tests 全绿 · rls:e2e 15 passed |

---

## 1. 判据纪律声明

本报告不把 commit 正文、代码注释、`generator_handoff` 当作通过依据（铁律 13）——它们是**被验对象的自述**。
下文每条结论都标注了产出它的那条命令与输出。凡「零命中 / 无残留」类结论，**先证明判据能看见目标**再取数
（`framework/patterns/audit-methodology.md` §8）。本报告中有 **1 次判据自证失败并被当场废弃**（§3.1），
如实登记。

---

## 2. (A) F007 复验 —— 扫描面收窄的独立判断

### 2.1 被判断的改动

`tests/unit/architecture-doc-freshness.test.ts` 新增 `DOCS_EXCLUDED = ['docs/test-reports', 'docs/archive']`，
并在 `collectDocs()` 递归入口短路。改动量 `+20 / -0`（`git diff --numstat 8c5fee1..41ba4db`）。

该改动由 commit `57998bf`（subject 标 **F008**）携带，正文有独立小节
「一处 F007 那道钉的真实缺陷（本轮修复时暴露，非 F008 范围但必须一起修）」明示。

### 2.2 acceptance ② 的三项要求，逐条对照

F007 acceptance ② 原文：*「判据须**不带左括号**且含 `docs/` 范围，并**先验活**」*。

| 要求 | 收窄后是否仍满足 | 依据 |
|---|---|---|
| 判据不带左括号 | **是**，收窄完全未触及判据形态 | 3 条 stale needle 与 4 条计数 pattern 均未改动（diff 只有 `DOCS_EXCLUDED` + 一行短路） |
| 含 `docs/` 范围 | **是** | 扫描仍从 `docs` 根起递归；保留 `docs/dev` `docs/specs` `docs/product` `docs/audits` `docs/reviews` `docs/test-cases` + docs 根 |
| 先验活 | **是，且未被收窄掏空** | 见 §2.3 |

**§8 的原始失效形态是「压根不搜 `docs/`」**（`audit-methodology.md`:168 原文：「人工批末复核用的 grep
又带左括号（`stepCountIs(`）**且不搜 `docs/`**」）。收窄后仍搜 `docs/`，且现行权威文档面 100% 在扫描内。
故**收窄不构成 acceptance ② 的削弱**。

### 2.3 先验活断言本身是否被收窄掏空（我加做的检查）

这是收窄的一个真实风险：若 liveness 探针的 needle 恰好只存在于被排除目录，探针要么翻红、要么静默退化。
实测两条 needle 的落点：

```
$ grep -rln "租户注入机制" docs/
docs/specs/M5.1b-TENANT-INJECTION-spec.md       <- 保留
docs/test-reports/M5.1b-verify-F007-L1.md       <- 被排除
docs/dev/architecture.md                        <- 保留，探针靠它活

$ grep -rln "MissingTenantScopeError" docs/      （节选）
docs/specs/M5.1-uncovered-entrypoints.md        <- 保留
docs/specs/M5.1b-TENANT-INJECTION-spec.md       <- 保留
```

两条 needle 在**保留面**内各有落点，探针不依赖被排除目录存活。基线实跑 41/41 绿，其中含该探针。

### 2.4 收窄是否必要 —— 我自己重放，结论：**必要，且成因是结构性的**

**变异 M1：** 把 `DOCS_EXCLUDED` 改回 `[]`（即 fix-1 之前的行为），实跑：

```
× 陈旧句零残留：注入机制已落地，文档不得再写「落地后才…」
AssertionError: 已作废的陈述仍在 docs/：「M5.1 注入机制落地后」: expected [ …(2) ] to deeply equal []
+ [
+   "docs/test-reports/M5.1b-verify-F007-L1.md:70",
+   "docs/test-reports/M5.1b-verify-F007-L1.md:110",
+ ]
 Tests  1 failed | 40 passed (41)
```

红是真的，命中文件与条数与 Generator 自述一致（**我的独立实跑**，非采信其叙述）。

**成因是结构性的，不是这一份报告写得不好：** 验收报告为可审必须**原文引用**被证伪的旧句
（「原句是 X，实测为假」）；而这道钉按**字面**匹配那些旧句。两条规矩直接对撞——
只要 Evaluator 履行「引用原文」的义务，仓库就翻红。这是自指陷阱，不收窄就只能要求
验收报告不许引用原文（损失可审性）。故**收窄的方向正确**。

同一自指陷阱的另一面在本批已有先例：Generator 写 `tenant-scope.ts` 注释时照抄旧句，被
`db-layer-importer-census` 的字面匹配当场钉红，只能改述。两处同因、两种解法（源码注释改述 /
文档面收窄）——对源码可要求改述，对验收报告不可，故解法分岔是合理的。

### 2.5 两条 finding（登记，不改判）

#### FINDING-1：登记的代价陈述有一条**事实错误**

代码注释（`architecture-doc-freshness.test.ts`:518-519）写：

> 该风险可接受，因为 test-reports/ **按目录约定只放验收产物、由 Evaluator 写**、且带批次日期

这句对 `docs/test-reports/user_report/` **不成立**，三个分句全错：

```
$ grep -n "user_report" harness-rules.md planner.md
harness-rules.md:263:│   └── user_report/        # 用户反馈报告（Planner 在新批次启动时必读）
planner.md:11:**0a. 用户反馈（`docs/test-reports/user_report/`）**
```

即：该子目录放的是**用户反馈**（非验收产物）、由用户/Planner 侧产出（非 Evaluator 写）、
且被 harness 明定为**每个新批次 Planner 必读的现行输入面**——它是活的，不是历史面。
而它现在整个落在排除区内。

**变异 M3（我自己找的盲区，实打）：** 往 `docs/test-reports/user_report/P2-CLEANUP-线上验收说明.md`
写入一条**作废的运维指令** + 一个假计数锚点：

```
请注意：在租户变量注入（spec D-7 / F009）落地之前不要打开 DB_APP_ROLE_RUNTIME。
引导白名单（999 个文件）。
```

实跑结果：`Test Files 1 passed (1) / Tests 41 passed (41)` —— **完全看不见**。

**危害评估（如实分级）：**
- **概率：低。** user_report 记的是产品侧用户反馈，复述注入机制内部句式的可能性小。
- **严重度：中。** 它是 Planner 每批必读的输入面；一条作废的**开关操作指令**残留在这里，
  会直接喂进下一批的规划输入。上面这条正是 `DB_APP_ROLE_RUNTIME` 的反向指令——
  本批的结论恰恰是该开关的机制已成立，这条旧指令若被读到就是反向误导。
- **收窄前后对比：** 收窄前这条能被抓住，但代价是仓库恒红（§2.4）。所以这是**真实的取舍**，
  不是白拿的便宜。

#### FINDING-2：最小性未达 —— `docs/archive` 的排除无任何实测红驱动，且存在更小的改法

**变异 M1b：** 只排除 `docs/test-reports`（不排除 `docs/archive`）：

```
 Test Files  1 passed (1)
      Tests  41 passed (41)
```

即 `docs/archive` 的排除**不是**被任何观测到的红逼出来的，是预防性扩大。注释里对它给的理由
是「归档同理」（论证，非测量），这点注释本身没有谎报为实测，可以接受；但登记为**最小性未达**。

**变异 M4：存在一个更小、更好的改法，且今天零代价。** 保留排除但把 `user_report` 重新纳入：

```ts
const EVAL_REINCLUDE = ['docs/test-reports/user_report'];
```
（需同时处理父目录短路，否则递归到不了该子目录——我第一版探针就栽在这，已修正）

- 当前实物下：`Tests 41 passed (41)`（不引入任何新红）
- 重放 M3 注入后：
  ```
  AssertionError: 已作废的陈述仍在 docs/：「在租户变量注入（spec D-7 / F009）落地之前不要打开」
  + [ "docs/test-reports/user_report/P2-CLEANUP-线上验收说明.md:131" ]
  ```
  **红并点名到文件:行** —— 证明这个更小的改法确实堵上 FINDING-1 的洞，而非只是「看起来更好」。

建议交 Planner 排期（M5.2 或 backlog），**不作为本轮 F007 的阻断项**：acceptance ② 的字面与
立意都已满足，FINDING-1/2 属于「可以更好」而非「不成立」。

### 2.6 收窄对计数锚点断言的影响 —— 实测为零

注释预测「M5.2 一改覆盖面，历史报告会集体翻红」。该预测**今天没有实测支撑**：

```
$ grep -rnE "引导白名单（[0-9]+ 个文件）|全仓非 db 层共 [0-9]+ 处|入口面实测 [0-9]+ 条、已包裹 [0-9]+ 条" \
    docs/test-reports/ docs/archive/
（无输出）
```

全部数字形态的计数锚点只在 `docs/dev/` 内（architecture.md:109/:697/:708 · deploy.md:139 ·
agent-architecture.md:106），排除区内一条都没有。这与 M1 的观测一致（`DOCS_EXCLUDED=[]` 时
**只有** stale-sentence 那一条断言红，计数类断言全绿）。

推论三条：
1. 收窄对计数锚点断言的**当前**效果为零，其唯一活跃效果在 stale-sentence 断言上；
2. 因此收窄**没有**加重首轮登记的 BLIND-1（计数锚点删除不翻红，因判据是 `anchors >= 1` 且有 3 份副本）——
   副本数与落点均未因收窄改变；
3. 注释里那句关于计数锚点的话应读作**前瞻性论证**而非实测结论。它是合理的预判（历史报告确会写
   当时的数），但登记时未区分「已测」与「预判」。

### 2.7 收窄后钉在现行文档面仍然活着（acceptance ⑤ 的独立重放）

我没有沿用 Generator 声称的变异点，自己另挑两处：

| 变异 | 注入点 | 结果 |
|---|---|---|
| M2a | `docs/dev/deploy.md` 追加「注入机制与引导白名单归 M5.1」 | **红并点名** `docs/dev/deploy.md:186` |
| M2b | `docs/product/gap-data-layer.md` 追加「M5.1 注入机制落地后…」 | **红并点名** `docs/product/gap-data-layer.md:141` |

M2b 顺带证明：注释里写「排除之后守的是 `docs/dev/` + `docs/specs/`」是**低估**——`docs/product/`
等目录同样在守护面内。低估方向保守，不构成误导，但描述不精确。

**另一处观察（非缺陷）：** 该用例对 3 条 stale needle 用 `for` 循环 + `expect`，**fail-fast**——
M2a 与 M2b 同时在场时只报出第一条命中的 needle（实测：只报 M2b，M2a 未被求值）。
不影响「会不会红」，只影响一次运行能报出几条。登记为可改进项。

### 2.8 三条 stale needle 是否为空断言（我加做的检查）

「零残留」断言若 needle 从未在文档中出现过，就是恒真的空话。逐条查史：

```
$ git log --oneline -S"M5.1 注入机制落地后" -- docs/dev/agent-architecture.md
3baf293 feat(M5.1b-...-F007): 注入机制文档同步...      <- 本批删除
9c0e305 feat(M5-AUTH-RLS-F013): 文档翻牌...            <- 更早引入

$ git log --oneline -S"在租户变量注入（spec D-7 / F009）落地之前不要打开" -- docs/dev/deploy.md
3baf293 / adeb9a9

$ git log --oneline -S"注入机制与引导白名单归 M5.1" -- docs/dev/architecture.md
3baf293 / 9c0e305
```

三条 needle **均确曾真实存在于现行文档并在本批被删**，故三条断言非空。
其中第二条含全角 `（spec D-7 / F009）`——这是**被检索散文原句自带**的字符，不是 `stepCountIs(`
那种「码形左括号致模式失配」的形态，与 §8 的禁忌不同源。我独立认可这一读法。

### 2.9 F007 判定

**PASS。** acceptance ② 三项要求在收窄后逐条仍成立（§2.2 / §2.3）；acceptance ⑤ 的变异证活由我
独立重放通过（§2.7）；收窄的必要性经我独立实测证实且成因结构性（§2.4）；acceptance ④ 的两个
既有钉（`rls-negative-suite.test.ts` / `project-scope-census.test.ts`）在 fix-1 中**未被触碰**
（`git diff --name-status 8c5fee1..41ba4db -- tests/` 不含此二文件），口径不变。
FINDING-1（代价陈述有一句事实错误）与 FINDING-2（最小性未达 + 存在已验证的更小改法）登记留档，
不构成 acceptance 不成立。

---

## 3. (B) F002 / F005 / F006 回归确认

### 3.1 三个 src 文件的改动是否只涉注释 —— 机械证明（含一次判据自证失败）

`git diff --name-status 8c5fee1..41ba4db -- src/` 只有三个文件：
`prisma.ts` · `runtime.ts` · `tenant-scope.ts`。

**第一版判据被我自己废弃（如实登记）：** 我先写了一个基于 `ts.createScanner` 的 token 流比对器。
按 §8 先做活性自测，结果两个探针**双双失败**：

- 探针 A（只追加一行注释，应判「相同」）→ 判 **DIFFERS**（scanner 未处理模板字面量重扫，
  文件尾部被吞进一个 template token，尾随注释因此进入 token 文本）
- 探针 B（改一处字面量，应判「不同」）→ 判 **IDENTICAL**（我的 `replace` 命中的首个匹配落在注释里）

若照单全收，我会得出一个方向完全反了的结论。**判据先验活救了这次。**

**第二版判据（已验活）：** 用 `ts.transpileModule({ removeComments: true })` 比对 emit 产物。

| 探针 | 预期 | 实测 |
|---|---|---|
| A：纯追加注释 | IDENTICAL | `emitLen old=2131 new=2131` → **IDENTICAL** ✅ |
| B：`tenantTxAls.run(` → `.runMUTANT(` | DIFFERS | `emitLen 2131 → 2137`，点名 `first diff line 48` → **DIFFERS** ✅ |

用已验活的判据比对 `8c5fee1` 版本与 HEAD 版本：

| 文件 | emit 长度 | 判定 |
|---|---|---|
| `src/lib/db/tenant-scope.ts` | old=2131 new=2131 | **IDENTICAL（仅注释）** |
| `src/lib/db/runtime.ts` | old=717 new=717 | **IDENTICAL（仅注释）** |
| `src/lib/db/prisma.ts` | old=632 new=632 | **IDENTICAL（仅注释）** |

**结论：三个 src 文件的可执行代码在 fix-1 中零改动**，F002/F005/F006 的产品行为面未被触动。

### 3.2 各 feature 的证据文件是否被触碰

`git diff --name-status 8c5fee1..41ba4db -- tests/ scripts/` 全集：

```
M  scripts/test/gate-smoke.ts                           (+8/-1)
M  tests/integration/f004-transaction-atomicity.test.ts (+142/-6)   <- F004
M  tests/unit/architecture-doc-freshness.test.ts        (+20/-0)    <- F007
M  tests/unit/bootstrap-whitelist-census.test.ts        (+75/-6)    <- F003
M  tests/unit/db-layer-importer-census.test.ts          (+92/-0)    <- F008
```

- **F002** 的承载证据是 `tests/integration/tenant-scope-semantics.test.ts`
  （据首轮报告 `M5.1b-verify-F002-F008.md`:43「14 用例…0 删除」）—— **未在改动清单内**
- **F005** 的 `als-context-propagation.test.ts` / `routine-tenant-explicit.test.ts` —— **未在改动清单内**
- **F006** 的 `tests/e2e/tenant-rls-min-loop.e2e.test.ts` —— **未在改动清单内**

即：产品代码 emit 不变 + 证据文件字节不变 ⇒ 三条 feature 的鉴别力在数学上与首轮相同。

### 3.3 不止于推论 —— 实跑 + 一次实打变异

| 面 | 命令 | 结果 |
|---|---|---|
| F002 | `npx vitest run tests/integration/{tenant-scope-semantics,tenant-injection-proxy,f004-transaction-atomicity}.test.ts` | 3 files / **33 passed** |
| F005 | `npx vitest run tests/integration/{als-context-propagation,routine-tenant-explicit}.test.ts tests/unit/session-tenant-context.test.ts` | 3 files / **33 passed** |
| F006 | `npm run rls:e2e` | 1 file / **15 passed** |

**变异 M5（防「全绿但钉已死」）：** 把 `tenant-scope.ts:182` 的 `set_config` 第三参 `true` → `false`
（F002 acceptance ④ 第一条），实跑：

```
× withTenant 结束后，连续 12 次无关查询都读不到 app.tenant_id
× 并发结束后连接池上同样不留残留
AssertionError: expected [ 'f002scope-89895-b3leak', …(11) ] to deeply equal []
AssertionError: expected [ 'f002scope-89895-conc-11', …(11) ] to deeply equal []
      Tests  2 failed | 12 passed (14)
```

B3 残留那两条钉在 HEAD 上**仍然咬得住**。还原后复跑 **14 passed (14)**。

### 3.4 变异还原对账

全部变异均在本 worktree 内进行，**未使用 `git checkout`**，一律反向编辑后逐位对账：

| 文件 | 还原后 sha256 | 与基线一致 |
|---|---|---|
| `tests/unit/architecture-doc-freshness.test.ts` | `423e8e0d…8742a4` | ✅ |
| `docs/dev/deploy.md` | `0ecab3a1…5a0b2e` | ✅ |
| `docs/product/gap-data-layer.md` | `7c174c3f…ed7174` | ✅ |
| `docs/test-reports/user_report/P2-CLEANUP-线上验收说明.md` | `640c554b…dfa2d8` | ✅ |
| `src/lib/db/tenant-scope.ts` | `git hash-object` = `1041cc79…` = `git rev-parse HEAD:` 同值 | ✅ |

更强的一道总账（对 git 对象库而非我自记基线）：

```
$ git diff --stat HEAD
（空）
$ git status --short
?? node_modules        <- 仅我建的符号链接
```

主仓 `src/` 与 `docs/dev/` **一个字节未动**。

---

## 4. (C) 全批 L1 独立复跑

| 项 | 命令 | 结果 |
|---|---|---|
| tsc | `npx tsc --noEmit` | **TSC_EXIT=0**（零输出） |
| lint | `npx next lint` | **`✔ No ESLint warnings or errors`**（LINT_EXIT=0） |
| vitest | `npx vitest run` | **148 files / 1875 tests 全绿**（两次：变异前 23:15、还原后 23:21） |
| rls:e2e | `npm run rls:e2e` | **1 file / 15 passed**（两次） |

**`npm run test:visual` 未跑，理由：** spec `M5.1b-TENANT-INJECTION-spec.md` §7 明文
「acceptance 边界 = features.json 逐项，不含『全套测试普遍绿』隐式门槛；本机 `test:visual`
恒 15 passed / 2 failed 属 `BL-VISUAL-DATA-ISOLATION`，**不计分**」。它是 spec 登记的不计分项，
跑与不跑都不改变本轮判定，故按登记跳过并在此说明（非遗漏）。

**并发干扰：** 另有 2 个 evaluator 在各自 worktree 并行、共用同一本机 dev Postgres。
本轮**未出现**任何疑似环境干扰的失败——两次全量与两次 e2e 结果完全一致，无需触发
「单独重跑确认可复现」流程。无「观察到但未复现」条目。

---

## 5. 附带观察（不属本轮 acceptance，登记供 Planner 排期）

### OBS-1：dev 库存在 2 行 F005 夹具孤儿（2026-08-04，非本批产物）

```
$ SELECT id, quote_literal(slug), quote_literal(name), "createdAt" FROM "Tenant";
 cmrrlq75d…| 'dev'            | 'Dev Tenant'                   | 2026-07-19
 cmsf47067…| （NULL）          | 'f005-52865-dup 工作室-第二次' | 2026-08-04 20:32:33
 cmsf470p4…| （NULL）          | 'f005-52865-case 工作室-大写'  | 2026-08-04 20:32:34
 cmsf4l4rr…| '__auth-audit__' | '认证审计（系统占位，无用户）' | 2026-08-04 20:43:32
```

来自 `tests/integration/auth-register.test.ts`（`TAG = f005-${process.pid}`，pid 52865，
对应 :177 `-第二次` 与 :239 `-大写`）——**M5-AUTH-RLS 批次的文件，不是 M5.1b 的 F005**。

**HEAD 不再泄漏**（实测）：单独重跑该文件后 `Tenant` 总数仍为 4，`max(createdAt)` 仍停在
2026-08-04，无今日新增。判定为 **2026-08-04 一次中断/被杀的测试进程留下的孤儿**，非本轮缺陷。

值得记的机制问题：夹具前缀是 **pid 作用域**的，各次运行只清自己的 pid 前缀，
于是中断留下的孤儿**永远不会被任何后续运行回收**。

### OBS-2：`OperationLog` 孤儿跨批次累积（539 行中 335 行孤儿）

```
 orphan_oplog_all_time | 335        (tenantId 指向已不存在的 Tenant)
 oplog_all_time        | 539

 day        | orphans
 2026-07-27 |   4
 2026-08-04 |  65
 2026-08-05 |  60
 2026-08-07 |  20
 2026-08-08 |  84
 2026-08-09 | 102     (近 2 小时，且 attached=0)
```

这正是 F006 acceptance ⑤ 点名的形态（「清单必须覆盖软引用表（OperationLog / PendingAction 等
**无 FK 不级联**）」）：夹具删掉了 `Tenant` 行，`OperationLog` 行留了下来。

**边界说明（避免误判为本批缺陷）：**
- F006 **自有**夹具前缀实测零残留：`Tenant slug LIKE 'f006rls-%'` = 0，
  故 acceptance ⑤ 的「**本次**残留」按其字面口径**成立**；
- 现象**早于**本批（7-27 / 8-04 / 8-05 已有）；
- 今日 102 行 actor 为 `delivery`(gate/auto) 与 `auth`(auto)，来自 `gate:smoke` / `delivery:e2e` /
  auth 测试面。**3 个 evaluator 共用同一 dev 库，我无法把今日增量干净归因到任一进程**——如实说明。

建议：把「软引用表孤儿」的清理从各夹具自扫门前雪，升级为一个可复跑的全库普查脚本（M5.2 或 backlog）。

### OBS-3：F007 的改动与 F008 同 commit，不可独立回滚

`git log --oneline 8c5fee1..41ba4db -- tests/unit/architecture-doc-freshness.test.ts`
→ 唯一命中 `57998bf fix(M5.1b-TENANT-INJECTION-**F008**): …`

该 commit 正文有独立小节明示带了 F007 的修改，**不是隐瞒**。但与铁律 1「每个 feature 独立 commit、
可独立审查回滚」有偏差：回滚 F008 会一并回滚 F007 的扫描面收窄，而后者一旦回滚，
仓库会立刻因验收报告的原文引用而翻红（§2.4）。登记为流程观察。

---

## 6. 判不了的部分（如实登记）

1. **注释中「M5.2 一改覆盖面，历史报告会集体翻红」** —— 前瞻性预测，当前实物中排除区内零个数字形态
   计数锚点（§2.6），**无法证实也无法证伪**。按「未测」登记，不作为通过或不通过的依据。
2. **今日 102 行 `OperationLog` 孤儿的进程归因** —— 3 个 evaluator 共用一库，无法归因（§OBS-2）。
3. **本报告不覆盖 F001 / F003 / F004 / F008** —— 不在本次派活范围内，由并行的其他 evaluator 负责。

---

## 7. 附：本轮全部变异清单（均已还原并对账）

| 编号 | 变异内容 | 观测 | 还原对账 |
|---|---|---|---|
| M1 | `DOCS_EXCLUDED` → `[]` | 1 failed，点名 `M5.1b-verify-F007-L1.md:70/:110` | sha256 一致 |
| M1b | `DOCS_EXCLUDED` → 只排 `docs/test-reports` | 41 passed（证 archive 排除无红驱动） | sha256 一致 |
| M2a | `docs/dev/deploy.md` 注入作废句 | 红，点名 `:186` | sha256 一致 |
| M2b | `docs/product/gap-data-layer.md` 注入作废句 | 红，点名 `:141` | sha256 一致 |
| M3 | `user_report/` 注入作废运维指令 + 假计数锚点 | **41 passed —— 看不见（盲区确认）** | sha256 一致 |
| M4 | 排除表 + `user_report` 重纳入 | 基线 41 passed；重放 M3 → 红并点名 | 反向编辑还原，`git diff HEAD` 空 |
| M5 | `set_config` 第三参 `true`→`false` | F002 B3 两条钉红 | `git hash-object` = HEAD blob |
| 判据自测 A/B | token 比对器活性自测 | **双双失败 → 判据废弃重写**（§3.1） | 探针文件在 scratchpad，未入仓 |

---

*产出者：Andy/evaluator-subagent（隔离上下文）· 2026-08-08 · HEAD `41ba4db`*
*本报告未修改 progress.json / features.json / 规格 / 配置 / 任何 src 或 docs/dev 文件。*
