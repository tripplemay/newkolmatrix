# M5.1b-TENANT-INJECTION · 复验报告（rv1）— F008 / F001

> **性质：** `status=reverifying`、`fix_rounds=1` 的**独立复验**。上一轮两条均判 PARTIAL。
> **执行：** 隔离 Evaluator（fresh context，独立 git worktree
> `/Users/yixingzhou/project/newkolmatrix/.claude/worktrees/agent-a9a3de942b573a97f`）
> **被验 HEAD：** `41ba4dba6c5ed5afd6e2eb2e48ed29d5ffd60fb0`（`git rev-parse HEAD` 自证）
> **日期：** 2026-08-08
> **落盘说明：** harness 禁止本实例写共享 checkout，本文件落在 worktree 内同名路径
> `docs/test-reports/M5.1b-rv1-F008-F001.md`，待编排者搬运到主仓。
> **判据纪律：** 每条结论只依据源码、实跑输出、真库里的行。
> commit 正文 / 代码注释 / `generator_handoff` 一律为**被验对象**，不作通过依据（铁律 13）。
> 上一轮 commit 正文里登记的变异记录**未采信**，三条我全部自行重跑。

---

## 总判

| feature | 上一轮 | 本轮 |
|---|---|---|
| **F008** 更正失实注释 + 防再犯机械钉 | PARTIAL | **PARTIAL** |
| **F001** 补验 M5.1 遗留实现 | PARTIAL | **PARTIAL**（唯一驱动 = acceptance ⑤，与 F008 同一条发现） |

**上一轮的两条不成立项，一条真修好、一条修到 2/3：**

- ①(a)「被指派更正的那段注释在批末是假的」→ **真修好**。整段重写，我把其中每一句事实性断言
  逐条 grep / 读码核过，**无一条为假**（§2）。
- ②「新写了一句无依据且为假的『已被机械守住』断言」→ **修到 2/3**。新的守护组确实存在、
  确实活着（三条规定变异我自跑，各红且只红该红的那条，§3.1）；但产品注释里那句
  「**② 三条已被实测证伪的绝对句不得复活**」**只兑现了两条**——首轮点名的第 2 条
  （「本模块此刻只交付**单层**语义」）不在黑名单里，**逐字复活后全仓 148 files / 1875 tests 全绿**（§3.3）。

即：上一轮的缺陷形态（**声称的覆盖面 > 实际覆盖面**）**未被完全消除，规模从 3/3 缩到 1/3**。

> **口径坦白（供 Planner 裁量，不替你决定）：** 「三条已被实测证伪的绝对句」有两种读法。
> 若读作「黑名单里的三个**字符串**」，该句为真（数组恰 3 条）；若读作「首轮**逐条点名**的三条句子」
> （测试文件自己写的是「形态取自首轮验收逐条点名的原文」，而首轮报告点名的正是三条句子），
> 该句只兑现 2 条。我按后者判，理由：读者被这句话安抚后就不再人工核，而**能被安抚掉的正是
> 那条没被守住的**。修法是一行——黑名单补一条，或把「三条已被实测证伪的绝对句」改成
> 「下列三个句式」。

---

## 1. 环境与可复现性

| 项 | 值 |
|---|---|
| 工作副本 | 独立 worktree；`node_modules` 符号链接主仓（未 npm install）；`.env` 复制自主仓 |
| 库 | 本机 dev Postgres `localhost:5434/kolmatrix`（L1）。**未连真实外部服务、未部署、零计费、零生产写入** |
| 开关默认态 | `grep -c DB_APP_ROLE_RUNTIME .env` → **0**（默认关，未改 `.env`） |
| 并发 | 另有 2 个 evaluator 各自 worktree 并行、共用同一 dev 库；夹具按 pid 前缀隔离 |
| 冒烟 | `npx vitest run tests/unit/health.test.ts` → 1 file / 32 tests passed |

### 1.1 L1 门（本轮实跑）

```
$ npx tsc --noEmit                → TSC_EXIT=0
$ npx next lint                   → ✔ No ESLint warnings or errors
$ npx vitest run                  → Test Files 148 passed (148) / Tests 1875 passed (1875)
```

### 1.2 一条「与我的 feature 无关的红」——已追到根因，是**我自己造成的**，不计缺陷

首次全量跑得 `1 failed`：`architecture-doc-freshness.test.ts >「未覆盖清单文件的合计行 = 普查实测」`
报 `expected [102,78,3,75] to deeply equal [103,78,3,75]`。

追因：普查脚本 `scripts/test/m51b-entrypoint-census.ts:157` 递归遍历 **`scripts/`**，
而我把残留探针 `scripts/test/eval-rv1-stray-probe.ts` 放在了那里 → `scanned` 多 1。
移走该文件后单独重跑 **41 passed**，全量 **148/1875 全绿**。

**登记为环境噪声而非产品缺陷**，并附带一条对后人有用的事实：**任何人在 `scripts/` 下放临时脚本
都会让这道钉翻红**（这正是它设计上要做的事——它在替文档记账，不是误报）。

---

## 2. F008 ①：重写后的说明段，逐句核对（我自己 grep，不采信注释自述）

被验实物：`src/lib/db/tenant-scope.ts:12-39`（`git diff 8c5fee1..41ba4db` 确认为本轮重写）。

### 2.1 as-built 语义四条（`:12-17`）—— 四条全部与代码相符

| 注释断言 | 实物 | 判 |
|---|---|---|
| 单层：开事务 → **第一条语句** SET LOCAL（`set_config` 第三参 **true**）→ ALS 内跑回调 | `:181-184`：`$transaction(async tx => { await tx.$queryRawUnsafe("SELECT set_config('app.tenant_id', $1, true)", tenantId); return tenantTxAls.run(...) })` —— 第三参确为 `true`，确为回调内第一条语句 | ✅ |
| 同租户嵌套：复用外层 tx，不开第二事务、**不重设变量** | `:173-179`：`return fn(outer.tx)`，该分支内无任何 `set_config` | ✅ |
| 跨租户嵌套：抛 `CrossTenantNestingError` | `:170-172` | ✅ |
| 嵌套再传事务选项：抛 `NestedTransactionOptionsError` | `:167` | ✅ |

### 2.2 其余事实性断言（`:18-39`）—— 逐条有据

| 断言 | 我跑的判据 | 判 |
|---|---|---|
| 「会话面入口的标准包裹在 `lib/db/tenant-entry.ts`」 | 实读 `src/lib/db/tenant-entry.ts:43-49`：`withSessionTenant` = `requireSessionIdentity()` → `withTenant(identity.tenantId, ...)` | ✅ |
| 「本段原先那句被同批 **F004/F005** 带假」 | `git log -S "withTenant(" -- <11 个调用点文件>` → 恰两条：`85e0362`(F004) 与 `7ec61df`(F005) | ✅ |
| 「批末实测 **15 处 / 11 文件**」 | 本轮独立复算（先做活性自证：判据能命中已知目标 `create.ts:65`）→ **CALL SITE 15 / DISTINCT FILE 11** | ✅ |
| 「当时那道钉的 `GUARDED` 只有 runtime 与 privileged 两个 importer 条目」 | `git show 8c5fee1:tests/unit/db-layer-importer-census.test.ts \| grep "module:"` → 恰 `'src/lib/db/runtime'` + `'src/lib/db/privileged'` | ✅ |
| 「刻意改述而非照抄原句：照抄会把自己钉红」 | **变异实跑**（§3.1 M-G2）：把「零 withTenant 调用点」照抄回段内 → ② 当场红并点名该句 | ✅ |
| 「两个独立 evaluator 各自查证到同一结论（三份报告）」 | 三份文件均在 `docs/test-reports/` 实存 | ✅ |
| 「会漂的计数放进 `M5.1-uncovered-entrypoints.md`，计数由 `architecture-doc-freshness` 与实物比对」 | 实读 `tests/unit/architecture-doc-freshness.test.ts:644-658`（合计行 vs `runCensus()` 四个数）。**活性旁证：§1.2 我无意中把它打红了** | ✅ |

### 2.3 F008 ①(b) `runtime.ts` importer 清单 —— 批末仍准

```
$ grep -rn "db/runtime\|from './runtime'" src/ --include='*.ts' --include='*.tsx'
src/instrumentation.ts:23:      import('./lib/db/runtime'),              ← ③
src/lib/db/prisma.ts:18:  （注释行，非 import）
src/lib/db/prisma.ts:23:import { getRuntimeDb } from './runtime';       ← ①
src/lib/db/runtime.ts:29: （注释行，非 import）
src/lib/db/tenant-scope.ts:43:import { getRuntimeDb } from './runtime'; ← ②
```
真实 importer **恰三处**，与 `runtime.ts:8-16` 的 ①②③ 逐条相同。
本轮新增的「二次更正」段（`:27-31`）声称「不再写行号」，且 instrumentation 的两条动态 import
现为 `./lib/db/runtime` + `./lib/db/app-role` —— 实读 `src/instrumentation.ts:22-25` **属实**，
且该段确无行号。**PASS**。

`privileged` 侧顺带复核：实物 importer 5 个，与 `GUARDED` 期望集合逐条相同
（`grep` 另命中 `runtime.ts`，但那是 `:3` 分工表的注释行；钉先剥注释，故不误判）。

---

## 3. F008 ③④：机械钉的鉴别力 —— 三条规定变异 + 我自造的绕过

**还原纪律：全程反向编辑 + `shasum -a 256` 逐位对账，一次未用 `git checkout`。收尾
`diff baseline.sha final.sha` 通过；主仓 `src/lib/db/{tenant-scope,prisma,runtime,privileged}.ts`
四个文件的 shasum 与我的基线逐位相同（未动一个字节）。**

基线：`npx vitest run tests/unit/db-layer-importer-census.test.ts` → **8 passed**。

### 3.1 新守护组的三条规定变异（**我自跑，不采信 commit 正文**）

| # | 变异 | 实跑结果 | 红的是不是该红的那条 |
|---|---|---|---|
| M-G1 | 删掉政策句（「注释里的可变数字要么有钉守着，要么不写」→ 改写） | **1 failed / 7 passed** | ✅ 只红 `🔒 ①`，报文点名缺哪句 |
| M-G2 | 把「产品代码（src/）零 withTenant 调用点」照抄回段内 | **1 failed / 7 passed** | ✅ 只红 `🔒 ②`，报文点名 `零 withTenant 调用点` |
| M-G3 | 段内写「产品代码目前有 3 处 withTenant 调用点」 | **1 failed / 7 passed** | ✅ 只红 `🔒 ③`，报文点名 `3 处 withTenant 调用点` |

**三条都真的会红，且各自只红对应那一条** —— 上一轮「声称在守、实际零覆盖」的形态在这三条上已消除。

### 3.2 F008 acceptance ④ 的两条原变异，本轮 HEAD 上复跑

| # | 变异 | 实跑结果 |
|---|---|---|
| F8-a | 新增 `src/lib/db/__eval_rv1_probe.ts`（`import { getRuntimeDb } from './runtime'`） | **1 failed**，报文 `多出（未登记的 importer）：src/lib/db/__eval_rv1_probe.ts` —— 红且点名 |
| F8-b | 期望集合摘掉 `'src/lib/db/prisma.ts'` | **1 failed**，报文 `多出（未登记的 importer）：src/lib/db/prisma.ts` |

还原：F8-a 用 `rm`（探针为我新建）；F8-b 反向编辑，`tests/unit/db-layer-importer-census.test.ts`
shasum 回到 `55eeb858…a14`（逐位一致）。

### 3.3 我自造的绕过形态（作者未试过）—— **7 条全部未被抓到**

B1–B5 一次注入、一次跑（负向断言是子串 / 正则匹配，加文本只会**增加**命中机会；
因此「合并全绿」⇒「逐条也全绿」，结论成立）：

| # | 我写进说明段的句子 | 抓到？ |
|---|---|---|
| B1 | 「产品代码（src/）里**一个 withTenant 调用点也没有**」（与黑名单同义，换个说法） | ❌ 未抓 |
| B2 | 「产品代码目前有**十五处** withTenant 调用点，覆盖十一个文件」（中文数字） | ❌ 未抓 |
| B3 | 「产品代码目前有 3 **个** withTenant 调用点」（「个」替「处」） | ❌ 未抓 |
| B4 | 「withTenant 调用点**共 3 处**」（语序颠倒） | ❌ 未抓 |
| B5 | 「本模块此刻只交付单层语义，嵌套请自行避免」 | ❌ 未抓 |
| — | 合并注入后实跑 | **8 passed（全绿）** |

| # | 变形 | 抓到？ |
|---|---|---|
| B6 | **逐字**复活首轮点名的第 2 条被证伪句：`// 本模块此刻只交付**单层**语义。` | ❌ 未抓 —— 单文件 8 passed，**全量 148 files / 1875 tests 全绿** |
| B7 | 把黑名单三句**原样**放进 `withTenant` 的 JSDoc 块（说明段之外 20 行处） | ❌ 未抓（`leadingLineComment` 只读文件开头的连续 `//` 块） |

**B2/B3/B4 不构成对注释的证伪**：`③` 那句自称只守「`N 处 withTenant 调用点`**形态**」的计数，
措辞是精确的，这几条落在它自称的射程外，属**射程窄**而非**说假话**，登记不扣分。

**B6 构成证伪**：`②` 那句自称守「三条已被实测证伪的绝对句」，而其中第 2 条
（在 HEAD 上仍是假话——嵌套语义就在同文件 `:166-180`）可以逐字写回去，**全仓无一条会红**。
这正是首轮报告用过的那把尺：「一句为假的陈述 + 一个全绿的仓 = 全仓没有任何一条断言在守它」。

**B7 是射程边界**：守护只覆盖文件头连续 `//` 块，同文件 JSDoc / 块注释是盲区。
下一次漂移未必发生在同一个位置。

### 3.4 F008 逐项结论

| acceptance | 判 | 依据 |
|---|---|---|
| ①(a) tenant-scope 注释改对 | **PASS** | §2.1 / §2.2 逐句核过，无一条为假 |
| ①(b) runtime 注释改对 | **PASS** | §2.3 |
| ② 改完自己 grep 核一遍，新写的每句断言都要有依据 | **不成立** | §3.3 B6：新写的「② 三条…不得复活」只兑现 2/3 |
| ③ 防再犯机械钉（口径 / 活性 / 与 F003 不派生） | **PASS** | §3.1 §3.2；`readFileSync` 递归、零 `git grep`、自带空转防呆 |
| ④ 变异证活 | **PASS** | §3.2 两条我实跑，红且点名 |
| ⑤ tsc / lint / 全量 vitest | **PASS** | §1.1 |

→ **F008 = PARTIAL**（唯一驱动 = ②）。

---

## 4. F001：原 acceptance 六条（`docs/specs/M5.1-F001-unverified.json`）逐条判

`npx vitest run tests/integration/tenant-injection-proxy.test.ts` → **11 passed (11)**（基线）。

### ① 双态语义三分支各有正向用例 —— **PASS**

三分支各有真库正向用例：(a) `txid_current()` 相等 + 未提交写入事务外不可见；
(b) `inspectConnectionRole(prisma)` 实测特权；(c) `toThrow(MissingTenantScopeError)` **钉类型不钉字符串**。
鉴别力由**我实跑的变异 A** 证活（§5.1）。
登记边界见 §6 发现 2（代理只装 `get`，写侧同样 fail-open）。

### ② 开关关 = 逐位不变 —— **PASS**（按 spec D-10 口径 2 判**实质性质**）

- 全量 `npx vitest run` → **148 files / 1875 tests 全绿**（exit 0）。
- 既有测试**未被为迁就 RLS 而翻修**（基线 = M5.1 之前 `b0b1120`）：
  `git diff --name-status b0b1120..HEAD -- tests/` → **7 个新增 A、2 个修改 M、零删除**。
  两个 M 均为**纯追加 / 纯扩展**：`architecture-doc-freshness` = **199 增 / 0 删**；
  `session-mock.ts` = 26 增 / 4 删，实读 diff：新增**可选**字段 `resolve?`，
  三个导出函数只在 `resolve` 返回 `undefined` 时回落真实现 —— 既有 4 个消费者逐位不变。
  实测 `resolve` 全仓**只有 1 个消费者**（`als-context-propagation.test.ts:94`；
  判据活性已证：同一 grep 能看见定义处 `session-mock.ts:46`）。
- 文件数对账：`git ls-tree -r --name-only b0b1120 -- tests` = **236** → `HEAD` = **243**（+7，零删除）。
- 开关默认关：`.env` 中 `DB_APP_ROLE_RUNTIME` **零命中**。
- 原字面（diff 不含 `tests/` 与除 `src/lib/db/` 外的 `src/`）在 F001 那次交付上仍成立：
  `git show --stat e6ecef1` → `src/` 触及面仅 `src/lib/db/`（4 文件）+ 1 个**新增**测试 + handoff json。

### ③ privilegedDb 与运行时 client 是两实例、连接串来源不同 —— **PASS**

用例两条均绿（开关关：两实例；开关开：`current_user` = `kol_app` vs `kol`，真库事实）。
鉴别力由**我自造的变异 N4** 证活（§5.3）：把开关做成静默失效后，该条当场红
（`expected 'kol' not to be 'kol'`）。

### ④ raw 转发落在 ALS 事务上 —— **PASS**

回滚路径判据链完整（代理 raw 写 → 同事务 raw 读 `count=1` → 运行时连接读 `count=0` → 抛错回滚 → 特权连接复查 `count=0`）。
鉴别力由**我实跑的变异 B** 证活（§5.2）。

**本轮新增（补上了上一轮登记的盲区）：标签模板版 `$queryRaw` 首次有实测用例**，
探针 `tests/integration/m51b-rv1-evaluator-probe.test.ts`：

```
[N1-a] proxy txid= 780198  tx txid= 780198      ← 标签模板 $queryRaw 确实落在同一事务
[N1-b] app.tenant_id via 标签模板 = rv1probe-97021
[N1-c] threw= MissingTenantScopeError            ← 开关开 + 无 ALS，标签模板版同样 fail-closed
```

**残留（沿用上一轮登记，本轮独立复现）：** 同组「提交路径」那条在变异 B 下**仍绿** ——
它只证「写得进去」，不证「落在同一事务」。

### ⑤ 变异证活（两条规定动作）—— **PASS**，见 §5

### ⑥ tsc / lint 绿 —— **PASS**，见 §1.1

### 4.1 M5.1b F001 的 ①~⑦ 逐条自查

| # | 要求 | 本轮执行情况 |
|---|---|---|
| ① | 逐条判 + 附命令输出 / 文件:行，不软化 | §4 六条各有实跑输出或 `文件:行`；判不了的写进 §7 盲区 |
| ② | 按 D-10 两条口径（验批末的树 / ② 改判为实质性质） | 已按此判；F002/F004/F008 对 `tenant-scope.ts` 的改动**未**当作 F001 缺陷 |
| ③ | 两条规定变异必须实施并实跑（硬门） | §5.1 §5.2，各贴实跑红输出；还原用反向编辑 + shasum，零 `git checkout` |
| ④ | ≥2 种作者未采用的写法/改动 | **4 组**（N1 标签模板 raw / N2 写侧陷阱 / N3 句柄逃逸 / N4 开关静默失效），另加打 F008 新断言的 B1–B7 共 7 条 |
| ⑤ | 核实 F008 的两条更正 | §2 —— `runtime.ts` **属实**；`tenant-scope.ts` 段内四条语义 + 历史叙述**均属实**，但同段新写的「② 三条…不得复活」只兑现 2/3（发现 1）→ **本条 PARTIAL，驱动 F001 总判** |
| ⑥ | 只写测试产物，不改产品代码 / 不改状态机文件 / 不 push | 全部变异逐位还原（§8 自证）；未改 `progress.json` / `features.json` / 规格 / 配置；未 push |
| ⑦ | L2 未授权 | 无一条 acceptance 需要 L2，全部在 L1 判定（§7.6） |

---

## 5. 规定动作 + 主动探查：本轮我实跑的全部变异

> 全部在本 worktree 内进行；**还原一律反向编辑 + shasum 对账，零 `git checkout`**。
> 基线 shasum：`tenant-scope=e25977b3…228` · `prisma=41b9b194…b3d` · `runtime=2c255c39…fb1` · `privileged=744e4bf8…9dd`。
> 收尾 `diff baseline.sha final.sha` → 无差异；`git status --short` → 仅 `?? node_modules` 与我的两件探针。

### 5.1 变异 A（规定）—— 代理 (c) 分支抛错改成「回落运行时 client」

`src/lib/db/prisma.ts:30`：`throw new MissingTenantScopeError();` → `return getRuntimeDb();`

```
 Tests  3 failed | 8 passed (11)     [exit 1]
 × ①(c) 模型 delegate 访问即抛，断言**类型**而非字符串   AssertionError: expected function to throw an error, but it didn't
 × ①(c) raw 方法访问同样抛（$queryRawUnsafe 不是旁路）
 × ①(c) 开关拨回未设即恢复（解析看当下 env，不是模块加载快照）
```
**红的三条全部落在 (c) 分支**，无一条误伤。还原后 shasum = `41b9b194…b3d`（逐位一致）。

### 5.2 变异 B（规定）—— 摘掉代理对 raw 方法的转发

`get` trap 前置分支：`$queryRawUnsafe` / `$executeRawUnsafe` 直接绑到 `getRuntimeDb()`。

```
 Tests  4 failed | 7 passed (11)     [exit 1]
 × ④ 回滚路径：事务内 raw 写 → 同事务 raw 读可见；事务外不可见；回滚零残留
     expected AssertionError: expected 1 to be +0 … to be Error: rollback-marker
 × ①(a) 代理与 tx 的 txid_current() 相同         expected '766255' to be '766254'
 × ①(a) 开关开时 ALS 作用域内照常解析到 tx        expected '766258' to be '766257'
 × ①(c) raw 方法访问同样抛
```
**spec D-11 点名要红的「④ 事务外不可见 / 回滚零残留」确实红**，另有 3 条一并红。
还原后 shasum = `41b9b194…b3d`。

**残留清理自证**（变异 B 期间 raw 写会绕过事务自动提交）：
```
$ node --env-file=.env --import tsx scripts/test/eval-rv1-stray-probe.ts
STRAY_COUNT= 0
[]
```

### 5.3 主动探查（M5.1b F001 acceptance ④）—— 4 组，均为上一轮**未采用**的形态

上一轮已探 P1 非 get 陷阱 / P1' Reflect·解构·Symbol / P2 绕过代理 import / P3 ALS 内取 `$transaction` /
P5 privilegedDb 降级。本轮刻意换面：

| # | 形态 | 实测 | 判 |
|---|---|---|---|
| **N1** | **标签模板版** `$queryRaw`（上一轮登记为盲区、零用例） | 落在同一事务（txid 相等 `780198`）；读得到 `app.tenant_id`；开关开 + 无 ALS 时抛 `MissingTenantScopeError` | 防线成立，**盲区已闭合** |
| **N2** | 代理**写侧**陷阱：`prisma.x = 1` / `Object.defineProperty` / `delete` | 三者**全部 no-throw**（开关开 + 无 ALS） | **抓到缺口** → 发现 2 |
| **N3** | model delegate **逃逸出** ALS 作用域后再用 | 抛 `PrismaClientKnownRequestError`（`Transaction already closed`）——**响亮失败，不是静默串租户** | 防线成立 |
| **N4** | **产品变异**：`getRuntimeDb()` 恒用特权串（开关静默失效） | ③「开关开：切 kol_app」当场红（`expected 'kol' not to be 'kol'`），其余 10 条绿 | 未抓到缺口（③ 有鉴别力） |

另有 §3.3 的 B1–B7 共 7 条，是打 **F008 新守护组**的绕过探查。

---

## 6. 发现

### 发现 1（驱动两条 PARTIAL）—— 「三条已被实测证伪的绝对句不得复活」只兑现 2/3

见 §3.3 B6。`src/lib/db/tenant-scope.ts:37` 声称守三条，
`tests/unit/db-layer-importer-census.test.ts:295-299` 的黑名单实为：

```
'零 withTenant 调用点'   → 覆盖首轮点名的第 1 条
'没有嵌套守卫'           → 覆盖第 3 条
'不要嵌套调用'           → 也覆盖第 3 条
                        ← 第 2 条「本模块此刻只交付**单层**语义」无对应条目
```

机械证明：把第 2 条逐字写回说明段（它在 HEAD 上仍是假话），
`npx vitest run` → **148 files / 1875 tests 全绿**。

**处置建议（供 Generator，非我执行）：** 二选一，一行即可 ——
(a) 黑名单补一条（如 `'只交付**单层**语义'`），并配一条变异实跑；
(b) 把「三条已被实测证伪的绝对句」改成「下列三个句式」，把话缩到与实物等宽。
**做 (a) 时请顺手把 §3.3 B7 一并考虑**：守护面若仍只覆盖文件头连续 `//` 块，
同文件 JSDoc 里的漂移依旧无人守。

### 发现 2（登记，不驱动判定）—— 代理的**写侧**同样 fail-open，是上一轮「只装 get」的延伸

上一轮登记了 `has` / `ownKeys` / `getOwnPropertyDescriptor` 三个读侧陷阱缺失。
本轮 N2 实测**写侧也一样**（开关开 + 无 ALS）：

```
[N2] 写侧陷阱实测= {"set":"no-throw","defineProperty":"no-throw","deleteProperty":"no-throw"}
```

写入落到代理的空 target `{}` 上，而 `get` 恒解析真实 delegate → **写进去的值再也读不出来**，
且全程不报错。当前 `src/` 无此写法（不可达），但 M5.2 把代理铺开到全站入口时，
第三方库对 client 做属性挂载（instrumentation / DI 容器 / 序列化很常见）会落进这条缝。
建议与上一轮的读侧建议合并处置。

### 发现 3（登记，不驱动判定）—— 「可变数字不写进注释」这条新政策**未在同一文件内贯彻**

本轮 `src/lib/db/prisma.ts:37-40` 新写了政策句「**注释里的可变数字要么有钉守着，要么不写**」，
并据此删掉了 `:35` 的三个数字。但**同一文件 `:10` 的计数原封未动**：

```
10: //      $transaction —— 作用域内调它会在运行期当场暴露，15 处调用点的迁移是 F004。
```

实测：F004 之前 `src/` 确有 **15 处** `$transaction` 调用点
（`git grep '\$transaction(' 85e0362^ -- src`，滤注释后 15），F004 实际迁走 **13 处**，
批末剩 **2 处**（`lib/auth/register.ts:110` 走注入的 `db`，属引导面；
`lib/db/tenant-scope.ts:181` 是 `withTenant` 自己）。该数字作为「当年的工作量范围」可读通，
作为现在时事实已陈旧，且**没有任何钉守它**——与它下方 27 行处刚立的政策直接冲突。

同类（更轻，仅提示自指不一致）：`tenant-scope.ts:20` 的小标题写「本段刻意不写…这类数字」，
而 `:22` 写着「批末实测 15 处 / 11 文件」。该数我复算属实，且带明确过去时语境，
钉的注释也显式豁免了历史叙述 —— **登记不扣分**。

### 发现 4（登记，不驱动判定）—— ④「提交路径」用例仍无鉴别力

上一轮登记过，本轮独立复现：变异 B 下该条**仍绿**。它证「写得进去」，不证「落在同一事务」。
建议补一条配对断言（提交前从运行时连接看不见 / 提交后看得见），或直接比 `txid_current()`。

---

## 7. 盲区登记（如实写，不为好看回避）

1. **未做「同一份用例在旧树与新树逐位对比」。** acceptance ② 的「逐位不变」我用的是**间接判据**
   （既有测试零删除零弱化 + 全量 148/1875 绿 + 文件数 236→243 精确对账 + 开关默认关 +
   F001 交付 diff 不含既有 `src/`）。真正的逐位对比需 checkout `b0b1120` 另建 worktree，本次未做。
2. **N2 写侧缺口的可利用性只证到「本仓 `src/` 当前无此写法」**，未审 `node_modules` 里是否有库
   对传入的 PrismaClient 做属性挂载。开关未开时不可达，未继续挖。
3. **`$executeRaw`（标签模板版）无用例。** 本轮 N1 只补了 `$queryRaw` 侧。代理是通用转发，
   静态上必然覆盖，但**没有实跑用例**钉住写侧标签模板——按本项目口径不算证活，如实登记为未验。
4. **`npm run rls:e2e`（开关开、真连 kol_app）未跑。** 它属 F006 验收面，不在 F008 / F001 的
   acceptance 内；且与并行 evaluator 共用同一 dev 库时，进程级 env 翻开关有互扰风险。登记为未执行。
5. **并发干扰**：本轮唯一一条非我主动变异导致的红已追到根因且为我自己造成（§1.2），
   移除后单文件 41 passed、全量 148/1875 绿，可复现。无「观察到但无法复现」的偶发红。
6. **L2 未授权项：无。** F008 与 F001 的全部 acceptance 均可在 L1（本机 dev 库 + 进程内）判定，
   无一条因 L2 未授权而 PARTIAL。

---

## 8. 附：Evaluator 产出的测试工件（留在隔离 worktree，未进主仓）

| 路径（worktree 内） | 用途 | 是否建议收编 |
|---|---|---|
| `tests/integration/m51b-rv1-evaluator-probe.test.ts` | N1 标签模板 raw / N2 写侧陷阱 / N3 句柄逃逸（5 条，全绿） | **N1 建议收编**（补的是真盲区）；**N2 / N3 不建议原样收编** —— 它们把**当前**行为钉成期望，日后若修好写侧 fail-open，N2 会反过来翻红 |
| ~~`scripts/test/eval-rv1-stray-probe.ts`~~ | 变异残留直查 | **用完已删**（留在 `scripts/` 下会让 doc-freshness 普查计数恒红，§1.2）。源码见下，可随时重建 |

```ts
// scripts/test/eval-rv1-stray-probe.ts —— 用完即删；跑法：node --env-file=.env --import tsx <path>
import { PrismaClient } from '@prisma/client';
async function main() {
  const db = new PrismaClient();
  const rows = await db.$queryRawUnsafe<Array<{ id: string; slug: string }>>(
    `SELECT id, slug FROM "Tenant" WHERE slug LIKE 'f001proxy-%' ORDER BY slug`,
  );
  console.log('STRAY_COUNT=', rows.length);
  console.log(rows);
  await db.$disconnect();
}
main();
```

带我的探针跑全量：**149 files / 1880 tests 全绿**（= 148/1875 + 我这 1 文件 5 条）。

**产品代码零改动自证：** 主仓 `src/lib/db/{tenant-scope,prisma,runtime,privileged}.ts` 的
`shasum -a 256` 与本轮基线**逐位相同**；worktree `git status --short` 仅剩 `?? node_modules`
与上表两件未跟踪工件。未 push，未改 `progress.json` / `features.json` / 规格 / 配置。
