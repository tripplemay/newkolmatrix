# M5.1b-TENANT-INJECTION 复验-4 报告 — F008 + 扫描器改动独立审计

- **批次：** M5.1b-TENANT-INJECTION
- **轮次：** reverifying / fix_rounds=4（第四次复验）
- **被验 HEAD：** `ff33b54410d0b600391f5cda71b34844b3dd5731`
- **Evaluator：** 隔离 subagent（worktree `/Users/yixingzhou/project/newkolmatrix/.claude/worktrees/agent-a650cc63f0a055d77`）
- **范围：** F008（本轮唯一被改 feature）+ 扫描器改动独立审计
- **L2：** 未授权，未执行（无外部服务 / 无部署 / 无计费 / 无生产写入）
- **落盘说明：** 主仓路径被 harness 拦（worktree 隔离），实际落盘于 worktree 内同名路径。

> 状态：进行中，逐项追加。

## 0. 改动面核实（自行取证，未采信转述）

```
git diff --stat a122b7d..HEAD -- src/      → (空)
git diff --stat a122b7d..HEAD -- src/ tests/ scripts/
  tests/unit/db-layer-importer-census.test.ts | 95 +++++++++--------- (78 insertions, 17 deletions)
git diff --stat 73e72d3..HEAD
  features.json | 2 +-   progress.json | 125 +--   tests/unit/db-layer-importer-census.test.ts | 95 ++-
```

**核实结论：属实。** `src/` 零改动，本轮唯一产品/测试面改动是 census 测试文件本身。
注意由此推出一件事：所谓「扫描器」并不在 `src/` 下，它是 census 测试文件内部的
`stripComments()` + `importSpecifiers()` 两个函数。commit 正文里「改一个字符」的说法指的是
测试文件内的正则，不是产品代码。

### 四个版本的正则（用 esbuild 从各 commit 的文件里**机械抽取**，非手抄）

| 版本 | static / side-effect | re-export | 备注 |
|---|---|---|---|
| ORIG `4114b6a` | `(?:[\s\S]*?\sfrom\s+)?` 贪婪可选 | `[\s\S]*?` | 无反引号、无 require |
| CHAR `1ea4abb..73e72d3` | `(?:[^'"`;]*?\sfrom\s+)?` 贪婪可选 | `[^'"`;]*?` | 补反引号 + require |
| **HEAD `ff33b54`** | `(?:[\s\S]*?\sfrom\s+)??` **惰性**可选 | `[^;]*?` | 字符类还原，可选组改惰性 |

> 方法说明：我没有手抄正则。用 `esbuild.transformSync` 把每个 commit 的
> `function stripComments` … `importSpecifiers` 整段 TS 转成 JS 后 import 进探针，
> 三版跑的是**同一份真实代码**。

## 1. 上轮两条已知漏抓重放 —— 两条都已闭合

先做**活性自测**：判据必须能在有 bug 的版本上把 bug 打出来，否则「现在抓得到」没有意义。
38 条探针 × 3 版本的对照表（完整输出见 §6 附）：

| 探针 | ORIG | CHAR | HEAD |
|---|---|---|---|
| B1 多行 import 行尾注释含**反引号** `// 见 \`runtime\`` | PASS | **FAIL** | **PASS** |
| B2 行尾注释含**单引号** `// it's fine` | PASS | **FAIL** | **PASS** |
| B3 行尾注释含**分号** `// foo; bar` | PASS | **FAIL** | **PASS** |
| B4 ES2022 `import { "weird-name" as w, x } from "./es2022"` | PASS | **FAIL** | **PASS** |
| B5 ES2022 `export { x as "a-b" } from "./es2022x"` | PASS | **FAIL** | **PASS** |

**活性成立**：这 5 条在 CHAR 版上实测为 FAIL（`got=[]`，整条漏抓），说明判据看得见目标；
在 HEAD 上全部 PASS。**两条已知漏抓确已闭合，非空转绿。**

顺带证实 fix-4 没有把 ORIG 那批修的东西改瞎：A1（侧效应后跟 from-import，ORIG `got=["zod"]`）、
A4（反引号动态 import，ORIG `got=[]`）、A5（CJS require，ORIG `got=[]`）、A6（字符串拼接 from
吞噬，ORIG `got=[" + "]`）在 HEAD 上全 PASS。

## 2. 扫描器改动独立审计

### 2.1 判据与活性自测

判据两条腿：
1. **探针面**：38 条自造探针（含合法但少见写法），三版并排跑，看差异方向；
2. **全量面**：对 `src/` 全部 **384** 个 `.ts/.tsx` 逐文件比对三版的 specifier **多重集**
   （不是集合——去重会把「重复捕获」类假阳性藏起来，我第一版就踩了这个，已改）。

活性自测见 §1：判据在 ORIG 上打出 13 条 FAIL、在 CHAR 上打出 16 条 FAIL，方向与两轮已知结论
一致，未出现「判据方向反了」。

### 2.2 全量 `src/` 384 文件比对

```
CHAR vs HEAD  多重集差异文件数 = 0
ORIG vs HEAD  多重集差异文件数 = 3
  src/app/AppWrappers.tsx                  4 -> 8   新捡回 4 条 CSS 侧效应 import
                                                    (styles/App.css / Contact.css / MiniCalendar.css / index.css)
  src/components/calendar/MiniCalendar.tsx 4 -> 5   新捡回 react-calendar/dist/Calendar.css
  src/app/admin/campaigns/page.tsx        18 -> 17  去掉 1 条重复捕获（真实假阳性）
```

**对 `src/` 净效果：新丢失 0 / 新捡回 5 / 消除假阳性 1。** 这一项与被验方声称一致，我独立复算得到同一组数。
**但请注意本轮改动（CHAR→HEAD）在真实 `src/` 上差异为 0** —— fix-4 的收益全部落在当前仓里
不存在的写法上，是**预防性**的，不是修好了当下正在漏的东西。

### 2.3 发现：re-export 那条是**修正 + 一处潜伏回归**

static 那条（`?` → `??`）我未找到任何反例，判为**纯修正**。
re-export 那条（`[^'"`;]*?` → `[^;]*?`）是**放宽**，它把 CHAR 挡住的一类跨语句假阳性重新放了进来。
触发条件：`export` 关键字与后文某个 ` from '…'` 之间**没有分号**。

原始探针输出（raw count，未去重）：

| 探针 | ORIG | CHAR | HEAD |
|---|---|---|---|
| E1 `export const dynamic = 'force-dynamic';` + import（**带分号**，即钉住的那条） | n=2 重复 | n=1 | n=1 |
| E2 同上但**不带分号**（ASI 风格） | n=2 重复 | **n=1** | **n=2 重复** |
| E3 `export const a = 1`（无分号）+ 后文 \`imported from 'lib/db/privileged'\` | n=1 **误捕** | **n=0** | **n=1 误捕** |
| E5/E6 无分号 export + 后文 `'derived from ' + 'x'` | n=1 `" + "` | **n=0** | **n=1 `" + "`** |
| E8 `export default function P(){…}` + JSX 属性里含 ` from "…"` | n=1 误捕 | **n=0** | **n=1 误捕** |
| E7 真 re-export `export { a } from 'lib/db/runtime';` | n=1 | n=1 | n=1（未误伤） |

E3 最尖锐：HEAD 会把一句**散文**里的 `from 'lib/db/privileged'` 当成真 importer 捕获，
而这正是被守护模块之一 —— 落到真文件里就是一条凭空多出来的 importer，会让普查钉误红并指错人。

**可达性评估（如实）：** 仓库用 Prettier（`prettier.config.js`）且实测 `src/` 下
9 处 `export const dynamic|runtime|maxDuration` **全部带分号**；全量 384 文件多重集比对
CHAR vs HEAD = 0，**当前 `src/` 零触发**。故这是**潜伏**回归，不是当下的洞。

**但两件事值得记账：**
- 新补的那条钉（`🔒 回归：re-export pattern 不得跨过语句边界重复计入下一条 import`）
  用的输入**带分号**（E1）。它挡得住「还原成 `[\s\S]*?`」这种大回退（见 §3 变异 3 实证），
  **挡不住 `[^;]*?` 这一档的放宽本身**——CHAR→HEAD 这次放宽发生时它是绿的。
  即：**钉的措辞（「不得跨过语句边界」）比它实际验证的范围宽。** 这正是本批反复出现的
  「声称的覆盖面 > 实际覆盖面」同一族。
- 该假阳性方向**未登记**：代码里只登记了 re-export 的**漏抓**侧（「多行 export 子句行尾注释含
  分号仍会漏」），没登记**误捕**侧（「export 与后文 ` from '…'` 间无分号时会跨语句粘」）。

## 3. 死钉 / 自陈 / 假阳性钉 —— 三条逐条实测

### 3.1 那条死钉现在真的活了（变异 1 证实）

上一版 `🔒 回归：后文…from 不得吞掉侧效应 specifier` 的输入是 `const s = 'a from b';`，
rv3 点名它在旧 pattern 下也绿 = 死钉。本轮改成 `const s = 'a from ' + 'b';`。

**变异 1**（把 `??` 改回 `?`，即撤销本轮唯一的功能性改动）实跑：

```
× 🔒 回归：侧效应 import 后跟 from-import，两个 specifier 都要抓到
× 🔒 回归：后文普通字符串里的 from 不得吞掉侧效应 specifier      ← 就是那条曾经的死钉
Tests  2 failed | 16 passed (18)
```

**红 2 条，且第二条正是那条死钉。判定：死钉已复活，鉴别力实证成立。**

### 3.2 那句自陈确实没了

被删的是：「声称的覆盖面与实际覆盖面由同一个数据结构产出，**不可能再对不上**」。
`grep -n "不可能再对不上" tests/unit/db-layer-importer-census.test.ts` 三处命中，逐条看落点：

| 行 | 落点 | 是否断言 |
|---|---|---|
| 410 | `* > fix-4 删除（用户裁决）：此处原接着写「…」。该句已被 rv2 与 rv3 两轮各自实测证伪` | 否，更正记录里的**原文引用** |
| 489 | `// fix-2 的「条数就是数据结构本身，不可能再对不上」→ 简繁形近字即可绕过。` | 否，举例说明**它是假的** |
| 509 | `const overclaims = ['即红并点名', '不可能再对不上', '机械守住']` | 否，**黑名单字符串字面量** |

**断言形态的自陈已移除**，幸存的三处全部是「引用它以说明它为假」。判定：属实。
现行叙述改为「本组断言**不对自身覆盖面作任何承诺**」。

### 3.3 新补的那条假阳性钉有鉴别力（变异 3 证实）

**变异 3**（re-export 还原成 `[\s\S]*?`）实跑：

```
× 🔒 回归：re-export pattern 不得跨过语句边界重复计入下一条 import
Tests  1 failed | 17 passed (18)
```

**红 1 条且正是它。鉴别力成立**（对「整个还原」这一档回退）。

**变异 4**（re-export 收紧成 fix-3 的 `[^'"`;]*?`）实跑：

```
× 🔒 回归：ES2022 字符串绑定名（合法模块语法）要抓到
Tests  1 failed | 17 passed (18)
```

说明 re-export 的取值区间被两侧钉夹住了。**但区间内部没夹住**：CHAR(`[^'"`;]*?`) 与
HEAD(`[^;]*?`) 在这条钉上**都是绿的**，而两者在 E2/E3/E5/E6/E8 上行为不同（§2.3）。
即：**这条钉挡得住大回退，挡不住本轮这一档的放宽本身。**

## 4. 已知边界登记核实

### 4.1 登记的四项 —— 逐条实测，全部属实

| 登记项 | HEAD 实测 | `src/` 用量核实 | 判定 |
|---|---|---|---|
| `import{a}from'x'`（无空格） | D1 FAIL，`got=[]` | `grep -rn "import{" src/` → 0 | 属实 |
| `import'x'`（无空格侧效应） | D2 FAIL，`got=[]` | `grep -rn "import['\"]" src/` → 0 | 属实 |
| 动态 import attributes（with / assert） | D3/D4 FAIL，`got=[]` | grep → 0 | 属实 |
| 字符串拼接动态路径 | C2 返回 `[]`，且已有一条**故意断言抓不到**的钉 | — | 属实 |
| 多行 export 子句行尾注释含分号 | D14 FAIL，`got=[]` | — | 属实 |

**没有「其实已经能抓到却还登记着」的僵尸条目。** 五项全部仍然为真。

### 4.2 我另找到的未登记形态（共 5 类，均为**潜伏**，当前 `src/` 零触发）

| # | 形态 | HEAD 行为 | 何时引入 | 是否登记 |
|---|---|---|---|---|
| U1 | 多行 import 的行尾注释里含 ` from '…'`：`import {\n  b, // moved from './old'\n} from './real';` | `got=["./old"]` —— **既误捕又漏抓真 specifier** | 三版皆有（既存） | **未登记** |
| U2 | `export` 与后文 ` from '…'` 之间**无分号**时跨语句误捕（§2.3 E2/E3/E5/E6/E8） | 误捕，E3 直接捏造出 `lib/db/privileged` | **本轮由 `[^;]*?` 重新放入**（CHAR 曾挡住） | **未登记**（只登记了漏抓侧） |
| U3 | `import(\`./${name}\`)` 模板串带替换 | 捕到字面量 `./${name}` | 加反引号时（fix-3）引入 | **未登记**（已登记的只有 `'./' + name` 拼接形态） |
| U4 | 普通字符串里写着一条 import 语句：`const tpl = "import { x } from './fake';";` | `got=["./fake"]` 误捕 | 三版皆有（既存） | **未登记**（commit 正文提过，代码「已知边界」段没有） |
| U5 | 动态 import 带尾逗号：`import('./tc',)` | `got=[]` 漏抓 | 三版皆有（既存） | **未登记** |

> **重要限定：** 代码里的「已知边界」段**没有声称自己完备**——它写的是
> 「本段仍只是『已经想到并验过的』清单」。所以 U1–U5 **不构成对任何已交付陈述的证伪**，
> 只是清单可以更长。这与前几轮「声称完备而实际不完备」是不同性质，本轮该文件在措辞上
> 确实收住了。U2 是其中唯一由**本轮**引入的。

### 4.3 决定性交叉验证：与 TypeScript AST 对照，全仓 0 分歧

不满足于探针，我用 `typescript` 编译器 API 做了一份**完全独立**的 ground truth
（`ts.createSourceFile` 遍历 `ImportDeclaration` / `ExportDeclaration` /
`ImportEqualsDeclaration` / 动态 `import()` / `require()`），对 `src/` 全部 384 文件比对：

```
### src/lib/db/runtime
  AST truth   : ["src/instrumentation.ts","src/lib/db/prisma.ts","src/lib/db/tenant-scope.ts"]
  regex(HEAD) : 同上          GUARDED exp : 同上
  AST vs exp   -> missing: []  unexpected: []
### src/lib/db/privileged
  AST truth   : ["src/app/api/auth/[...nextauth]/route.ts","src/app/api/auth/register/route.ts",
                 "src/lib/agent/context.ts","src/lib/auth/index.ts","src/lib/auth/register.ts"]
  regex(HEAD) : 同上          GUARDED exp : 同上
  AST vs exp   -> missing: []  unexpected: []

全 specifier 无过滤比对：disagreeing files: 0/384  (missed 0, extra 0)
```

**在当前这棵树上，这个正则扫描器与真 AST 解析逐条等价，且 `GUARDED` 期望集合与实物完全一致。**
这也解释了为什么 U1–U5 全是潜伏的：它们在 `src/` 里一次都没出现。
这是本轮最强的一条证据 —— F008 要守的那件事（importer 清单与实物同步）**当前是对的**，
且不是靠「扫描器看不见所以全绿」蒙对的（活性已由变异 6 与 §1 探针证明）。

## 5. 被裁决暂缓四项的登记核实

不据此驱动判定（规格层裁决），只核「登记是否与实物相符」。**四项全部属实，均仍在树上。**

| 暂缓项 | 核实方式 | 结果 |
|---|---|---|
| 扩叙述面守护到 `runtime.ts` / `prisma.ts` | 叙述面 describe 只 `readFileSync(TENANT_SCOPE_FILE)`（:438）；`grep` 全 `tests/` 无任何测试读 runtime.ts / prisma.ts 叙述 | 属实，仍未覆盖 |
| 给 `🔒④` 换语义级判据 | :509 仍是三个字面串黑名单 `['即红并点名','不可能再对不上','机械守住']` + 一条 `toContain` 正向串 | 属实，仍是词法级 |
| `🔒②b` 查 pattern 有效性 | **变异 5 实跑**：把 claim ② 的 pattern `调`→`調`（简繁形近字），同时把被证伪原句「产品代码（src/）零 withTenant 调用点」**逐字写回** `tenant-scope.ts` → **18 passed 全绿，一条不红** | 属实，绕过仍成立 |
| census 测试自身叙述面无钉 | `grep -rn "db-layer-importer-census" tests/ scripts/` → 仅 3 处**文字提及**，无任何测试读取该文件内容 | 属实，仍无钉 |

被验方在 handoff 中称这四项「在树上仍成立，是经裁决暂缓不是被隐瞒」——**该陈述经我独立核实属实**。

## 6. 变异证活记录（6 条，全部实跑；还原一律反向编辑 + shasum 对账，未用 git checkout）

基线：`tests/unit/db-layer-importer-census.test.ts`
sha256 `e5dc323f2137ecd938533d6c04920b753cd10df24f4f57e81f24a19f112ef231`；
`src/lib/db/tenant-scope.ts` sha256 `7cd74122cf230ae565101879b464105ddae8b814b848b4d5756d6111c26aaf13`；
基线运行 **18 passed**。

| # | 变异内容 | 实测结果 | 红的是不是该红的那条 | 还原对账 |
|---|---|---|---|---|
| 1 | static `)??[` → `)?[`（撤销本轮唯一功能改动） | **2 failed / 16 passed** | 是：侧效应吞噬条 + **那条复活的死钉** | sha 一致 |
| 2 | static 还原成 fix-3 字符类 `[^'"`;]*?` | **2 failed / 16 passed** | 是：行尾注释条 + ES2022 条 | sha 一致 |
| 3 | re-export → `[\s\S]*?` | **1 failed / 17 passed** | 是：新补的 re-export 假阳性条 | sha 一致 |
| 4 | re-export → `[^'"`;]*?`（fix-3 版） | **1 failed / 17 passed** | 是：ES2022 条（export 半边） | sha 一致 |
| 5 | claim② pattern `调`→`調` **且** 把被证伪原句写回 `tenant-scope.ts` | **18 passed 全绿** | —（本变异意在证明**绕过仍存在**，见 §5） | 两文件 sha 均一致 |
| 6 | `GUARDED` 里摘掉 `src/lib/db/prisma.ts` | **1 failed / 17 passed**，报文含「多出（未登记的 importer）：src/lib/db/prisma.ts」 | 是：普查钉本体，**且点名了文件**（acceptance ③④） | sha 一致 |

变异全部还原后 `git status --short src/ tests/` **无输出**（工作区干净）。

## 7. L1 回归（本轮实跑）

| 项 | 命令 | 结果 |
|---|---|---|
| 类型 | `npx tsc --noEmit` | exit 0 |
| lint | `npx next lint` | `✔ No ESLint warnings or errors` |
| 全量单测 | `npx vitest run` | **148 files / 1885 tests 全绿**（11.11s） |

被验方 handoff 声称的 `1885` 与 `148` 两个数字，我独立复跑得到同一组数，属实。
handoff 里「rls:e2e / gate:smoke / delivery:e2e / reach:e2e 本轮未跑」的如实登记，我认可其理由
（本轮 `src/` diff 为空），**我本轮亦未跑这四条**，一并登记为未核。
L2（真实外部服务 / 计费 / 生产写入）未授权，未执行。

## 8. 结论

### F008 —— **PASS**

acceptance 五项逐条核对：

| 项 | 判定 | 依据 |
|---|---|---|
| ① 两条注释逐条改对 | PASS | `runtime.ts` 头注列的三个 importer 与 **AST ground truth 逐条相等**；`tenant-scope.ts` 已不再出现「全仓零 withTenant 调用点」类断言，改为不含计数的设计性描述 |
| ② 改完自行核实、事实性陈述有命令依据 | PASS | 我独立复算了它声称的每一组数（384 文件三版互比：新丢失 0 / 新捡回 5 / 消假阳性 1；CHAR↔HEAD 0/0；1885 全绿），**无一条被证伪** |
| ③ 机械钉扫 src/、不一致即红并点名 | PASS | 变异 6：红 1 条且报文点名 `src/lib/db/prisma.ts` |
| ④ 变异证活、不得用 git checkout 还原 | PASS | 本轮我自行跑了 6 条变异，红的条数与条目均符合预期；还原用反向编辑 + shasum 逐位对账 |
| ⑤ tsc / lint / 全量 vitest 绿 | PASS | 见 §7，三项实跑 |

rv3 点名的三条实证缺陷**全部闭合且经变异证实**：

- 两条已知漏抓（行尾注释含引号 / ES2022 字符串绑定名）→ §1，CHAR 上 FAIL、HEAD 上 PASS，活性成立
- 死钉 → §3.1，变异 1 下真的红了
- 自陈 → §3.2，断言形态已移除

**判 PASS 的关键正向证据**不是「测试全绿」，而是 §4.3：扫描器在整棵树上与 TypeScript AST
**逐条等价（0/384 分歧）**，且 `GUARDED` 期望与实物完全一致。这条判据先证明了「看得见」
（§1 探针 + 变异 6），再报「无差异」，符合「0 findings 必须先证明判据能看见目标」的要求。

### 记入 backlog 的残留（不阻塞本轮，均为潜伏、当前 `src/` 零触发）

1. **U2（本轮引入，建议优先）**：re-export 的 `[^;]*?` 在「export 与后文 ` from '…'` 之间无分号」
   时会跨语句误捕，E3 探针下**凭空捏造出被守护模块 `lib/db/privileged` 作为 importer**。
   代码只登记了该 pattern 的漏抓侧，未登记误捕侧；新补的那条钉用的是带分号输入，
   **对本档放宽无鉴别力**。当前不可达（Prettier 强制分号，`src/` 9 处 route segment config 全带分号，
   全量比对 CHAR↔HEAD = 0）。根治方向是把 re-export 写成结构化子句而非「任意字符 + from」。
2. **U1 / U3 / U4 / U5**：另四类未登记形态（§4.2），三版皆有或早于本轮引入，当前零触发。
3. 用户已裁决暂缓的四项（§5）——经核实登记与实物相符，原样保留在 backlog。

### 独立性与边界声明

- 本次验收在隔离 worktree `/…/agent-a650cc63f0a055d77` 内进行，被验 HEAD `ff33b54`。
- **主仓 `src/` 未动一个字节**；本 worktree 内的 6 条变异全部反向编辑还原并 shasum 逐位对账。
- 未修改 `progress.json` / `features.json` / 规格 / 配置；未 push。
- 结论只依据实物（代码、探针输出、变异实跑、AST 对照）。commit 正文、代码注释、
  `generator_handoff` 均只作为**待核对象**，其中被我逐条核对的陈述见 §0 / §5 / §7。

### 我自己的盲区（如实登记）

- 探针语料 46 条（38 + 8）由我构造，**不完备**；U1–U5 是我想到的，想不到的仍可能存在。
  AST 对照弥补了这一点，但只对**当前这棵树**成立——它证明的是「此刻等价」，不是「永远等价」。
- 未跑 `rls:e2e` / `gate:smoke` / `delivery:e2e` / `reach:e2e`（本轮 `src/` diff 为空，判为无关面）。
- 未评估 F001–F007（本轮未改动，非我本轮射程）。
- 未做 L2（未授权）。
