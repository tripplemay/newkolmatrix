# M5.1b-TENANT-INJECTION — 复验-4：F001–F007 回归确认 + 全批 L1 独立复跑

> **落盘路径说明：** 本 evaluator 以 worktree 隔离运行，工具层**拒绝**向主仓
> `/Users/yixingzhou/project/newkolmatrix/docs/test-reports/` 写入（报错原文：
> "This agent is isolated in the worktree ... Edit the worktree copy of this file instead of
> the shared-checkout path."）。故本报告落盘于 worktree 内同名路径：
> `/Users/yixingzhou/project/newkolmatrix/.claude/worktrees/agent-a728de1a5e594ea12/docs/test-reports/M5.1b-rv4-regression-L1.md`
> 需由编排者搬运到主仓。

- **Evaluator：** 隔离 subagent（fresh context）
- **被验 HEAD：** `ff33b54410d0b600391f5cda71b34844b3dd5731`
- **轮次：** `status=reverifying`，`fix_rounds=4`
- **职责面：** (A) F001–F007 回归确认 · (B) 全批 L1 独立复跑
  - F008（importer 普查钉本体）由另一位 evaluator 负责，本报告不下 F008 判定
- **写入方式：** 边验边追加（本轮已有两个 evaluator 进程因长时无输出被 watchdog 判 stalled；故骨架先落盘）

---

## 0. 进度日志（append-only）

| # | 检查项 | 状态 |
|---|---|---|
| 0 | worktree 建立 + node_modules 符号链接 + .env 复制 + HEAD 核对 = ff33b54 | 完成 |
| 1 | `src/` 零改动核实（自设判据 + 活性自测） | 完成 → §1 |
| 2 | L1: `tsc --noEmit` | 完成 exit 0 |

---

## 1. `src/` 本轮零改动核实（自设判据 + 活性自测）

**不采信** handoff 里的「src/ diff 为空」自述，也**不用** `git diff`（diff 为空可能来自 pathspec 打错、rename 检测等）。改用**内容寻址的树对象 ID 直接比对**——git 的 tree OID 是子树全部内容的 Merkle 根，两个 OID 相等 ⇔ 子树逐字节相同，与 diff 算法无关。

### 1.1 活性自测（先证明判据看得见改动）

判据必须能对已知含 `src/` 改动的区间报「不同」，否则「相同」毫无意义。

```
$ git log --oneline -25 -- src/ | head -5
1ea4abb fix(M5.1b-...-F008): 修 importer 扫描器的两处真实漏抓 ...
df36a3f fix(M5.1b-...-F008): 让「守的条数」由数据结构产出 ...
57998bf fix(M5.1b-...-F008): 重写批末失实的说明段 ...
7ec61df feat(M5.1b-...-F005): ALS 三执行上下文传播接线 + 实测
85e0362 feat(M5.1b-...-F004): 13 处 $transaction 迁移到 withTenant

$ git rev-parse 85e0362^:src   → 082ebb949d4ab90a2c4ff96d695a2cf1d0523f6e
$ git rev-parse 85e0362:src    → be1910fa5fd6bbcd11d96026253bc218eed3d04a
```

两值**不同** → 判据对真实 `src/` 改动敏感。**活性通过。**

（另记两条阴性对照：`5c1b5e7:src` == `af8ca20:src` == `be1910f`，`2bc02aa:src` == `1ea4abb:src` == `542baa1` —— 这两对确实是 src 无改动的提交对，与 `git log -- src/` 的结果自洽。）

### 1.2 本轮判定

```
$ git rev-parse a122b7d:src → 542baa157ac70bf4cd2c6186bf6e1ecfc1268d95
$ git rev-parse ff33b54:src → 542baa157ac70bf4cd2c6186bf6e1ecfc1268d95
```

两值**相同** → 区间 `a122b7d..ff33b54` 内 `src/` 子树逐字节未变。

### 1.3 工作树未被污染

```
$ git status --porcelain -- src/     → （空）
$ git status --porcelain             → ?? docs/test-reports/M5.1b-rv4-regression-L1.md
                                       ?? node_modules
```
仅本报告与 node_modules 符号链接为未追踪项，`src/` 工作树 == HEAD。

### 1.4 变异还原基线（供 §4 对账）—— 附一条判据本身被证伪的记录

初次取基线用的是 `find src -type f -exec cat {} + | shasum -a 256`，得 `48de6f38…`。
稍后在**未改动任何文件**的情况下重取，得 `9b43bba0…` —— 不一致。
按「判不了就先查判据」的纪律，对该命令做了三连重放：

```
$ find src -type f -exec cat {} + | shasum -a 256   → 383a1f24…
$ find src -type f -exec cat {} + | shasum -a 256   → 32362fde…
$ find src -type f -exec cat {} + | shasum -a 256   → 08b65488…
```

**同一份未改动的内容，三次三个值** → `-exec … {} +` 的批处理/遍历顺序不稳定，
**该命令根本不能用作还原对账判据**（会给出假的「还原失败」信号）。
同期 `git status --porcelain -- src/` 为空、`find src -type f | wc -l` 恒 396，
证明内容确实没变，变的是判据本身。

改用**排序后**的稳定形式，三连重放一致：

```
$ find src -type f | sort | xargs cat | shasum -a 256   → b1111ab59f917f7f056395e8cdb78198c78680d48097350964a8c2f5c1e1f69d
$ （重放 ×2）                                            → 同值 ×2
```

**本报告此后一律以 `b1111ab5…`（排序内容哈希）+ `git status --porcelain -- src/` 为空
+ 单文件 `shasum` 三项并用作为还原判据。**

单文件基线：

| 文件 | 变异前 shasum-256 |
|---|---|
| `src/lib/agent/gate/gate.ts` | `58c8e812a0bccee49989039497890d96aa5f4b3673adac6677e563c74b34976d` |
| `src/lib/db/tenant-scope.ts` | `7cd74122cf230ae565101879b464105ddae8b814b848b4d5756d6111c26aaf13` |

**结论：`src_unchanged_this_round = true`（判据：git tree OID，活性已证）。**

---

## 2. L1 独立复跑

| 命令 | 结果 | 证据 |
|---|---|---|
| `npx tsc --noEmit` | **PASS** exit 0 | 输出 0 字节（无任何诊断） |
| `npx next lint` | **PASS** exit 0 | `✔ No ESLint warnings or errors` |
| `npx vitest run` | **PASS** exit 0 | `Test Files 148 passed (148)` / `Tests 1885 passed (1885)` / Duration 11.00s |
| `npm run rls:e2e` | **PASS** exit 0 | `Test Files 1 passed` / `Tests 15 passed (15)` |
| `npm run gate:smoke` | **PASS** exit 0 | `[gate-smoke] ✅ 全部断言通过（两步票据 + 7 态 + 并发竞态 + D20 变异）` |
| `npm run delivery:e2e` | **PASS** exit 0 | `[delivery-e2e] ✅ 交付闭环全绿` + `真实资金动作 0 · 真实 key 发放 0` |
| `npm run reach:e2e` | **PASS** exit 0（降级模式） | `SKIP_DRAFT_LLM=true npm run reach:e2e` → `[reach-e2e] ✅ E2E 闭环全绿` + `L2 用量申报：gateway chat 0 次 · 真实投递 0 封（mock）` |
| `npm run test:visual` | **不跑** | 见 §2.4 |

被验方 handoff 登记本轮**未跑** rls:e2e / gate:smoke / delivery:e2e / reach:e2e。上表四条**由本 evaluator 独立补跑**，均为实测输出。

### 2.1 rls:e2e 不是空跑（先验活再采信）

`rls:e2e` 全程仅 2.13s，需排除「配置没收到文件/静默 skip 就绿」这种恒绿形态。三条机械依据：

1. **收集面结构性存在**：`vitest.e2e.config.ts` 的 `include` 为 `['tests/e2e/**/*.e2e.test.ts']`，
   `ls tests/e2e/` → 只有 `tenant-rls-min-loop.e2e.test.ts` 一个文件，与 `Test Files 1 passed` 相符。
2. **15 条用例名逐条列出**（`--reporter=verbose` 实跑），六段闭环 (a)–(f) 全部在场，且带三条反向自证：

```
✓ §1 ... 运行时 client 的连接角色 = 非特权（不是"配了串但没用上"）
✓ §1 ... 哨兵：开关开 + kol_app 连接 → status=ok 且 enforced=true
✓ §1 ... 🔒 反向自证：同样开关开、连接串配回特权 → 抛 PrivilegedConnectionError
✓ §2 (a) 登录 ... 正确凭据 → 认到本人，且解析出的租户是 A            216ms
✓ §2 (a) 登录 ... 错误口令 → null（证明上一条不是"恒返回一个用户"）  214ms
✓ §2 (a) 登录 ... 🔒 白名单不是可选项：同一条查询走 kol_app 且无租户变量 → 恒 null
✓ §3 (b) 注册 ... 注册成功，且三行都在库里、同属新租户                233ms
✓ §3 (b) 注册 ... 🔒 「同事务」的机械判据：中途失败 → 先写的租户行也不许留下  222ms
✓ §4 (c) API route：GET /api/nav-badges ... A 的会话 → 200 + A 自己的计数
✓ §4 (c) ... B 的会话 → 拿到的是 B 的计数（同一条 route，换个会话换一套数）
✓ §5 (d) RSC page：/admin/knowledge ... A 的会话 → 渲染出 A 的游戏，且**看不见** B 的游戏
✓ §6 (e) 例程：ROUTINES health-scan ... 跑完并把留痕写进 A 名下
✓ §7 (f) 🔒 raw SQL 直查 B 的行：A 的作用域里 0 行，特权连接上 1 行
✓ §7 (f) 🔒 产品路径同款：按主键取 B 的 PendingAction（无租户 where）→ null
✓ §7 (f) 🔒 写侧同样拦住：在 A 的作用域里往 B 名下插行 → 被 WITH CHECK 拒
```

3. **耗时形态证明真打了库与真算了 bcrypt**：登录/注册四条各 214–233ms（bcrypt cost 12 的量级），
   其余纯 DB 往返 1–12ms。若是 skip/mock，这四条不可能稳定落在 200ms+。

三条合起来 → rls:e2e 是**实跑**，不是恒绿。（§4 另有一条对它的实打变异。）

### 2.4 `npm run test:visual` 为什么不跑

spec §7 明文登记：「本机 `test:visual` 恒 15 passed / 2 failed 属 `BL-VISUAL-DATA-ISOLATION`，**不计分**」。
且本轮 `src/` 零改动（§1 已机械证实）、被改文件是一个 unit 测试，不可能影响像素快照。
故按 spec 边界不跑，**不计入全绿也不计入失败**。

---

## 3. F001–F007 回归确认

### 3.1 前提：本轮回归面为什么窄

§1 已机械证实 `src/` 子树 OID 在 `a122b7d..ff33b54` 内**逐字节未变**。因此 F001–F007 的**被测实现本体**本轮零改动，
回归风险只剩一条通路：**被改的 `tests/unit/db-layer-importer-census.test.ts` 是否牵连其他 feature 的承载钉**。
（该文件自身的 pattern 正确性属 F008 面，由另一位 evaluator 判，本报告不下 F008 判定。）

牵连面的机械界定：该文件是否被其他测试文件 import / 是否写入共享状态。

```
$ grep -rn "db-layer-importer-census" tests/ src/ scripts/ --include=*.ts
tests/unit/db-layer-importer-census.test.ts:...（仅自身文件内的自指注释）
```
无任何其他文件 import 它；它只做 `readFileSync` 递归读 `src/`，不写盘、不改共享夹具。
→ **结构上无法牵连**其余 feature 的承载钉。下面仍逐条实跑确认，不靠这条推理。

### 3.2 七条 feature 的承载测试面实跑（全部实测，非推断）

一次性收集 11 个承载文件：`Test Files 11 passed (11)` / `Tests 159 passed (159)`。逐文件：

| Feature | 承载测试面 | 实测 |
|---|---|---|
| **F001** 三层 client + ALS 感知代理 | `tests/integration/tenant-injection-proxy.test.ts` | 11 passed |
| **F002** withTenant 硬语义 + B1-4/B3 回归 | `tests/integration/tenant-scope-semantics.test.ts` | 14 passed |
| **F003** 引导白名单 + 越权面普查钉 | `tests/unit/bootstrap-whitelist-census.test.ts` | 5 passed |
| **F004** 15 处 $transaction 迁移 | `tests/integration/f004-transaction-atomicity.test.ts` | 8 passed |
| **F005** ALS 三上下文传播 | `tests/integration/als-context-propagation.test.ts`<br>`tests/integration/routine-tenant-explicit.test.ts`<br>`tests/unit/session-tenant-context.test.ts` | 13 passed<br>6 passed<br>14 passed |
| **F006** 开关开六段最小闭环 | `tests/e2e/tenant-rls-min-loop.e2e.test.ts`（`npm run rls:e2e`） | 15 passed（§2.1 已验活） |
| **F007** 文档钉 + 既有钉状态复核 | `tests/unit/architecture-doc-freshness.test.ts`<br>`tests/integration/rls-negative-suite.test.ts`<br>`tests/unit/project-scope-census.test.ts` | 41 passed<br>14 passed<br>15 passed |
| （F008 面，仅记不判） | `tests/unit/db-layer-importer-census.test.ts` | 18 passed |

七条 feature 的承载面**无一翻红**。叠加 §2 的全量 1885 + rls/gate/delivery/reach 四套 e2e 全绿。

---

## 4. 变异证活（做了 3 条，要求下限 2 条）

**选条理由（不是随机挑的）：**
- **M1 → F003**：普查钉是「全绿但钉已死」风险最高的形态 —— 扫描器一旦空转/收集不到文件就**恒空绿**，
  而恒空绿与「真的没有违规」在输出上**完全一样**。必须外部植入一个已知违规才能分辨。
- **M2 → F004**：本批要根治的原始缺陷就是审计 B1-4「回滚后那一行仍存在」。F004 的全部意义是
  「迁移到 withTenant 后原子性归调用方」。若这道钉死了，**真实数据完整性回归会静默通过**。
- **M3 → F007**：本批**反复复发**的缺陷族正是「文档/注释声称与实物不符，而守它的钉是死的」
  （rv1/rv2/rv3 连续三轮各被证伪一次）。故对文档新鲜度钉单独证活。

### M1 · F003 越权面普查钉

**变异**：新建 `src/lib/db/__rv4_mutation_probe.ts`（**不在** `BOOTSTRAP_WHITELIST` 内）真实使用 `privilegedDb`。

**实测（红，且点名）：**
```
× src/ 下使用 privilegedDb 的文件必须全部在白名单内 3ms
AssertionError: 发现**未登记**的特权连接使用点：src/lib/db/__rv4_mutation_probe.ts
+   "src/lib/db/__rv4_mutation_probe.ts",
      Tests  1 failed | 4 passed (5)
```
→ 钉**活**，且满足 acceptance ④「必须红**并点名**」。

**还原**（删除自建文件，未用 `git checkout`）：
```
$ git status --porcelain -- src/              → （空）
$ find src -type f | wc -l                    → 396
$ find src -type f | sort | xargs cat | shasum -a 256
  b1111ab59f917f7f056395e8cdb78198c78680d48097350964a8c2f5c1e1f69d  ← 与 §1.4 基线一致
```

### M2 · F004 原子性钉（B1-4 根治点）

**变异**：把 `src/lib/agent/gate/gate.ts:250` 的 `confirmPendingAction` **反向迁移**——
回调不再用 withTenant 给的 `tx`，改从运行时 client 取一个独立连接：

```ts
-  await withTenant(ctx.tenantId, async (tx) => {
+  await withTenant(ctx.tenantId, async (_txIgnoredByMutation) => {
+    const tx = (await import('lib/db/runtime')).getRuntimeDb();
```
（语义 = 迁移被撤销：写入在自己的连接上自动提交，外层回滚带不走它。）

**实测（红，且正是 B1-4 的形态）：**
```
× confirmPendingAction（gate.ts:249）随外层回滚 → 状态仍是 pending 97ms
AssertionError: expected 'confirmed' to be 'pending' // Object.is equality
      Tests  1 failed | 7 passed (8)
```
→ 钉**活**，且**精准**：8 条里只红 1 条，不是一改就满盘皆红那种无鉴别力的红。

**还原**（反向编辑，未用 `git checkout`）：
```
$ shasum -a 256 src/lib/agent/gate/gate.ts
  58c8e812a0bccee49989039497890d96aa5f4b3673adac6677e563c74b34976d  ← 与 §1.4 基线一致
$ git status --porcelain -- src/   → （空）
$ find src -type f | sort | xargs cat | shasum -a 256
  b1111ab59f917f7f056395e8cdb78198c78680d48097350964a8c2f5c1e1f69d  ← 与 §1.4 基线一致
```

### M3 · F007 文档新鲜度钉

**变异**：改 `docs/specs/M5.1-uncovered-entrypoints.md:23` 合计行一个计数
（`| **合计** | **102** | **78** | **3** | **75** |` → `… **4** | **74** |`）。

**实测（红，且报出实物与文档的差值）：**
```
× 未覆盖清单文件的合计行 = 普查实测（忘重跑 `npm run census:entrypoints -- --write` → 红） 9ms
AssertionError: 合计行与普查实测不符：| **合计** | **102** | **78** | **4** | **74** |:
  expected [ 102, 78, 4, 74 ] to deeply equal [ 102, 78, 3, 75 ]
      Tests  1 failed | 40 passed (41)
```
→ 钉**活**（计数确实与 `runCensus()` 的实物普查比对，不是自比自），41 条里只红 1 条。

**还原**（反向编辑，未用 `git checkout`）：
```
$ shasum -a 256 docs/specs/M5.1-uncovered-entrypoints.md
  b665ee6cdd3447241222c0cd9dceb9bacc9016c72102cdc435a39b410ced1f39  ← 与变异前一致
```

**三条变异合计：3/3 钉活、3/3 已还原且逐位对账通过、0 次使用 `git checkout`、主仓 `src/` 未动一字节。**

---

## 5. dev 库残留核对（F006 清理条款）

判据自定，**先验活**：临时探针（写在 worktree 根的 `.rv4-*.ts`，**不入 `scripts/`**，跑完已删）
直连 dev 库按各套件的夹具前缀（从各测试源码实读，非猜测）逐表计数。

### 5.1 判据活性自测

```
[活性] 植入探针行后：name 判据看见 1 行，slug 判据看见 1 行（须 >=1）
[活性] 删除探针行后：name 判据看见 0 行
```
先 `tenant.create({slug:'f006rls-LIVENESSPROBE-rv4'})` → 判据看见 1 行 → 删掉 → 判据回 0。
**判据能看见目标，"0 残留"这个结论才有意义。**

### 5.2 本批夹具残留普查（结论：0）

```
OK  0  F006 rls:e2e 租户(name) / 租户(slug) / 用户(email) / 用户(@f006.local)
OK  0  F006 rls:e2e 项目 / 游戏 / 留痕(summary) / pendingAction(payloadHash)
OK  0  F004 原子性夹具(f004 slug) / gate:smoke / delivery:e2e / reach:e2e 夹具
[残留普查] 合计 = 0
[全库量级] tenant=4 user=1 project=4 game=4 operationLog=769
[全库租户 slug] __auth-audit__ | dev | （空）| （空）
```
全库量级非零（769 条留痕、4 租户）→ 排除「连了空库所以恒 0」。
**F006 acceptance ⑤「dev 库无本次残留」成立。**

### 5.3 但普查顺带查出两件事（如实登记，不并入 PASS/FAIL）

**(a) 两条空 slug 孤儿租户 —— 早于本批，非本批产物**

```
slug=null name="f005-52865-dup 工作室-第二次"  createdAt=2026-08-04T20:32:33Z  user=0 project=0 game=0
slug=null name="f005-52865-case 工作室-大写"  createdAt=2026-08-04T20:32:34Z  user=0 project=0 game=0
```
`createdAt` 为 **2026-08-04**，本批立项是 2026-08-07；`f005-` 前缀属 **M5-AUTH-RLS 的 F005（开放注册）**，
不是 M5.1b 的 F005。→ **跨批次孤儿，不计入本批残留**（与 handoff 已登记的「dev 库跨批次孤儿行」同一族）。

**(b) `tests/integration/f004-transaction-atomicity.test.ts` 每跑一次泄漏孤儿行 —— 机械证实**

不是推断，是同一命令前后两次快照：

```
=== 跑 f004 之前 ===  operationLog=775  tenant=4  pendingAction=213  孤儿留痕=571
$ npx vitest run tests/integration/f004-transaction-atomicity.test.ts   → Tests 8 passed (8)
=== 跑 f004 之后 ===  operationLog=781  tenant=4  pendingAction=216  孤儿留痕=577
```
（孤儿留痕判据 = `LEFT JOIN "Tenant" … WHERE t.id IS NULL`，即 tenantId 指向已不存在的租户。）

**每跑一次 +6 条孤儿 OperationLog、+3 条 PendingAction，单调增长且永不回收**；租户本身被删干净了
（tenant 恒 4），泄的正是母 spec §5.2 点名的**软引用表**（`OperationLog` / `PendingAction` 无 FK 不级联）。
本 evaluator 近 3 小时的跑测已贡献 36 条，涉及 11 个租户，其中 10 个租户已删、留痕成孤儿。

**定性（不软化也不越界）：** 这**不构成任何一条 acceptance 的 FAIL**——
母 spec §6 明定「acceptance 边界 = features.json 各条 acceptance 逐项」，
而 §5.2 夹具纪律是 **F006 acceptance ⑤** 引用的条款、其射程是 F006 最小闭环的 A/B 夹具（实测 0 残留，已 PASS）；
**F004 的 acceptance 六条里没有清理条款**。故 F004 判 PASS。
但该泄漏是真实的、可复现的、单调增长的卫生缺陷，且会喂大 `BL-VISUAL-DATA-ISOLATION`
（母 spec §5.2 明写孤儿行「会污染 dashboard/today 视觉基线」）——**建议进 backlog**，
与已登记的「dev 库跨批次孤儿行」合并处理。

---

## 3.3 F001 的一条不继承的独立抽查

F001 acceptance ⑤ 要求「F008 改后的注释表述**自己再 grep 核一遍**是否属实」。
虽然 `src/` 本轮零改动（⇒ 与 rv3 判定时逐字节相同），但「继承上轮结论」正是本批反复栽跟头的地方，
故对 `runtime.ts` 那份 importer 清单（① prisma.ts ② tenant-scope.ts ③ instrumentation.ts）**自己重 grep**：

```
$ grep -rn --include="*.ts" --include="*.tsx" "db/runtime" src | grep -v "^src/lib/db/runtime.ts:"
src/instrumentation.ts:23:      import('./lib/db/runtime'),
src/lib/db/prisma.ts:20:// lib/db/runtime.ts（D-2 三层分工）…        ← 注释行，非 import

$ grep -rn --include="*.ts" --include="*.tsx" -E "from '\./runtime'|import\('\./runtime'\)" src
src/lib/db/prisma.ts:25:import { getRuntimeDb } from './runtime';
src/lib/db/tenant-scope.ts:56:import { getRuntimeDb } from './runtime';
```
→ 实测 importer **恰三处**，与注释 ①②③ 逐条相符（含 instrumentation.ts 的动态 import 形态）。
另：`tenant-scope.ts` 的说明段经 fix-3 后已不含任何可漂的计数声称（明写「刻意不写有几处调用点」），
**无可证伪的事实性断言** → 该条无回归。

---

## 6. 盲区与未验项（如实登记，不为好看回避）

1. **F008 不在本报告射程。** 本轮唯一改动文件 `tests/unit/db-layer-importer-census.test.ts` 正是 F008 的实现本体，
   其正则 pattern 有变化。「被守两模块的期望清单是否仍相符 / 新 pattern 有无新漏抓」由另一位 evaluator 判。
   本报告只证明**它不牵连其余 feature 的承载钉**（§3.1 结构论证 + §3.2 实跑）。
2. **本轮是回归确认，不是对 F001–F007 acceptance 的重新逐条判定。** 我的 PASS 依据是
   「实现本体逐字节未变（§1 机械证实）+ 承载测试面全绿（§3.2 实跑）+ 三条钉证活（§4）」。
   各条 acceptance 首次判定是否判对，属历轮 evaluator 的结论，本轮**未重做**。
3. **变异只做了 3 条**（F003 / F004 / F007）。**F001 / F002 / F005 / F006 的钉本轮未做实打变异**，
   其鉴别力依赖历轮报告的记录，本轮只对 F006 做了「非空跑」的三条旁证（§2.1）而非变异。
4. **L2 未授权**：`reach:e2e` 以 `SKIP_DRAFT_LLM=true` 降级跑，**真网关起草路径未验**
   （实测申报 `gateway chat 0 次 · 真实投递 0 封`）。真投递 / 真模型面 = **[L2] 未执行，待授权**。
5. **`test:visual` 未跑**（spec §7 登记的不计分项，理由见 §2.4）——故「视觉基线是否被 §5.3(b) 的孤儿行推动」本轮无实测。
6. **库残留判据的射程 = 我从各测试源码实读到的前缀集合**。若某套件用了我没读到的前缀，
   §5.2 的「0」看不见它。§5.3(b) 的孤儿行普查（`LEFT JOIN` 判据）不依赖前缀，是这一条的部分兜底。
7. **单机单库**：dev 库为本机共享，其他进程若并发写入会影响 §5.3(b) 的前后计数；
   本轮两次快照间只跑了那一条 f004 命令，但无法排除后台写入（量级 +6/+3 与用例数自洽，故判为可信）。

---

## 7. 结论

### 7.1 L1 独立复跑（被验方本轮全部未跑的四条，由本 evaluator 补跑）

| 命令 | 结果 |
|---|---|
| `npx tsc --noEmit` | PASS（exit 0，零诊断） |
| `npx next lint` | PASS（`✔ No ESLint warnings or errors`） |
| `npx vitest run` | PASS（148 files / **1885** tests 全绿） |
| `npm run rls:e2e` | PASS（15/15，已验非空跑） |
| `npm run gate:smoke` | PASS（exit 0） |
| `npm run delivery:e2e` | PASS（exit 0，真实资金动作 0） |
| `npm run reach:e2e` | PASS（**降级模式** `SKIP_DRAFT_LLM=true`；gateway chat 0 次） |
| `npm run test:visual` | 不跑（spec §7 不计分项） |

> 被验方 handoff 自报 vitest 为「148 files / 1885 tests 全绿」，本 evaluator 独立复跑得**同值**。
> （此处仅为独立复算相符的记录，不以其自述作为通过依据。）

### 7.2 F001–F007 判定

**七条全部 PASS**（回归面判定，边界见 §6.2）。

| Feature | 判定 | 依据 |
|---|---|---|
| F001 | PASS | 实现本体逐字节未变；`tenant-injection-proxy` 11/11；importer 清单独立重 grep 相符（§3.3） |
| F002 | PASS | `tenant-scope-semantics` 14/14；`withTenant` 源码未变 |
| F003 | PASS | `bootstrap-whitelist-census` 5/5；**M1 变异证活：红且点名**（§4） |
| F004 | PASS | `f004-transaction-atomicity` 8/8；**M2 变异证活：红且精准**（§4）。§5.3(b) 的孤儿行泄漏不属其 acceptance 任何一条，另行登记 |
| F005 | PASS | `als-context-propagation` 13/13 + `routine-tenant-explicit` 6/6 + `session-tenant-context` 14/14 |
| F006 | PASS | `rls:e2e` 15/15 六段闭环齐全且已验非空跑（§2.1）；**dev 库本批残留实测 0 且判据已验活**（§5.1–5.2） |
| F007 | PASS | `architecture-doc-freshness` 41/41 + `rls-negative-suite` 14/14 + `project-scope-census` 15/15；**M3 变异证活：红且点名**（§4） |

### 7.3 本轮附带产出的两条非 acceptance 发现

- **判据本身被证伪一次**：`find … -exec cat {} + | shasum` 对**未改动内容**三次给出三个不同哈希，
  不能用作还原对账判据（§1.4）。已改用排序形式并三连验稳。
- **`f004-transaction-atomicity.test.ts` 每跑一次泄漏 6 条孤儿 `OperationLog` + 3 条 `PendingAction`**，
  单调增长永不回收（前后快照机械证实，§5.3(b)）。**不构成 FAIL**（不在任何 acceptance 射程内），
  建议并入已登记的「dev 库跨批次孤儿行」进 backlog。

### 7.4 产物纪律自证

```
$ git status --porcelain
?? docs/test-reports/M5.1b-rv4-regression-L1.md
?? node_modules
$ find src -type f | sort | xargs cat | shasum -a 256
b1111ab59f917f7f056395e8cdb78198c78680d48097350964a8c2f5c1e1f69d   ← 与变异前基线一致
```
主仓 `src/` 未动一字节；三条变异全部反向编辑还原、逐位对账通过、**0 次 `git checkout`**；
临时探针（worktree 根 `.rv4-*.ts`，未入 `scripts/`）已删除；
未修改 `progress.json` / `features.json` / 规格 / 配置；未 push。
