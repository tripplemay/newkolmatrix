# M5.1b-TENANT-INJECTION · F001 补验报告

> **性质：** M5.1 遗留实现（三层 client + ALS 感知代理 + `withTenant`）的**独立验收**。
> **执行：** 隔离 Evaluator subagent（fresh context，独立 git worktree
> `/Users/yixingzhou/project/newkolmatrix/.claude/worktrees/agent-ae3996a2aa9b53981`）
> **被验树：** `8c5fee13311caad05cf0d8e666e26c8c1f815b1b`（批末，非 `e6ecef1` 快照 —— spec D-10 口径 1）
> **原 acceptance 来源：** `docs/specs/M5.1-F001-unverified.json`（`verified: false`，六条原文未重写）
> **日期：** 2026-08-08
> **判据纪律：** 本报告每条结论只依据源码、实跑输出、库里的行。
> `generator_handoff` / commit message / 代码注释一律作为**被验对象**，不作通过依据（铁律 13）。

---

## 总判：**PARTIAL**

| 面 | 结论 |
|---|---|
| 原 acceptance ①~⑥（M5.1 实现本体） | **六条全部 PASS**，每条附实跑输出或 `文件:行` |
| M5.1b F001 acceptance ⑤（F008 两条更正须核实） | **一真一假** —— `runtime.ts` 真；`tenant-scope.ts` **在批末已再次失实**，且其「已由机械钉守住」的自述经实测证伪 |

机制本体（代理三分支 / 三层分工 / raw 转发 / 开关关逐位不变）**成立且有机械证据**，两条规定变异均实跑证活。
判 PARTIAL 的唯一驱动是下面 **发现 1**：一条经证伪→更正→**在同一批次内再次失实**的产品代码注释，
正是 D-12 立项要消灭的缺陷类别；判 PASS 等于把一条已证实的假陈述洗白。

---

## 1. 环境与可复现性

| 项 | 值 |
|---|---|
| 工作副本 | 独立 worktree，`node_modules` 符号链接主仓，`.env` 复制自主仓 |
| 库 | 本机 dev Postgres `localhost:5434/kolmatrix`（L1；**未连任何真实外部服务、未部署、零计费、零生产写入**） |
| 开关默认态 | `.env` 与 `.env.example` **均未设** `DB_APP_ROLE_RUNTIME`（`grep` 零命中）→ 默认关 |
| 并发 | 另有 4 个 evaluator 在各自 worktree 并行、共用同一 dev 库；夹具按 pid 前缀隔离 |
| 冒烟 | `npx vitest run tests/unit/health.test.ts` → 1 file / 32 tests passed |

---

## 2. 原 acceptance 逐条判定

### ① 双态语义三分支各有正向用例 —— **PASS**

判据文件：`tests/integration/tenant-injection-proxy.test.ts`（11 条），实现：`src/lib/db/prisma.ts:27-47`。

```
$ npx vitest run tests/integration/tenant-injection-proxy.test.ts --reporter=verbose
 ✓ ①(a) … 代理与 tx 的 txid_current() 相同 = 同一个事务（机械判据，不接受「看代码」） 29ms
 ✓ ①(a) … typed 模型调用也落在同一事务：代理写入 tx 内可见、事务外（运行时连接）不可见 40ms
 ✓ ①(a) … 开关开（DB_APP_ROLE_RUNTIME=1）时 ALS 作用域内照常解析到 tx，不抛 16ms
 ✓ ①(b) … 代理背后是特权连接（与 rls-negative-suite §0 同口径），普通查询照常 5ms
 ✓ ①(c) … 模型 delegate 访问即抛，断言**类型**而非字符串 0ms
 ✓ ①(c) … raw 方法访问同样抛（$queryRawUnsafe 不是旁路） 0ms
 ✓ ①(c) … 开关拨回未设即恢复（解析看当下 env，不是模块加载快照） 1ms
 Test Files  1 passed (1)   Tests  11 passed (11)
```

- (a) 判据是 `txid_current()` 相等（`:108`）+ 未提交写入从运行时连接不可见（`:122`）——真库事实，非「看代码」。
- (b) `inspectConnectionRole(prisma)` 实测特权（`:147`）。
- (c) 断言 `toThrow(MissingTenantScopeError)` + `toBeInstanceOf`（`:155,163,169`）——**钉类型不钉字符串**，
  「返回了 client」与「抛了别的错」两种退化都不放过。鉴别力由变异 A 实跑证活（§3）。

> **登记的边界（不改判，见发现 2）：** (c) 的 fail-closed 只覆盖 **`get` trap**。
> 代理只装了 `get`，`has` / `ownKeys` / `getOwnPropertyDescriptor` 落到空 target `{}`。

### ② 开关关 = 逐位不变 —— **PASS**（按 spec D-10 口径 2 判「实质性质」）

**(a) 全量测试面：**

```
$ npx vitest run
 Test Files  148 passed (148)
      Tests  1869 passed (1869)     [exit 0]
```

**(b) 既有测试文件未被为迁就 RLS 而翻修**（基线 = M5.1 之前的 `b0b1120`）：

```
$ git diff --name-status b0b1120..HEAD -- tests/
A  tests/e2e/tenant-rls-min-loop.e2e.test.ts
A  tests/integration/als-context-propagation.test.ts
A  tests/integration/f004-transaction-atomicity.test.ts
A  tests/integration/tenant-injection-proxy.test.ts
A  tests/integration/tenant-scope-semantics.test.ts
M  tests/support/session-mock.ts
M  tests/unit/architecture-doc-freshness.test.ts
A  tests/unit/bootstrap-whitelist-census.test.ts
A  tests/unit/db-layer-importer-census.test.ts
```

- **零删除、零重写**：7 个新增，2 个修改。
- `tests/unit/architecture-doc-freshness.test.ts`：`git diff --numstat` = **179 增 / 0 删**，纯追加。
- `tests/support/session-mock.ts`：新增**可选**字段 `resolve?`（`:46`）；三个导出函数只在
  `resolve` 返回 `undefined` 时回落真实现。实测**全仓只有 1 个消费者用了它**
  （`grep -rn "resolve:" tests/` → 仅 `tests/integration/als-context-propagation.test.ts:94`，
  判据活性已证：同一 grep 能看见定义处 `tests/support/session-mock.ts:46`），
  4 个既有消费者（`materials-upload` / `m45-g4-f004-planack.probe` / `m45-g5-f006-f007.probe` /
  `rls-negative-suite`）行为逐位不变。
- 文件数对账：`git ls-tree -r b0b1120` = **142** → `HEAD` = **149**（+7，零删除）；
  其中 1 个落在 `tests/e2e/`（`vitest.config.ts:42` 的 include 不含该目录）→ 默认收集面
  `142 + 6 = 148`，与实跑 148 **逐位对得上**。

**(c) 原字面（diff 不含 `tests/` 与除 `src/lib/db/` 外的 `src/`）在 F001 那次交付上仍成立：**

```
$ git show --stat e6ecef1
 src/lib/db/prisma.ts | 73 +++--   src/lib/db/privileged.ts | 33 ++
 src/lib/db/runtime.ts | 61 ++     src/lib/db/tenant-scope.ts | 75 ++
 tests/integration/tenant-injection-proxy.test.ts | 258 ++
 docs/test-reports/generator-handoff-…json | 24 ++
```
`src/` 触及面 = 仅 `src/lib/db/`。`tests/` 下 1 个**新增**文件（spec-lock review §「`tests/` 新文件不算越界的理由」已裁）。

**(d) 批末的 `src/` 导入面变动（D-10 口径 1 判为合法演进，实测登记）：**

```
$ git grep -l "db/prisma" b0b1120 -- src | wc -l   → 73
$ git grep -l "db/prisma" HEAD     -- src | wc -l   → 70
基线有 / HEAD 无（6 个，全部是 F003 的引导白名单 + 哨兵改道）：
  src/app/api/auth/[...nextauth]/route.ts · src/app/api/auth/register/route.ts
  src/instrumentation.ts · src/lib/agent/context.ts · src/lib/auth/index.ts · src/lib/auth/register.ts
HEAD 有 / 基线无（3 个，全部是新 db 层文件）：
  src/lib/db/runtime.ts · src/lib/db/tenant-entry.ts · src/lib/db/tenant-scope.ts
```
逐条与 spec D-13 更正段 / F003 白名单 as-built 对得上，无一处是「为迁就 RLS 而改的业务调用点」。

### ③ privilegedDb 与运行时 client：两实例、连接串来源不同 —— **PASS**

用例：`tenant-injection-proxy.test.ts:184-211`（两条，均绿）。
另经 Evaluator 独立探针实测（作者未采用的口径：同时问 `current_user` 与 `current_database`）：

```
[P4]  runtime= { u: 'kol_app', d: 'kolmatrix' } | privileged= { u: 'kol', d: 'kolmatrix' }
[P4b] 开关开→关 拨回同一进程：on= kol_app  off= kol（两个不同实例，getRuntimeDb() 缓存键是连接串）
```
同库、不同角色 → 「两个实例 + 连接串来源不同」为真库事实。鉴别力由变异 P5 证活（§4）。

### ④ raw 转发落在 ALS 事务上 —— **PASS**

用例：`tenant-injection-proxy.test.ts:217-257`（回滚路径 + 提交路径，均绿）。
回滚路径的判据链完整：经代理 `$executeRawUnsafe` 写 → 经代理 `$queryRawUnsafe` 读 `count=1`
→ 运行时连接读 `count=0`（未提交）→ 抛错回滚 → 特权连接复查 `count=0`。
鉴别力由变异 B 实跑证活（§3）。

> **登记：** 同组的「提交路径」那条在变异 B 下**仍绿**（raw 改走运行时连接后写入照样自动提交、事务外照样可见）。
> 即该条只证「写得进去」，不证「落在同一事务」。spec D-11 点名要红的是「事务外不可见 / 回滚零残留」那条 —— 它红了。

### ⑤ 变异证活 —— **PASS**（见 §3 全文）

### ⑥ tsc / lint 绿 —— **PASS**

```
$ npx tsc --noEmit          → TSC_EXIT=0
$ npx next lint             → LINT_EXIT=0 ; "✔ No ESLint warnings or errors"
```

---

## 3. 规定动作：两条变异实跑（spec D-11 硬门 / M5.1b F001 acceptance ③）

**还原纪律：反向编辑 + `shasum -a 256` 逐位对账，全程未用 `git checkout`。主仓 `src/` 一个字节未动。**

基线（变异前）：
```
e05eec50…a9b  src/lib/db/prisma.ts
744e4bf8…9dd  src/lib/db/privileged.ts
```

### 变异 A —— 代理 (c) 分支的抛错改成「回落运行时 client」

改动（worktree 内 `src/lib/db/prisma.ts:30`）：
`if (isAppRoleRuntimeEnabled()) throw new MissingTenantScopeError();`
→ `if (isAppRoleRuntimeEnabled()) return getRuntimeDb();`

```
 Tests  3 failed | 8 passed (11)        [exit 1]
 × ①(c) 模型 delegate 访问即抛，断言**类型**而非字符串
     AssertionError: expected function to throw an error, but it didn't   (:155)
 × ①(c) raw 方法访问同样抛（$queryRawUnsafe 不是旁路）                      (:169)
 × ①(c) 开关拨回未设即恢复（解析看当下 env，不是模块加载快照）              (:174)
```
**结论：①(c) 的三条用例确有鉴别力。** 还原后 `shasum` = `e05eec50…a9b`（与基线逐位相同），`git status --short src/` 为空。

### 变异 B —— 摘掉代理对 raw 方法的转发（raw 恒走运行时 client）

改动（`src/lib/db/prisma.ts` 的 `get` trap 前置分支）：`$queryRawUnsafe` / `$executeRawUnsafe` 直接绑到 `getRuntimeDb()`。

```
 Tests  4 failed | 7 passed (11)        [exit 1]
 × ④ 回滚路径：事务内 raw 写 → 同事务 raw 读可见；事务外不可见；回滚零残留
     AssertionError: expected AssertionError: expected 1 to be +0 … to be Error: rollback-marker   (:238)
     （回滚后 Tenant 行仍在 → 那条 INSERT 落在事务外自动提交了）
 × ①(a) 代理与 tx 的 txid_current() 相同   expected '722729' to be '722728'   (:108)
 × ①(a) 开关开时 ALS 作用域内照常解析到 tx  expected '722732' to be '722731'   (:138)
 × ①(c) raw 方法访问同样抛                                                    (:169)
```
**结论：spec D-11 点名的「④ 事务外不可见 / 回滚零残留」确实红，且另有 3 条一并红。**
还原后 `shasum` = `e05eec50…a9b`，重跑 `11 passed`。

### 变异残留清理自证

变异 B 期间 raw 写入会绕过事务自动提交（用例第二层前缀普查正是为此而设）。变异结束后独立探针直查：

```
$ node --env-file=.env --import tsx scripts/test/eval-f001-stray-probe.ts
STRAY_COUNT= 0
[]      -- SELECT id, slug FROM "Tenant" WHERE slug LIKE 'f001proxy-%'
```

---

## 4. 主动探查：作者未采用的写法（M5.1b F001 acceptance ④）

共 5 组，其中 2 组是**产品代码变异**、3 组是**访问形态探针**。
探针源码：worktree `tests/integration/m51b-f001-evaluator-probe.test.ts`（9 条，全绿 —— 它记录**实际行为**，不预设应然）。

| # | 攻击面 | 实测结果 | 判 |
|---|---|---|---|
| **P1** | 非 `get` 形态：`'tenant' in prisma` / `Object.keys(prisma)` / `{...prisma}` / `getOwnPropertyDescriptor` —— 开关开且无 ALS | **均不抛**，分别得 `false` / `[]` / `{}` / `undefined` | **抓到缺口** → 发现 2 |
| P1' | 对照：`Reflect.get(prisma,'tenant')` / 解构 `const {tenant}=prisma` / `Symbol.for('nodejs.util.inspect.custom')` | 三者**均抛** `MissingTenantScopeError` | 代理挡得住 |
| **P2** | 绕过代理：新建 `src/lib/db/eval-p2-bypass.ts` 直接 `import { getRuntimeDb }` + `privilegedDb` | **两道钉同时红并点名文件**：`db-layer-importer-census`「多出（未登记的 importer）：src/lib/db/eval-p2-bypass.ts」；`bootstrap-whitelist-census`「发现**未登记**的特权连接使用点：src/lib/db/eval-p2-bypass.ts」 | 未抓到缺口（防线成立） |
| **P3** | ALS 作用域内再取 `$transaction`（`prisma.ts:10` 声称「运行期当场暴露」） | `typeof prisma.$transaction = undefined`；调用得 `TypeError: …$transaction is not a function`；外层 `txid=725276`，**未开出第二事务** | 该注释属实 |
| **P5** | 变异：`privilegedDb` 改读 `DATABASE_URL_APP`（后门降级为 kol_app） | ③ 两条 + ④ 提交路径共 **3 条红** | 未抓到缺口（③ 有鉴别力） |

P2 的钉红出后已 `rm` 该文件并复核 `git status --short src/` 为空；P5 已反向编辑还原，`shasum` = `744e4bf8…9dd`。

---

## 5. 发现

### 发现 1（驱动 PARTIAL）—— `tenant-scope.ts:12-22` 的「更正后」注释在批末**再次失实**，且其「已由机械钉守住」的自述为假

M5.1b F001 acceptance ⑤ 明写：「改后的表述**自己再 grep 核一遍**是否属实（不得只看它改了就算数）」。逐条核后：

| 位置 | 现文（批末） | 实测 | 判 |
|---|---|---|---|
| `src/lib/db/tenant-scope.ts:13` | 「本模块此刻只交付**单层**语义」 | **假** —— 同文件 `:143-168` 已含嵌套语义：`CrossTenantNestingError`（`:153-155`）、同租户复用外层 tx（`:162`）、`NestedTransactionOptionsError`（`:150`） | FAIL |
| `src/lib/db/tenant-scope.ts:13-14` | 「**产品代码（src/）零 withTenant 调用点**」 | **假** —— 实测 **15 处**：`build-plans.ts:221` `approve-plan.ts:75` `set-goal.ts:81` `ingest.ts:50` `create.ts:65` `gate.ts:250,365,433` `parse.ts:347` `register.ts:97,204,290` `tenant-entry.ts:48` `scheduler.ts:94` `env-advance.ts:130` | FAIL |
| `src/lib/db/tenant-scope.ts:15-16` | 「在 F002 落地前**不要嵌套调用** withTenant：现在没有嵌套守卫」 | **假** —— 守卫在 `:148-163`；且 `tenant-entry.ts:24-27` 明写嵌套已是预期形态 | FAIL |
| `src/lib/db/tenant-scope.ts:22` | 「本段的 importer / 调用点类陈述现由 `tests/unit/db-layer-importer-census.test.ts` **机械守住**」 | **假** —— 该钉的 `GUARDED`（`:42-69`）只有两个条目：`src/lib/db/runtime` 与 `src/lib/db/privileged` 的 **importer 清单**，**不含 tenant-scope、不含任何 withTenant 调用点判据**。机械反证：`src/` 从 0 变成 15 处调用点，而该钉全程绿（全量 148 files 全绿） | FAIL |

**为什么这条要紧：** D-12 立本项修复的理由正是「注释与实物不符」。更正后的文本在**同一批次内**因 F002/F004/F005 的落地而再次变假，而 `:22` 那句「已机械守住」把「不会再漂」的错觉写进了源码——恰恰是它没被任何钉守住。这不是措辞瑕疵，是**同一缺陷类别的复发 + 一条对防线覆盖面的失实声明**。

判据活性证明（`audit-methodology.md` §8）：同一套 grep 判据能看见目标 —— `grep -rn "withTenant(" src`
既命中 15 处真实调用点，也命中定义处 `tenant-scope.ts:143`；`grep -rn "tenant-scope" tests/unit/` 只回
`db-layer-importer-census.test.ts:47,52`（作为 runtime 的 **importer 条目**，非被守护模块）与两条注释引用。

**附带同类（同一处置批次一并修更省事，不单独计分）：**
`src/lib/db/prisma.ts:35`「全部既有调用点（**73 个 src 文件** / 206 处模型调用 / **15 处 `$transaction`**）经此代理」
—— 批末实测：`grep -rl "db/prisma" src | wc -l` = **70**；`src/` 下真实 `$transaction(` 调用点仅剩 **2 处**
（`lib/auth/register.ts:110` 走注入进来的 `db`，属引导面；`lib/db/tenant-scope.ts:164` 是 `withTenant` 自己）。
所述**性质**（既有调用点经代理）仍成立，三个**数字**已陈旧。

### 发现 2（登记，不驱动判定）—— 代理只装 `get` trap，非 `get` 形态在开关开时是 fail-**open**

`src/lib/db/prisma.ts:38-47` 的 `new Proxy({} as PrismaClient, { get })` 只有 `get` 陷阱，
target 是空对象 `{}`。开关开 + 无 ALS 作用域时实测：

```
[P1-in]   threw= null result= false          # 'tenant' in prisma
[P1-keys] threw= null keys= [] spreadKeys= [] # Object.keys(prisma) / {...prisma}
[P1-desc] threw= null desc= undefined         # getOwnPropertyDescriptor(prisma,'tenant')
```

即：这几种形态**不抛 `MissingTenantScopeError`，而是静默给出「什么都没有」** —— 与 (c) 分支
立誓要消灭的「静默零行」是同一种失败形状，只是走的不是 `get`。

**当前不可达**（已 grep 证伪存在性，判据活性由 P1 探针的正向命中反证）：
`grep -rn "\.\.\.prisma\b" src` / `grep -rnE "in prisma\b" src` /
`grep -rnE "Object\.(keys|entries|assign)\(\s*prisma" src` **三条均零命中**。
故**不改判 ①**，但 M5.2 把 `prisma` 代理往全站入口铺开时，任何第三方库对 client 做
`Object.keys` / 展开 / `in` 探测（序列化、日志、DI 容器很常见）都会落进这条缝。
建议补 `has` / `ownKeys` / `getOwnPropertyDescriptor` 三个陷阱同样过 `resolveDelegate()`。

### 发现 3（登记，不驱动判定）—— ④ 的「提交路径」用例无鉴别力

见 §2 ④ 的登记：变异 B 下该条仍绿。它证「写得进去」，不证「落在同一事务」。
建议加一条「提交前从运行时连接看不见 / 提交后看得见」的配对断言，或直接比 `txid_current()`。

### 发现 4（登记，不驱动判定）—— `runtime.ts:21-25` 更正记录的行号已漂

该段（历史性更正记录）写「`src/instrumentation.ts` 动态 import 的是 `./lib/db/prisma` 与 `./lib/db/app-role`
（**见该文件 :14-15**）」。批末 `src/instrumentation.ts:14-15` 是注释行，真实的动态 import 在 `:22-25`，
且其中一条已由 F003 改成 `import('./lib/db/runtime')`。段落作为历史记录本身无害，行号指错。

---

## 6. F008 更正核实结论（M5.1b F001 acceptance ⑤）

| 更正项 | 批末实测 | 判 |
|---|---|---|
| `src/lib/db/runtime.ts:8-16` 的 importer 清单 | **属实**。`grep -rn "db/runtime" src` + `grep -rn "from './runtime'" src` 合并判据（能看见目标：3 处真实 + 1 处注释）→ `src/` 内 importer 恰三处：`src/lib/db/prisma.ts:23` · `src/lib/db/tenant-scope.ts:26` · `src/instrumentation.ts:23`（动态 import），与文件头清单**逐条相同** | **PASS** |
| `src/lib/db/tenant-scope.ts:13-14`（及同段 `:15-16`、`:22`） | **不属实**，见发现 1 | **FAIL** |
| 「importer 清单升级为机械钉」 | `tests/unit/db-layer-importer-census.test.ts` **确为真钉**：P2 实测新增 importer 即红并点名；自带活性证明（`:182-225`：剥注释、认三种 import 语法、幻影块注释回归、路径归一）。但其射程**只覆盖 runtime / privileged 的 importer 面**，不覆盖 `tenant-scope.ts:13-22` 的调用点类陈述 | **部分成立** |

---

## 7. 盲区登记（如实写，不为好看回避）

1. **未跑 M5.1 之前的树做逐位基线对比。** acceptance ② 的「逐位不变」我用的是**间接判据**（既有测试零删除零弱化 +
   全量 148/1869 绿 + 文件数 142→149 精确对账 + 开关默认关）。真正的「同一份用例在旧树与新树上输出逐位相同」
   需要 checkout `b0b1120` 另建 worktree 跑一遍——那是对共享 checkout 的 git 操作，本次未做。
2. **P1 缺口的可利用性只证到「本仓 `src/` 当前无此写法」**，未审 `node_modules` 中会不会有库对传入的
   PrismaClient 做 `Object.keys` / 展开（例如日志/序列化中间件）。开关未开时该路径不可达，故未继续挖。
3. **未覆盖 `$queryRaw` / `$executeRaw`（标签模板版）的转发用例。** 代理是通用转发，静态上必然覆盖，
   但**没有实跑用例**钉住它 —— 属「读代码推断」，按本项目口径不算证活，如实登记为未验。
4. **并发干扰未观察到。** 本次所有失败均为我主动施加的变异所致，无一条是「观察到但未复现」的偶发红；
   变异还原后逐次重跑均绿（11 passed / 34 passed / 148 files）。
5. **L2 未授权项：无。** F001 六条 acceptance 全部可在 L1（本机 dev 库 + 进程内）判定，
   无一条需要真实外部服务 / 部署 / 计费 / 生产写入，故**不存在因 L2 未授权而 PARTIAL 的条目**。

---

## 8. 附：Evaluator 产出的测试工件

两件工件留在隔离 worktree（未进主仓，避免并行 evaluator 的全量文件数对账被搅动）：

| 路径（worktree 内） | 用途 |
|---|---|
| `tests/integration/m51b-f001-evaluator-probe.test.ts` | §4 的主动探查探针（P1 / P1' / P3 / P4，9 条） |
| `scripts/test/eval-f001-stray-probe.ts` | §3 变异残留直查（`Tenant WHERE slug LIKE 'f001proxy-%'`） |

**产品代码零改动自证：** 全部变异均以反向编辑还原，收尾
`shasum -a 256 src/lib/db/{prisma,tenant-scope,runtime,privileged}.ts tests/integration/tenant-injection-proxy.test.ts`
与变异前基线文件 `diff` **完全一致**；`git status --short` 仅剩上表两件未跟踪工件与 `node_modules` 符号链接。

---

## 9. 建议的处置（供 Planner / Generator，非 Evaluator 越权指令）

1. **必修（发现 1）：** 重写 `src/lib/db/tenant-scope.ts:12-22` 使之与批末实物相符
   （嵌套语义已交付 / `src/` 15 处调用点 / 守卫已在）；`:22` 那句「已由 db-layer-importer-census 机械守住」
   要么删掉，要么**真的加一条钉**去守调用点类陈述。顺手把 `src/lib/db/prisma.ts:35` 的三个数字改成
   批末实测值或改为不写死数字。
2. **建议（发现 2）：** 给代理补 `has` / `ownKeys` / `getOwnPropertyDescriptor` 三个陷阱，
   在 M5.2 铺开前把这条 fail-open 缝关掉。
3. **建议（发现 3）：** 给 ④ 的提交路径用例补一条能被变异 B 打红的配对断言。
