# M5.1b-TENANT-INJECTION · 隔离验收 — F002 / F008

> **角色：** Evaluator（隔离上下文 subagent，fresh context）
> **日期：** 2026-08-08
> **被验 HEAD：** `8c5fee13311caad05cf0d8e666e26c8c1f815b1b`
> **工作副本：** `/Users/yixingzhou/project/newkolmatrix/.claude/worktrees/agent-a2cd52a9af0c5f424`
>（`node_modules` 符号链接主仓，`.env` 拷贝自主仓；主仓 `src/` 零改动）
> **L2 边界：** 未授权，未连真实外部服务 / 未部署 / 无计费。本机 dev Postgres（`localhost:5434`）与进程内测试属允许范围。
> **判据纪律：** commit 正文 / 代码注释 / `generator_handoff` 一律作为**被验对象的自述**，不作通过依据。
> 下文每条判定后面跟的是我自己实跑的命令输出或 `文件:行`。

---

## 总判

| feature | 判定 |
|---|---|
| **F002** withTenant 包装器 + B1-4 / B3 硬回归 | **PASS** |
| **F008** 更正两条失实注释 + 防再犯机械钉 | **PARTIAL** |

F008 的机械钉（acceptance ③④）与 runtime.ts 注释（①(b)）实测成立且变异证活通过；
**但 acceptance ①(a) 所改的那段注释在批末的树上重新变成假的，且 F008 自己新写的
「本段…现由机械钉守住」这句是无依据断言（钉根本不覆盖它）** —— 后者正是 acceptance ②
逐字禁止的形态（「改成另一句同样未核的话 = 未修复」）。

---

## 环境前置检查（L1，排除已知误报）

| 项 | 实测 |
|---|---|
| worktree 隔离 | `git rev-parse --show-toplevel` = worktree 自身路径，非主仓 |
| 依赖 | `node_modules` → 主仓符号链接（未 npm install） |
| 冒烟 | `npx vitest run tests/unit/health.test.ts` → 32 passed |
| DB | `.env` 含 `DATABASE_URL`（kol）与 `DATABASE_URL_APP`（kol_app），`DB_APP_ROLE_RUNTIME` **未设**（与 spec §7 一致） |
| 并发 | 另有 4 个 evaluator 共用同一 dev Postgres；本报告涉及的夹具全部按 `pid` 前缀隔离（`f002scope-${process.pid}`），未观察到任何跨实例串扰失败 |

---

# F002 — withTenant 包装器（PASS）

被验实物：`src/lib/db/tenant-scope.ts:143-168`（`withTenant` 本体）+
`tests/integration/tenant-scope-semantics.test.ts`（14 用例，`git show b3b15be --name-status` = `A`，
`git log --all -- <file>` 只有 b3b15be 一条 → **确为 F002 新增，非既有文件改名**）。

基线跑：
```
$ npx vitest run tests/integration/tenant-scope-semantics.test.ts
 Test Files  1 passed (1)
      Tests  14 passed (14)
```

## ① 三条硬语义各有用例

### ①(a) 事务内 SET LOCAL —— **PASS**

- 实现物：`src/lib/db/tenant-scope.ts:164-166`
  ```ts
  return getRuntimeDb().$transaction(async (tx) => {
    await tx.$queryRawUnsafe(`SELECT set_config('app.tenant_id', $1, true)`, tenantId);
    return tenantTxAls.run({ tenantId, tx }, () => fn(tx));
  }, txOptions);
  ```
  事务回调的**第一条语句**即 `set_config(..., true)`，第三参字面量 `true`。
- 用例：`tests/integration/tenant-scope-semantics.test.ts:124`（回调拿到控制权时变量**已经**设好
  → 证明 set_config 先于 fn）与 `:131`（同事务内读得到，且事务外 `txid_current()` 不同）。
- **判据是行为级而非源文匹配**：我判这比 grep 源码字面**更强**——它同时钉住「设了」「设在这个事务里」
  「设在 fn 之前」三件事；而「第三参是 `true`（is_local）」这一半由变异 M1（见 ④-1）机械钉住。

**§4 硬约束（不得以「F001 已实现 set_config」抵扣）—— 未违反：**
- F002 交出的是**新增文件 14 条新用例**（`git show b3b15be --stat`：`tests/integration/tenant-scope-semantics.test.ts | 323 +++++`，0 删除）；
- 交付叙述中**未出现**「已在 F001 落地，此处复用」类抵扣语（`git show b3b15be` 正文 + `progress.json.generator_handoff` 均实读）；
- 且第三参变异我自己实跑复现（④-1）。→ **无 VIOLATION**。

### ①(b) 同租户嵌套复用既有 tx，不开第二事务 —— **PASS**

- 实现物：`tenant-scope.ts:148-163`，同租户分支 `return fn(outer.tx);`（不开事务、不重设变量）。
- 机械判据（acceptance 明令不接受「看代码没开」）：`:150` 用例断言内外层 `txid_current()` **相同**；
  `:170` 断言 ALS 里的 `tx` 引用相同；`:161` 断言内层读到的仍是外层设的租户。
- 变异 M2 实跑证活（见 ④-2）：改成再开一个事务后，该用例报
  `AssertionError: expected '722674' to be '722673'` —— 事务号真的不同了，说明这条断言在测真东西。

### ①(c) 不同租户嵌套抛错 —— **PASS**

- 实现物：`tenant-scope.ts:153-155` 抛 `CrossTenantNestingError`（带 outer/inner tenantId）。
- 用例 `:186` 断言错误**类型**（不是字符串），`:195` 核 `outerTenantId` / `innerTenantId` 字段值。
- 变异 M3 实跑证活：摘掉抛错后报 `promise resolved "'should-not-reach'" instead of rejecting`
  —— 「静默沿用外层」这条退化路径被当场抓住。

## ② B1-4 原子性回归（预写死判据）—— **PASS**

- 用例 `:216`（单层）/`:232`（嵌套，两行都不许剩）/`:252`（错误对象 `toBe` 原样上抛）
  /`:262`（提交路径确实落库，防「压根没写成」的空绿）。
- **真查库计数**，且是从**特权连接**查（`:56 countTenantBySlug` 走 `privilegedDb.$queryRawUnsafe`，
  绕开一切事务与作用域）—— 满足「不接受『Prisma 没报错』当证据」。
- `:224` 还在同事务内先自证「确实写进去了」（`countTenantInTx(tx, slug) === 1`），
  这一条排掉了「计数 0 是因为压根没写」的伪绿。
- 变异 M2 下 ②(ii) 报 `AssertionError: expected 1 to be +0` —— **审计 B1-4 的原样复现**
  （内层独立提交，外层回滚带不走）。

## ③ B3 残留回归（预写死判据）—— **PASS**

- 用例 `:291`（withTenant 结束后连查 **12** 次 `current_setting('app.tenant_id', true)` 全为 null）、
  `:306`（**12 路并发** withTenant 各自只看得见自己的 id）、`:315`（并发后同样无残留）。
- 变异 M1 下前后两条同时红，报文
  `expected [ 'f002scope-74934-b3leak', …(11) ] to deeply equal []`
  —— **12 次全部读到泄漏值**。这同时证明了两件事：判据有鉴别力；且这 12 次查询确实落在同一条
  归还给池的连接上（否则不会 12/12 命中），即该断言不是「碰巧为 null」。

## ④ 变异证活四条 —— **PASS（四条我全部独立实跑，另加跑一条更危险的变体）**

还原一律用**反向字符串替换**（未用 `git checkout`），每次以 `shasum -a 256` 逐位对账。
`src/lib/db/tenant-scope.ts` 基线 sha = `6bec89fd3d3b2baad83e2f3de173dd23142cdb275e4d4ae0fb0dd0a5139fa85e`，
四次变异后均还原回同一 sha；结束时 `git status --short` 仅剩 `?? node_modules`（符号链接）。

| # | 变异内容 | 实跑结果（红了几条 / 红的是不是该红的那条） |
|---|---|---|
| M1 | `set_config` 第三参 `true` → `false` | **2 failed / 12 passed**，两条全在 `③ B3 残留回归`：`withTenant 结束后…读不到 app.tenant_id` 与 `并发结束后…不留残留`。**命中即 ③，无误伤。** |
| M2 | 同租户嵌套改成再开一个事务（`return fn(outer.tx)` → 新开 `$transaction` + 重设变量） | **3 failed / 11 passed**：`①(b)` 两条（`expected '722674' to be '722673'`、`expected false to be true`）+ **`②(ii)` `expected 1 to be +0`**。acceptance 要求的 ② 命中。 |
| M3 | 摘掉跨租户嵌套的抛错 | **2 failed / 12 passed**，两条全在 `①(c)`（`promise resolved "'should-not-reach'"`、`expected null to be an instance of CrossTenantNestingError`）。**命中即 ①(c)，无误伤。** |
| M4a | 吞掉错误传播（`$transaction` **外**层 try/catch 后 return） | **5 failed / 9 passed**：`②(i)(ii)(iii)` 全中 + `①(c)` 两条连带。acceptance 要求的 ② 命中。 |
| M4b（我额外加的更危险变体） | 在**事务回调内**吞错（→ 事务会**提交**，行真的落库） | **5 failed / 9 passed**，同上分布。②(i)(ii)(iii) 全中。 |

> M4b 是我自己补的：acceptance 只写「catch 后 return」，而「catch 在事务内」才是真正会把脏行提交的
> 那一种。两种写法测试都拦住了，说明 ② 不依赖「错误恰好穿透到调用方」这一条路径。

## ⑤ 清理段纪律 —— **PASS（有实测证据，不只读代码）**

- 代码面：`:84-117` afterAll 两层——第一层按登记 id 精确删、第二层**不从登记表派生**的全表前缀普查；
  **每一步各自独立 `try/catch` 且只 `console.warn`**（`:89`、`:101`、`:114`），清理段自身不再抛；
  清理键是 `slug LIKE 'f002scope-%'`（不依赖「被测行为正确」才存在的字段）。
- 实测面：M2 变异会让内层写**独立提交**成一条**没进过登记表**的脏行（那次 ②(ii) 实测到 `1`）。
  全部变异跑完并还原后我独立查库：
  ```
  SELECT slug FROM "Tenant" WHERE slug LIKE 'f002scope-%'  →  leftover f002scope rows: 0
  ```
  → 第二层普查确实兜住了第一层兜不住的那类残留。这是「两层不重叠」的实证，不是设计意图陈述。

## ⑥ tsc / lint —— **PASS**

```
$ npx tsc --noEmit           → 无输出，exit 0
$ npx next lint              → ✔ No ESLint warnings or errors
$ npx vitest run（全量）      → Test Files 148 passed (148) / Tests 1869 passed (1869)
```
（全量非 F002 acceptance 门槛，但我跑了，作为「变异全部还原干净」的旁证。）

### F002 结论：**PASS**，六项全中，四条变异 + 一条自加变体全部实跑红且红在正确的位置。

---

# F008 — 更正两条失实注释 + 防再犯机械钉（PARTIAL）

被验实物：`src/lib/db/tenant-scope.ts:12-22` · `src/lib/db/runtime.ts:8-29` ·
`tests/unit/db-layer-importer-census.test.ts`（`git show 6a120a7 --stat` 确认三者同批）。

## ①(b) runtime.ts importer 清单 —— **PASS（我自己 grep 核过，不采信注释自述）**

现文 `runtime.ts:8-16` 声明「src/ 下恰三处：① lib/db/prisma.ts ② lib/db/tenant-scope.ts
③ src/instrumentation.ts」。独立实测：
```
$ grep -rn "from './runtime'" src
src/lib/db/prisma.ts:23:import { getRuntimeDb } from './runtime';
src/lib/db/tenant-scope.ts:26:import { getRuntimeDb } from './runtime';
$ grep -rn "db/runtime" src
src/instrumentation.ts:23:      import('./lib/db/runtime'),
src/lib/db/prisma.ts:18:// …（注释行，非 import）
```
→ **实物恰 3 处，与注释逐条对上。** 注意其中 ③ 是 F003 后来接进去的：F008 落地当时（`6a120a7`）
清单写的是 2 处且期望集合也是 2 处（`git show 6a120a7:tests/unit/db-layer-importer-census.test.ts`
实读），F003 接线时被这道钉逼着同步改了清单与期望集合 —— 这是**钉真的在起作用**的旁证。

> 一处小瑕（登记，不单独扣分）：`runtime.ts:21-25` 的「更正记录」里
> 「`src/instrumentation.ts` 动态 import 的是 `./lib/db/prisma` 与 `./lib/db/app-role`（见该文件 :14-15），
> 并不 import 本模块」是**现在时叙述**，但批末该文件 `:23-24` 实为 `./lib/db/runtime` + `./lib/db/app-role`，
> 且 `:14-15` 已不是那两行。作为「历史更正记录」可读通，作为现在时事实陈述已失实。

## ③ 防再犯机械钉 —— **PASS**

`tests/unit/db-layer-importer-census.test.ts` 实读核对：
- 扫描用 `readFileSync` / `readdirSync` 递归遍历 `src/`（`:27`、`:73-83`），**全文件零 `git grep` / 零 `execSync`**；
- 报文点名文件（`:167-176` 的 `多出（未登记的 importer）` / `缺失`）；
- 自带活性证明用例（`:182-225`）：三种 import 语法认得出、注释与同名标识符不误判、
  行注释里的 `/*` 不会开幻影块注释吞掉后续 import、三种路径写法归一一致、作用域包不误判；
- 还有一条「扫描器不空转」的防呆（`:156`：`files.length > 50` 且必须含 `runtime.ts`）——
  满足 `audit-methodology.md` §8「0 findings 的判据必须先证明它看得见目标」。
- **与 F003 白名单钉不互相派生**（实读 `tests/unit/bootstrap-whitelist-census.test.ts`）：
  两文件各自 `readFileSync` 独立扫描、各自维护期望集合，互不 import；判据面不同
  （F008 扫 **import specifier**，F003 扫**标识符实际取用**），两者互补形态已写在
  `bootstrap-whitelist-census.test.ts:10-11`。`grep -n "db-layer-importer-census" tests/unit/*.ts`
  只命中注释引用，无代码级依赖。

## ④ 变异证活两条 —— **PASS（两条我独立实跑）**

| # | 变异内容 | 实跑结果 |
|---|---|---|
| a | 新增 `src/lib/db/__mut_probe_f008.ts`（`import { getRuntimeDb } from './runtime'`） | **1 failed / 3 passed**，红的是 `src/lib/db/runtime 的 importer 清单与实物一致`，报文点名：`多出（未登记的 importer）：src/lib/db/__mut_probe_f008.ts`。**红了且点名了。** |
| b | 从期望集合摘掉 `'src/lib/db/prisma.ts'` | **1 failed / 3 passed**，报文 `多出（未登记的 importer）：src/lib/db/prisma.ts`。 |

还原：a 用 `rm` 删探针文件（探针为我新建，非产品代码改写）；b 用反向字符串替换，
`tests/unit/db-layer-importer-census.test.ts` sha 还原回 `79197ecb0f87d07331788d333ef12244fe270337fe26d6d4dfc55eb411ea635d`（逐位一致）。
**未用 `git checkout`**。结束 `git status --short` 仅 `?? node_modules`。

## ⑤ tsc / lint / 全量 vitest —— **PASS**

同 F002 ⑥ 的三条输出：tsc exit 0 · lint 无告警 · **148 files / 1869 tests 全绿**。

## ①(a) 更正 tenant-scope.ts 的注释 —— **FAIL（批末的树上重新失实）**

现文 `src/lib/db/tenant-scope.ts:12-16`：
```
// 【嵌套语义归 F002】…都是 F002 的 acceptance。本模块此刻只交付**单层**语义——**产品代码
// （src/）零 withTenant 调用点**（15 处 $transaction 迁移是 F004），提前写嵌套分支 = 没有
// 测试锚的死代码。在 F002 落地前**不要嵌套调用** withTenant：现在没有嵌套守卫，嵌套会静默
// 开第二个事务（正是审计 B1-4 的病灶形态）。
```

在被验 HEAD 上，这段里有**三条事实性断言同时为假**：

1. **「产品代码（src/）零 withTenant 调用点」—— 假。** 实测 **15 处调用点 / 11 个文件**：
   ```
   $ grep -rn "withTenant(" src | grep -v "^src/lib/db/tenant-scope.ts" | wc -l
         15
   ```
   命中文件：`lib/projects/create.ts:65` · `lib/match/approve-plan.ts:75` · `lib/match/build-plans.ts:221` ·
   `lib/projects/set-goal.ts:81` · `lib/signals/ingest.ts:50` · `lib/agent/gate/gate.ts:250,365,433` ·
   `lib/delivery/register.ts:97,204,290` · `lib/knowledge/parse.ts:347` · `lib/db/tenant-entry.ts:48` ·
   `lib/jobs/scheduler.ts:94` · `lib/domain/env-advance.ts:130`。
2. **「本模块此刻只交付**单层**语义」—— 假。** 嵌套语义就在**同一个文件** `:148-163`
   （同租户复用 `return fn(outer.tx)`、跨租户 `throw new CrossTenantNestingError`）。
   我的变异 M2 / M3 已实跑证明这两条分支是活的。
3. **「在 F002 落地前不要嵌套调用 withTenant：现在没有嵌套守卫，嵌套会静默开第二个事务」—— 假。**
   F002 已落地（`b3b15be`），守卫存在且被 14 条用例钉住。

**归因如实登记：** F008 落地当时（`6a120a7`，2026-08-06 22:52）这段是**成立的**——彼时 `src/` 确实零调用点。
它是被同批后续的 F002（`b3b15be`，加嵌套语义，改的就是这个文件）与 F004（`85e0362`，迁 13 处调用点）
带假的，而两者都没回来同步这段注释。按 spec D-10「验的是批末的树」的口径，**交付到验收台上的实物里，
F008 被指派去修的那段注释是假的** —— 这正是 F008 存在的理由（「注释与实物不符」）在同一批次内原地复发。

## ② 「改完自己 grep 核一遍」—— **FAIL（新写了一句无依据且为假的断言）**

F008 在同一段末尾新增了这一行（`src/lib/db/tenant-scope.ts:22`，`git show 6a120a7` 确认是本 feature 新写）：
```
// > 本段的 importer / 调用点类陈述现由 tests/unit/db-layer-importer-census.test.ts 机械守住。
```

**这句是假的，两半都假：**

- 那道钉的守护面写死在 `tests/unit/db-layer-importer-census.test.ts:41-70` 的 `GUARDED` 数组里，
  只有两项：`src/lib/db/runtime` 与 `src/lib/db/privileged`。**没有 `tenant-scope` 这一项**，
  全文件也**没有任何一条关于 withTenant 调用点数量的断言**（`grep -n "withTenant" tests/unit/db-layer-importer-census.test.ts`
  只命中 `:22`、`:201` 两处——都是「同名标识符不得误判」的假阳性说明，与调用点计数无关）。
- **机械证明（不靠读代码推断）：** 在同一个 HEAD 上，
  「产品代码零 withTenant 调用点」这句已被 15 个反例证伪，而那道钉
  `npx vitest run tests/unit/db-layer-importer-census.test.ts` → **4 passed**，
  全量 `npx vitest run` → **1869 passed / 0 failed**。
  **一句为假的陈述 + 一个全绿的仓 = 全仓没有任何一条断言在守它。**

acceptance ② 的原文是「**新写的每一句事实性断言都要有对应的一条命令输出作依据**…
改成另一句同样未核的话 = 未修复」。这一句正是那种话——而且它是**「已被守住」型**断言，
比普通失实更坏：它让后来人以为这段的漂移已经有机械兜底，从而不再人工核。
（本批的实际结局恰好演示了这一点：F002 / F004 改完之后没人回来同步这段。）

> 附：`git show 6a120a7` 正文列的四条自核 grep（runtime importer 恰 2 处 / instrumentation 不 import runtime /
> 产品代码 withTenant 零命中 / 测试侧 5 处）我逐条比对过，**那四条在 F008 落地当时确实成立**。
> 缺的不是这四条，缺的是「本段现由 X 机械守住」这第五句 —— 它从未被任何命令核过，也核不出来。

### F008 结论：**PARTIAL**

- 成立：①(b) runtime.ts 清单（我独立 grep 核过，批末仍准）· ③ 机械钉（口径、活性证明、与 F003 不派生）
  · ④ 两条变异实跑红且点名 · ⑤ L1 全绿。
- 不成立：①(a) 被指派更正的那段注释**在批末的树上是假的**（三条断言同时为假）；
  ② 新写了一句**无依据且为假**的「已被机械守住」断言。

**修复建议（供 Generator，非我执行）：** 两条要一起做，只改文字会再漂一次——
(1) 把 `tenant-scope.ts:12-16` 按批末实物重写（嵌套语义已交付；`src/` 有 15 处调用点，清单可由钉产出）；
(2) 要么把 `:22` 那句删掉/改成如实的「本段无机械守护」，要么**真的**给这道钉加一条守
`src/lib/db/tenant-scope` 的 importer/调用点条目（`GUARDED` 加一项即可，且必须配变异证活）。
后者才是把 ② 从「自觉」变成「机械」的那一步。

---

## 观察到但未复现 / 未判定的项

| 项 | 说明 |
|---|---|
| 跨实例串扰 | 全程未观察到与本 feature 无关的失败。全量 `vitest run` 一次通过（148/148），无需重跑确认。 |
| `test:visual` | 未跑。spec §7 已登记本机恒 15 passed / 2 failed 属 `BL-VISUAL-DATA-ISOLATION`，不计分；且本批零 UI 变更。 |
| `npm run rls:e2e`（开关开、连 kol_app、RLS 真生效） | **未跑**——它属 F006 的验收面，不在 F002 / F008 的 acceptance 里；且与其它并行 evaluator 共用同一 dev 库时开关开的进程级 env 翻转有互扰风险。**登记为未执行，不计入本次两条 feature 的判定。** |

## 盲区（如实登记）

1. **F002 的全部实测都在 `DB_APP_ROLE_RUNTIME` 关（= 特权连接）下完成**——这是 spec §7 / 测试文件
   `:40-45` 明定的口径。因此本次证的是「withTenant 的**事务语义**正确」，**没有**证「开关开、
   连 `kol_app`、RLS policy 真拦截时它同样正确」。后者归 F006。
2. **连接池行为**：③ 的 12 次事务后查询能抓到 M1 泄漏（12/12），说明这些查询确实复用了同一条连接；
   但本机 dev 的池规模与生产不同，**「池更大时是否仍必现」未测**。这不削弱变异的鉴别力
   （必现即可证活），但不足以外推到生产池配置。
3. **`NestedTransactionOptionsError`（`tenant-scope.ts:132-141,150`）无任何测试覆盖**——
   `grep -rn "NestedTransactionOptionsError" tests` **零命中**。它属 F004 引入，不在 F002 / F008 的
   acceptance 内，故不计入本次判定；但它是一条**会抛的产品路径且零用例**，且 `generator_handoff`
   自己点名「M5.2 入口面一包就会变成嵌套传选项 → 必抛」。**登记，供 Planner 排期。**
4. F002 的 ①(a) 我采信了**行为级**断言（回调入口处变量已设 + M1 变异）而非源文匹配 `set_config` 字面。
   若后人把实现改成「在 `$transaction` 外、`BEGIN` 之前用 `set_config(..., false)` 设」，
   ①(a) 的两条会绿而 ③ 会红——**组合起来仍拦得住**，但单看 ①(a) 拦不住。已核，非缺陷，登记口径。

---

## 附：本次实跑过的命令（举证面）

`git rev-parse --show-toplevel` / `git rev-parse HEAD` ·
`npx vitest run tests/unit/health.test.ts` ·
`npx vitest run tests/integration/tenant-scope-semantics.test.ts`（基线 + 5 次变异 + 还原后复跑）·
`npx vitest run tests/unit/db-layer-importer-census.test.ts`（基线 + 2 次变异 + 还原后复跑）·
`npx vitest run`（全量）· `npx tsc --noEmit` · `npx next lint` ·
`grep -rn "withTenant(" src` · `grep -rn "db/runtime" src` · `grep -rn "from './runtime'" src` ·
`grep -rn "db/tenant-scope" src` · `grep -rn "NestedTransactionOptionsError" tests src` ·
`grep -n "withTenant" tests/unit/db-layer-importer-census.test.ts` ·
`git show 6a120a7 --stat` / `git show 6a120a7 -- src/lib/db/{tenant-scope,runtime}.ts` ·
`git show 6a120a7:tests/unit/db-layer-importer-census.test.ts` ·
`git show b3b15be --name-status` · `git log --all -- tests/integration/tenant-scope-semantics.test.ts` ·
`shasum -a 256 src/lib/db/tenant-scope.ts`（每次变异前后）·
`shasum -a 256 tests/unit/db-layer-importer-census.test.ts`（变异前后）·
`git status --short`（每次还原后）·
`SELECT slug FROM "Tenant" WHERE slug LIKE 'f002scope-%'`（夹具残留核查）

**主仓 `src/` 全程零改动**（所有变异只发生在本 worktree，且已逐位还原）。
