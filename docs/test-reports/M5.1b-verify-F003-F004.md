# M5.1b-TENANT-INJECTION — F003 / F004 独立验收报告

| 项 | 值 |
|---|---|
| 批次 | M5.1b-TENANT-INJECTION |
| 被验 HEAD | `8c5fee13311caad05cf0d8e666e26c8c1f815b1b` |
| 验收范围 | **F003**（引导白名单 + 源码级普查钉）· **F004**（`$transaction` 迁移到 `withTenant`） |
| 执行者 | 隔离 evaluator subagent（独立 worktree `.claude/worktrees/agent-acba4bd132bf1395c`） |
| 日期 | 2026-08-08 |
| **结论** | **F003 = PARTIAL** · **F004 = PARTIAL** |

> 判据纪律：commit message / 代码注释 / `generator_handoff` 一律作为**被验对象**处理，不作通过依据（铁律 13）。
> 下列每条判定后面跟的是我自己跑出来的命令输出或 `文件:行`。

---

## 0. 环境与并发说明

- worktree 内 `node_modules` 符号链接主仓，`.env` 复制自主仓（含 `DATABASE_URL` / `DATABASE_URL_APP`）。
- 冒烟：`npx vitest run tests/unit/health.test.ts` → 1 file / 32 tests passed。
- 另有 4 个 evaluator 并行共用同一本机 dev Postgres。**本轮未观察到任何与 F003/F004 无关的失败**，
  因此无「观察到但未复现」条目。
- **L2 边界申报**：acceptance F004-④ 强制要求跑 `npm run reach:e2e`，该脚本的 `draft_email` 段
  **走真网关**，本次实测产生 1 次 chat 调用（脚本自报 `model=deepseek-v3 in=282 out=139 est=~$0.000126`）。
  真实投递 0 封、真实资金动作 0、生产写入 0、未触发部署。除此之外无任何外部服务调用。

---

## 1. 我自己重跑的两份普查（不采信被验方登记的数字）

### 1.1 引导点普查（F003 acceptance ①）

判据自定 = 「在 `withTenant` 作用域不可能存在时执行的 DB 访问」，落成机械模式：
`<ident>.<model>.<op>(` / `$transaction(` / `runConnectionRoleSentinel(`，扫描面 = 认证面 +
slug 解析 + 启动哨兵，剥行注释。**先证活**（`audit-methodology.md` §8）：模式必须先抓到
母 spec D-5 表第 1 行的已知形态 `prisma.user.findUnique({where:{email}})` —— 实测抓到，且对
纯散文提及 `// 15 处 $transaction 迁移是 F004` 不误判。

在**改动前的树** `ee494be`（F002 完成、F003 未开工）上实测：

```
--- src/instrumentation.ts        17: await runConnectionRoleSentinel(prisma);
--- src/lib/agent/context.ts      31: const tenant = await prisma.tenant.findUnique({ where: { slug } });
--- src/lib/auth/audit.ts        108: const row = await db.tenant.upsert({
                                 138: await db.operationLog.create({
--- src/lib/auth/index.ts         25: return prisma.user.findUnique({
                                  61: const target = await prisma.user.findUnique({
--- src/lib/auth/register.ts     107: return await db.$transaction(async (tx) => {  (+ 内层 tx.* 3 处)
TOTAL sites = 10 | files = 5
```

我的模式**看不见**两个路由文件的留痕（那里的 DB 访问是「把 client 传给 `writeAuthAudit`」的间接形态）。
逐文件复核补上：

```
git show ee494be:src/app/api/auth/register/route.ts      → :18 import { prisma } … :42 writeAuthAudit(… :48 prisma,
git show "ee494be:src/app/api/auth/[...nextauth]/route.ts" → :21 import { prisma } … :55 writeAuthAudit(… :57 prisma,
```

**逐条核对 spec D-13 对母 spec D-5 的三处更正 —— 三条全部独立复现，无一虚记：**

| D-13 更正 | 我的实测 | 判定 |
|---|---|---|
| ① 母表漏列 `api/auth/register/route.ts` 与 `api/auth/[...nextauth]/route.ts` | 上面两行 `git show` 实证：改动前两文件确实各持一份 client 做限速留痕，且在解析入参 / 建立会话之前 | **属实** |
| ② 母表把 `src/instrumentation.ts` 判为「应走 privilegedDb」有误 | 我自建探针（`DB_APP_ROLE_RUNTIME=1`）三分支实跑：<br>`传 prisma 代理 → {"info":null,"enforced":true,"status":"unknown"}`<br>`传 getRuntimeDb() → {"info":{"role":"kol_app","superuser":false,"bypassRls":false},"enforced":true,"status":"ok"}`<br>`传 privilegedDb → THREW PrivilegedConnectionError（角色 kol，rolsuper=true）` | **属实**（安全装置静默失效可复现；移出白名单的判断成立） |
| ③ 行号漂移：`writeAuthAudit` 实为 `index.ts:51,70`；注册注入缝实为 `register.ts:99` | `git show ee494be:src/lib/auth/index.ts \| grep -n writeAuthAudit` → `51` / `70`；<br>`git show ee494be:src/lib/auth/register.ts \| grep -n 'deps.db ?? defaultPrisma'` → `99` | **属实** |

**as-built 白名单实测（HEAD）** —— `grep -rl privilegedDb src/` 命中 10 文件，剔除仅注释/字符串提及的
4 个（`instrumentation.ts` / `db/runtime.ts` / `db/tenant-scope.ts` / `jobs/scheduler.ts`）与模块自身，
真实使用点 = **5 文件**，与写死在 `tests/unit/bootstrap-whitelist-census.test.ts:46-78` 的白名单一致。

### 1.2 `$transaction` 普查（F004 acceptance ①）

同样先证活（`.{$transaction}(` 必须抓到 `register.ts` 的已知形态、且不把散文提及计入）。

**改动前树 `09ea010`（F003 完成、F004 未开工）实测 15 处 / 11 文件：**

```
src/lib/agent/gate/gate.ts:249,364,431      src/lib/auth/register.ts:110
src/lib/db/tenant-scope.ts:118              src/lib/delivery/register.ts:96,203,289
src/lib/domain/env-advance.ts:128           src/lib/knowledge/parse.ts:346
src/lib/match/approve-plan.ts:72            src/lib/match/build-plans.ts:220
src/lib/projects/create.ts:64               src/lib/projects/set-goal.ts:80
src/lib/signals/ingest.ts:49
```

其中 `tenant-scope.ts:118` 是 `withTenant` **自身的实现**，非迁移对象 → **产品调用点 = 14 处 / 10 文件**。
再剔除 `auth/register.ts:110`（F003 白名单事务，acceptance ② 明令不迁）→ **迁移目标 = 13 处**。

**与 spec 对账：** `docs/specs/M5.1b-carryover-features.json:31` 写「实测 15 处 / 10 文件」，但其后逐条
列举只有 **14 条**。spec 的口径数与其自身枚举数不自洽；以我的实测为准 = 14 处产品调用点 / 10 文件。
被验方登记的 14/10 与我的实测**逐位相符**。

> **铁律 13 记一笔（不影响计数判定）：** 迁移 commit `85e0362` 正文断言「spec 的表把
> `src/lib/delivery/register.ts:9`（一行注释）算进了调用点」。实物核对：spec 的枚举里只有
> `delivery/register.ts:96,203,289`，**没有 `:9`**。该归因是无机械依据的推测。

**HEAD 实测：** 真实调用点仅剩 `auth/register.ts:110`（白名单）与 `tenant-scope.ts:164`（`withTenant` 自身）
→ 13 处确已迁走。

---

## 2. F003 逐条判定 → **PARTIAL**

### ① 先普查后改 + 判据先证活 + 差异逐条登记 —— **PASS**
见 §1.1。三处更正我全部独立复现；白名单未照抄 spec 表。

### ② 白名单每处改用 `privilegedDb` 且旁注理由 / 开关关行为不变 —— **PASS**
5 处 import 旁均有「为什么必须在租户已知之前碰库」的理由：
`src/lib/auth/index.ts:10-14` · `src/lib/auth/register.ts:17-19` · `src/lib/agent/context.ts:16-19` ·
`src/app/api/auth/register/route.ts:18-20` · `src/app/api/auth/[...nextauth]/route.ts:21-22`。

auth 相关既有测试实跑：`npx vitest run tests/unit/auth-config.test.ts tests/unit/auth-credentials.test.ts
tests/unit/auth-middleware-gate.test.ts tests/unit/auth-status-code-census.test.ts tests/unit/rate-limit.test.ts
tests/integration/auth-register.test.ts tests/integration/auth-rate-limit.test.ts`
→ **7 files / 207 tests passed**。

### ③ 普查钉（扫 src/ 全仓、写死白名单、readFileSync 递归、不用 git grep、红且点名）—— **PASS（附口径偏离登记）**
`tests/unit/bootstrap-whitelist-census.test.ts` —— `collectSources` 用 `readdirSync/statSync/readFileSync`
递归（:80-87），无 `git grep`；白名单为字面量数组（:46-78）；违规时 message 点名文件（变异实证见 ④）。

**口径偏离（记录，不判 FAIL）：** acceptance ③ 字面要求「任何 **import `db/privileged`** 的文件」，
实现判据是「**`privilegedDb` 标识符的真实使用**」。import-specifier 那一面由 F008 的钉
（`tests/unit/db-layer-importer-census.test.ts:55-69`，同样是写死的期望数组）承担。两道钉合起来覆盖了
acceptance ③ 的字面要求，故不判缺项；但 F003 单钉本身不满足字面表述。

### ④ 变异证活三条 —— **PASS**（全部我自己实跑，非只读推断）

| 变异 | 实测输出 | 结果 |
|---|---|---|
| 1. 新建不在白名单的 `src/lib/__evalmut_plain.ts` 且 `import { privilegedDb }` 后使用 | `AssertionError: 发现**未登记**的特权连接使用点：src/lib/__evalmut_plain.ts` | **RED 且点名** ✅ |
| 2. 白名单摘掉 `src/lib/agent/context.ts`（源码仍在用） | `AssertionError: 发现**未登记**的特权连接使用点：src/lib/agent/context.ts` | **RED** ✅ |
| 3a. 作者未采用写法：`import * as pdb` 后 `pdb.privilegedDb` | F003 钉 RED 点名 + F008 钉 RED | **抓到** ✅ |
| 3b. 作者未采用写法：`export * from 'lib/db/privileged'`（标识符从不出现） | F003 钉 **绿（漏）**；F008 钉 RED（`多出（未登记的 importer）：src/lib/__evalmut_plain.ts`） | **F003 漏，F008 兜住** |
| 3c. 登记的盲区形态：`const KEY='privileged'+'Db'; (await import('lib/db/privileged'))[KEY]` | F003 钉 **绿（漏）**；F008 钉 RED | **与钉文件头 :24-28 的登记逐字相符** |
| 3d. **我加打的一条：拼接路径 + 拼接键**（`const P='lib/db/'+'privileged'`） | `Test Files 2 passed / Tests 9 passed` —— **两道钉全绿** | **两道钉同时漏** ⚠️ |

acceptance ④ 只要求「如实报告抓不抓得到」，登记内容经实测**属实**，故判 PASS。
但 3d 证伪了钉文件头 `tests/unit/bootstrap-whitelist-census.test.ts:29` 的那句
「判据不同 ⇒ **盲区不重叠**。**实测互补，不是声称互补。**」—— 存在同时逃出两道钉的形态
（该形态本身已在 F008 钉 :24-25 登记为已知边界，但 F003 头里的「不重叠」结论过强）。
→ 记入盲区登记，不单独判 FAIL。

### ⑤ 白名单**恒不可从别处推导**（不读 env / 不读配置 / **不从源码扫描结果反推**）—— **FAIL**

实现的守卫在 `tests/unit/bootstrap-whitelist-census.test.ts:185-191`，只做两件事：
`/BOOTSTRAP_WHITELIST[\s\S]{0,200}process\.env/ === false` 与 `/const BOOTSTRAP_WHITELIST[^=]*=\s*\[/ === true`。

**变异实证（我跑的）：** 把白名单换成**直接从扫描结果反推**——

```ts
function deriveFromScan() {
  return collectSources(SRC_ROOT)
    .filter((f) => usesPrivileged(readFileSync(f, 'utf8')))
    .map(toPosix).filter((f) => f !== 'src/lib/db/privileged.ts')
    .map((file) => ({ file, reason: '…（≥30 字的占位理由）' }));
}
const BOOTSTRAP_WHITELIST = [...deriveFromScan()];
```

实测：`Test Files 1 passed (1) / Tests 5 passed (5)` —— **五条全绿**。

也就是说，acceptance ⑤ 三个禁止项里最要紧的那个（「不从源码扫描结果反推」）**没有任何机械拦截**；
而一旦被这样改写，整道 F003 钉会退化为恒真（`unauthorized` 按构造恒为空集），却仍然全绿。
守卫只挡住了 `process.env` 这一种最不像的写法，且 D-8.1 要求的「否定断言必须配变异证活」在这条上从未做过。

> 补充：中途我先用 25 字的短理由做同一变异，被「reason 长度 > 30」那条偶然拦下——把理由写长即全绿。
> 也就是说唯一的阻力来自一条与 ⑤ 无关的断言，且轻易绕过。
>
> **当前代码本身没有缺陷**（白名单确是字面量数组），失效的是「保证它以后也不能被推导」的那道闸门。

### ⑥ tsc / lint —— **PASS**
`npx tsc --noEmit` → `exit=0`；`npx next lint` → `✔ No ESLint warnings or errors`（exit=0）。

### ⑦ 批次硬约束（spec §4 第 2 条，挂在 F003 上）—— **FAIL**

> 原文：「**F003 须显式决定**要不要把 `tests/` 侧的 `db/privileged` / `db/runtime` importer 纳入普查钉
> 射程（其 acceptance ③ 现只写 `src/`）。**决定写进代码注释，不得默认略过。**」

全仓检索，**找不到任何这样的代码注释**：

```
grep -rn "射程|tests/ 侧|tests 侧" --include=*.ts --include=*.tsx --include=*.md .
  → 只有 docs/specs/M5.1b-TENANT-INJECTION-spec.md:121 与 docs/test-reports/M5.1-F001-spec-lock-review.md:66
    （即约束原文与其来源），src/ 与 tests/ 侧零命中
grep -rn "不含 tests|不扫 tests|只扫 src|扫描面" tests/unit/bootstrap-whitelist-census.test.ts \
       tests/unit/db-layer-importer-census.test.ts src/lib/db/*.ts     → 零命中
git show fadc1f4 | grep -n "tests/"   → 仅 4 处，均为「本钉文件路径」「project-scope-census 范式引用」，
                                        无一处是射程决定
```

两道钉的 `SRC_ROOT` 都硬编码 `'src'`（`bootstrap-whitelist-census.test.ts:36`、
`db-layer-importer-census.test.ts:31`），**默认略过了 tests/，且未写明这是决定**。

该问题不是空谈：`tests/` 侧当前确有 importer——
`tests/integration/tenant-scope-semantics.test.ts:27-28` · `tests/integration/als-context-propagation.test.ts:156,158` ·
`tests/integration/tenant-injection-proxy.test.ts:24-25` · `tests/integration/f004-transaction-atomicity.test.ts:24` ·
`tests/e2e/tenant-rls-min-loop.e2e.test.ts:57,59`。

---

## 3. F004 逐条判定 → **PARTIAL**

### ① 迁移前重跑普查 + 与 spec 15 处对账 —— **PASS**
见 §1.2。实测 14 处产品调用点 / 10 文件，与被验方登记逐位相符；spec 自身「15 处」与其枚举 14 条不自洽，
已如实登记。（对差异**成因**的归因不实，记铁律 13 一笔，不改判定。）

### ② 逐处迁移可审 + register 白名单单独断言 —— **PASS**
HEAD 普查证实 13 处已迁走（§1.2）；`85e0362` 的正文按「回调形态 11 处 / 数组批量 2 处」并逐文件写明
租户表达式来源，diff 覆盖 10 文件，可对账。

白名单反向断言在 `tests/integration/f004-transaction-atomicity.test.ts:223-256`。
**我做的鉴别力变异**：把 `src/lib/auth/register.ts:20` 的
`import { privilegedDb as defaultPrisma } from 'lib/db/privileged'` 改成
`import { prisma as defaultPrisma } from 'lib/db/prisma'`（= 把注册混进租户路径）：

```
FAIL f004-transaction-atomicity.test.ts > acceptance ②：… 走 privilegedDb，不随租户事务回滚
  AssertionError: expected TypeError: db.$transaction is not a funct… to be Error: [F004] 外层刻意抛错以触发回滚
FAIL bootstrap-whitelist-census.test.ts > 白名单里的每一条都必须仍在实际使用
  AssertionError: 白名单里这些文件已不再使用 privilegedDb：src/lib/auth/register.ts
```
两道断言同时红 → 该条有真实鉴别力。

### ③ 7 个「原子性即全部意义」点各有一条「回滚后零残留」行为级用例 —— **FAIL（7 缺 1：gate.ts:364）**

先跑基线：`npx vitest run tests/integration/f004-transaction-atomicity.test.ts` → **6 passed**。
该文件覆盖 gate.ts:249 / gate.ts:431 / delivery register ×3（外层 `withTenant` 同租户 → 外层抛错 → 零残留），
判据比「内部注入失败」强，成立。`auth/register.ts:110` 由 `tests/integration/auth-register.test.ts:192`
（真 Postgres 事务内最后一步失败 → 前两张表全回滚）覆盖，属实。

**缺口在 gate.ts:364（`executePendingAction`）。** 被验方声明它由既有用例覆盖：
`tests/integration/create-share-link.test.ts:218` 与 `tests/integration/payout-gate.test.ts:274`。
这两条**都不具鉴别力** —— 它们走的是「工具在写入之前就拒绝」，根本没有业务写入发生，
所以「无 ShareLink 行 / 无 Payout 行」并不是回滚的结果。

**变异实证（我跑的）：** 把 `src/lib/agent/gate/gate.ts:372` 的 `db: tx` 改成 `db: getRuntimeDb()`
（业务写入逃出调用方事务，正是 B1-4 病灶）：

```
npx vitest run tests/integration/create-share-link.test.ts tests/integration/payout-gate.test.ts
  → Tests  23 passed (23)            ← 全绿，没红
npm run gate:smoke
  → ✓ G7: failed 无 irrev 行（业务写入随事务回滚）   ← 也没红，整套 exit=0
```

为排除「我的变异是空转」，我另建了一条**purpose-built 探针**（注册一个「先经 `ctx.db` 真写一行 OperationLog，
再抛错」的 outbound 工具，走完整两步票据）：

```
变异在场：FAIL … 业务写入必须随 gate 事务回滚: expected 1 to be +0
还原之后：Tests 1 passed (1)
```

→ 变异有效、产品性质正确（gate.ts:364 的业务写入**确实**在调用方事务内），
但**没有任何在册用例守着它**；这一处一旦回归，整个默认测试面 + `gate:smoke` 都不会翻红。

> 附：也不能简单套用本文件的「外层 `withTenant` 包一层」范式——gate.ts:364 传了
> `{ timeout: 90_000, maxWait: 5_000 }`，嵌套传选项会抛 `NestedTransactionOptionsError`
> （`src/lib/db/tenant-scope.ts:150`）。补用例需走「工具内写入 + 后续步骤失败」的形态（如我的探针）。

### ④ gate:smoke / delivery:e2e / reach:e2e 开关关全绿 —— **PASS**（输出原样）

```
[gate-smoke] ✅ 全部断言通过（两步票据 + 7 态 + 并发竞态 + D20 变异）
[gate-smoke] 测试数据已清理（含 F003 夹具）

[delivery-e2e] ✅ 交付闭环全绿
[delivery-e2e] 用量申报：真实资金动作 0 · 真实 key 发放 0（mock 适配器；本批无 REAL 分支）
[delivery-e2e] 夹具已清理

[reach-e2e] ✅ E2E 闭环全绿
[reach-e2e] L2 用量申报：gateway chat 1 次 · 真实投递 0 封（mock）
[reach-e2e] 夹具已清理
```

### ⑤ 变异证活：摘掉 `withTenant` 的回滚传播 → ③ 对应用例必须红 —— **PASS**

我没有沿用被验方那条（只红 1 条），而是直接打机制本体：把
`src/lib/db/tenant-scope.ts:162` 的嵌套复用分支 `return fn(outer.tx);` 换成「嵌套时另开独立事务」。

```
× confirmPendingAction（gate.ts:249）随外层回滚 → 状态仍是 pending    expected 'confirmed' to be 'pending'
× rejectPendingAction（gate.ts:431）随外层回滚 → 状态仍是 pending      expected 'rejected'  to be 'pending'
× registerDealRefs（:96）…                                             expected 'sign-f004' to be null
× verifyDeliverable（:203）…                                           expected 'met'       to be 'missing'
× registerKeyPool（:289）…                                             expected 2           to be +0
Tests  5 failed | 1 passed (6)
```
5/6 红，且唯一保持绿的正是走 `privilegedDb` 的 register 反向断言（应当不受影响）—— 鉴别力成立。

### ⑥ tsc / lint / 全量 vitest —— **PASS**
`npx tsc --noEmit` exit=0 · `npx next lint` `✔ No ESLint warnings or errors` ·
`npx vitest run` → **Test Files 148 passed (148) / Tests 1869 passed (1869)**。

---

## 4. 变异清单与还原对账（**未用 `git checkout`**）

| # | 变异内容 | 观察 | 还原证明 |
|---|---|---|---|
| M1 | 新建 `src/lib/__evalmut_plain.ts`（4 种写法轮换） | 见 §2④ | `rm` 后 `ls src/lib/__evalmut*` → `no matches found`；`git status --short` 仅 `?? node_modules` |
| M2 | `bootstrap-whitelist-census.test.ts` 摘掉 `agent/context.ts` 条目 | RED | 反向编辑后 `shasum -a 256` = `a920e960…f65` = 基线 ✅ |
| M3 | 同文件白名单改为 `[...deriveFromScan()]` | **5 条全绿** | 反向恢复后 `shasum` = `a920e960…f65` ✅ |
| M4 | `src/lib/db/tenant-scope.ts` 摘掉嵌套复用（回滚传播） | 5/6 RED | 反向编辑后 `shasum` = `6bec89fd…85e` = 基线 ✅ |
| M5 | `src/lib/agent/gate/gate.ts` `db: tx` → `db: getRuntimeDb()`（+ 一行 import） | 既有用例与 gate:smoke **全绿**；自建探针 RED | 反向编辑后 `shasum` = `58c8e812…76d` = 基线 ✅ |
| M6 | `src/lib/auth/register.ts` import 改指 ALS 代理 | 2 条 RED | 反向编辑后 `shasum` = `ed05589e…b8c` = 基线 ✅ |
| P1 | 临时探针 `tests/integration/__eval_gate364_probe.test.ts` | —— | 已 `rm`，`git status --short` 仅 `?? node_modules` |
| P2 | 临时探针 `scripts/test/__eval_sentinel_probe.ts` / `__eval_stray_check.ts` / `__eval_cleanup.ts` | —— | 已 `rm`，同上 |

**主仓 `/Users/yixingzhou/project/newkolmatrix` 的 `src/` 全程未触碰**（全部变异只发生在本 worktree）。

**残留自证：** `SELECT slug FROM "Tenant" WHERE slug LIKE 'evalprobe364-%' OR 'f004atomic-%' OR '%evalmut%'` → `[]`；
M5 变异期间逃出事务的 1 行 `OperationLog`（`summary LIKE 'evalprobe364-%'`）已定向删除，复查 `remaining = 0`。
还原后复跑 `f004-transaction-atomicity + 两道普查钉` → **3 files / 15 tests passed**。

---

## 5. 盲区登记（判不了 / 未覆盖，如实写）

1. **两道钉的共同盲区**：拼接路径 + 拼接键（`import('lib/db/'+'privileged')` 取 `['privileged'+'Db']`）
   同时逃出 F003 与 F008 两道钉（实测两钉全绿）。F008 钉 :24-25 已登记「拼接动态路径扫不到」，
   但 F003 钉 :29「盲区不重叠」的结论过强。
2. **「先普查后改」的时序本身不可事后机械核实**——我只能核实普查**结论**是否正确（已逐条复现），
   无法证明它发生在改动之前。
3. **开关开（`DB_APP_ROLE_RUNTIME=1`）下的 F003/F004 行为未系统性覆盖**：本轮只在哨兵探针里
   短暂开过开关。开关开的面归 F006 的 `npm run rls:e2e`，不在本次 F003/F004 射程内。
4. **生产/staging 实测未做**（L2 未授权）：本报告全部结论来自本机 dev Postgres 与进程内测试。
5. `reach:e2e` 的 1 次真网关调用是 acceptance ④ 强制项的副产品，已在 §0 申报。

---

## 6. 修复建议（供 fixing 阶段参考，非 Evaluator 越界实现）

- **F003-⑤**：把「不从扫描结果反推」做成有鉴别力的断言。可行方向：在钉文件里对自身源码断言
  `BOOTSTRAP_WHITELIST` 的初始化表达式**只含对象字面量**（禁 spread / 禁函数调用 / 禁标识符引用），
  并对该断言补一条变异证活（`[...deriveFromScan()]` 必须红）。
- **F003-⑦**：在 `bootstrap-whitelist-census.test.ts` 与 `db-layer-importer-census.test.ts` 的
  `SRC_ROOT` 旁写明射程决定（收 / 不收 `tests/`）及理由——spec §4 要的就是这句注释。
- **F004-③**：为 gate.ts:364 补一条真有写入的回滚用例（形态可参考本报告的探针：注册一个
  「经 `ctx.db` 写一行后抛错」的 test-only outbound 工具，断言那一行零残留）；
  不能用「外层 `withTenant` 包一层」，会撞 `NestedTransactionOptionsError`。
  同时建议把 `create-share-link.test.ts:218` / `payout-gate.test.ts:274` 从「gate.ts:364 的原子性覆盖」
  这个身份上摘掉——它们证的是「工具会拒绝」，不是「写了再回滚」。

---

*本报告由隔离 evaluator subagent 在独立 worktree 内产出，结论未经任何协商。*
