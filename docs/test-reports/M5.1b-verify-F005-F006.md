# M5.1b-TENANT-INJECTION — F005 / F006 独立验收报告

> **Evaluator：** 隔离 subagent（fresh context，独立 git worktree
> `/Users/yixingzhou/project/newkolmatrix/.claude/worktrees/agent-ac2464fff9dc3d176`）
> **被验 HEAD：** `8c5fee13311caad05cf0d8e666e26c8c1f815b1b`
> **日期：** 2026-08-08
> **判据来源：** `features.json` F005 / F006 acceptance 原文 ·
> `docs/specs/M5.1b-TENANT-INJECTION-spec.md`（§5.1 / §7）· `docs/specs/M5.1-TENANT-INJECTION-spec.md`（D-6 / D-8）
> **判据纪律：** commit message / 代码注释 / `generator_handoff` 一律视为被验对象的自述，
> **不作为通过依据**（铁律 13）。以下每条结论后面跟的是本次实跑的命令输出或 `文件:行`。

## 结论

| feature | 判定 |
|---|---|
| **F005** ALS 三执行上下文传播实测 | **PASS** |
| **F006** `DB_APP_ROLE_RUNTIME=1` 最小闭环 e2e + 跨租户零行 | **PASS** |

两条均以**变异实跑**证活，非只读推断。所有变异用反向编辑还原，还原后
`git diff --stat HEAD` 为**空**（逐位回到被验 HEAD）。主仓 `src/` 全程零改动
（`find /Users/yixingzhou/project/newkolmatrix/src -newermt "2026-08-08 06:50" -type f` → `COUNT=0`）。

---

## 0. 环境与基线

```
pwd                     → .../.claude/worktrees/agent-ac2464fff9dc3d176
git rev-parse HEAD      → 8c5fee13311caad05cf0d8e666e26c8c1f815b1b
node_modules            → 符号链接到主仓（未 npm install）
.env                    → 复制自主仓，含 DATABASE_URL + DATABASE_URL_APP
npx vitest run tests/unit/health.test.ts → 1 passed / 32 tests（冒烟通过）
```

**并发环境登记：** 另有 4 个 evaluator 在各自 worktree 并行跑，共用同一本机 dev Postgres。
本报告涉及的失败**全部为本人主动施加变异所致**，且逐条可复现；未观察到任何与本 feature
无关的偶发失败。

---

## F005 — ALS 在 RSC / route handler / scheduler 三执行上下文的传播实测

**主证据文件：** `tests/integration/als-context-propagation.test.ts`（605 行 / 13 用例）
**接线面（F005 提交 `7ec61df` 触及的产品文件）：**
`src/lib/db/tenant-entry.ts`（新增 `withSessionTenant`）·
`src/app/api/nav-badges/route.ts` · `src/app/admin/knowledge/page.tsx` ·
`src/lib/jobs/scheduler.ts`（health-scan）· `src/lib/db/tenant-scope.ts`（选型注释）

基线实跑：

```
$ npx vitest run tests/integration/als-context-propagation.test.ts
 Test Files  1 passed (1)
      Tests  13 passed (13)
```

### ① 三个执行上下文各一条真跑用例（非 mock ALS / 非手工 als.run）— PASS

**核实的是「用例走的是不是产品入口本体」，不是转述。**

| 上下文 | 被调用的产品入口 | 证据 |
|---|---|---|
| (a) RSC | `src/app/admin/knowledge/page.tsx` 的 **default export** | 测试 `:154` `import KnowledgePage from '../../src/app/admin/knowledge/page'`，`:406` 直接 `await KnowledgePage({searchParams})` |
| (b) route handler | `src/app/api/nav-badges/route.ts` 的 **GET** | 测试 `:155` `import { GET as navBadgesGet }`，`:379` `await navBadgesGet()` |
| (c) scheduler | `src/lib/jobs/scheduler.ts` 的 `ROUTINES['health-scan'].run` | 测试 `:164` `import { ROUTINES }`，`:428-432` `def.run(SLUG.a)` |

租户 ALS **不是 mock**：三条入口经 `withSessionTenant` / `withTenant` 建立的是产品
`tenant-scope.ts:99` 的那个 `tenantTxAls`；测试文件里另建的 `AsyncLocalStorage` 只有两处，
用途分别是「测试侧承载并发会话身份」（`:194`）与「§③ enterWith/run 对照探针」（`:565` `:576`），
**都不是被测的租户 ALS**。

探针是**包**不是**替换**：`vi.mock(..., importOriginal)` 后仍调 `actual.getNavBadgeCounts(...)`
（`:117-152`），因此同一条用例既断言「数据访问点看到的 tenantId」也断言「真读回本租户的数据」。

每个上下文另有一条**反证**（`:395` `:418` `:450`）：同一 leaf 在作用域外直调 →
`rejects.toBeInstanceOf(MissingTenantScopeError)`。这条把「正向为什么绿」钉死。

> **登记的口径边界（非缺陷，但记下）：** 探针落在 leaf 函数入口而非最内层 `prisma.x.count()`
> 调用点；RSC 用例断言的是 server component 返回的 element props，不做 React 渲染。
> 前者由「反证用例 + txid 断言」补偿，后者由 `page.tsx:23-29` 的显式作用域边界说明覆盖。

### ② 跨请求不串（并发 ≥2 租户）— PASS

三条并发用例（route / RSC / scheduler，`:463-541`），每条：
`Promise.all` 起 A/B 两路 → 断言各自的返回数据 → `expectScopeMatchesQuery(seen)` 逐条配对
（`:247-254`：这次查询的租户 == ALS 租户 == DB 会话变量租户）→ 断言两路 txid 不同。

**「集合相等会恒绿」这个盲区已被排除，且由我实跑证明：** 见下方变异 M4。

> 登记的口径边界：并发用例把整个 `lib/auth/session-tenant` 模块换成注入缝
> （`tests/support/session-mock.ts`），即「会话身份从哪来」是假的；入口 →
> `withSessionTenant` → `withTenant` → ALS → 代理 → 数据访问点全链是真的。
> 测试文件 `:70-91` 显式登记了原因（vitest 4.1.10 对 `session-tenant.ts` 那个**动态**
> import 的 mock 在 `Promise.all` 下会泄漏真模块）。顺序用例（①）不受此限，走的是
> 真 `readSessionIdentity`（seam `resolve` 返回 `undefined` → 回落真实现，`:196`）。
> acceptance ② 要证的是「并发下各读到自己的租户」，被替换的部分不在该性质的证明路径上。

### ③ enterWith vs run 的选型有实测依据（不接受「按惯例」）— PASS

选型理由写在 **`src/lib/db/tenant-scope.ts:82-98`**（`run` 而非 `enterWith`），
形态是「两个都可用，选 run 的理由是 Y」，Y = **enterWith 没有出口**。

**我不接受源码自述，另写独立探针复跑（不复用他们的用例）：**

```
$ node --import tsx eval-probe/enterwith.mts
{
 "claim1_readable_inside": "X",               # enterWith 在异步回调内同样读得到 → 成立
 "claim2a_leak_when_before_await": "BEFORE",  # 写在第一个 await 之前 → 泄漏到调用方 → 成立
 "claim2b_leak_when_after_await": null,       # 写在之后 → 不泄漏 → 成立
 "claim2c_leak_with_run": null,               # run → 不泄漏 → 成立
 "node": "v25.7.0"
}
```

其中 **claim2b（「写在 await 之后则不泄漏」）是源码注释写了、而交付的用例文件没有覆盖的那半句**——
本次由我独立补测，结论与注释一致。第三条依据（事务提交后 tx 句柄已死、再用必抛）由交付用例
`:596-603` 实跑覆盖。

> 探针踩到的一个坑（记录以免后人重犯）：首版探针四条断言共用同一个 ALS 实例、靠
> `enterWith(undefined)` 复位，结果 claim2a 的泄漏直接把 2b/2c 污染成同一个值
> （全读到 `"BEFORE"`）。改成**每条一个独立 ALS 实例**后结果如上——这个坑本身正是
> 「enterWith 没有出口」的又一次实证。

### ④ 变异证活 — PASS（四条全部由我实跑，非引用其交付叙述）

**授权范围内在本 worktree 临时改产品代码，跑完反向编辑还原，`shasum -a 256 -c` 逐位对账；
不用 `git checkout`。**

| # | 变异 | 实测红了几条 | 红的是不是该红的那条 |
|---|---|---|---|
| **M1** | `route.ts` 摘掉 `withSessionTenant`（改回 `requireSessionIdentity` + 直调 leaf） | **2 failed / 11 passed** | ①(b) route + ② route。stderr：`[api/nav-badges] 失败: MissingTenantScopeError: [db] DB_APP_ROLE_RUNTIME=1…`，返回 500 而非「200 + 零计数」 |
| **M2** | `page.tsx` 摘掉 `withSessionTenant` | **2 failed / 11 passed** | ①(a) RSC + ② RSC，`MissingTenantScopeError`（栈指向 `prisma.ts:30`） |
| **M3** | `scheduler.ts` health-scan 摘掉 `withTenant` | **2 failed / 11 passed** | ①(c) scheduler + ② scheduler，`MissingTenantScopeError` |
| **M4** | `tenant-entry.ts` 把入口包裹的租户换成**兄弟租户**（slug 尾 `-a`/`-b` 真互换） | **4 failed / 9 passed** | ①(b) ①(a) ② route ② RSC |

M4 是本次最关键的一条（编排者点名的并发盲区）。抓住它的**不是**集合相等断言，而是逐条配对：

```
 FAIL  ... > ② 并发不同租户互不串（应用层） > route：A / B 两个并发请求各读到自己的计数…
AssertionError: expected { …(2) } to deeply equal { …(2) }
-   "als": "cmsk9e0yn00009y33y4pgnv2i"      (expected = leafTenantId)
+   "als": "cmsk9e0z1000c9y3343oj46p2"      (received = 被换成的兄弟租户)
 ❯ expectScopeMatchesQuery tests/integration/als-context-propagation.test.ts:249:54
```

即：**A↔B 互换上下文后集合仍是 {A,B}，但用例仍然红**——该并发断言不是「集合相等」的空绿形态。

**还原对账（M1–M4 每条跑完各做一次）：**

```
$ shasum -a 256 -c eval-probe/baseline.sha
src/app/api/nav-badges/route.ts: OK
src/app/admin/knowledge/page.tsx: OK
src/lib/jobs/scheduler.ts: OK
src/lib/db/tenant-entry.ts: OK
...
```

### ⑤ 传播不了的情况如实报告 — PASS

交付登记「三个上下文都传播得了，无需替代接线」。三条正向用例 + 三条反证用例实跑支持该结论。
文件头另登记两条限制，**我逐条核过是事实、不是托词**：

- RSC 作用域只覆盖数据装配段，不覆盖 JSX（`page.tsx:23-32` 有调用点旁的理由）；
- 「RSC loader 的兜底 catch 会把 fail-closed 吞成静默空态」——核实：
  `src/lib/insight/cross-surface-data.ts:122-127` 与 `src/lib/insight/surface-data.ts:151-157`
  确实是 `catch (err) { console.error(…); return EMPTY_…; }`。该风险登记属实，M5.2 需逐个处置。

### ⑥ tsc / lint — PASS

```
$ npx tsc --noEmit        → 无输出，TSC_EXIT=0
$ npx next lint           → ✔ No ESLint warnings or errors
```

### F005 无回归旁证

```
$ npx vitest run tests/integration/{db-app-role-sentinel,health-scan-routine,nav-badges,
    rls-negative-suite,rls-tenant-isolation,routine-tenant-explicit}.test.ts \
    tests/unit/{session-tenant-context,db-layer-importer-census,bootstrap-whitelist-census,
    project-scope-census}.test.ts
 Test Files  10 passed (10)
      Tests  91 passed (91)
```

---

## F006 — `DB_APP_ROLE_RUNTIME=1` 最小闭环 e2e + 跨租户零行

**主证据文件：** `tests/e2e/tenant-rls-min-loop.e2e.test.ts`（470 行 / 15 用例）·
`vitest.e2e.config.ts` · `package.json:49`

基线实跑（连跑两次，均绿）：

```
$ npm run rls:e2e
> DB_APP_ROLE_RUNTIME=1 vitest run --config vitest.e2e.config.ts
 Test Files  1 passed (1)
      Tests  15 passed (15)
```

### ① 进程级 env 覆盖 / 不改 `.env` 默认值 / 不进默认收集范围 — PASS

- **脚本入口：** `package.json:49` → `"rls:e2e": "DB_APP_ROLE_RUNTIME=1 vitest run --config vitest.e2e.config.ts"`
  ——开关是**命令行 env 前缀**，不是文件。
- **`.env` 默认值未改：** `grep -n "DB_APP_ROLE_RUNTIME" .env` → 无命中（保持未设，符合母 spec §5.1）；
  `git log --oneline 6a120a7~1..HEAD -- .env.example` → **空**（整批未动 .env.example）。
- **「不进默认收集范围」是结构事实，不是注释也不是 skip 分支：**
  `vitest.config.ts:42` 的 `include` = `['tests/unit/**/*.test.ts','tests/integration/**/*.test.ts']`，
  根本不含 `tests/e2e/`。机械核对：

```
$ npx vitest list | grep -c ""              → 1869      （默认收集的用例数）
$ npx vitest list | grep -c "tests/e2e"     → 0          （默认收集里一条 e2e 都没有）
$ DB_APP_ROLE_RUNTIME=1 npx vitest list --config vitest.e2e.config.ts
  → 15 条，全在 tests/e2e/tenant-rls-min-loop.e2e.test.ts
```

- **F006 提交面：** `git show --stat 86331c0` → `package.json` / `tests/e2e/…` / `vitest.e2e.config.ts`
  共 3 文件，**无 src/ 改动、无 .env 改动**。

### ② 六段闭环逐段断言 — PASS（全部在 kol_app 连接上）

前提由文件 `:67-88` 的四道 preflight 硬抛（不 skip）保证：有 `DATABASE_URL`、有
`DATABASE_URL_APP`、开关已开、`resolveRuntimeDatabaseUrl() === appUrl`。

| 段 | 用例 | 判定依据 |
|---|---|---|
| (a) 登录 | §2 三条 | 走 `authorizeCredentials + prismaAuthorizeDeps`（`src/lib/auth/index.ts:29-35` = F003 白名单 `privilegedDb`）；错误口令 → null 排除「恒返回一个用户」；**常驻 B2-1 断言**：同一条查询走 `getRuntimeDb()`（kol_app、无租户变量）→ **恒 null**，而特权连接查得到 |
| (b) 注册 | §3 两条 | 三行（Tenant/User/OperationLog）都在且同属新租户；**同事务用失败方向证**——重复 email → `email_taken` 且先写的租户行零残留 |
| (c) API route | §4 两条 | `GET /api/nav-badges`：A 会话得 A 的数、B 会话得 B 的数 |
| (d) RSC page | §5 一条 | `/admin/knowledge`：A 渲染出 A 的游戏且**看不见** B 的 |
| (e) 例程 | §6 一条 | `ROUTINES['health-scan'].run(SLUG.a)` 跑完，留痕增量 = A 的项目数（写路径过 WITH CHECK） |
| (f) 跨租户零行 | §7 三条 | 见下 |

**(f) 的 DB 层证据是真的，我逐项核过：**

- SQL 原文（`:408`）：`SELECT count(*)::int AS c FROM "Game" WHERE id = $1`
  —— **不含任何租户谓词**，只按主键取。故「查不到」不可能来自应用层 where。
- 该 raw 查询经产品代理 `prisma.$queryRawUnsafe` 落到 `withTenant` 的 tx 上
  （`src/lib/db/prisma.ts:38-47` 的 `Reflect.get + bind`），tx 来自
  `getRuntimeDb()`（`tenant-scope.ts:164`）= 此刻的 kol_app 连接。
- 三重对照齐备：**正对照** `own === 1`（排除「连接坏了/表名写错」这类平凡零行）；
  **零行** `foreign === 0`；**反对照** 同一行在 `privilegedDb` 上 `=== 1`（证明它确实存在）。
- 产品路径同款（`:435`）：`withTenant(A)` 里 `prisma.pendingAction.findUnique({id: B 的行})` → `null`。
- **写侧也钉了**（`:448`）：A 的作用域里往 B 名下插行 → 抛，且 `privilegedDb` 复核确认真没写进去。

**这一段的 DB 层性质由我用变异 M7 反向证活（见下），不是只读推断。**

> 登记一条口径观察（非缺陷）：(d) 那条断言在「运行时被换成特权连接」时**仍然绿**
> （见 M7 的红项清单里没有 §5），因为 `getKnowledgePageData` 自带应用层 `where tenantId`。
> acceptance 对 (d) 只要求「渲染出本租户数据」，DB 层证据的义务落在 (f)，(f) 扛住了。

### ③ 哨兵双向 — PASS

- 正向（`:215`）：开关开 + kol_app → `runConnectionRoleSentinel` resolves `{status:'ok', enforced:true}`；
  另有（`:207`）`inspectConnectionRole(getRuntimeDb())` → `{superuser:false, bypassRls:false}`
  （防「配了串但没用上」）。
- 反向自证（`:221`）：同一函数、同一份 env，只换连接（传 `privilegedDb`）→
  `rejects.toBeInstanceOf(PrivilegedConnectionError)`。
- **接线层面的额外证活（M7）：** 把运行时连接串真换成特权后，正向那条哨兵用例当场翻红：
  `AssertionError: promise rejected "PrivilegedConnectionError: …" instead of resolving`。

### ④ 变异证活 — PASS（两条 acceptance 指名的变异我都实跑复现，另加两条加验）

| # | 变异 | 实测 | 红的是不是该红的那条 |
|---|---|---|---|
| **M5** | F003 白名单登录查询 `privilegedDb` → `getRuntimeDb()`（kol_app 直连、无租户变量） | **1 failed / 14 passed** | §2「正确凭据 → 认到本人」——**这正是审计 B2-1「切过去就没人能登录」的原始形态**（不报错、就是查不到） |
| **M5b** | 同一处改成 `prisma` **ALS 代理**（acceptance ④-1 的字面形态） | **2 failed / 13 passed** | §2 两条，均 `MissingTenantScopeError`（第二条：`promise rejected "MissingTenantScopeError…" instead of resolving`） |
| **M6** | `nav-badges` 入口摘掉 ALS 作用域 | **2 failed / 13 passed** | §4 两条，stderr 明示 `[api/nav-badges] 失败: MissingTenantScopeError: …`，栈指向 `route.ts:20` —— 不是「200 + 零计数」 |
| **M7**（加验，非 acceptance 指名） | 进程级 env 把 `DATABASE_URL_APP` 指向**特权串**（不改 .env） | **6 failed / 9 passed** | §1 两条 + §2 的 B2-1 常驻断言 + **§7 全部三条**：`expected 1 to be +0`（跨租户行看得见了）、`expected {…} to be null`、`promise resolved instead of rejecting`（越权写成功了）。**这条证明 §7 的零行确实由 RLS 承担，而不是应用层 where 顺带的结果。** |
| **M8**（加验，非 acceptance 指名） | `registerAccount` 摘掉 `$transaction`（三步各自自动提交） | **1 failed / 14 passed** + 文件级 FAIL | §3「同事务的机械判据」→ `AssertionError: expected 1 to be +0`（孤儿租户留下了）。附带证明**夹具普查层 ② 有效**：`afterAll` 的前缀普查抓到了未登记的孤儿租户并抛错 |

M5 与 M5b 的差别值得记一笔：acceptance ④-1 写的是「改回租户路径」，两种改法结果不同——
代理版红成 `MissingTenantScopeError`（2 条），直连 kol_app 版红成**静默查不到**（1 条）。
后者才是审计 B2-1 的原貌。两种都实跑了，两种都红，结论不受改法选择影响。

**还原对账：**

```
$ shasum -a 256 -c eval-probe/baseline.sha          （每条变异跑完各一次）
src/lib/auth/index.ts: OK
src/lib/auth/register.ts: OK
src/app/api/nav-badges/route.ts: OK
...
$ git diff --stat HEAD                              （全部变异结束后）
（空 —— 工作树与被验 HEAD 8c5fee1 逐位一致）
```

### ⑤ 夹具纪律 / git 干净 / dev 库无残留 — PASS（实跑输出为证）

- **独一前缀：** `TAG = f006rls-${process.pid}`（`:93`），A/B 两租户 + 注册段新租户均带该前缀。
- **两层清理：** ① 按登记 id 精确删，**软引用表逐个点名**（`:165-172`：operationLog /
  pendingAction / gameKnowledge / material / game / project / user / tenant），每步独立
  try/catch 只告警；② **不从登记表派生**的全表前缀普查（`:182-196`），非零即抛。
- **git 干净（跑完立刻查）：**

```
$ npm run rls:e2e   → 15 passed
$ git status --short
?? node_modules          ← 本 evaluator 自建的符号链接，非 feature 产物
$ git status --porcelain | wc -l → 1
```

- **dev 库无残留（我做的是全库前后对照，不只信它自己的普查）：**

```
$ node --env-file=.env --import tsx eval-probe/counts.mts     # 跑之前
{"tenants":4,"users":1,"logs":374,"pendings":24,"games":4,"projects":4,
 "orphanLogs":"170","orphanPending":"24"}
$ npm run rls:e2e                                             # 15 passed
$ node --env-file=.env --import tsx eval-probe/counts.mts     # 跑之后
{"tenants":4,"users":1,"logs":374,"pendings":24,"games":4,"projects":4,
 "orphanLogs":"170","orphanPending":"24"}      ← 全部逐位相同，含孤儿行统计
```

同法对 F005 也做了一次：`als-context-propagation` 跑完六项计数同样零变化。
前缀普查独立复核：`{"f006rls-":{tenant:0,user:0,project:0,game:0,pending:0,log:0},
"f005als-":{…全 0}}`。

> **登记一处判据盲区（不影响本条判定，但值得写进 M5.2）：** 普查层 ② 对 `OperationLog`
> 用的谓词是 `summary contains TAG`。而产品代码写的留痕（注册段 `[auth] register ok
> domain=f006.local`、health-scan 巡检痕）**不含 TAG**，因此这类行只被层 ① 的「按 tenantId
> 精确删」覆盖。若层 ①（每步 try/catch 只告警）静默失败，层 ② 看不见这批孤儿留痕——
> 只能通过 `tenant.count(name startsWith TAG)` 间接察觉。M8 变异实测确认间接那条通路是通的
> （孤儿租户被抓到并抛错），且全库前后对照实测零增量，故本次判 PASS。

### ⑥ tsc / lint — PASS

与 F005 同一次实跑（工作树处于还原后的被验 HEAD 状态）：
`npx tsc --noEmit` 退出 0；`npx next lint` → `✔ No ESLint warnings or errors`。

---

## 未执行 / 判不了的项（L2 未授权或超出本次射程）

| 项 | 状态 |
|---|---|
| 生产 / staging 环境下的开关开启实测 | **[L2] 未执行，待授权**。本次全部在本机 dev Postgres + 本机进程内完成，未触发部署、未连真实外部服务、未产生计费或生产写入 |
| 全量 `npx vitest run`（148+ 文件） | **未执行**。母 spec §6 明确 acceptance 边界不含「全套测试普遍绿」；且本次有 4 个 evaluator 并发共用同一 dev 库，全量跑的失败无法归因。改为跑了 10 个与本 feature 直接相关的文件（91 passed），另用 `vitest list` 机械核对默认收集范围 |
| F001–F004 / F007 / F008 | 不在本次射程（由其他 evaluator 并行验收） |

## 观察到但未复现的异常

无。本次全部失败均为本人主动施加的变异，逐条可复现；未出现与 F005/F006 无关的偶发红项。

（并发环境副产品，登记备查：验收期间 dev 库的 `orphanLogs` 由 170 增至 198、
`orphanPending` 由 24 增至 36，抽样内容为 `f004_confirm` / `f004_reject` 闸门痕与
`[auth] register ok domain=test.local`，另有一个 `f001proxy-80825-raw-cm` 残留租户——
均出自其他 evaluator 并行跑的其他测试面，**不是 F005/F006 的产物**，前后对照已证。）

## 本次 evaluator 自身的清场

- 全部变异用反向编辑还原，未使用 `git checkout`；`git diff --stat HEAD` 为空。
- 临时探针目录 `eval-probe/` 与 `node_modules` 符号链接已删除，
  worktree `git status --porcelain | wc -l` → **0**。
- M8 变异产生的孤儿租户 `f006rls-87878-orphan-tenant` 已由我清理，
  复查 `{"f006rls-":{tenant:0,…}}`。
- 主仓 `src/` 一个字节未动（`find … -newermt "2026-08-08 06:50"` → `COUNT=0`）。
- 未修改主仓 `progress.json` / `features.json` / 规格 / 配置；未 push。
