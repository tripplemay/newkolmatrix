# M5.1b-TENANT-INJECTION — F007 验收 + 全批 L1 底线独立复跑

> **角色：** Evaluator（隔离上下文 subagent，独立 worktree）
> **被验 HEAD：** `8c5fee13311caad05cf0d8e666e26c8c1f815b1b`
> **工作副本：** `/Users/yixingzhou/project/newkolmatrix/.claude/worktrees/agent-ab046e391b2b1e24f`
>（`node_modules` 符号链接主仓，`.env` 拷贝自主仓）
> **日期：** 2026-08-08
> **范围：** F007 逐条 acceptance + 全批 L1（tsc / lint / 全量 vitest）独立复跑
> **判据纪律：** 结论只依据实物与实跑输出。commit message、代码注释、`generator_handoff`
> 一律作为**被验对象的自述**处理，不作为通过依据（铁律 13）。

---

## 0. 结论摘要

| 项 | 判定 |
|---|---|
| **F007 总判定** | **PASS**（七条 acceptance 全部满足；另登记 2 条不影响判定的盲区） |
| 全批 L1 底线 | **绿**：tsc exit 0 / lint 0 warning / vitest 148 files · 1869 tests 全通过 |
| L2 | 未执行（未授权）。本批 F007 无需 L2——交付物为文档 / 测试 / 普查脚本，零外部服务依赖 |

**两条登记项（均为已交付物的强度边界，不构成 acceptance 缺项）：**

1. **[BLIND-1]** 「入口面实测 N 条、已包裹 M 条」断言只查**值是否对**，不查**锚点是否还在该文件**。
   实测：把该句从 `docs/dev/deploy.md`（部署手册，最需要这条警告的地方）整句摘掉，
   全量 148 files / 1869 tests **依然全绿**。原因是断言判据为「docs/ 全域 anchors ≥ 1 条且逐条值正确」，
   而 `architecture.md` 另有两份副本兜着。
2. **[BLIND-2]** 入口普查的租户来源分类器**看不见直接 `new PrismaClient()`**。
   实测有 5 个真正带租户读写库的文件不在 78 条入口面里（3 个被扫到但判 `none`，2 个 `.mjs` 根本未进扫描面）。
   这 5 个都不经 ALS 代理，故**不影响** deploy.md「开关一开 75 条 fail-closed」这个承重数字。

---

## 1. 全批 L1 底线（独立复跑，本报告的硬性附加任务）

三条均在本 worktree 从零实跑，非引用他人结论。

| 命令 | 实测输出 | 判定 |
|---|---|---|
| `npx tsc --noEmit` | 零输出，`TSC_EXIT=0` | ✅ |
| `npx next lint` | `✔ No ESLint warnings or errors`（exit 0） | ✅ |
| `npx vitest run` | `Test Files 148 passed (148)` / `Tests 1869 passed (1869)`，exit 0 | ✅ |

`vitest` 全量共跑 **3 次**（基线 1 次 + 变异 M2 期间 1 次 + 变异全部还原后 1 次），
三次均 148/1869 全绿。**并发环境未观察到任何失败**——本次没有出现需要「单独重跑确认可复现」的条目，
故不存在「观察到但未复现」的登记项。

> `next lint` 另打印两条与本批无关的既有噪声：`next lint is deprecated`（Next 16 迁移提示）、
> `inferred workspace root`（worktree 下有第二份 `package-lock.json` 所致，是我的验收环境副作用，非产品缺陷）。

### 为什么不跑 `npm run test:visual`

spec `docs/specs/M5.1b-TENANT-INJECTION-spec.md` §7（行 152-153）显式登记：本机 `test:visual`
恒 15 passed / 2 failed，归 `BL-VISUAL-DATA-ISOLATION`，**不计分**。
另经实物核对，F007 提交 `3baf293` 的 7 个文件为
`docs/dev/*.md` ×3 · `docs/specs/*.md` ×1 · `package.json` · `scripts/test/` ×1 · `tests/unit/` ×1，
**`src/` 零改动** → 零 UI 变更，视觉基线不构成本 feature 的判据。故不跑，且不记缺项。

---

## 2. F007 逐条 acceptance

### ① 文档同步三处 — **PASS**

acceptance 点名了具体行号，逐处核到实物：

| 要求 | 实物核对 | 判定 |
|---|---|---|
| `docs/dev/architecture.md`（注入机制 as-built） | 新增 §7.1.1「租户注入机制（as-built，M5.1 + M5.1b）」，`architecture.md:669-709`；另翻牌旧句（`:86` 选型表 · `:109` 概览 · `:215` 边界声明 · `:1638` NFR-S9 · ADR-04 行） | ✅ |
| `docs/dev/agent-architecture.md:105` 旧句改实况 | `:105` 原未来态旧句已被替换为「RLS 对 agent 层当前仍零影响，**但原因已经变了**」；`:106` 新增开关打开后的行为 | ✅ |
| `docs/dev/deploy.md:130` 开关段 | `:130` 正是 `4. **应用运行时切换 kol_app 是一个显式开关**` 条；`:131-155` 重写 | ✅ |
| ↳ 须写明「本批只到最小闭环」 | `deploy.md:139`「**但覆盖面只到最小闭环。**」 | ✅ |
| ↳ 须写明「全站收口在 M5.2」 | `deploy.md:143`「**全站收口归 M5.2。**」 | ✅ |
| ↳ 须写明「生产切换的前置」 | `deploy.md:145-152`「**打开前置（逐条满足才动）：**」共 4 条 | ✅ |
| ↳ 须写明「回滚」 | `deploy.md:154-155`「**回滚：把 `DB_APP_ROLE_RUNTIME` 从 `.env` 移除…重启 app 服务即可**」 | ✅ |

**as-built 陈述的实物反查**（文档说的是不是真的，逐条自己数）：

| 文档陈述 | 我的独立核对命令 | 实测 | 一致 |
|---|---|---|---|
| 数据层 6 个模块 | `ls src/lib/db/*.ts \| wc -l` | 6（app-role / prisma / privileged / runtime / tenant-entry / tenant-scope） | ✅ |
| 引导白名单 5 个文件 | `grep -rn "from '…db/privileged'" src/` | 5，且与 spec D-13 的 as-built 名单**逐个同名** | ✅ |
| 非 db 层 raw SQL 共 6 处 | `grep -rno "queryRaw\|executeRaw" src/ \| grep -v "^src/lib/db/"` | 6：`generate-candidates:144` · `evaluate-creator:105` · `search-kols:69,76` · `sync:213,227` — 与文档逐项拆分完全吻合 | ✅ |
| 代理三分支 | 读 `src/lib/db/prisma.ts:27-32` | `resolveDelegate()`：① 有 scope → `scope.tx`；② 开关未开 → `getRuntimeDb()`；③ 开关开 → 抛 `MissingTenantScopeError` | ✅ |
| `withTenant` 三条嵌套语义 | 读 `src/lib/db/tenant-scope.ts` | `:165` `set_config('app.tenant_id', $1, true)`；`:150` 嵌套传选项抛 `NestedTransactionOptionsError`；`:154` 跨租户抛 `CrossTenantNestingError`；同租户复用外层 | ✅ |
| 回滚「无数据面变更」 | 读 `src/lib/db/runtime.ts:60-70` | `getRuntimeDb()` 每次调用重解析连接串、按串缓存；未设开关 → `url=null` → 回落 schema datasource 的 `DATABASE_URL`。移除 env 键重启即回原状，确无迁移/数据面动作 | ✅ |
| 入口面 78 / 已包裹 3 | 见 §2.3 我自己重跑的普查 | 78 / 3 | ✅ |

**反向漂移复核**（`role-context/evaluator.md`「文档新鲜度 clause」）：
`grep -rn "M5\.1\b" docs/dev/{architecture,agent-architecture,deploy}.md | grep -v M5.1b`
→ 仅 3 处残留，全部是**合法引用**（清单文件名 `M5.1-uncovered-entrypoints.md` ×2、
「比 M5.1 之前好」的历史对比 ×1）。无一处仍把注入机制归属于「未来的 M5.1」。

### ② doc-freshness 断言扩展 — **PASS**

新增 7 条，落在 `tests/unit/architecture-doc-freshness.test.ts:552-639`。
`git show --numstat 3baf293 -- tests/unit/architecture-doc-freshness.test.ts` → **`179  0`**
（179 增、**0 删**）：纯增量，未削弱任何既有断言。文件基线 41 passed。

acceptance 对判据形态有三条硬要求，逐条核：

| 要求 | 核对 | 判定 |
|---|---|---|
| **判据不带左括号** | 7 条的 needle / pattern 均无「代码调用式左括号」：`'租户注入机制'`、`'MissingTenantScopeError'`、`/全仓非 db 层共 (\d+) 处/`、`/入口面实测 (\d+) 条、已包裹 (\d+) 条/`、`` `${m}.ts` ``、`'\| **合计**'` 等。仅有的两处全角 `（` 是被检索**散文原句自带**的字符（如 deploy.md 旧告警的精确引用），不是 `stepCountIs(` 那种码形括号——正是 §8 要求的形态 | ✅ |
| **含 `docs/` 范围** | `collectDocs('docs')` 递归 `readdirSync` 收全部 `.md`，非 `git grep`（避开「新文件未 commit 恒空绿」）。**实测佐证**：我的变异 M1 让断言报出 `docs/dev/agent-architecture.md:106`，证明扫描器确实抵达 `docs/dev/` | ✅ |
| **先验活**（audit-methodology §8） | 第一条用例即检测器活性证明（`:553-561`）：要求 `grepDocs` 能看见两个**已知在场**的串。另外 4 条计数断言各自内嵌 `expect(anchors.length).toBeGreaterThan(0)`，等价于逐条自验活 | ✅ |

**三条「陈旧句零残留」needle 不是空炮** —— 我回 `git show 3baf293` 的删除行核对，
三个 needle 全部是本批之前**真实在场**的原文：
agent-architecture.md 的旧未来态注入机制句（被删行）·
`'在租户变量注入（spec D-7 / F009）落地之前不要打开'`（deploy.md 被删行）·
`'注入机制与引导白名单归 M5.1'`（architecture.md:109 被删行）。

### ③ 未覆盖入口清单落盘（须由普查产出） — **PASS**

acceptance 的核心要求是「**由普查产出，不是手写**」。我用比「看它有没有脚本」更强的判据验证：

```
node --import tsx  →  renderMarkdown(runCensus()) === readFileSync('docs/specs/M5.1-uncovered-entrypoints.md')
IDENTICAL = true
```

落盘文件与**当场重新生成**的结果**逐字节相同**。这排除了「先手写、再补个脚本装样子」的形态。

**我自己重跑普查**（`npm run census:entrypoints`，非引用其数字）：

```
[census] 入口合计 78（已覆盖 3 / 未覆盖 75）
  api-route: 29   page: 6   routine: 4   script: 39
```
扫描 102 / 入口面 78 / 已覆盖 3 / 未覆盖 75，`scannedTotals = {api-route:31, page:18, routine:4, script:49}`，
`adminPageCount = 13`。**与清单表格逐格一致**，与 project-status 记载一致。

| acceptance 子项 | 核对 | 判定 |
|---|---|---|
| 给出普查命令 | 清单头写明 `npm run census:entrypoints -- --write`；`package.json:50` 确有该 script | ✅ |
| 每条标注类型与租户来源 | 未覆盖 75 条按 4 个 kind 分节，每行带「会话面 / 无会话面 / 引导面」 | ✅ |
| 与 spec 底数对账 | 清单 §「与 spec 底数的对账」逐条登记，**含如实登记差异**：例程 spec 写 3 / 实测 4（按 ROUTINES 注册表而非 routines/ 目录计数）、script spec 写 ~20 / 实测入口面 39。我核 `src/lib/jobs/scheduler.ts:85-` 确有 4 条注册（health-scan / nightly-screen / kol-sync / weekly-draft），kol-sync 执行体确在 `lib/kol-sync/sync.ts` —— 对账属实 | ✅ |
| 引导面不误列为待办 | 2 条 auth route 标「引导面」并在判定口径段写明「是**不该做**的登记项」 | ✅ |

**已覆盖 3 条我逐条反查了真伪**（假阳性是危险方向）：

- `src/app/api/nav-badges/route.ts` — 单个 GET handler，整体包在 `withSessionTenant` 内 ✅
- `src/app/admin/knowledge/page.tsx:30` — `withSessionTenant` 包住 `getKnowledgePageData`，
  且源码注释交代了「作用域止于数据装配」的边界 ✅
- `scheduler.ts` `health-scan` 块 `:94` — 确有 `withTenant(tenantId, …)`；
  其余三条例程块确无 → 3 covered / 3 uncovered 判定属实 ✅

**普查自身的量尺问题已被处理**：`SELF` 常量（`m51b-entrypoint-census.ts:133`）把普查模块自己排除，
理由写在源码里。这与 `.auto-memory` 记载的「普查模块会把自己算进去」是同一个坑，此处已避开。

### ④ 既有钉的状态复核 — **PASS**（且我用了比被验方更严的判据）

acceptance 要求「在本批下口径不变；无理由改动视为越界」，须用 git 实物核。

> **判据分歧，如实登记：** commit `3baf293` 正文声称的证据区间是 `af8ca20..HEAD`。
> 我核 `git log` 发现 **`af8ca20` 位于本批中段**（在 F004 与 F005 之间），
> 该区间**不覆盖 F008 / F002 / F003 / F004** 四条 feature。故我改用真正的批次起点重核。

批次起点 = `90b61f8`（`chore(M5.1b-TENANT-INJECTION): planning 完成 — spec 落盘 + 8 features，进入 building`）。

```
git log  --oneline 90b61f8..HEAD -- tests/integration/rls-negative-suite.test.ts tests/unit/project-scope-census.test.ts
  → 无输出
git diff --stat    90b61f8..HEAD -- (同上两文件)
  → 无输出
git log -1 --oneline -- (各自)
  → 2ff56d2 feat(M5-AUTH-RLS-F011)  （两文件末次改动均远早于本批）
```

**在全批范围下依然零改动** —— 结论与被验方一致，但依据比它给的更强。

口径内容我也读了实物，确认「不变」是**仍然正确**而非仅仅「没动」：

- `tests/integration/rls-negative-suite.test.ts:169-176` §0 —
  `expect(isPrivilegedConnection(info), …).toBe(true)`，且附带「切换那天这条会红，逼你回来改文件头」的自毁式注释 ✅
- `tests/unit/project-scope-census.test.ts` 文件头 —
  「现在它**仍是主力**，不是"有 RLS 了可以松一口气的二线"」原样保留 ✅

两条钉在我的全量 vitest 中均通过 → 运行时默认未切的前提在批末仍成立。

### ⑤ 变异证活 — **PASS**（我另挑了被验方没挑的断言）

被验方 commit 声称做了 4 条变异（白名单计数 / 新增 db 模块 / deploy 入口面计数 / 陈旧句复活）。
按指令我**不复现它挑的那条**，另选两条它没碰过的断言实打，全部在本 worktree 实跑：

**M1 — 「非 db 层 raw SQL 处数」断言（被验方未挑）**
新建 `src/lib/match/f007-eval-rawsql-probe.ts`（含一处 `prisma.$queryRaw`），模拟「新增 raw SQL 站点但忘翻文档」：

```
× 非 db 层 raw SQL 处数 = 实物（新增 raw SQL 忘翻文档 → 红）
AssertionError: docs/dev/agent-architecture.md:106 的 raw SQL 处数与实物（7）不符:
  expected 6 to be 7
Tests  1 failed | 40 passed (41)
```
→ **红，且点名 `文件:行`**。同时证明扫描器确实覆盖到 `docs/dev/`。
还原：`rm` 该新建文件 → `git status` 仅剩我的 `.evalscratch/` 与 `node_modules` 软链。

**M3 — 「未覆盖清单合计行」断言（被验方未挑）**
把清单合计行的扫描数 `**102**` 改成 `**103**`：

```
× 未覆盖清单文件的合计行 = 普查实测（忘重跑 `npm run census:entrypoints -- --write` → 红）
AssertionError: 合计行与普查实测不符：| **合计** | **103** | **78** | **3** | **75** |:
  expected [ 103, 78, 3, 75 ] to deeply equal [ 102, 78, 3, 75 ]
Tests  1 failed | 40 passed (41)
```
→ **红，且打印出违规行全文**。

**还原对账（不得用 `git checkout`，已遵守）：** 全部用反向编辑还原，随后 `shasum -a 256 -c` 逐位对账：

```
docs/dev/deploy.md: OK               docs/dev/architecture.md: OK
docs/dev/agent-architecture.md: OK   docs/specs/M5.1-uncovered-entrypoints.md: OK
src/lib/kol-sync/sync.ts: OK         tests/unit/architecture-doc-freshness.test.ts: OK
```
还原后全量 `npx vitest run` → 148 files / 1869 tests 全绿。
主仓 `/Users/yixingzhou/project/newkolmatrix` 的 `src/` 与 `docs/dev/` **一个字节未动**（我只往 `docs/test-reports/` 写本报告）。

### ⑥ backlog 销账口径 — **PASS**

| 要求 | 核对 | 判定 |
|---|---|---|
| Generator 不改 backlog.json | `git log --oneline 90b61f8..HEAD -- backlog.json` → **无输出**（全批零改动） | ✅ |
| 条目已是 `BL-M52-TENANT-COVERAGE` | `backlog.json:3` `"id": "BL-M52-TENANT-COVERAGE"`；全文无 `BL-M51-TENANT-INJECTION` 残留 | ✅ |
| handoff 里说明本批兑现边界 | `progress.json.generator_handoff.backlog_reconciliation` 存在，明确区分「已成立的机制层」与「未做的覆盖面（78 入口 / 3 已包 / 75 未覆盖）」，并写明生产行为零变化 | ✅ |

> 销账动作本身归 Planner 的 done 阶段，不在 F007 交付面内。

### ⑦ tsc / lint / 全量 vitest 绿 — **PASS**

见 §1，三条均由我独立实跑，非引用。

---

## 3. 登记的盲区（不改变 F007 判定，供 M5.2 与后续批次接手）

### [BLIND-1] 计数锚点「被删除」时断言看不见（**实跑证实**）

「入口面实测 N 条、已包裹 M 条」这句在 docs/ 里有 **3 份**副本：
`deploy.md:139` · `architecture.md:109` · `architecture.md:708`。
断言逻辑是「至少 1 条锚点 + 逐条值正确」，因此**删掉其中一份不会红**。

我实打验证 —— 把该句从 **`deploy.md`**（三处中运维后果最重的一处：它就是那条
「现在打开会怎样」的告警）整句摘掉：

```
npx vitest run tests/unit/architecture-doc-freshness.test.ts  →  41 passed
npx vitest run（全量）                                        →  148 files / 1869 tests 全绿
```

**全仓无一条测试翻红。** 已按反向编辑还原并 shasum 对账 OK。

这正是 `audit-methodology.md` §7 规律 2 的同族形态（钉子覆盖面 = 它守护的那句话的**真子集**）：
断言守住了「数字别过期」，没守住「这句话别消失」。
建议 M5.2 把锚点断言从「≥1 处且值对」改成「**指定文件各自必须有**」（登记表形态）。

### [BLIND-2] 普查的租户来源分类器看不见直接 `new PrismaClient()`

`classifyTenantSource()`（`m51b-entrypoint-census.ts:71-86`）只认 5 种标记：
`withSessionTenant`/`requireSession*` · 零参 `buildToolContext` · `systemContext`/`systemTenantId` ·
import `db/privileged` · import `db/prisma`。
**自建 `new PrismaClient()` 一种都不匹配 → 判 `none` → 被剔出入口面。**

我实测量化，共 **5 个真带租户读写库的文件不在 78 条里**：

| 文件 | 情况 | 为何漏 |
|---|---|---|
| `scripts/test/m2a-f001-db-verify.ts` | 真做 `data: { tenantId: proj.tenantId, … }` 的**写入** | 扫到了，判 `none` |
| `scripts/test/m2a-readiness-db-state.ts` | `prisma.tenant.count()` | 扫到了，判 `none` |
| `scripts/test/m2a-readiness-residue-probe.ts` | 自建 client | 扫到了，判 `none` |
| `scripts/test/rv/rv-kpi.mjs` | `where: { tenantId: dev.id, … }` 计数 | `.mjs` **未进扫描面**（只扫 `.ts`） |
| `scripts/test/rv/rv-census.mjs` | 6 张表按 `tenantId` 计数 | 同上 |

**严重度评估（为什么不判 PARTIAL）：** 这 5 个都用自己的 `PrismaClient`，
**完全不经 `src/lib/db` 的 ALS 代理**，故开关打开时它们既不会抛 `MissingTenantScopeError`
也不参与 fail-closed 面。deploy.md 那句承重的
「**现在打开 = 那 75 条入口全部当场抛 `MissingTenantScopeError`**」**因此没有被高估**——
这是 acceptance ③ 与 ① 真正要交付的数字，它是准的。

漏的是清单作为「M5.2 工作面台账」的**完备性**：清单的「判定口径」段列了 5 条分类规则，
但**没有登记「自建 PrismaClient 不计入」这条已知边界**。
（对比：`db-layer-importer-census.test.ts` 就把自己的已知边界「字符串拼接的动态路径扫不到」
写进了文件头 —— 同批内已有更好的范例。）

**附带的两条潜在（当前无实例，故非缺陷）：** 普查只扫 `src/app` 下的 `route.ts` / `page.tsx`，
不扫 `layout.tsx`（现有 2 个 layout 实测均不碰库）与 `.mts`（现有 1 个实测不碰库）；
将来若有 layout 或 `.mjs` 入口碰库，会静默不进台账。

### 已核而**不**构成盲区的一项

`src/instrumentation.ts` 直接碰库（`runConnectionRoleSentinel(getRuntimeDb())`）且不在普查台账里。
但我核到它由**另一道互不派生的钉**守着 —— `tests/unit/db-layer-importer-census.test.ts:45`
把它登记为运行时 client 的合法 importer。且它取的是 `getRuntimeDb()` 本体而非代理，
开关打开时不会 fail-closed，确实不属于 M5.2 的接线工作面。**故不计为漏项。**
`src/middleware.ts` 实测零 DB 引用，同样不构成漏项。

---

## 4. 判据活性自证（本报告自己的「0 findings」不许空口）

本报告出现的否定型结论，逐条附上「判据能看见目标」的证明：

| 否定结论 | 活性证明 |
|---|---|
| 「陈旧句零残留」 | 不采信被验方的断言结果，我另用 `grep -rn "M5\.1\b" docs/dev/…` 正向搜，**确实搜出 3 处命中**（经核为合法引用）→ 判据非瞎 |
| 「既有钉零改动」 | 同一条 `git log` 命令换成不限路径时**有大量输出**；限定这两个路径才为空 → 空输出是真空，不是命令写错 |
| 「backlog.json 零改动」 | 同上区间对其他路径有输出 |
| 「doc-freshness 断言有鉴别力」 | M1 / M3 两次实跑翻红并点名，非只读推断 |
| 「删锚点不会红」 | 实跑全量 1869 tests 确认全绿，非推断 |

---

## 5. 未执行项

| 项 | 原因 |
|---|---|
| L2（真实外部服务 / 计费 / 生产写入 / 部署） | **未授权**。且 F007 交付物为文档 + 测试 + 普查脚本，无 L2 依赖面 |
| `npm run test:visual` | spec §7 登记为不计分（本机恒 15 passed / 2 failed，归 `BL-VISUAL-DATA-ISOLATION`）；且 F007 `src/` 零改动 → 零 UI 变更。见 §1 |
| `npm run rls:e2e` | 属 F006 判据，不在本次分派的 F007 + L1 范围内。未跑，不做任何断言 |

---

## 6. 附：本次验收改动过的文件（全部已还原）

| 文件 | 变异 | 还原方式 | 对账 |
|---|---|---|---|
| `src/lib/match/f007-eval-rawsql-probe.ts`（新建） | M1 | `rm` | `git status` 无残留 |
| `docs/dev/deploy.md` | M2（摘锚点句） | 反向编辑 | `shasum -a 256 -c` → OK |
| `docs/specs/M5.1-uncovered-entrypoints.md` | M3（合计行 102→103） | 反向编辑 | `shasum -a 256 -c` → OK |

全程**未使用 `git checkout` 还原**。工作树终态 `git status --short` 仅剩
`?? .evalscratch/`（我的临时脚本）与 `?? node_modules`（符号链接）。
主仓 `src/` 与 `docs/dev/` 零改动。

---

*Evaluator（隔离 subagent） · 2026-08-08 · 被验 HEAD `8c5fee1`*
