# M5.1c-CENSUS-SCANNER · 复验-1 **回归面**报告

> 角色：独立 Evaluator（隔离上下文，独立 worktree）
> 被验 HEAD：`de2b27cc1d30e6c2900c0a1e0e62d07dcf567204`
> 修复前基线：`bb28842`（首轮验收产物落盘后）· 本批起点：`bf26603`（立项，status=building）
> 工作区：`wt-rv1b-reg`（只读产品代码；仅新增 `tests/tools/rv1b/` 探针与本报告）
> 落盘方式：**增量**——每验完一块即追加该块判定与实跑输出，不攒到最后统一写。

**本报告的射程**：首轮判 **PASS** 的 acceptance 条目在 HEAD 上**重放**是否仍成立，
以及**修复动作有没有带坏别的东西**。首轮判 PARTIAL 的两条（F001 ② / F002 ⑤）由另一路复验方负责，
本报告**不重判**它们，只在它们与回归面相交时如实登记。

---

## 0. 结论速览

| 块 | 覆盖 | 判定 |
|---|---|---|
| A · L1 全量 + spec §6 不计分项 | tsc / lint / vitest / test:visual | **PASS** — 见 §1 |
| B · F001 首轮 PASS 条目重放 | ① ③ ④ ⑤ ⑥ ⑦ | **PASS**（6/6 仍成立）— 见 §2 |
| C · F002 首轮 PASS 条目重放 | ① ② ③ ④ ⑥ | **PASS**（5/5 仍成立，② 附 W1）— 见 §3 |
| D · 新增文件污染检查 | 入口普查计数 · 其它钉扫描面 | **PASS**（对照实验零差异）— 见 §4 |
| E · 被修复顺带改坏的既有断言 | 鉴别力是否流失 | **PASS**（未发现，附 W2）— 见 §5 |

> **回归面判定 = PASS（无回归）。** 完整表述与两条 soft-watch 见 §6。
> 首轮判 PARTIAL 的 F001 ② / F002 ⑤ **不在本报告射程**，由另一路复验方判定。

---

## 1. 块 A —— 环境前置与 L1 全量

### 1.1 环境（L1 误报排除）

按 `framework/patterns/testing-env-patterns.md` §3，跑 tsc 前先重生 Prisma Client，
排除「`Property 'x' does not exist on PrismaClient`」这一类**本机环境态**误报：

```
$ node -v && npm -v
v25.7.0
10.8.2

$ npx prisma generate
prisma exit=0
```

### 1.2 L1 三项（HEAD 原样，自跑，不采信任何报告里的数字）

```
$ npx tsc --noEmit         → tsc_exit=0    （输出 0 行）
$ npx next lint            → lint_exit=0   ✔ No ESLint warnings or errors
$ npx vitest run           → vitest_exit=0
   Test Files  148 passed (148)
        Tests  1888 passed (1888)
     Duration  15.44s
```

**块 A（L1 部分）判定：PASS。** 三项全绿，与 F001⑦ / F002⑥ 所要求的「tsc/lint + 全量 vitest 绿」相符。
注：`next lint` 打印的 deprecation 提示是 Next.js 16 迁移预告，非本批引入，退出码 0。

### 1.3 spec §6 登记的不计分项 —— `npm run test:visual`

首跑 `visual_exit=1`，但**原因是环境而非回归**：本 worktree 是全新签出，没有 `.next` 产物，
Playwright 的 `webServer` 起不来（`ENOENT: no such file or directory, lstat '.next/static'`）。
按 L1 误报排除纪律，先补齐前置再测：

```
$ npx next build      → build_exit=0
$ npm run test:visual
  # 前置 fixtures（vitest）
  Test Files  2 passed (2)
       Tests  4 passed (4)
  # Playwright
  ✓ = 27    ✘ = 2
  ✘ 13 [chromium] tests/visual/dashboard.spec.ts:8:5  › today dashboard visual baseline
  ✘ 24 [chromium] tests/visual/workbench.spec.ts:43:5 › project env=match visual baseline
```

**判定：不计分，且与登记基线一致。** spec §6 登记的是「本机恒 15 passed / **2 failed**」——
**失败条数 2 与失败身份**（都是依赖库内数据的快照基线，归 `BL-VISUAL-DATA-ISOLATION`）与登记相符；
通过数从 15 涨到 27 是 M5.1b 之后陆续新增视觉用例所致，非本批产物。本批 UI 改动为零
（`src/` 仅 `lib/db/privileged.ts` 的注释，见 §3.4），不构成对该两条的归因。

---

## 2. 块 B —— F001 首轮 PASS 条目在 HEAD 上重放

### 2.1 acceptance ⑤ · GUARDED 期望清单不得变更 —— **仍成立（PASS）**

**(a) 清单逐字未变**（不是眼看 diff，是抽块做 sha256；探针 `tests/tools/rv1b/rv1b-guarded-frozen.mjs`）。
覆盖团队要求的两个口径：本批起点 `bf26603` 与修复前基线 `bb28842`，另加修复前一刻 `fc2dd20`：

```
rev        sha256(前16)      行数
bf26603    c49240226f6677ad  37
bb28842    c49240226f6677ad  37
fc2dd20    c49240226f6677ad  37
HEAD       c49240226f6677ad  37

DISTINCT_HASHES=1  → GUARDED 全程逐字未变
SELFTEST: 人为改一条期望路径 → hash 不同（判据活）
```

**(b) 与实物相符 —— 用独立 oracle 复算，不复用被验实现**
（探针 `tests/tools/rv1b/rv1b-guarded-oracle.mjs`：走 TS 内置的 `ts.preProcessFile()` 预处理器，
与被验实现的 `createSourceFile + forEachChild` 是两条不同代码路径）：

```
src/ 文件数 = 384

【src/lib/db/runtime】 相符 ✓
  期望(3)：src/instrumentation.ts, src/lib/db/prisma.ts, src/lib/db/tenant-scope.ts
  独立实测(3)：同上   多出：无   缺失：无

【src/lib/db/privileged】 相符 ✓
  期望(5)：src/app/api/auth/[...nextauth]/route.ts, src/app/api/auth/register/route.ts,
           src/lib/agent/context.ts, src/lib/auth/index.ts, src/lib/auth/register.ts
  独立实测(5)：同上   多出：无   缺失：无

SELFTEST 正向：import src/lib/db/prisma 的文件数 = 67（须 >0）
SELFTEST 负向：import 一个不存在的模块的文件数 = 0（须 =0）
RESULT=ALL_MATCH
```

清单未被改动、实测与期望逐条相符，且「改期望去迁就实现」这一禁止形态没有发生（哈希全程同一）。

### 2.2 acceptance ④ · 等价性审计 —— **仍成立，仍 0 分歧（PASS）**

HEAD 上原样重跑 `scripts/test/m51c-scanner-equivalence.ts`：

```
扫描器等价性审计（M5.1c F001 / spec D-4）
  基线（正则）：bf26603:tests/unit/db-layer-importer-census.test.ts
  当前（AST）：工作树 tests/unit/db-layer-importer-census.test.ts

[自测] 正向 ✓（探针 "import{a}from'./probe-known-divergence';"：老=[] 新=["./probe-known-divergence"]）
[自测] 负向 ✓（两侧一致的输入未误报）

扫描面：384 个 .ts/.tsx
specifier 总数（多重集，未去重）：老 1241 / 新 1241
有差异的文件：0

✓ 全仓 src/ 逐文件多重集完全一致——换代未改变对真实代码的扫描结果。
```

判据自身的活性自测（正负双向）随跑随过，多重集未去重，符合 spec D-4 ①②③。

**附带确认：被审计的实现本体在 fix 轮确实没动**（否则首轮的 0 分歧结论要重新起算）。
探针 `tests/tools/rv1b/rv1b-impl-frozen.mjs`，抽函数体做 sha256：

```
函数                    e509e06       fc2dd20       HEAD
importSpecifiers      70a04e4673e7  70a04e4673e7  70a04e4673e7   全程未变
resolveToSrcModule    aae37ebd4f9e  aae37ebd4f9e  aae37ebd4f9e   全程未变
actualImporters       aaddee13ec89  aaddee13ec89  aaddee13ec89   全程未变
collectSources        2275fb1ec315  2275fb1ec315  2275fb1ec315   全程未变

SELFTEST：把 isStringLiteralLike 改成 isStringLiteral → 哈希 不同（判据活）
```

与修复清单自陈的「AST 实现本体不动」一致 —— 这一条**核实通过**。

### 2.3 acceptance ① · 五个遍历面各有必红变异 —— **仍成立（PASS）**

首轮验收方的变异台 `tests/tools/m51c-evaluator-f001-mutations.mjs` 把基线 sha 硬编码为修复前的
`a769878…`，fix 轮改过该文件后它会直接抛「开工时 sha 就不是基线」。**该产物是历史记录，不动它**；
复验方另起一台 `tests/tools/rv1b/rv1b-mutations.mjs`，基线换成 HEAD 的 `efc46ad…`，
沿用同一套三种红法口径与「反向编辑 + sha256 对账」还原纪律。

```
[基线] sha256=efc46adedd427d40…  PASS-ALL（全绿）  跑 21 条

R1  摘掉 visitor 分支① ImportDeclaration        → ASSERT-RED（红 6 / 跑 21）
      红：🔒 遍历面① ImportDeclaration：具名 / 默认 / 侧效应 / type / attributes   ← 对应那条
R2  摘掉 visitor 分支② ExportDeclaration        → ASSERT-RED（红 2 / 跑 21）
      红：🔒 遍历面② ExportDeclaration：re-export / export * / export * as ns / export type
R3  摘掉 visitor 分支③ ImportEqualsDeclaration  → ASSERT-RED（红 1 / 跑 21）
      红：🔒 遍历面③ ImportEqualsDeclaration：import Foo = require(...)
R4  摘掉 visitor 分支④ 动态 import()            → ASSERT-RED（红 4 / 跑 21）
      红：🔒 遍历面④ 动态 import()：含第二参数与尾逗号
R5  摘掉 visitor 分支⑤ CJS require()            → ASSERT-RED（红 1 / 跑 21）
      红：🔒 遍历面⑤ CJS require()：认调用，不认同名的别的东西
```

五个分支**各自红掉自己对应的那条用例**，无一漏网 → ① 在 HEAD 上重放仍成立。

**顺带核实 fix 轮新写进注释的红格数（铁律 13 抽查）：** commit `ef80538` 与文件注释声称
「摘 ImportDeclaration → 红 6 · 摘 ExportDeclaration → 红 2 · 摘 ImportEquals → 红 1 ·
摘动态 import() → 红 4 · 摘 require() → 红 1」。复验方独立实测 **6 / 2 / 1 / 4 / 1** —— **逐个吻合**。
首轮 N-1 点名的那句假陈述（「且只红它」）已被删除，替换上去的数字经得起复算。

### 2.4 acceptance ⑥ · 保留下来的旧用例逐条重验 —— **仍成立（PASS）**

fix 轮**没有增删任何用例**，只改了两条的标题与三条探针的输入：

```
$ 用例条数    fc2dd20: 19 个 it(...)   HEAD: 19 个 it(...)     （另 +2 条由 GUARDED 循环生成 = 跑 21）
$ 标题差异（fc2dd20 → HEAD）
< 🔒 字面出现不算 import：注释 / 字符串 / 同名标识符
> 🔒 字面出现不算 import：注释 / 字符串 / 同名标识符（守多抓方向）
< 📜 语料：正则时代六种漏抓/误捕形态在 AST 下的实际表现（无独立鉴别力，见上方注释）
> 📜 语料：正则时代七种漏抓/误捕形态 (a)–(g) 在 AST 下的实际表现（无独立鉴别力，见上方注释）
```

**全部 21 条的鉴别力矩阵**（复验方 13 条变异，每条都实跑；「能让它红的变异」栏非空 = 该用例活着）：

| # | 用例 | 能让它红的变异 |
|---|---|---|
| 1 | 预扫确实取到了 specifier | R1 R4 R9 |
| 2 | src/ 下确实扫到了文件 | **R8** |
| 3 | src/lib/db/runtime 清单与实物一致 | R1 R8 R9 **R10 R11** |
| 4 | src/lib/db/privileged 清单与实物一致 | R1 R8 R9 |
| 5 | 🔒 遍历面① ImportDeclaration | R1 R7 R9 |
| 6 | 🔒 遍历面② ExportDeclaration | R2 R7 R9 |
| 7 | 🔒 遍历面③ ImportEqualsDeclaration | R3 R7 R9 |
| 8 | 🔒 遍历面④ 动态 import() | R4 R7 R9 |
| 9 | 🔒 遍历面⑤ CJS require() | R5 R7 R9 |
| 10 | 🔒 字面量形态：反引号无替换要认 | R4 **R6** R7 R9 |
| 11 | 🔒 ScriptKind 随传入文件名走 | R1 R7 R9 |
| 12 | 🔒 字面出现不算 import（**fix 轮改过标题**） | **R7** |
| 13 | 🔒 已登记边界仍如实（故意断言抓不到） | **R7** |
| 14 | 📜 语料 (a)–(g)（**fix 轮换过三条输入**） | R1 R2 R7 R9 |
| 15 | 路径归一：三种写法指向同一模块 | **R12** |
| 16 | 扫描器不空转：文件确实读到了内容 | **R13** |
| 17 | 🔒 ① 政策句不得被删 | **R13** |
| 18 | 🔒 ④ 注释只许指路，不许承诺覆盖面 | R13 · 另见 §3.3 |
| 19 | 🔒 ② 三条被证伪陈述不得复活 | 见 §3.3（叙述面正向变异） |
| 20 | 🔒 ②b 覆盖面自洽 | 见 §3.3 |
| 21 | 🔒 ③ 不得再写会漂的计数 | 见 §3.3 |

**21 / 21 条全部可证活，无恒真用例。** 其中前 18 条由本节 13 条变异覆盖，
第 19–21 条属叙述面负向断言（断「某文字不得出现」），须用**写回禁词**的正向变异才能证活 → §3.3。

**关键回归结论：被 fix 轮改动的那两条用例，鉴别力没有流失。**
- 第 12 条（改了标题，加「守多抓方向」）：R7（把任意字符串字面量都当 specifier）**红了它**，
  且这正是它声称守的方向。改名没有让它失去原有鉴别力。
- 第 14 条（换掉 (e)(f)(g) 三条探针输入）：R1 / R2 / R7 / R9 均能红它。
  它自陈「无独立鉴别力」——R9（判据全死）确实红它，说明它并非恒真，只是不提供**独立于**遍历面的鉴别力，
  与注释的自陈相符，未夸大。

### 2.5 acceptance ③ · 「故意断言抓不到」的边界用例 —— **仍成立（PASS）**

该用例（矩阵第 13 条）在 HEAD 上**实跑**（21 条基线全绿里含它），且 R7 能红它 →
它不是一句声明，是活的断言。fix 轮未改动它。

### 2.6 变异台还原对账

13 条变异全部走「反向编辑 + sha256 逐位对账」，无一使用 `git checkout`：

```
全部变异结束，tests/unit/db-layer-importer-census.test.ts
sha256 = efc46adedd427d40c0b844a1541b2ff27519e3815322071f77897b494f99604c
✓ 与基线逐位相同（工作树干净）
```

---

## 3. 块 C —— F002 首轮 PASS 条目在 HEAD 上重放

### 3.1 acceptance ③ · `🔒④` 黑名单保留 + 仍有鉴别力 —— **仍成立（PASS）**

**(a) 「保留」半边：黑名单与 CLAIMS 逐字未动**（探针 `tests/tools/rv1b/rv1b-blacklist-frozen.mjs`）：

```
【🔒④ 黑名单字面量】
  bf26603   4014bb24ea25cdd8
  8cc63d2   4014bb24ea25cdd8
  bb28842   4014bb24ea25cdd8
  fc2dd20   4014bb24ea25cdd8
  HEAD      4014bb24ea25cdd8
  DISTINCT=1 → 全程逐字未变 ✓

【FALSIFIED_CLAIMS 数组】  五个 rev 全部 9235826ecae12be5   DISTINCT=1 → 全程逐字未变 ✓

黑名单实物（HEAD）：const overclaims = ['即红并点名', '不可能再对不上', '机械守住'].filter
SELFTEST：改黑名单里一个字 → hash 不同（判据活）
```

> ⚠ **本探针首版是死的，如实登记**：初版锚点写成 `end: '];'`，而实物是 `].filter(`，
> 五个 rev 全部抽到 `ABSENT`，却因「五个 ABSENT 彼此相同」输出了 `DISTINCT=1 → 全程逐字未变 ✓`。
> 即 audit-methodology §8 那个形态：**判据抽不到东西时会给出「一致」的假绿**。
> 已改正锚点，并加一道 `ABSENT → 立即 exit 2` 的硬闸。上表是改正后的读数。

**(b) 「鉴别力」半边：往现行叙述写回黑名单词 → 必须红并点名**
（探针 `tests/tools/rv1b/rv1b-narrative-mutations.mjs`；被变异的是产品文件
`src/lib/db/tenant-scope.ts`，每条变异后反向编辑还原 + sha256 对账，**未使用 `git checkout`**）：

```
[基线] src/lib/db/tenant-scope.ts sha=7cd74122cf230ae5…  跑 21 条  红 0 条

N1 黑名单词①「即红并点名」写回现行叙述      → 红 1  期望红 → 符合 ✓
     红：🔒 ④ 被守文件的注释只许指路，不许承诺本组的覆盖面
     点名：…现行叙述里出现了承诺覆盖面的措辞：即红并点名
N2 黑名单词②「不可能再对不上」写回现行叙述  → 红 1  符合 ✓  点名：…：不可能再对不上
N3 黑名单词③「机械守住」写回现行叙述        → 红 1  符合 ✓  点名：…：机械守住
N4 【盲区对照】同词写进 `// >` 更正行        → 红 0  期望不红 → 符合 ✓（S2 已登记盲区，行为与登记一致）

src/lib/db/tenant-scope.ts
sha256 = 7cd74122cf230ae565101879b464105ddae8b814b848b4d5756d6111c26aaf13
✓ 与基线逐位相同（产品文件已还原，工作树干净）
```

三个黑名单词**逐个红且逐个点名**，已登记盲区的行为也与登记一致 → ③ 重放仍成立。

**补完 §2.4 鉴别力矩阵的第 19–21 条**（叙述面负向断言，须用写回禁词的正向变异证活）：

```
N5 🔒② 复活被证伪陈述①「只交付单层语义」   → 红 1：🔒 ② …一条都不得复活   点名：① 本模块此刻只交付「单层」语义
N6 🔒② 复活被证伪陈述②「零 withTenant 调用点」→ 红 1                      点名：② 产品代码（src/）零 withTenant 调用点
N7 🔒② 复活被证伪陈述③「没有嵌套守卫」      → 红 1                      点名：③ 现在没有嵌套守卫 / F002 落地前不要嵌套调用
N8 🔒③ 写回会漂的计数「15 处 withTenant 调用点」→ 红 1：🔒 ③ …会漂的计数  点名：15 处 withTenant 调用点
R14 把一条 claim 的 patterns 清空            → 红 1：🔒 ②b 覆盖面自洽
R15 给 FALSIFIED_CLAIMS 加第 4 条            → 红 1：🔒 ②b 覆盖面自洽
```

至此 **21 / 21 条用例全部可证活，零恒真**。

### 3.2 acceptance ① · 普查后清理 —— **仍成立（PASS）**

HEAD 上重跑普查判据 `tests/tools/m51c-f002-narrative-census.ts`：

```
# rev=WORKTREE
注释行总数=287  现行叙述=238  历史更正(// >)=49
## A 档（Generator 自陈模式）命中：0
## B 档（Evaluator 自拟更宽模式）命中：1
  [B08 任何…都会 / 一律会 / 均会]
     src/lib/db/tenant-scope.ts:73  * …「返回了 client」和「抛了别的错」两种退化都逃不过用例。
```

A 档仍 0 命中；B 档剩 1 条，与首轮 F002 报告登记的 soft-watch **S1** 同一条，非本轮新增。

**一条有力的回归读数 —— 行数账目自己作证：**

| | 首轮（fc2dd20 时） | HEAD | 差 |
|---|---|---|---|
| 注释行总数 | 279 | 287 | +8 |
| **现行叙述** | **238** | **238** | **0** |
| 历史更正 `// >` | 41 | 49 | +8 |

fix 轮净增的 8 行**全部落在 `// >` 历史更正区，现行叙述一行未增**。
这正是 D-2「清理对象是现行叙述」所要的形态：修复没有借更正之名把新叙述塞回现行面。

### 3.3 acceptance ② · `// >` 历史更正记录不得被抹掉 —— **仍成立（PASS），附 soft-watch W1**

本条是 fix 轮的**风险正面**：`9922206` 直接改写了两行既有 `// >`。逐行核对
（探针 `tests/tools/rv1b/rv1b-history-preserved.mjs`，对 `src/lib/db/*.ts` 四个文件、
三个基线各自比对每条 `// >` 的实质内容在 HEAD 是否仍可寻得）：

```
########## 基线 fc2dd20 → HEAD ##########
  src/lib/db/privileged.ts:   基线 6 行 // >  → 找不到实质内容的 1 行
      ✗ ② 这句话把两道钉当成了一道，而两个文件的头注都写着「别互相派生」。
  src/lib/db/tenant-scope.ts: 基线 11 行 → 0 行
  src/lib/db/runtime.ts:      基线 22 行 → 0 行
  src/lib/db/prisma.ts:       基线 0 行  → 0 行
=== 判据活性自测 ===
  正向：人为删掉一行 // > 后，判据看不见了（活）
  负向：未删时判据看得见 = true
```

> ⚠ **本探针也自摆过一次乌龙，如实登记**：初版 `norm()` 保留标点，把
> 「…**能绕过它**；」判成「已被抹掉」——实际新更正段**原文引用了它**，只是省掉句末 `；`。
> 去标点后重判：该行**仍在**。上表是改正后的读数（丢失数由 2 降为 1）。

**对剩下这 1 行的归因（逐条查证，不凭叙述）：**

| 查证项 | 命令与结果 |
|---|---|
| 旧措辞是否被原文引用 | 去标点比对 → **找不到**，确系改写而非引用 |
| 旧措辞本身是否属实 | `grep -c "别互相派生"`：`db-layer-importer-census.test.ts` = **1**，`bootstrap-whitelist-census.test.ts` = **0**。「**两个**文件的头注都**写着**『别互相派生』」作为**字面引用**不成立 |
| 新措辞是否属实 | 前者头注写「【与 F003 白名单钉的分工——**别互相派生**】」；后者头注写「（两者**不得互相派生**）」「刻意**不从对方派生**」。新措辞「两个文件的头注都**要求**二者不得互相派生」→ **成立** |

**判 PASS 的理由：** 本条保护的是「不得**为了清理而抹掉**历史记录，让『为什么改成这样』失去依据」。
实测：**没有任何一条 `// >` 记录被删除**（三个基线下 `// >` 行数只增不减：6→14、11→11、22→22），
更正段整体仍在、`为什么改成这样` 的依据仍在；被改的那一行是**把一句不实的字面引用改准**，
方向与本条意图一致，且新措辞经独立核查成立。

**soft-watch W1（不构成扣分，登记备查）：** 该行改写**未被 fix-1 更正段记录**——
新增的 `// > fix-1 再更正` 只交代了 ① 的数量问题，对 ② 的改写只字未提。
`// >` 区的价值恰在于「记录本身不被静默改写」；建议后续批次补一句说明。

### 3.4 acceptance ⑥ · `src/` 除注释外零改动 —— **仍成立（PASS）**

沿用首轮验收方的独立 oracle `tests/tools/m51c-f002-code-identity.ts`（双判据交叉：
`transpileModule({removeComments:true})` 的 emit + ts scanner 的 token 流）。
**该 oracle 未被修复方改写**——`git log` 显示它只出现在首轮验收 commit `8ef928b`，fix 轮未触碰。

判据活性自测（1 负例 + 4 正例，全 PASS）：

```
PASS  负例：只改注释 → emit 相同 ✓ token 相同 ✓
PASS  正例1：字面量 1→2 → emit 不同 ✓ token 不同 ✓（比对器没瞎）
PASS  正例2：标识符 n→m → 两判据均判不同
PASS  正例3：删一条语句 → 两判据均判不同
PASS  正例4：字符串字面量内容变化 → 两判据均判不同（不会被当成注释）
SELFTEST: ALL PASS
```

两个口径各跑一遍：

```
# 口径 1 —— 本批起点 bf26603..HEAD 改动的 src/ 文件：2 个
  src/lib/db/privileged.ts   emit 相同 = true   token 流 相同 = true  => 仅注释改动
  src/lib/db/runtime.ts      emit 相同 = true   token 流 相同 = true  => 仅注释改动
含真实代码改动的文件数 = 0

# 口径 2 —— 修复前基线 bb28842..HEAD 改动的 src/ 文件：1 个
  src/lib/db/privileged.ts   emit 相同 = true   token 流 相同 = true  => 仅注释改动
含真实代码改动的文件数 = 0
```

**F001 ⑦「`src/` 产品代码零改动」同此结论**：本批 `src/` 只有注释变动，运行时代码路径零触碰。

### 3.5 acceptance ④ · 三项「不做」写进 commit 正文 —— **仍成立（PASS）**

见 §5.2 的 commit 正文核对。

---

## 4. 块 D —— 修复引入的新文件是否污染别的判据

fix 轮新增两个文件：`tests/tools/m51c-f001-mutations.mjs` · `tests/tools/m51c-f001-probe-bench.mjs`。
本批**已在首轮踩过一次「探针污染普查计数」**，故此处按三道独立判据核。

### 4.1 全仓扫描根清点

```
$ grep -rln 'readdirSync|globSync|collectSources' tests scripts
```

逐个查其扫描根，无一落在 `tests/`：

| 扫描器 | 扫描根 | 新文件是否落入 |
|---|---|---|
| `db-layer-importer-census.test.ts` | `src` | 否 |
| `bootstrap-whitelist-census.test.ts` | `src` | 否 |
| `project-scope-census.test.ts` | `src` | 否 |
| `auth-status-code-census.test.ts` | `src/app`, `src/lib` | 否 |
| `tool-module-cycles.test.ts` | `src/lib/agent/tools`, `src/lib/agent` | 否 |
| `architecture-doc-freshness.test.ts` | `src`, `docs`, `prisma/migrations`, `src/lib/db` | 否 |
| `batch-confirm.test.ts` | `src/app/api/actions` | 否 |
| `m51c-scanner-equivalence.ts` | `src` | 否 |
| **`m51b-entrypoint-census.ts`（入口普查）** | `src/app`, **`scripts/**/*.ts`** | 否（新文件在 `tests/`，且是 `.mjs`） |

### 4.2 入口普查实测计数

探针 `tests/tools/rv1b/rv1b-census-pollution.mjs`（只读，不带 `--write`）：

```
TOTAL=78 COVERED=3
M51C_IN_CENSUS=0
SCRIPT_ENTRIES=39
SELFTEST_positive_route_ts=29 (须 >0，否则匹配器本身是死的)
```

`78 / 3` 与 `project-status.md` 登记的「入口面 78 条仅包裹 3 条」逐位相符；
`M51C_IN_CENSUS=0` 说明本批任何 m51c 文件都没进入普查条目；
自测证明「命中检测」不是恒空绿。**入口普查计数未被污染。**

### 4.3 决定性判据：把新文件移出扫描面，全量重跑

不靠推断，直接做对照实验（`mv` 移出 → 跑 → `mv` 移回 → `shasum -c` 对账，**未用 `git checkout`**）：

```
# 新文件在位
 Test Files  148 passed (148)      Tests  1888 passed (1888)

# 新文件移出后
 Test Files  148 passed (148)      Tests  1888 passed (1888)

# 还原对账
tests/tools/m51c-f001-mutations.mjs: OK
tests/tools/m51c-f001-probe-bench.mjs: OK
```

**结论：块 D = PASS。** 两个新文件在位与否，全量测试的文件数、用例数、通过数完全一致 →
它们不参与任何判据的取值，未污染入口普查计数，也未进入其它钉的扫描面。

> 附带自查：复验方自己新增的 `docs/test-reports/M5.1c-rv1-regression.md` 与
> `tests/tools/rv1b/*.mjs` 同样未污染 —— 创建它们前后全量均为 148 files / 1888 tests。

---

## 5. 块 E —— 是否有被修复动作顺带改坏的既有断言

### 5.1 用例增删与改名的影响面

fix 轮**零增删用例**（19 个 `it(` → 19 个），只改名 2 条、换输入 3 条。两条改名用例经
§2.4 矩阵实测**均仍可证活**（R7 分别红它们），鉴别力没有随改名流失。

**无悬挂引用**：全仓搜旧标题「正则时代六种漏抓」「同名标识符'」——
除本报告自身的引用外零命中，没有别的文件/脚本依赖被改掉的旧标题。

**等价性审计脚本未被改坏**：`m51c-scanner-equivalence.ts` 以文本方式从测试文件里抽取
`importSpecifiers` 函数体（`extractFunction`）。fix 轮改动该文件后它仍抽取成功、
正负自测仍通过、结论仍 0 分歧（§2.2）——抽取锚点未被改名/改标破坏。

### 5.2 fix 轮 commit 正文的可核查断言逐条复算（铁律 13）

| commit | 自陈 | 复验方独立实测 | 结论 |
|---|---|---|---|
| `ef80538` | `vitest run` 148 files / **1888 tests 全绿** | 148 / 1888 全绿 | **属实** |
| `ef80538` | tsc exit 0 · lint「No ESLint warnings or errors」 | tsc_exit=0（0 行输出）· lint 同字样 | **属实** |
| `ef80538` | `git status --porcelain src/` 零输出 | 零输出 | **属实** |
| `ef80538` | GUARDED 清单与 AST 实现本体**未动** | 五 rev 哈希全同（§2.1a / §2.2） | **属实** |
| `ef80538` | 摘各分支红 6 / 2 / 1 / 4 / 1 | 独立实测 6 / 2 / 1 / 4 / 1 | **属实** |
| `ef80538` | **M10 仍全绿**，未加新钉遮掩 | R16 独立复跑 → `PASS-ALL（全绿）`；全仓无新增钉 | **属实（且是主动披露盲区）** |
| `9922206` | 148 files / 1888 tests 全绿 | 同上 | **属实** |
| `9922206` | 承诺句普查（剔 `// >`）**零命中** | A 档 0 命中（§3.2） | **属实** |
| `9922206` | `src/` 除注释外零改动 | 双判据 emit+token 均相同（§3.4） | **属实** |
| `a2834fa` | 三项「不做」写进正文 | 正文 §「明确不做」逐项列出 | **属实**（F002 ④） |

注释面新写入的 `(a)–(g)` 换代前红格数亦逐条复算（`m51c-f001-probe-bench.mjs`，
其正负自测随跑随过，世代按哈希去重得 5 代）：

```
(a) 2/5 · (b) 2/5 · (c1)(c2)(c3) 各 2/5 · (d1)(d2) 各 2/5
(e') 5/5 · (f') 5/5 · (f'') 3/5 · (g') 1/5
```

与文件注释里写的「(a) 2/5 · (b) 2/5 · (c) 各 2/5 · (d) 各 2/5 · (e) 5/5 ·
(f) 行尾注释 5/5 / 模板串 3/5 · (g) 1/5」**逐条吻合**。
首轮 N-1 点名的假陈述（「且只红它」）已删除，新写的数字经得起独立复算。

### 5.3 块 E 结论

**未发现被修复动作顺带改坏的既有断言。** 21 / 21 条用例全部可证活；
两条改名用例鉴别力完好；无悬挂引用；依赖该文件的外部脚本（等价性审计）未被破坏。

**soft-watch W2（不扣分）：** `m51c-f001-probe-bench.mjs` 里把 `(e)(f)(g)` 标为「**现行**」、
`(e')(f')(g')` 标为「候选」，但 fix 后测试文件采用的已经是 `(e')(f')(g')`。
即该工具的标签停留在修复前的语境，未来读者可能把「现行」误读成 HEAD 的现状。
属工具内标签陈旧，不影响任何判据取值。

---

## 6. 总判定

| 块 | 判定 |
|---|---|
| A · L1 全量 + spec §6 不计分项 | **PASS** |
| B · F001 首轮 PASS 条目（① ③ ④ ⑤ ⑥ ⑦）重放 | **PASS**（6 / 6 仍成立） |
| C · F002 首轮 PASS 条目（① ② ③ ④ ⑥）重放 | **PASS**（5 / 5 仍成立，② 附 soft-watch W1） |
| D · 新增文件污染检查 | **PASS**（对照实验零差异） |
| E · 被修复顺带改坏的既有断言 | **PASS**（未发现，附 soft-watch W2） |

### 回归面判定：**PASS —— 无回归**

首轮判 PASS 的 11 个 acceptance 条目（F001 ①③④⑤⑥⑦ + F002 ①②③④⑥）在 HEAD 上**逐条重放仍成立**；
修复动作未带坏任何既有判据；两个新增文件对其它判据零影响。

> **本报告不覆盖也不重判**首轮判 PARTIAL 的两条（F001 ② / F002 ⑤）——那是另一路复验方的射程。
> 本批最终 feature 级判定须合并两路结论后方可作出。

### 登记的两条 soft-watch（均不构成扣分）

- **W1** —— `src/lib/db/privileged.ts` 的一行 `// >` 历史更正被**静默改写**（内容由不实的字面引用
  改为属实的表述，方向正确且新措辞经核查成立；但 fix-1 更正段只交代了 ① 的改动，未提这一处）。
  `// >` 区的价值在于记录本身不被静默改写，建议后续批次补一句说明。
- **W2** —— `m51c-f001-probe-bench.mjs` 的「现行 / 候选」标签停留在修复前语境（见 §5.3）。

### 复验方自身两次判据失灵（如实登记，均已改正后重测）

诚实起见记下：本轮复验方自制的两个探针**各自先给过一次假读数**，都由自测/交叉核对当场揪出：

1. `rv1b-blacklist-frozen.mjs` 初版锚点错（`];` vs `].filter`），五个 rev 全抽到 `ABSENT`，
   却因「五个 ABSENT 相同」输出「全程逐字未变 ✓」的**假绿**。已加 `ABSENT → exit 2` 硬闸。
2. `rv1b-history-preserved.mjs` 初版 `norm()` 保留标点，把一条**已被原文引用**的旧措辞
   误报成「历史被抹掉」（**假红**）。已改为去标点比对。

两者正是 `audit-methodology.md` §8「判据先验活」反复点名的形态，一次假绿一次假红。
上文所有读数均为改正后重跑的结果。

---

## 7. 复现清单

```bash
# L1 全量（跑前先 npx prisma generate）
npx tsc --noEmit && npx next lint && npx vitest run

# 不计分项（需先 npx next build，否则 webServer 起不来）
npm run test:visual

# 首轮 PASS 条目重放
npx tsx scripts/test/m51c-scanner-equivalence.ts            # F001 ④ 等价性 0 分歧
npx tsx tests/tools/m51c-f002-code-identity.ts --selftest   # F002 ⑥ 判据自测
npx tsx tests/tools/m51c-f002-code-identity.ts bf26603 HEAD # F002 ⑥ 口径1
npx tsx tests/tools/m51c-f002-code-identity.ts bb28842 HEAD # F002 ⑥ 口径2
npx tsx tests/tools/m51c-f002-narrative-census.ts           # F002 ① 普查
node tests/tools/m51c-f001-probe-bench.mjs                  # (a)–(g) 红格数复算

# 复验方本轮新增探针（tests/tools/rv1b/）
node tests/tools/rv1b/rv1b-census-pollution.mjs       # P1 入口普查污染
node tests/tools/rv1b/rv1b-guarded-frozen.mjs         # P2 GUARDED 逐字未变
node tests/tools/rv1b/rv1b-guarded-oracle.mjs         # P3 GUARDED 独立 oracle
node tests/tools/rv1b/rv1b-impl-frozen.mjs            # P4 AST 实现本体未变
node tests/tools/rv1b/rv1b-mutations.mjs              # P5 变异台（R1–R16）
node tests/tools/rv1b/rv1b-narrative-mutations.mjs    # P6 叙述面变异（N1–N8）
node tests/tools/rv1b/rv1b-blacklist-frozen.mjs       # P7 黑名单逐字未变
node tests/tools/rv1b/rv1b-history-preserved.mjs      # P8 `// >` 历史未被抹掉
```

**变异纪律：** 全部变异（R1–R16 对测试文件、N1–N8 对产品文件 `tenant-scope.ts`）
一律「反向编辑 + sha256 逐位对账」还原，**无一使用 `git checkout`**；
收尾 `git status --short -- src/ tests/unit tests/integration scripts/ prisma/` 为空，
全量 vitest 复跑 148 files / 1888 tests 全绿。

**L2 未授权：** 未连任何真实外部服务、未触发部署、未产生计费或生产写入。
本报告全部读数取自本机 L1 与静态判据。
