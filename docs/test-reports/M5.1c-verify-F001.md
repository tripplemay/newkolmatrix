# M5.1c-CENSUS-SCANNER — F001 独立验收报告

> 批次：`M5.1c-CENSUS-SCANNER` · 阶段：`verifying`（fix_rounds=0，首轮）
> 被验 HEAD：`bb2884220a8f7d989aa02b92156467b84d7c87bb`
> F001 实现 commit：`e509e06`
> 验收方：隔离 evaluator subagent（fresh context，独立 worktree `wt-f001`，detached HEAD）
> 日期：2026-08-09 · 环境：darwin / node v25.7.0 / npm 10.8.2 / typescript 5.9.3 / vitest 4.1.10
> L2 未授权：不连真实外部服务 / 不触发部署 / 不产生计费或生产写入。本报告全部为 L1。

---

## 0. 结论速览

| # | acceptance 摘要 | 判定 |
|---|---|---|
| ① | 五个遍历面各有正向用例 + 各打一个必须让它红的变异 | **PASS**（附注 N-1） |
| ② | U1–U5 与已知边界多数条目实测溶掉，**逐条给出「换代前漏 / 换代后抓到」的对照输出** | **PARTIAL** |
| ③ | 仍抓不到的边界如实登记 + 「故意断言抓不到」的用例须实跑 | **PASS** |
| ④ | 等价性审计：全量 `src/` 逐文件多重集（不去重）+ 逐条归因 + 判据活性自测 | **PASS** |
| ⑤ | GUARDED 期望清单不得变更 + 实测集合与期望逐条相符 | **PASS** |
| ⑥ | 保留下来的旧用例逐条重验 | **PASS**（附注 N-2） |
| ⑦ | `src/` 产品代码零改动；tsc/lint + 全量 vitest 绿 | **PASS** |

**F001 = PARTIAL。** 换代本身的实质是过硬的：四代判据横向对照 26 条探针，原版错 12 / fix-3 错 14 /
fix-4 错 9 / **AST 错 0**；独立 oracle 复核 384 文件 1241 条 specifier 逐文件多重集吻合。
卡住的是 acceptance ② 的**举证要求**：U1 与 U2 两条被钉住的探针，在**换代前的三代正则实现上一样是绿的**——
即它们不构成「换代前漏 / 换代后抓到」的对照，而文件却在注释里明写这两种形态「正则版实测红过」
「fix-4 版会凭空捏造 importer……现在有实测钉着」。这是本批立项要根治的那一族（声称 > 实际）
在 F001 交付物内部的第四次复发，且可由一条命令证伪。

**独立性声明：** 本报告不采信 commit 正文 / session_notes 的任何叙述性结论；每条判定均附可复现命令与本机实跑输出。
变异一律**反向编辑还原 + `sha256` 逐位对账**，全程未使用 `git checkout`；14+2 次变异后文件 sha256
逐位回到基线 `a769878040b17c467f349883a2d2e6401fa0d36f087b1b99453a8ea64eb224ee`。
本轮未修改任何产品代码，也未改动 `tests/unit/db-layer-importer-census.test.ts` 的最终状态。

---

## 1. 环境前置（L1 误报排除）

对照 `framework/patterns/testing-env-patterns.md`：

| 检查 | 结果 |
|---|---|
| Node 版本 vs `.nvmrc` | 仓内无 `.nvmrc`、`package.json` 无 `engines` → 不适用；本机 v25.7.0 |
| `prisma generate` 先于 tsc | 本批零 schema 改动；`node_modules` 为主仓软链（`postinstall` 已跑过），tsc exit 0 → 不适用 |
| DB / 外部服务 | F001 全部为纯文本 / AST 单测，零 DB、零网络 → 不适用 |
| `npm run test:visual` | spec §6 明示不计分，未执行 |

```
$ node -v && node -e "console.log(require('typescript').version)"
v25.7.0
5.9.3
```

---

## 2. 逐条验收

### ① 遍历面逐个覆盖 + 每条各有必红变异 — **PASS**

**正向用例在位**（`tests/unit/db-layer-importer-census.test.ts:255-305`）：
遍历面①ImportDeclaration / ②ExportDeclaration（含 `export *`、`export * as ns`、`export type`）/
③ImportEqualsDeclaration / ④动态 `import()` / ⑤`require()`，五条 `🔒` 用例齐备。

**变异证活由验收方独立复打**（不采信 commit 正文）。变异台：`scripts/test/m51c-evaluator-f001-mutations.mjs`
（区分三种红法：`PASS-ALL` / `ASSERT-RED(N 条)` / `FILE-RED(0 条跑起来)`——不区分就会把收集期抛
误读成「全绿无鉴别力」）。

```
$ node scripts/test/m51c-evaluator-f001-mutations.mjs
[基线] sha256=a769878040b17c46…  PASS-ALL（全绿）  跑 21 条

E1   ASSERT-RED（红 6 / 跑 21）  摘掉 visitor 分支① ImportDeclaration
       红：预扫确实取到了 specifier / runtime 清单 / privileged 清单 /
           🔒 遍历面① / 🔒 ScriptKind / 📜 语料
E2   ASSERT-RED（红 2 / 跑 21）  摘掉 visitor 分支② ExportDeclaration
       红：🔒 遍历面② · 📜 语料
E3   ASSERT-RED（红 1 / 跑 21）  摘掉 visitor 分支③ ImportEqualsDeclaration
       红：🔒 遍历面③
E4   ASSERT-RED（红 4 / 跑 21）  摘掉 visitor 分支④ 动态 import()
       红：预扫 / runtime 清单 / 🔒 遍历面④ / 🔒 字面量形态
E5   ASSERT-RED（红 1 / 跑 21）  摘掉 visitor 分支⑤ CJS require()
       红：🔒 遍历面⑤
...
全部变异结束，sha256 = a769878040b17c467f349883a2d2e6401fa0d36f087b1b99453a8ea64eb224ee
✓ 与基线逐位相同
```

五个分支**各有一条摘掉它就红的变异，且红的正是对应那条用例** → 逐条成立。
实测红数与 commit 正文 M1–M5 所记（6/2/1/4/1）**逐条相同**，交付叙述在这一节是有机械依据的。

> **N-1（附注，不影响 ① 判定，但需更正）：** 文件 `:251` 的分节注释写
> 「**每条各自摘掉对应 visitor 分支即红，且只红它**」。按字面读，E1 红 6 条、E2 红 2 条、E4 红 4 条 →
> **该句为假**。按「五条遍历面用例之内只红它」读则成立。同一 commit 的正文自己记的是「M1→红6」，
> 与该句互相矛盾。属可一行修正的叙述面过度声称（与 F002 的清理方向同族）。

### ② U1–U5 与已知边界实测溶掉 + 逐条对照输出 — **PARTIAL**

验收方另建**四代判据横向对照台** `scripts/test/m51c-evaluator-f001-generations.ts`：
「换代前」在本仓不是一个版本而是三个（原版 `6a120a7` / fix-3 `1ea4abb` / fix-4 `c251fe9`＝立项 HEAD 内容），
三代漏抓面互不相同，只拿一代作对照会把「另一代才有的病」说成「换代前的病」。四代实现全部**机械抽取 + esbuild 求值**，不手抄。

```
$ node --import tsx scripts/test/m51c-evaluator-f001-generations.ts
=== 各代不符期望的探针数 ===
  原版 6a120a7     12 / 26
  fix-3 1ea4abb    14 / 26
  fix-4 c251fe9     9 / 26
  AST 工作树         0 / 26
```

**溶掉的部分（实质达标）** —— 摘录逐条对照（期望 / 三代 / AST）：

| 探针 | 期望 | 原版 | fix-3 | fix-4 | AST |
|---|---|---|---|---|---|
| U3 模板串带替换 | `[]` | ✓ `[]` | ✗ `["./${name}"]` | ✗ `["./${name}"]` | ✓ `[]` |
| U4 字符串里的假 import | `[]` | ✗ `["./fake"]` | ✗ `["./fake"]` | ✗ `["./fake"]` | ✓ `[]` |
| U5 动态 import 尾逗号 | `["./trail"]` | ✗ `[]` | ✗ `[]` | ✗ `[]` | ✓ `["./trail"]` |
| 无空格 `import{a}from'x'` | `["x"]` | ✗ `[]` | ✗ `[]` | ✗ `[]` | ✓ `["x"]` |
| 无空格 `import'x'` | `["x"]` | ✗ `[]` | ✗ `[]` | ✗ `[]` | ✓ `["x"]` |
| 动态 import 第二参数（attributes） | `["./x"]` | ✗ `[]` | ✗ `[]` | ✗ `[]` | ✓ `["./x"]` |
| 侧效应吞噬 | `["./runtime","zod"]` | ✗ `["zod"]` | ✓ | ✓ | ✓ |
| 行尾注释含反引号/撇号/分号 | 抓到 | ✓ | ✗ `[]`（三条全漏） | ✓ | ✓ |
| ES2022 带引号绑定名（import/export 两侧） | 抓到 | ✓ | ✗ `[]` | ✓ | ✓ |
| `import Foo = require()` | `["./legacy"]` | ✗ `[]` | ✗ `[]` | ✓ | ✓ |
| 静态 import attributes `with` / `assert` | `["./d.json"]` | ✓ | ✓ | ✓ | ✓ |

**不达标的部分（本条判 PARTIAL 的机械依据）：**

**② -a　U1 钉错了形态，钉的那条在三代正则上一样绿。**
文件 `:403-406`（📜 语料 (e)）钉的是**单行** import：

```ts
expect(importSpecifiers("import { a } from './new'; // 旧版 from './old'")).toEqual(['./new']);
```

实测四代**全部**返回 `["./new"]`：

```
● U1 行尾注释含 from './old'（钉在 📜 语料 (e)）    期望 ["./new"]
    ✓ 原版 6a120a7   ["./new"]      ✓ fix-3 1ea4abb  ["./new"]
    ✓ fix-4 c251fe9  ["./new"]      ✓ AST 工作树      ["./new"]
```

而 acceptance 描述的「既误捕又漏真 specifier」真形态是**多行** import 的行尾注释，验收方补测：

```
● U1′ 多行 import + 行尾注释含 from './old'（真形态）  期望 ["./new"]
    ✗ 原版 6a120a7   ["./old"]      ✗ fix-3 1ea4abb  ["./old"]
    ✗ fix-4 c251fe9  ["./old"]      ✓ AST 工作树      ["./new"]
```

→ **能力是真的**（AST 溶掉了 U1），但**交付物钉的探针不构成对照**，且注释 `:403` 写着
「正则版既误捕注释里的 `'./old'`，又漏掉真的 `'./new'`」——对所钉的那个输入而言为假。

**② -b　U2 同样钉错形态，而 U2 是本批立项的头号理由。**
文件 `:407-410`（📜 语料 (f)）钉的是**整行 `//` 注释**的散文：

```ts
const u2 = "export const a = 1\n// 说明：本模块的数据 from 'lib/db/privileged' 而来";
expect(importSpecifiers(u2), 'U2 复发：凭空捏造了一个 importer').toEqual([]);
```

正则版的 `stripComments` 先剥掉整行 `//`，散文根本进不了正则，故**三代全绿**：

```
● U2 钉在文件里的那条探针（后文散文是整行 // 注释）   期望 []
    ✓ 原版  []    ✓ fix-3  []    ✓ fix-4  []    ✓ AST  []
```

散文只要不在整行注释里（行尾注释 / 普通代码），fix-4 立刻凭空捏造 importer：

```
● U2′ 散文在行尾注释里                              期望 []
    ✗ 原版 ["lib/db/privileged"]  ✗ fix-3 ["lib/db/privileged"]
    ✗ fix-4 ["lib/db/privileged"]  ✓ AST []
● U2″ 散文是普通代码（模板串）                       期望 []
    ✗ 原版 ["lib/db/privileged"]  ✓ fix-3 []
    ✗ fix-4 ["lib/db/privileged"]  ✓ AST []
```

→ 该断言的失败信息写着「U2 复发：凭空捏造了一个 importer」，但它**在真的会捏造的实现上也是绿的**——
这正是 M5.1b F003 被点名过的「恒绿的假保证」同款形态。commit 正文「U2 那条是本批立项的头号理由，
**现在有实测钉着**」，以及文件 `:373`「下面每种形态在正则版上都**实测红过**（来源逐条标注）」，
两句均被上表逐条证伪（(e)(f) 两种形态在三代正则上均未红）。

**② -c　「逐条给出对照输出」这项交付物不存在。**
`git show --stat e509e06` 只含 4 个文件（`docs/specs/M5.1-uncovered-entrypoints.md` · `package.json` ·
`scripts/test/m51c-scanner-equivalence.ts` · 被验测试文件）；commit 正文只给了**聚合**结论（0 分歧）
与**一条**探针（无空格写法）的两侧输出，没有 U1–U5 的逐条「换代前漏 / 换代后抓到」。
本报告上面的表是**验收方补做的**，不能顶替交付要求。

**修复建议（最小）：** 把 (e)(f) 两条探针换成 U1′ / U2′ 这类**在换代前会红**的形态（可各保留一条现有输入
作为「两侧都对」的对照，但不得声称它红过）；`:373` 的「每种形态都实测红过」按实测改述；
逐条对照输出可直接复用 `scripts/test/m51c-evaluator-f001-generations.ts` 的输出落进 commit 正文或报告。

### ③ 已登记边界 + 「故意断言抓不到」的用例 — **PASS**

用例在 `:349-355`，三条断言（`import('./' + name)` / `` import(`./${name}`) `` / `import(pathFromConfig)`），
**实跑通过**（§2 首段 21 条全绿里含此条）。它不是空声明，且**能被证活**：

```
E7   ASSERT-RED（红 2 / 跑 21）  字面量判据放宽：连带替换的模板串也收
       红：🔒 字面量形态：反引号无替换要认，带替换的模板串必须不认
       红：🔒 已登记边界仍如实：运行期才定的路径确实抓不到（不是声称，是实测）← 该条红
E6   ASSERT-RED（红 1 / 跑 21）  收窄 isStringLiteralLike → isStringLiteral
       红：🔒 字面量形态（反方向）
```

登记内容与实测一致：三代正则在这三条上也都返回 `[]`（见 §2 表末三行），说明这确是**静态分析固有边界**
而非换代引入的退步。文件头 `:32-37` 明写「本段不对『除此之外全都抓得到』作任何断言；实际射程 = 五个分支」——
无完备性承诺，与 D-2 的删承诺路线一致。

> **验收方新测到的一种射程外形态（信息项，不构成缺陷）：** 模块增广 `declare module 'x' { … }`
> （出现在已是外部模块的文件里）不被五分支收取。实测归因：
> ```
> [P2] 顶层 import + declare module   preProcessFile.importedFiles → ["pkg-x","pkg-x"]
> [P3] 实物 src/types/next-auth.d.ts  → ["next-auth","next-auth","@auth/core/jwt"]
> ```
> 它不是 import 边，且本仓这三处全是裸包名（`next-auth` / `@auth/core/jwt` / `react-table`），
> 归一后不可能等于两个被守模块 → 对钉的判定零影响。因为文件头**没有**作完备性承诺，
> 这个发现证伪不了任何一句话——这恰好是 D-2「不写承诺就不存在声称>实际」策略生效的一个正面样本。

### ④ 等价性审计 — **PASS**

```
$ npm run census:scanner-equiv
  基线（正则）：bf26603:tests/unit/db-layer-importer-census.test.ts
  当前（AST）：工作树 tests/unit/db-layer-importer-census.test.ts
[自测] 正向 ✓（探针 "import{a}from'./probe-known-divergence';"：老=[] 新=["./probe-known-divergence"]）
[自测] 负向 ✓（两侧一致的输入未误报）
扫描面：384 个 .ts/.tsx
specifier 总数（多重集，未去重）：老 1241 / 新 1241
有差异的文件：0
✓ 全仓 src/ 逐文件多重集完全一致
EXIT=0
```

逐条核对 acceptance ④：

- **全量 `src/` 逐文件**：384 个 `.ts/.tsx`，与 spec D-1 引用的复验方数字一致 ✓
- **多重集、不去重**：`multisetDiff` 按出现次数算（`scripts/test/m51c-scanner-equivalence.ts:106-115`）✓
- **差异逐条归因**：实测 0 差异，无可归因项（vacuous）；验收方另用独立 oracle 找出 2 处差异并已归因（见下）
- **判据先做活性自测**：正负双向自测在主流程前跑，正向探针拿的是**已知两侧分歧**的输入 ✓
- **反退化闸门可用（验收方补验）**：把 base 指到换代之后必须抛，实跑：

```
$ npm run census:scanner-equiv -- --base e509e06
Error: 基线 e509e06 的 importSpecifiers 不像正则实现（缺少标记：RegExpExecArray, patterns, re.exec）。
本审计要求 base 指向**换代之前**的 commit；指向换代之后会让新旧两侧是同一份实现，产出稳定的「0 分歧」假结论。
EXIT=1
```

**验收方独立 oracle 复核**（`scripts/test/m51c-evaluator-f001-crosscheck.ts`）：
spec D-4 标题要求「独立于被验实现的 oracle」，而 acceptance ④ 正文规定的是「新旧实现对照」——
两者口径不同。按 harness 「acceptance 即边界」，以 ④ 正文判 PASS；同时验收方补一个真正独立的 oracle
（`ts.preProcessFile`，TypeScript 自带预处理扫描器，与本仓手写 visitor 不同代码路径）：

```
oracle 自测：正向 import{a}from'./no-space'; → ["./no-space"]；负向 字符串里的假 import → []
扫描面：384 个 .ts/.tsx
specifier 总数：oracle 1244 / 新实现 1241
有差异的文件：2
  src/types/next-auth.d.ts        仅 oracle 抓到：next-auth, @auth/core/jwt
  src/types/react-table-config.d.ts  仅 oracle 抓到：react-table
```

**逐条归因：** 3 条差异全部是**模块增广** `declare module 'pkg'`（探针 [P2]/[P3] 已定位，见 ③ 的信息项）。
`preProcessFile` 把增广目标计入 importedFiles，五分支不计——**判为正确差异，不是回归**：增广是类型层声明，
不构成 import 边；且三者皆裸包名，对被守模块零影响。**结论：新实现与 TypeScript 自带预处理器在
384 文件 1241 条 specifier 上逐文件多重集吻合，「0 分歧」不是两侧同错造成的。**

### ⑤ GUARDED 期望清单不得变更 — **PASS**

```
$ diff <(git show bf26603:…test.ts | sed -n '/^const GUARDED/,/^];/p' | grep -E "^\s+'src/") \
       <(sed -n '/^const GUARDED/,/^];/p' …test.ts | grep -E "^\s+'src/")
→ 无输出（逐字相同）
      'src/instrumentation.ts' / 'src/lib/db/prisma.ts' / 'src/lib/db/tenant-scope.ts'
      'src/app/api/auth/[...nextauth]/route.ts' / 'src/app/api/auth/register/route.ts'
      'src/lib/agent/context.ts' / 'src/lib/auth/index.ts' / 'src/lib/auth/register.ts'
```

**实测集合与期望逐条相符**，且用**验收方自写的解析器 + 独立 oracle**复算（不复用被验实现的 `resolveToSrcModule`）：

```
src/lib/db/runtime  (3) → src/instrumentation · src/lib/db/prisma · src/lib/db/tenant-scope
src/lib/db/privileged (5) → src/app/api/auth/[...nextauth]/route · src/app/api/auth/register/route
                            src/lib/agent/context · src/lib/auth/index · src/lib/auth/register
```

两条清单**双向证活**（塞假的红、摘真的也红，排除「期望被改成迁就实现」）：

```
E11  ASSERT-RED（红 1 / 跑 21）  GUARDED 期望里塞一个不存在的 importer → runtime 清单红
E12  ASSERT-RED（红 1 / 跑 21）  GUARDED 期望里摘掉一个真实 importer   → runtime 清单红
E16  ASSERT-RED（红 3 / 跑 21）  扫描面缩到 src/lib/db → 空转哨兵 + 两条清单同时红
```

### ⑥ 保留下来的旧用例逐条重验 — **PASS**（附注 N-2）

旧文件（`bf26603`）第一个 describe 共 11 条用例。逐条追踪去向并**各自重验**：

| 旧用例 | 去向 | 在 AST 实现上的证活变异 |
|---|---|---|
| `src/ 下确实扫到了文件` | 原样保留 | **E16** 红 ✓ |
| `{module} 的 importer 清单与实物一致` ×2 | 原样保留 | **E11 / E12 / E16** 红 ✓ |
| `活性证明：能认出真实 import，且不被注释与同名标识符骗到` | 拆四份：正向→遍历面①②④；幻影块注释→📜语料(g)；路径归一→独立用例；注释/字符串/同名→`🔒 字面出现不算 import` | E1/E2/E4 · E13 · **E15** ✓ |
| `🔒 回归：反引号静态路径动态 import` | 改写上移「字面量形态」 | **E6 / E7** 双向红 ✓ |
| `🔒 回归：CJS require 形态` | 改写上移「遍历面⑤」 | **E5** 红 ✓ |
| `🔒 回归：re-export 不得跨语句重复计入` | 改写上移「遍历面②」 | **E2** 红 ✓ |
| `🔒 回归：侧效应吞噬` / `后文字符串 from` / `行尾注释含引号` / `ES2022 绑定名` 四条 | 降级并入 `📜 语料`，注释明写「无独立鉴别力，是语料不是守卫」 | 无独立变异（**属实**：E1/E2/E14/E15 才红），处置正确 ✓ |
| `🔒 已登记边界仍如实` | 保留并扩到三条 | **E7** 红 ✓ |

- commit 正文「3 条上移 + 4 条并入语料」的账目与实物**逐条相符**，未发现「以它以前是活的为由保留」。
- 全量 21 条用例中，**每一条都至少有一个能让它红的变异**（E1–E16 覆盖），无恒真用例。
  E14（visit 整体空转）红 11 条、E15（任意字符串都当 specifier）红 10 条，两向合并可覆盖第一个 describe 全部 15 条。
- 第二个 describe（M5.1b fix-1 的说明段那组 6 条）经 diff 确认**逐字未动**，不在 F001 射程内。

> **N-2（附注，不改判定）：** `🔒 字面出现不算 import：注释 / 字符串 / 同名标识符`（`:337`）**只在 E15
> 这种极端假阳性变异下红**，摘任何一个 visitor 分支、乃至 E14 把判据整个打死，它都保持绿。
> 即：它没有独立鉴别力，与被降级为 `📜` 的那 4 条同类，却带着 `🔒` 前缀（旧版这部分内容还**没有** `🔒`，
> 是本次换代时被升格的），且 commit 正文的「逐条重验」账目未提及它。建议按文件自己的 `🔒/📜` 约定改标或加注。
> 判 PASS 的理由：它**可证活**（E15），不是恒真用例，且 acceptance ② 要求的 U4 断言正寄存于此，不宜删除。

### ⑦ `src/` 零改动 + tsc/lint/全量 vitest 绿 — **PASS**

```
$ git show --stat --format="" e509e06
 docs/specs/M5.1-uncovered-entrypoints.md    |   6 +-
 package.json                                |   3 +-
 scripts/test/m51c-scanner-equivalence.ts    | 227 ++++++++++++++
 tests/unit/db-layer-importer-census.test.ts | 440 ++++++++++++++++------
 4 files changed, 478 insertions(+), 198 deletions(-)
→ 零 src/ 路径，产品代码未动 ✓

$ npx tsc --noEmit                → TSC_EXIT=0
$ npm run lint                    → ✔ No ESLint warnings or errors  (LINT_EXIT=0)
$ npx vitest run                  → Test Files 148 passed (148)
                                    Tests 1888 passed (1888)   VITEST_EXIT=0
$ npx vitest run tests/unit/db-layer-importer-census.test.ts
                                  → 21 passed (21)
```

commit 正文所记「148 files / 1888 tests 全绿」「该文件 18 → 21 条」与本机实跑**逐字吻合**。
`package.json` 只新增 `census:scanner-equiv` 一条脚本；`docs/specs/M5.1-uncovered-entrypoints.md`
的 49→50 / 102→103 由 F007 doc-freshness 钉逼出，全量 vitest 绿即其自证。

---

## 3. 交付叙述抽查（铁律 13）

对 commit `e509e06` 正文的**可机械核对陈述**逐条复算：

| 陈述 | 复算结果 |
|---|---|
| 等价性审计 384 文件 / 老 1241 / 新 1241 / 0 差异 | ✓ 实跑逐字相同 |
| M1→红6 · M2→红2 · M3→红1 · M4→红4 · M5→红1 · M6→红1 · M7→红2 · M8→红1 | ✓ 与 E1–E8 逐条相同 |
| M9→文件级红（0 条跑起来） | ✓ E9 = FILE-RED |
| M10 全绿（自陈盲区，不掩盖） | ✓ E10 = PASS-ALL（21 条一条不红），如实 |
| M11→红1 | ✓ E12 相同；验收方另补反方向 E12 亦红 |
| 11 次变异后 sha256 回到基线 `a769878040b17c46…` | ✓ 基线 sha 一致，本轮 16 次变异后同样逐位回归 |
| GUARDED 实物 = 3 条 / 5 条，与期望同 | ✓ 独立解析器复算一致 |
| tsc/lint/1888 全绿、`git status --porcelain src/` 零输出 | ✓ |
| **「U2 那条……现在有实测钉着」** | ✗ **证伪**：所钉探针在会捏造的三代实现上全绿（②-b） |
| **文件 `:373`「下面每种形态在正则版上都实测红过」** | ✗ **证伪**：(e)(f) 两种形态三代均未红（②-a/-b） |
| **文件 `:251`「摘掉对应分支即红，且只红它」** | ✗ 字面为假（E1 红 6 / E2 红 2 / E4 红 4），正文自记 M1→红6 与之矛盾（N-1） |

正文中**数值类**陈述 8/8 属实且可复算，这一点明显好于 M5.1b；被证伪的 3 条全部落在
「**这条钉守住了 X**」这一句式上——与前四轮同一族。

---

## 4. 复现清单

```bash
cd <worktree>
npx vitest run tests/unit/db-layer-importer-census.test.ts --reporter=verbose   # 21 passed
npx tsc --noEmit && npm run lint && npx vitest run                              # L1 三件套
npm run census:scanner-equiv                                                    # 0 分歧
npm run census:scanner-equiv -- --base e509e06                                  # 反退化闸门必须抛

# 验收方产物（本轮新增，未 commit，随报告交回编排者）
node --import tsx scripts/test/m51c-evaluator-f001-crosscheck.ts     # 独立 oracle + 探针表 + GUARDED 复算
node --import tsx scripts/test/m51c-evaluator-f001-generations.ts    # 四代判据横向对照（26 探针）
node scripts/test/m51c-evaluator-f001-mutations.mjs                  # 16 条变异，反向编辑还原 + sha 对账
```

产物清单（均为测试产物 / 报告，未触碰产品代码与被验测试文件的最终状态）：

- `docs/test-reports/M5.1c-verify-F001.md`（本文件）
- `scripts/test/m51c-evaluator-f001-crosscheck.ts`
- `scripts/test/m51c-evaluator-f001-generations.ts`
- `scripts/test/m51c-evaluator-f001-mutations.mjs`

工作树最终状态：`git status --porcelain` 除上述四个 `??` 与 `node_modules` 软链外为空；
`tests/unit/db-layer-importer-census.test.ts` sha256 = `a769878040b17c467f349883a2d2e6401fa0d36f087b1b99453a8ea64eb224ee`（＝基线）。

---

## 5. 结论

**F001 = PARTIAL**（① ③ ④ ⑤ ⑥ ⑦ PASS，② PARTIAL）。

必修（1 项，② 的三个子项）：
1. `📜 语料` (e)(f) 两条探针换成**在换代前会红**的形态（U1′ 多行 import 行尾注释 / U2′ 散文不在整行注释里），
   让「U2 有实测钉着」这句话真的成立；
2. `:373`「每种形态在正则版上都实测红过」按实测改述（或删——D-2 路线本就是删承诺）；
3. 补齐 acceptance ② 要求的**逐条**「换代前漏 / 换代后抓到」对照输出（可复用四代对照台的输出）。

建议同批顺手（不单独构成 FAIL）：
4. `:251`「且只红它」按实测改述（N-1）；
5. `🔒 字面出现不算 import` 按文件自己的 `🔒/📜` 约定改标或加注（N-2）。

不必做：换代的实现本身、等价性审计脚本、GUARDED 清单、`src/` —— 均经独立复核通过，**不要动**。

---

## 编排者附记（非验收方内容，不改动上文任何判定）

> 由编排者在把本报告收进主仓时追加，**上文一字未改**（含全部 PASS / PARTIAL / FAIL 判定与措辞）。

**验收方产物的落点改了：** 三个 F001 探针脚本原写在 `scripts/test/`，收进主仓时移到
`tests/tools/`，与 F002 验收方的落点一致。理由是机械的、不是偏好：

```
$ # 探针留在 scripts/test/ 时
$ npx vitest run tests/unit/architecture-doc-freshness.test.ts
AssertionError: 合计行与普查实测不符：expected [103, 78, 3, 75] to deeply equal [105, 80, 3, 77]
$ # 移到 tests/tools/ 后
      Tests  41 passed (41)
```

入口普查把 `scripts/` 下的 `.ts` 计进扫描面，两个探针脚本还被判成**入口面**（78 → 80），
于是会以「未覆盖的租户入口」身份混进 `docs/specs/M5.1-uncovered-entrypoints.md` ——
那份清单是 M5.2 的输入，污染它比挪个目录代价大。F002 验收方 §1.3 独立给出了同一条理由。

**故上文「复现清单」里的路径请作替换：** `scripts/test/m51c-evaluator-f001-*` →
`tests/tools/m51c-evaluator-f001-*`。脚本内容逐字节未改，移动后实跑正常。
