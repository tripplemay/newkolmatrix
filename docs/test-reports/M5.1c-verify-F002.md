# M5.1c-CENSUS-SCANNER · F002 独立验收报告

> 批次：`M5.1c-CENSUS-SCANNER` ｜ feature：**F002 — src/lib/db 叙述面承诺句清理（删而非加钉）**
> 被验 HEAD：`bb2884220a8f7d989aa02b92156467b84d7c87bb`
> F002 交付 commit：`a2834fa9fa0d024f82e9ef3721a5de4de5806fa9`（F002 前基线 `8cc63d2`）
> 验收者：隔离 evaluator subagent（fresh context，独立 worktree `wt-f002`）
> 日期：2026-08-09
> **L2 未授权** —— 不连真实外部服务、不触发部署、无计费与生产写入。本报告全部为 L1。

## 判定总表

| acceptance | 内容 | 判定 |
|---|---|---|
| ① | 普查后清理 + 判据先证明能看见目标 | **PASS**（附 soft-watch S1） |
| ② | `// >` 历史更正记录不得被抹掉 | **PASS** |
| ③ | 保留 `🔒④` 黑名单并复核其鉴别力 | **PASS**（附盲区实测登记 S2） |
| ④ | 三项「不做」须写进 commit 正文 | **PASS** |
| ⑤ | 清理后自核：每句事实性断言须有命令输出作依据 | **PARTIAL** ⬅ 唯一不达标项 |
| ⑥ | `src/` 除注释外零改动 + tsc/lint/vitest 绿 | **PASS** |

**feature 级判定：PARTIAL。** 阻断点只有一处，见 §6.1：F002 在「为删掉一句过度承诺而写下的更正段」里，
新写了一句**与其点名引用的原文相反**的事实断言。改动量约一行，但它正是本批次立项要根治的那一族。

---

## 0. 验收纪律声明

- 每一条判定只依据**实跑输出**。Generator 的 commit 正文与 `session_notes` 一律当作**待证伪的断言**，
  不作为证据；凡本报告写「已核」的，下面都贴了那一条命令的输出。
- 不修改任何产品代码。变异一律「注入 → 实跑 → **反向字符串替换**还原 → `sha256` 逐位对账」，
  **不使用 `git checkout`**；夹具 `tests/tools/m51c-f002-mutate.ts` 把三步绑成一次调用，还原失败即非零退出。
- 验收侧探针放 `tests/tools/`（**不是** `scripts/test/`），理由见 §1.3 —— 放错目录会污染普查计数。

### 被验文件基线 sha256（变异还原对账基准；报告末尾 §10 有收尾对账）

```
$ shasum -a 256 src/lib/db/*.ts
ab5daeed5b02228ce4e9dbbac7af20e3a1e576b0576ffbbd285361d766245903  src/lib/db/app-role.ts
b7debb48840ebaa9f41d63e9b3dff94e8215aafdb437b5a4dba6663067cfa869  src/lib/db/prisma.ts
0fed4879eb8c5bed5ef9391913560e914bb6638e429b1730c4fc7e71807bb3cc  src/lib/db/privileged.ts
ca6a40190b49e6b063375a9d9e128b682ae3b6e1ad0c7b3663e48bf90b98288a  src/lib/db/runtime.ts
fe61e1db8c482ac8c132e2060b8b10e062e0adbfe5fa890f7b609a1916a87cbd  src/lib/db/tenant-entry.ts
7cd74122cf230ae565101879b464105ddae8b814b848b4d5756d6111c26aaf13  src/lib/db/tenant-scope.ts
```

> `7cd74122cf230ae5…` 即 Generator commit 正文里反复对账的那个值 —— 实测确为 `tenant-scope.ts`，对得上。

### 验收侧产物清单

| 文件 | 用途 |
|---|---|
| `tests/tools/m51c-f002-narrative-census.ts` | 叙述面承诺句普查（独立 oracle，AST 抽注释 + A/B 双档模式 + 正负自测） |
| `tests/tools/m51c-f002-code-identity.ts` | 「除注释外零改动」判据（emit + token 流**双判据交叉** + 正负自测） |
| `tests/tools/m51c-f002-mutate.ts` | 变异夹具（注入 / 实跑 / 反向编辑还原 / sha256 对账） |
| `docs/test-reports/M5.1c-verify-F002.md` | 本报告 |

---

## 1. 环境与 L1 基线

### 1.1 环境

| 项 | 值 |
|---|---|
| worktree | `…/wt-f002`（`node_modules` 为指向主仓的 symlink，仅共享依赖；vitest root 实测 = worktree） |
| node | `v25.7.0`（仓内无 `.nvmrc`、`package.json` 无 `engines`，无版本门可核 —— 登记为观察项） |
| vitest | `v4.1.10`，`include = tests/unit/**` + `tests/integration/**`（集成测试打真库） |
| 本机 dev 库 | `newkolmatrix-dev-db` Up 7 days (healthy) |
| L2 | **未执行**（未授权） |

### 1.2 L1 三项（HEAD 原样，探针已移出扫描面）

```
$ npx prisma generate           → exit 0     （先于 tsc，testing-env-patterns §15）
$ npx tsc --noEmit              → exit 0
$ npm run lint                  → ✔ No ESLint warnings or errors
$ npx vitest run
   Test Files  148 passed (148)
        Tests  1888 passed (1888)
```

与 Generator commit 正文自陈（`tsc exit 0` / `No ESLint warnings or errors` / `148 files / 1888 tests 全绿`）
**逐项相符**。

### 1.3 ⚠️ 一次自制仪器造成的假红（如实登记，最终未计入判定）

首跑全量得到 `1 failed | 147 passed` / `2 failed | 1886 passed`，红在
`tests/unit/architecture-doc-freshness.test.ts`：

```
AssertionError: docs/dev/architecture.md:109 的入口面计数与普查实测不符
  - Expected  { covered: 3, total: 79 }
  + Received  { covered: 3, total: 78 }
AssertionError: 合计行与普查实测不符：| **合计** | **103** | **78** | **3** | **75** |
  - Expected [105, 79, 3, 76]
  + Received [103, 78, 3, 75]
```

差值恰为 `scanned +2 / entries +1 / uncovered +1`。溯源：`scripts/test/m51b-entrypoint-census.ts:157`
**递归遍历 `scripts/` 下全部 `.ts`** 并计入普查底数（该脚本第 286 行自陈「实测偏多主要是
`scripts/test/` 下历次批次留下的验收探针也带租户」）。我把两个验收探针写进 `scripts/test/`
就把底数顶高了 2。机械证明：

```
$ mv scripts/test/m51c-f002-*.ts /tmp/m51c-eval-park/
$ npx vitest run tests/unit/architecture-doc-freshness.test.ts
   Tests  41 passed (41)
$ cp /tmp/m51c-eval-park/*.ts tests/tools/ && npx vitest run
   Test Files  148 passed (148)
        Tests  1888 passed (1888)
```

**结论：该 2 红是验收侧仪器污染，不是产品缺陷，不计入 F002 判定。**
（`project-status.md` 已登记的「探针污染普查计数」那一坑；本轮第 1 次「判据先验活」救场。）

---

## 2. acceptance ① — 普查后清理 + 判据先证明能看见目标 → **PASS**

### 2.1 不复用 Generator 的判据，另建独立 oracle

`tests/tools/m51c-f002-narrative-census.ts`，与被验方刻意不同源：

| | Generator 自陈判据 | 本工具 |
|---|---|---|
| 叙述面抽取 | grep 注释**行**，剔 `// >` | **TS scanner 抽真实注释 trivia**（字符串字面量 / 标识符里的同形文字不会被误当叙述面） |
| 模式 | 单一模式 | **A 档** = 逐字复刻 Generator 自陈模式（复核其结论）；**B 档** = 自拟 12 组**更宽**模式（找它漏掉的形态） |

### 2.2 判据活性自测（正负双向，17 条全 PASS）

```
$ npx tsx tests/tools/m51c-f002-narrative-census.ts --selftest
PASS  正向A：「本段陈述现由 xxx 钉机械守住」被 A 档看见
PASS  正向A：「新增 importer 即红并点名」被 A 档看见
PASS  正向A：「声称与实际不可能再对不上」被 A 档看见
PASS  正向A：「这份清单是完备的」被 A 档看见
PASS  正向A：「已知形态一条不漏」被 A 档看见
PASS  正向A：「该面已被全部覆盖」被 A 档看见
PASS  正向B：「这道钉保证不会有漏网之鱼」被 B 档看见   / 对照：A 档看不见
PASS  正向B：「任何新 importer 都会让它红」被 B 档看见  / 对照：A 档看不见
PASS  正向B：「该清单已由那道钉守着」被 B 档看见        / 对照：A 档看不见
PASS  正向B：「这条路径必然被拦下」被 B 档看见          / 对照：A 档看不见
PASS  负向：`// >` 行被归为历史，不计入现行叙述（= 本判据已知盲区）
PASS  负向：字符串字面量内的同形文字不被计入注释面
PASS  负向：标识符不被计入注释面
SELFTEST: ALL PASS
```

acceptance ① 要求「拿一条已知在场的承诺句验证模式能抓到它」—— 上面 6 条正向 A 用的正是
acceptance 逐字点名的六种措辞，**逐条被看见**，该要求满足。

### 2.3 换代前后对照（A 档：复核 Generator 自陈的 1 → 0）

```
$ npx tsx tests/tools/m51c-f002-narrative-census.ts --rev 8cc63d2
# rev=8cc63d2   注释行总数=263  现行叙述=232  历史更正(// >)=31
## A 档命中：1
  src/lib/db/privileged.ts:10  // 都会让那条钉红并点名文件。…

$ npx tsx tests/tools/m51c-f002-narrative-census.ts
# rev=WORKTREE  注释行总数=279  现行叙述=238  历史更正(// >)=41
## A 档命中：0
```

**Generator 自陈的「改前 1 条 / 改后 0 条」经独立 oracle 复现，属实。**

### 2.4 B 档（更宽）：查出 A 档看不见的两条，其中一条 F002 顺手清掉了

```
--- rev=8cc63d2（改前）---
  [B01 守住/守死/钉死/钉住]   privileged.ts:9   …并由源码级普查钉钉死——白名单外的任何新 import
  [B03 点名类承诺]           privileged.ts:10  …都会让那条钉红并点名文件。
  [B08 任何…都会/一律会/均会] tenant-scope.ts:73 * …「返回了 client」和「抛了别的错」两种退化都逃不过用例。
  合计 3
--- rev=WORKTREE（改后）---
  [B08 …]                   tenant-scope.ts:73 （同上，残留）
  合计 1
```

- privileged.ts 的两条（含 A 档模式**看不见**的「钉死」）已随清理一并消失 ✓ —— 清理的实际射程略大于自陈模式。
- **残留一条**：`tenant-scope.ts:73`，定性见 §9 soft-watch **S1**。

### 2.5 为什么 ① 判 PASS 而不是 PARTIAL

残留的那句「两种退化都逃不过用例」我做了正面证伪尝试，**两种退化都实测被抓**：

```
$ MODE=replace npx tsx tests/tools/m51c-f002-mutate.ts src/lib/db/prisma.ts \
    '  if (isAppRoleRuntimeEnabled()) throw new MissingTenantScopeError();' \
    '  if (isAppRoleRuntimeEnabled()) { /* MUT-D1: 退化为回落 client */ }' ALL
   Test Files  2 failed | 146 passed (148)
        Tests  6 failed | 1882 passed (1888)
   AssertionError: promise resolved "{ today: 1, projects: 2 }" instead of rejecting
   AssertionError: expected function to throw an error, but it didn't
   sha AFTER = b7debb48…（RESTORE OK）

$ …同上，注入 throw new Error('…MUT-D2…')                            # 「抛了别的错」
   Test Files  2 failed | 146 passed (148)
        Tests  6 failed | 1882 passed (1888)
   AssertionError: expected Error: [db] MUT-D2… to be an instance of MissingTenantScopeError
   sha AFTER = b7debb48…（RESTORE OK）
```

该句**不是假的**（D-2 要根治的是「声称 > 实际」，而这句声称 = 实际，各 6 条红为证），
且主语是 `MissingTenantScopeError` 的用例覆盖，不是 D-2 点名的「那两道普查钉的覆盖面」，
又早于 F002 存在、F002 未触碰。Generator 也在 commit 正文预先登记了
「本次普查的召回**不等于**『src/lib/db 叙述面已无问题』」。综合判 **PASS + soft-watch S1**。

---

## 3. acceptance ② — `// >` 历史更正记录未被抹除 → **PASS**

```
$ git show a2834fa -- src/lib/db/ | grep -E '^-' | grep -v '^---' | grep -c '// >'
0

$ git show a2834fa -- src/lib/db/ | grep -E '^-' | grep -v '^---'
-// 文件集，由 F003 普查产出、逐处写明理由，并由源码级普查钉钉死——白名单外的任何新 import
-// 都会让那条钉红并点名文件。本模块刻意**不读** DB_APP_ROLE_RUNTIME / DATABASE_URL_APP：
-// **本注释不对那道钉的覆盖面作任何承诺** —— 它抓得到什么、抓不到什么，以那个文件的
-// 「扫描口径与两类已知假阳性」「已知边界」两段为准（那里有实测支撑的回归用例）。
```

全 commit 只删 4 行，**无一行属 `// >` 更正块**；清理对象确为现行叙述。
独立 oracle 的行计数交叉佐证：历史更正行 **31 → 41（只增不减）**，现行叙述 232 → 238。

---

## 4. acceptance ③ — `🔒④` 保留且仍有鉴别力 → **PASS**

### 4.1 「保留」：黑名单逐字未动

```
$ git show "8cc63d2:tests/unit/db-layer-importer-census.test.ts" | grep -n "const overclaims" -A 3
561:    const overclaims = ['即红并点名', '不可能再对不上', '机械守住'].filter((p) =>
$ git show "HEAD:tests/unit/db-layer-importer-census.test.ts" | grep -n "const overclaims" -A 3
561:    const overclaims = ['即红并点名', '不可能再对不上', '机械守住'].filter((p) =>

$ git diff 8cc63d2..HEAD -- tests/unit/db-layer-importer-census.test.ts | wc -l
0        # 整个文件逐字节未动
```

### 4.2 「仍有鉴别力」：四次变异，全部实跑（含还原对账）

| # | 变异 | 结果 | 红了几条 | 报文是否点名 |
|---|---|---|---|---|
| M-A1 | 现行叙述写回「即红并点名」 | 🔴 | **1**（只有 🔒④） | ✅「…出现了承诺覆盖面的措辞：即红并点名」 |
| M-A2 | 写回「不可能再对不上」 | 🔴 | **1** | ✅「…：不可能再对不上」 |
| M-A3 | 写回「机械守住」 | 🔴 | **1** | ✅「…：机械守住」 |
| M-A4 | **摘掉**正向锚句「不对那组钉的覆盖面作任何承诺」 | 🔴 | **1** | ✅「注释必须明写…并指向本文件」 |

M-A1 原始输出（其余三条同构）：

```
=== MUTATION M-A1 即红并点名 ===
sha BEFORE : 7cd74122cf230ae5…
sha MUTATED: 52d629bdc03d6143…
--- vitest exit=1 ---
sha AFTER  : 7cd74122cf230ae5…
RESTORE OK (sha256 逐位一致)
git diff --stat -- src/lib/db/tenant-scope.ts => (空，与 HEAD 一致)
 Test Files  1 failed (1)
      Tests  1 failed | 20 passed (21)
  × 🔒 ④ 被守文件的注释只许指路，不许承诺本组的覆盖面（用户裁决，fix-3）
AssertionError: tenant-scope.ts 的现行叙述里出现了承诺覆盖面的措辞：即红并点名:
  expected [ '即红并点名' ] to deeply equal []
```

> 按 `project-status.md`「变异要看**红了几条**」的纪律：四次都是 **恰好 1 条**红，
> 且红的就是 🔒④ 本体 —— 不存在「借别的钉的红冒充自己鉴别力」。

### 4.3 盲区实测登记（S2；均为 spec / 用例已自陈的，本报告只补实测）

| 盲区 | 实测 | 定性 |
|---|---|---|
| B-1：三条黑名单词全写成 `// >` 前缀 | 注入后 `Tests 21 passed (21)`，**exit=0，零红** | 🔒④ 用例第 555 行已自陈「有人若把新的过度声称写成 `// >`，这条看不见」——**实测坐实**，非新缺陷 |
| B-2：把 F002 刚删掉的那句承诺**原样写回 `privileged.ts` 现行叙述** | 全量 `Test Files 148 passed / Tests 1888 passed`，**exit=0，零红** | 🔒④ 只读 `tenant-scope.ts`；spec D-2 明确「不扩守护面」。故 **F002 实际清理的两个文件本身无钉守护**，清理成果靠自觉维持 |

B-2 原始输出：

```
=== MUTATION B-2 privileged.ts 承诺复活（全量）===
injected: "\n// 白名单外的任何新 import 都会让那道钉即红并点名文件，这份清单是完备的，机械守住，一条不漏。"
--- vitest exit=0 ---
 Test Files  148 passed (148)
      Tests  1888 passed (1888)
RESTORE OK (sha256 逐位一致)
```

acceptance ③ 只要求「保留黑名单 + 复核其鉴别力」，两条都满足 → **PASS**；B-2 属 spec 主动划走的
守护面，记 soft-watch，不扣分。

---

## 5. acceptance ④ — 三项「不做」 → **PASS**

```
$ git show a2834fa --name-only --format=""
src/lib/db/privileged.ts
src/lib/db/runtime.ts

$ git diff --stat 8cc63d2..HEAD -- tests/
(空 —— 本批 F002 之后 tests/ 零改动)

$ git log -1 --format=%B a2834fa | grep -n "明确不做" -A 3
43:## 明确不做（spec D-2 排除，写在这里以免被读成遗漏）
45-🔒②b 语义化 · 扩守护面到 runtime.ts/prisma.ts · census 自身叙述面上钉。三项留在 BL-M51B-CARRYOVER。
```

三项逐条机械核对：

| 「不做」项 | 是否真的没做 | 依据 |
|---|---|---|
| `🔒②b` 语义化 | ✅ 没做 | 该测试文件 `8cc63d2..HEAD` diff = **0 行** |
| 扩守护面到 `runtime.ts`/`prisma.ts` | ✅ 没做 | 同上；且 §4.3 B-2 实测证明连 `privileged.ts` 也无守护 |
| census 自身叙述面上钉 | ✅ 没做 | F002 仅触及 2 个 src 文件，零测试改动 |

「零新增断言」的自陈同样成立（F002 未改任何测试文件）。commit 正文按要求写明 → **PASS**。

---

## 6. acceptance ⑤ — 清理后自核 → **PARTIAL**

我把 F002 新增叙述里的**每一句事实性断言**拆出来逐条找机械依据。共 10 条：8 条核实、1 条**证伪**、1 条措辞不准。

### 6.1 ❌ 证伪的一条（阻断点）

**位置：`src/lib/db/privileged.ts:19-20`（F002 新写的 `// >` 更正段）**

```
// > ① 那道白名单钉扫的是 **privilegedDb 标识符的使用**，不是 import —— 而它自己的
// >   「盲区登记」段就列着四种作者未采用的写法能绕过它；
```

**它点名引用的原文（`tests/unit/bootstrap-whitelist-census.test.ts:21-26`）：**

```
// 【盲区登记（实测，不是估计）】建钉时用四种「作者未采用的写法」打过本钉：
//   ✅ 抓到：`import * as ns` 后 `ns.privilegedDb` · 重命名 import（`as backdoor`）·
//            动态 `import()` 后取属性 · 顶部仍有 import 的动态键取用
//   ❌ **漏**：标识符从不作为完整 token 出现的动态键取用，例如
//              `const KEY = 'privileged' + 'Db'; (await import('./db/privileged'))[KEY]`
```

**机械计数：**

```
$ node -e "…按 ✅/❌ 切分该段…"
✅ 抓到 = 4 种
    1. `import * as ns` 后 `ns.privilegedDb`
    2. 重命名 import（`as backdoor`）
    3. 动态 `import()` 后取属性
    4. 顶部仍有 import 的动态键取用
❌ 漏  = 1 种
    1. 标识符从不作为完整 token 出现的动态键取用（拼接键）
```

那段列的**四种是「抓到」的，能绕过的只有一种**。F002 把数量「四」挂到了「能绕过」这一栏上。

**再做一次正面证伪**（拿它所称「能绕过」的第 1 种写法真去打那道钉）：

```
$ npx tsx tests/tools/m51c-f002-mutate.ts src/lib/db/tenant-entry.ts \
    "import { withTenant, … } from 'lib/db/tenant-scope';" \
    "\nimport * as __probeNs from 'lib/db/privileged';\nexport const __probe = __probeNs.privilegedDb;" \
    tests/unit/bootstrap-whitelist-census.test.ts
--- vitest exit=1 ---
 Tests  1 failed | 4 passed (5)
  × src/ 下使用 privilegedDb 的文件必须全部在白名单内
AssertionError: 发现**未登记**的特权连接使用点：src/lib/db/tenant-entry.ts
sha AFTER = fe61e1db…（RESTORE OK）
```

**该写法不但没绕过，还被当场点名。** 断言「四种…能绕过它」由此**双向证伪**：
既与被引原文相反，也与实跑相反。

**为什么这一条必须计为 PARTIAL 而不是笔误 soft-watch：**

1. acceptance ⑤ 的原文是「改后的**每一句**事实性断言都要有对应的一条命令输出作依据（铁律 13）」。
   这一句没有依据，且拿得出的依据是反的。
2. 位置在**「为删掉一句过度承诺而写下的那段更正」里** —— 与 M5.1b 四轮里三轮的复发位置完全同构。
   本批 D-2 立项理由正是根治该族。方向虽是镜像的（这次是**把钉说弱**而非说强），
   但「落笔时未核、事后被证伪」这个机制一模一样。
3. 修复成本极小（一句话的量词与归属），但**必须由 Generator 改**——我不改产品代码。

**建议改法（供参考，非强制）：** 把该分句改成指路而不带计数，例如
「而它自己的『盲区登记』段就写着：四种探针写法均被抓到，另有一种**拼接键**取用抓不到」，
或直接删去数量、只说「该段登记了它抓不到的形态」。

### 6.2 ⚠️ 措辞不准的一条（不单独扣分，随 6.1 一并修更省事）

`privileged.ts:21`：「② 这句话把两道钉当成了一道，而两个文件的头注都写着「别互相派生」。」

```
$ grep -c 别互相派生   tests/unit/db-layer-importer-census.test.ts   → 1
$ grep -c 别互相派生   tests/unit/bootstrap-whitelist-census.test.ts → 0
$ grep -c 不得互相派生 tests/unit/bootstrap-whitelist-census.test.ts → 1
```

用「」括起来作字面引用，但 bootstrap 侧写的是「**不得**互相派生」。语义完全一致，
**实质断言成立**，仅引号用法不严。

### 6.3 ✅ 核实通过的 8 条

| # | 断言（位置） | 机械依据 |
|---|---|---|
| 1 | privileged.ts:9-11「有两道源码级普查钉…越权面 / 文档面」 | bootstrap:6-7「扫 privilegedDb 标识符的真实使用…**越权面**」；importer:4-5「扫 import specifier…**文档漂移面**」 |
| 2 | privileged.ts:12-13「以那两个文件的头注与用例名为准（有变异证活实跑记录，也有盲区登记）」 | `grep -c`：bootstrap 变异=1 / 盲区=4；importer 变异=6 / 盲区=2 / 边界=6。bootstrap:210-216 确为实跑记录（「派生清单下全仓 1869 条无一察觉」） |
| 3 | privileged.ts:19「那道白名单钉扫的是标识符的使用，不是 import」 | 探针 P-1 报文即「发现未登记的特权连接**使用点**」；bootstrap:6 原文一致 |
| 4 | runtime.ts:20-21「一律以那个文件的头注与用例名为准（有变异证活实跑记录，也有边界登记）」 | importer:123「每个分支各有一条用例，且各自变异证活过」；importer:32「【本版仍抓不到的（如实登记…）】」 |
| 5 | runtime.ts:23-24「上一版把读者指向那个文件的两个段落标题」 | `git show a2834fa` 删除行原文逐字对上 |
| 6 | runtime.ts:24-25「F001 换 AST 时重写文件头，**前一个**标题随之不存在」 | `git show <rev>:… \| grep -c "扫描口径与两类已知假阳性"` → `e509e06^`=**1**、`8cc63d2`=**0**、`HEAD`=**0** |
| 7 | 同上，隐含「后一个标题仍在」 | `grep -c "已知边界"` → `HEAD`=**2**（importer:350 / :465） |
| 8 | runtime.ts:26「段落标题…**本段没有钉守它**」 | §4.3 B-2 实测：整套 1888 条对该面零反应 |

### 6.4 commit 正文自陈的复核（铁律 13 同样管 commit 正文）

| 自陈 | 复核 |
|---|---|
| 「改前扫命中 1 条，改后 0 条」 | ✅ 独立 oracle 复现（§2.3） |
| 「六种措辞各注一条，六条全部命中」 | ✅ 复现（§2.2） |
| 「同一句写进 `// >` 则命中 0」 | ✅ 复现（§2.2 负向 + §4.3 B-1） |
| 「三条黑名单逐条实跑，红并点名」 | ✅ 复现，且每次恰 1 条红（§4.2） |
| 「改动 2 个 src 文件，含真实代码改动 0 个」 | ✅ 双判据交叉复现（§7） |
| 「tsc 0 / lint 无警告 / 148 files 1888 tests 全绿」 | ✅ 逐项复现（§1.2） |
| 「**改的两处里只有一处是这个模式抓到的**；召回 ≠ 叙述面已无问题」 | ✅ 属实且**主动示弱**，B 档实测印证（privileged.ts:9 的「钉死」A 档确实看不见） |

---

## 7. acceptance ⑥ — `src/` 除注释外零改动 → **PASS**

判据刻意做成**双判据交叉**，且与 Generator 只有一条同源：

- 判据 1：`ts.transpileModule({ removeComments: true })` 比对 emit（与 Generator 同法，用于复核其自陈）
- 判据 2：**token 流**比对 —— `ts.createScanner(skipTrivia: true)` 逐 token 取 `(kind, text)`，**完全不经 emitter**

判据活性自测（1 负例 + 4 正例，全 PASS）：

```
$ npx tsx tests/tools/m51c-f002-code-identity.ts --selftest
PASS  负例：只改注释 → emit 相同 ✓ token 相同 ✓
PASS  正例1：字面量 1→2 → emit 不同 ✓ token 不同 ✓（比对器没瞎）
PASS  正例2：标识符 n→m → 两判据均判不同
PASS  正例3：删一条语句 → 两判据均判不同
PASS  正例4：字符串字面量内容变化 → 两判据均判不同（不会被当成注释）
SELFTEST: ALL PASS
```

实测：

```
$ npx tsx tests/tools/m51c-f002-code-identity.ts 8cc63d2 HEAD
# 8cc63d2..HEAD 改动的 src/ 文件：2 个
  src/lib/db/privileged.ts   emit 相同 = true   token 相同 = true   => 仅注释改动
  src/lib/db/runtime.ts      emit 相同 = true   token 相同 = true   => 仅注释改动
含真实代码改动的文件数 = 0        exit=0
```

叠加 §1.2 的 tsc / lint / vitest 三绿 → **PASS**。

---

## 8. 判定与理由（逐条）

| acceptance | 判定 | 一句话理由 |
|---|---|---|
| ① 普查后清理 + 判据先证明能看见目标 | **PASS** | 六种点名措辞逐条被独立 oracle 看见；A 档 1→0 复现；更宽的 B 档另揪出的两条里，一条已被顺手清掉、一条经变异实测**为真**且属另一主语（S1） |
| ② `// >` 不得被抹掉 | **PASS** | 全 commit 删 4 行，`// >` 删除数 = 0；历史行 31→41 只增不减 |
| ③ 保留 `🔒④` + 复核鉴别力 | **PASS** | 黑名单文件逐字节未动；3 条写回 + 1 条摘锚句，四次变异均**恰 1 条红**且报文点名；两处盲区补了实测（S2） |
| ④ 三项「不做」写进 commit 正文 | **PASS** | commit 有「## 明确不做」段；三项逐条机械核对确未做（tests/ diff = 0） |
| ⑤ 清理后自核 | **PARTIAL** | 10 条新断言中 8 条核实、1 条措辞不准、**1 条被双向证伪**（§6.1）——且复发在「为删承诺而写的更正段」里 |
| ⑥ `src/` 除注释外零改动 + L1 绿 | **PASS** | 双判据交叉（emit + token 流），各自正负自测后一致判「0 个文件含真实代码改动」；tsc/lint/vitest 三绿 |

**feature 判定：PARTIAL** —— 唯一阻断点是 §6.1 那一句，改动量约一行。

---

## 9. Soft-watch / 射程外发现（均不改变上表判定）

| # | 内容 | 证据 | 建议 |
|---|---|---|---|
| **S1** | `tenant-scope.ts:73` 残留一句覆盖面承诺「「返回了 client」和「抛了别的错」两种退化都逃不过用例」，未指向任何文件 | B 档命中；**实测为真**（D1/D2 各 6 条红） | 下批顺手改成指路（点名 `tests/integration/tenant-injection-proxy.test.ts §①(c)`）。因**为真**且主语非 D-2 目标，本批不扣分 |
| **S2** | F002 实际清理的两个文件（`privileged.ts` / `runtime.ts`）**无任何钉守护**：原样写回被删的承诺，1888 条全绿 | §4.3 B-2 | spec D-2 主动划走，属**已知**且**已裁决**的残留风险，登记备查 |
| **S3** | `backlog.json` 的 `BL-DB-STALE-COUNTS` 标题与描述都写「**三处**陈旧计数」，实际至少**四处** —— 漏的第四处就在被引用行的隔壁：`src/lib/db/prisma.ts:4`「既有 **142** 个测试文件按 D-8 不翻修」（实测 149 个 `.test.ts(x)` / vitest 148 files） | `grep -n "142 个测试文件" src/lib/db/prisma.ts` → 命中；`find tests -name '*.test.ts*' \| wc -l` → 149 | **建议排期前先把该 backlog 条目的「三处」改掉**，否则修的时候会照着清单漏一处。这也是同一族（清单声称 ≠ 实际）在归档层的一次复现 |
| **S4** | commit 正文「射程外三处发现」里 tenant-entry / app-role 两处数字经**独立复核属实** | ① `grep -n "135 个测试文件" src/lib/db/app-role.ts` 命中，`find tests -name '*.test.ts*' \| wc -l` = **149** ② `npx tsx scripts/test/m51b-entrypoint-census.ts` → `入口合计 78（已覆盖 3 / 未覆盖 75）`、`api-route: 29`、`page: 6`，即注释的「30 route / 12 admin page」实为剩 **28 / 5**，与 commit 正文一致 | 无需动作，仅记账 |
| **S5** | 仓内无 `.nvmrc`、`package.json` 无 `engines`；本机 node `v25.7.0`。`testing-env-patterns §16` 要求的「Node 版本与 `.nvmrc` 一致」在本仓**无对照物可核** | `ls -a \| grep nvmrc` 空 | 与本 feature 无关，登记为环境观察项 |

---

## 10. 收尾对账（变异全部还原，产品代码零改动）

```
$ shasum -a 256 src/lib/db/*.ts
ab5daeed5b02228ce4e9dbbac7af20e3a1e576b0576ffbbd285361d766245903  src/lib/db/app-role.ts
b7debb48840ebaa9f41d63e9b3dff94e8215aafdb437b5a4dba6663067cfa869  src/lib/db/prisma.ts
0fed4879eb8c5bed5ef9391913560e914bb6638e429b1730c4fc7e71807bb3cc  src/lib/db/privileged.ts
ca6a40190b49e6b063375a9d9e128b682ae3b6e1ad0c7b3663e48bf90b98288a  src/lib/db/runtime.ts
fe61e1db8c482ac8c132e2060b8b10e062e0adbfe5fa890f7b609a1916a87cbd  src/lib/db/tenant-entry.ts
7cd74122cf230ae565101879b464105ddae8b814b848b4d5756d6111c26aaf13  src/lib/db/tenant-scope.ts
```

**与 §0 基线逐位一致**（本轮共 8 次变异：M-A1/A2/A3/A4 · B-1 · B-2 · D1 · D2 · P-1，
每次均由夹具反向编辑还原并当场对账，全程未使用 `git checkout`）。

```
$ git status --short src/ prisma/
(无输出 = 产品代码零改动)

$ git status --short | grep -v node_modules
?? docs/test-reports/M5.1c-verify-F002.md
?? tests/tools/
```

---

## 11. 状态机建议（由编排者落盘，本报告不代写 progress.json）

> 本 worktree 与可能并行进行的 F001 验收共享同一份状态文件，为避免互相覆盖，
> 我**不直接写** `progress.json` / `features.json`，改以结构化输出交回，请**原样**合并。

```json
{
  "feature_id": "F002",
  "result": "PARTIAL",
  "acceptance": {
    "①": "PASS", "②": "PASS", "③": "PASS",
    "④": "PASS", "⑤": "PARTIAL", "⑥": "PASS"
  },
  "blocking_issue": "src/lib/db/privileged.ts:19-20 新增的 `// >` 更正段写「「盲区登记」段就列着四种作者未采用的写法能绕过它」，与其点名引用的 tests/unit/bootstrap-whitelist-census.test.ts:21-26 相反（该段列的是 4 种**被抓到** + 1 种**漏**），且探针实测第 1 种写法被当场点名红。违反 acceptance ⑤「每一句事实性断言都要有命令输出作依据」。",
  "fix_scope": "一句话量级：改正数量与归属，或删去计数只作指路。不涉及代码。",
  "suggested_status": "fixing",
  "features_json": { "F002": "pending" }
}
```

---

*报告完 —— 结论基于实物与实跑输出，不因进度压力软化；如需对任一条判定复核，上文每条都给了可原样重跑的命令。*

---

## 编排者附记（非验收方内容，不改动上文任何判定）

> 由编排者在把本报告收进主仓时追加，**上文一字未改**（含全部 PASS / PARTIAL / FAIL 判定与措辞）。

**验收方产物的落点改了：** 三个 F001 探针脚本原写在 `scripts/test/`，收进主仓时移到
`tests/tools/`，与 F002 验收方的落点一致。理由是机械的、不是偏好：

```
$ # 探针留在 scripts/test/ 时
$ npx vitest run tests/unit/architecture-doc-freshness.test.ts
AssertionError: 合计行与普查实测不符：expected [103, 78, 3, 75] to deeply equal [105, 80, 3, 77]
$ # 移到 tests/tools/ 后
      Tests  41 passed (41)
```

入口普查把 `scripts/` 下的 `.ts` 计进扫描面，两个探针脚本还被判成**入口面**（78 → 80），
于是会以「未覆盖的租户入口」身份混进 `docs/specs/M5.1-uncovered-entrypoints.md` ——
那份清单是 M5.2 的输入，污染它比挪个目录代价大。F002 验收方 §1.3 独立给出了同一条理由。

**故上文「复现清单」里的路径请作替换：** `scripts/test/m51c-evaluator-f001-*` →
`tests/tools/m51c-evaluator-f001-*`。脚本内容逐字节未改，移动后实跑正常。
