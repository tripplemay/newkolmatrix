{
  "sprint": "M1-D-KNOWLEDGE",
  "features": [
    {
      "id": "F001",
      "title": "schema：Material + GameKnowledge 两表 + zod 契约",
      "priority": "high",
      "executor": "generator",
      "status": "completed",
      "acceptance": "expand 迁移新增 Material（gameId/type 枚举 lore~video/source/fileName/storageRef/mimeType/sizeBytes/parseStatus 枚举 pending~failed @default(pending)/parseError?/parsedAt?）与 GameKnowledge（gameId/kind 枚举 selling_point|audience|compliance_redline/content/structured Json?/sourceMaterialIds String[] 非空溯源 FR-11.9 应用层校验/confidence?/generatedBy 默认 strategy/supersededById?）两模型，均带 tenantId+publicId，软引用沿 D13 先例；src/lib/data/schemas/knowledge.ts 落 zod（表深字段 + LLM 解析产物 schema）；单测：脏数据宽松降级 null 不抛错（D2）；schema+migration+引用代码同 commit；lint + tsc + test:unit 绿",
      "description": "spec §2 F001。字段按 architecture §7.6（:900-908）；勘查实证现无两表（schema.prisma:146-159 Game 最小占位注记「素材上传/AI 解析 → M1」）。"
    },
    {
      "id": "F002",
      "title": "存储通道 + 上传 API（本地盘卷，U2）",
      "priority": "high",
      "executor": "generator",
      "status": "completed",
      "acceptance": "src/lib/knowledge/storage.ts：MATERIALS_DIR（dev 默认 ./.materials 入 gitignore，prod /app/materials）+ {gameId}/{cuid}-{safeName} 落盘 + 路径穿越防护；POST /api/materials：formData（file+gameId+type）→ 类型白名单 pdf/txt/md/csv/png/jpg/webp + 20MB 上限 + 【P5】图片最短边>10px 前置校验（vision 实测约束）→ 存盘 → Material 落库 pending；【P6】video/不可解析类型落库即 failed+parseError 明示；【P8】rate-limit 10 req/min/tenantId fail-open + DISABLE_UPLOAD_RATELIMIT；GET /api/materials?gameId= 轮询列表；集成测试：上传落盘落库/路径穿越拒/超限拒/小图拒；lint + tsc 绿",
      "description": "spec §2 F002 + U2/P1/P5/P6/P8。route handler 非 server action（P1：formData 大文件无 1MB 默认限制）。"
    },
    {
      "id": "F003",
      "title": "解析管道（文本 chat + 图片 vision）+ 状态机变异测试",
      "priority": "high",
      "executor": "generator",
      "status": "completed",
      "acceptance": "src/lib/knowledge/parse.ts parseMaterial：pending→parsing→按 mimeType 分流（文本直读/pdf 文本抽取；图片 image part 走 chatModel(AIGCGATEWAY_VISION_MODEL 默认 qwen3.5-flash，立项实测可用)）→ generateText → zod 校验 → GameKnowledge 三类 kind 生成 +【P3】supersede 同事务（新链头+旧条目 supersededById，读取恒取 supersededById IS NULL）→ parsed；失败 → failed+parseError 状态机内消化不外抛；POST /api/materials/{id}/parse（上传后自动触发+重新分析复用；【P2】同步+轮询不建队列+进程内防重入拒并发）；logUsage 照记；【D20】状态机变异测试：四态流转+supersede 链+链头读取各配变异断言；【P7】单测 mock 网关（解析调用可注入替换），真网关属 L2 留验收授权；lint + tsc + test:unit 绿",
      "description": "spec §2 F003 + U3/P2/P3/P4/P7 + D20 义务（architecture :479）。src/ 首个 server 侧一次性 generateText 调用点（勘查实证现仅 route.ts:109 streamText）。"
    },
    {
      "id": "F004",
      "title": "knowledge 页面接真 + mock 退役",
      "priority": "high",
      "executor": "generator",
      "status": "completed",
      "acceptance": "knowledge/page.tsx RSC 直读（force-dynamic 必须声明 + build 后 prerender-manifest 不含该路由，v1.0.9 §6）：Game 全列+Material 列表+GameKnowledge 链头组装可序列化 prop（沿 ProjectDetailData 范式)；KnowledgeWorkbench 保 'use client'：UploadZone→POST /api/materials→自动 parse→轮询至 parsed/failed→router.refresh；重新分析接 parse API；mock 时序（setTimeout 1100）与 mock/knowledge.ts 退役——按被删路径 needle 全仓 grep 零代码残留（v1.0.9）；D2 空态/降级可见文案（无素材/failed 红态+parseError/特点卡待解析占位，保区块结构）；knowledge.png 基线漂移逐处对账后重生（waitFor 按新实况校准+空态硬断言 §4.3）；运行时改→验→复原实证；两视口实测；lint + tsc + test:unit + test:visual 绿",
      "description": "spec §2 F004。勘查实证页面/组件全建好只换供给侧（KnowledgeWorkbench 130 行 + 三卡组件，数据契约 mock/knowledge.ts:34-52）。"
    },
    {
      "id": "F005",
      "title": "prompt ⑤层知识注入（U4）",
      "priority": "medium",
      "executor": "generator",
      "status": "pending",
      "acceptance": "registry.ts persona 扩 knowledgeKinds?：strategy=三类全量、match=['audience']、reach=['selling_point']、compliance=['compliance_redline']（architecture :907 映射，本批仅注入 prompt）；新建 src/lib/agent/knowledge-context.ts gameKnowledgeSection(projectId,kinds)：经 Project.gameId 查链头拼知识段（含溯源计数），无知识返回空串不注水；route.ts:102 装配点拼接（ctx.projectId 空则跳过）；单测断言 prompt 拼接产物含知识段+kinds 过滤正确（mock 数据不打网关）；真对话实测属 L2 留验收授权；lint + tsc + test:unit 绿",
      "description": "spec §2 F005 + U4。立项补核实证装配点 = route.ts:102 persona.systemPrompt 拼接处；registry 现无 knowledgeKinds（grep 零命中）。"
    },
    {
      "id": "F006",
      "title": "部署面：materials 卷 + env + 文档",
      "priority": "low",
      "executor": "generator",
      "status": "pending",
      "acceptance": "docker-compose.prod.yml 新增 newkolmatrix-materials 卷挂 app /app/materials + env MATERIALS_DIR/AIGCGATEWAY_VISION_MODEL；.env.example 补两项注释；.gitignore 补 .materials/；docs/dev/deploy.md 更新（materials 卷备份纳入 R7 记账）+ architecture.md §7.6/§11.3 as-built 翻牌；探针/测试漂移扫描（v1.0.5：grep 引用部署文件的探针）；lint + tsc 绿；部署验证随上线执行",
      "description": "spec §2 F006。勘查实证 compose 现仅 pgdata 卷（:69-71），app 无文件卷。"
    }
  ]
}
