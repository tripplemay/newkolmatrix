import ComingSoon from 'components/common/ComingSoon';
// AGENT-FOUNDATION F008 — Agent 记录（编队自动动作留痕，只读可筛）
export default function RunsPage() {
  return <ComingSoon title="Agent 记录" description="编队自动动作完整留痕（谁/何时/做了什么/类型）· 永久可查可筛不可篡改 — 真实落地 → M3(OperationLog)" />;
}
